# 踩坑问题清单

Crawl4AI 独立部署 + Docker / Nginx 配置过程中遇到的问题及解决方案。

---

## 1. Windows ↔ Linux CRLF 换行符问题

**现象**: 容器启动后立即退出，日志显示 `set: pipefail\r: invalid option name`

**原因**: Windows 下 SCP 到服务器的 shell 脚本保留 CRLF 换行符，bash 将 `\r` 视为命令名的一部分。

**解决**: 服务器端执行 `sed -i 's/\r$//' <script>` 移除回车符。涉及的脚本：
- `entrypoint.sh`
- `supervisord.conf`

**预防**: SCP 前在本地用 `dos2unix` 转换，或服务器端始终检查换行符。

---

## 2. Docker Volume 挂载权限问题

**现象**: 容器日志报 `PermissionError: [Errno 13] Permission denied: '/home/appuser/.crawl4ai/html_content'`

**原因**: Docker Compose 中声明了 `crawl4ai_data:/home/appuser/.crawl4ai` 卷，但镜像内该目录不存在。Docker 创建空卷时用 `root` 所有权，而容器以 `appuser` 用户运行。

**解决**: 在 Dockerfile 中 `USER appuser` 之前添加：
```dockerfile
RUN mkdir -p /home/appuser/.crawl4ai \
    && chown -R appuser:appuser /home/appuser/.crawl4ai
```
当卷首次挂载时，Docker 会将镜像中的目录内容（包括权限）复制到卷中。

**关键原则**: 任何声明为 Docker Volume 的路径，必须在镜像中预创建并设置正确的所有权和权限。

---

## 3. Nginx sub-path 反向代理重定向问题

**现象**: 通过 `http://47.98.102.55/5d41402abc4b2a76b9719d911017c592/crawl4ai/` 访问 crawl4ai 时，所有重定向（307）返回错误路径，缺少 sub-path 前缀。

**排查过程**:

### 3a. `proxy_redirect` 与变量 proxy_pass 不兼容
- 最初使用 `set $crawl4ai_upstream crawl4ai:11235; proxy_pass http://$crawl4ai_upstream/;` 实现动态 DNS 解析
- **发现**: Nginx 文档明确：`proxy_redirect` 指令在 `proxy_pass` 使用变量时**完全被忽略**
- **解决**: 改为直接 `proxy_pass http://crawl4ai:11235/;`（同一 Docker 网络的容器名稳定，不需要动态 DNS）

### 3b. `proxy_redirect` 匹配模式错误
- 最初尝试匹配相对路径 `proxy_redirect / /prefix/;` 和 `proxy_redirect ~^/(.*)$ /prefix/$1;`
- 但 crawl4ai 的 FastAPI 根据 `Host` 请求头生成**绝对 URL** 的 Location header：`http://47.98.102.55/playground/`（而非 `/playground`）
- **正确做法**: 匹配包含 host 的完整前缀：
  ```nginx
  proxy_redirect http://$host/ /5d41402abc4b2a76b9719d911017c592/crawl4ai/;
  ```

### 3c. `sub_filter` 与 `proxy_buffering off` 不兼容
- 尝试用 `sub_filter` 修复 playground HTML 中硬编码的 API 路径
- `proxy.conf` 中 `proxy_buffering off;` 导致 nginx 不缓存响应体，`sub_filter` 完全无效
- **最终方案**: 不依赖 `sub_filter`，直接修改 crawl4ai 源码中的 playground HTML

**最终 nginx 配置**:
```nginx
location ^~ /5d41402abc4b2a76b9719d911017c592/crawl4ai/ {
    proxy_pass http://crawl4ai:11235/;       # 必须不用变量
    include proxy.conf;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_redirect http://$host/ /5d41402abc4b2a76b9719d911017c592/crawl4ai/;
}
```

**关键原则**:
- `proxy_redirect` 需要 `proxy_pass` 不使用变量
- 检查上游返回的 Location 是相对路径还是绝对 URL，据此写匹配规则
- `sub_filter` 需要 `proxy_buffering on`，但流式 API 会受影响

---

## 4. SPA / 前端应用 sub-path 部署的 API 路径问题

**现象**: Playground 页面加载正常，但 API 调用 `POST /crawl` 返回 405（没有 sub-path 前缀），`POST /config/dump` 同样 405。

**原因**: crawl4ai 的 playground HTML 是纯静态 SPA，JavaScript 中硬编码了绝对 API 路径：
```javascript
const endpointMap = {
    crawl: '/crawl',
    crawl_stream: '/crawl/stream',
    md: '/md',
    llm: '/llm'
};
// ...
const res = await authFetch('/config/dump', {...});
```

当通过 sub-path 代理访问时，`/crawl` 指向 `http://host/crawl` 而非 `http://host/prefix/crawl`。

**解决方案**: 修改 playground HTML 源码，注入动态 base URL 计算逻辑：
```html
<head><script>window.CRAWL4AI_BASE=window.location.pathname.replace(/\/playground\/?$/,"");</script>
```
然后将所有硬编码路径改为 `CRAWL4AI_BASE + '/crawl'` 等形式。

**部署方式**: 通过 Docker Compose bind mount 将修改后的 HTML 挂载到容器（容器为 `read_only: true`，无法 `docker cp`）：
```yaml
volumes:
  - /home/bid-agent-konus/crawl4ai/deploy/docker/static/playground/index.html:/app/static/playground/index.html:ro
```

**关键原则**: 任何部署在 sub-path 下的 SPA，其 JS 代码中的 API 路径必须是相对路径或可动态计算，不能硬编码绝对路径。

---

## 5. Nginx 配置热更新注意事项

**现象**: 修改 `ragflow.conf.python` 后 `nginx -s reload`，但新配置未生效。

**原因**: RAGFlow 容器的 entrypoint.sh 在启动时将 `ragflow.conf.python` 复制到 `ragflow.conf`，但 nginx reload 不会重新执行 entrypoint。

**解决**: 每次更新配置后，手动执行：
```bash
docker exec docker-ragflow-cpu-1 cp -f /etc/nginx/conf.d/ragflow.conf.python /etc/nginx/conf.d/ragflow.conf
docker exec docker-ragflow-cpu-1 nginx -s reload
```

**关键原则**: 了解容器内 nginx 的配置文件链 — entrypoint 在启动时做一次性文件复制，热更新需手动复制。

---

## 6. Docker 构建层缓存与 COPY 失效

**现象**: 修复 `entrypoint.sh` 的 CRLF 后第二次构建，即使不加 `--no-cache`，`COPY . /tmp/project/` 之后的步骤全部重建。

**原因**: `COPY` 指令检查文件内容校验和，文件内容变更（CRLF→LF）会导致该层及所有后续层缓存失效。

**不是问题而是预期行为**: Docker BuildKit 的正确行为，无法避免。构建时间约 30-40 分钟（含 playwright 浏览器下载）。

---

## 7. Python `-c` 内联脚本在 SSH 中的转义问题

**现象**: 尝试 `ssh ... python3 -c "..."` 执行多行 Python 代码时，引号嵌套和反斜杠导致语法错误或字符串截断。

**原因**: Windows cmd/bash/ssh 三层的引号解析 + Python 字符串转义，四个层级叠加极度复杂。

**解决方案**:
1. 优先创建本地 `.py` 文件 → SCP 上传 → SSH 执行
2. SCP 路径使用正斜杠：`scp D:/AI/ragflow2/script.py root@host:/tmp/`
3. 避免 `python -c` 内联复杂逻辑

---

## 8. Peewee `MySQLMigrator` 不支持 `create_table()` 导致启动崩溃循环

**现象**: 容器启动后立即退出并无限重启，日志显示：
```
AttributeError: 'MySQLMigrator' object has no attribute 'create_table'. Did you mean: 'rename_table'?
```

**原因**: `migrate_db()` 中使用了 `migrator.create_table(Model, safe=True)`，但当前项目使用的 Peewee 版本中 `MySQLMigrator` 没有 `create_table` 方法。项目中其他地方建表使用的是 `Model.create_table(safe=True)` 模式。

**错误代码**:
```python
migrator.create_table(CollaborationDocumentACL, safe=True)
migrator.create_table(CollaborationComment, safe=True)
```

**正确代码**:
```python
CollaborationDocumentACL.create_table(safe=True)
CollaborationComment.create_table(safe=True)
```

**关键原则**: Peewee 的 `playhouse.migrate.MySQLMigrator` 仅提供 schema 变更操作（add_column、rename_column、alter_column_type 等），不含建表操作。新增表必须通过 Model 实例的 `create_table()` 方法，与 `init_database_tables()` 中的用法一致。

---

## 9. 协作 WebSocket 连接失败：nginx 未加载含 WebSocket 配置的文件

**现象**: 协作文档编辑器中，两个账号打开同一文档，右上角只显示"1人在线"（仅当前用户），无法看到其他在线协作者。浏览器控制台显示：
```
WebSocket connection to 'ws://.../api/v1/collaboration/ws/...' failed:
[Collab WS] CLOSE: 1006
```
WebSocket 从未成功建立（无 `[Collab WS] OPEN:` 日志），close code 1006 = 异常关闭。

**原因**: `docker-compose.yml` 挂载了 `ragflow.conf.python`（包含 collaboration WebSocket 的 `Upgrade` 头配置），但 `nginx.conf` 只 include 了基础镜像自带的 `ragflow.conf`（无 WebSocket 配置）：
```nginx
# nginx.conf（原始）
include /etc/nginx/conf.d/ragflow.conf;           # ← 基础镜像的配置，无 WebSocket
# ragflow.conf.python 虽已挂载到容器，但 nginx 未加载
```
WebSocket 请求命中的是 `location ~ ^/(v1|api)` 普通代理规则，缺少 `proxy_http_version 1.1` + `Upgrade` 头，WebSocket 握手失败。

**排查方法**: 浏览器控制台 F12 → Network → WS 标签页，查看 WebSocket 连接状态。或在 yjs-provider.ts 中添加 `console.warn` 在 `ws.onopen/onclose/onerror` 中定位。

**解决方案**:
1. **立即生效**（无需重启容器）：容器内手动复制 + reload：
   ```bash
   docker exec docker-ragflow-cpu-1 cp -f /etc/nginx/conf.d/ragflow.conf.python /etc/nginx/conf.d/ragflow.conf
   docker exec docker-ragflow-cpu-1 nginx -s reload
   ```
2. **永久修复**：修改 `nginx.conf` 直接 include `ragflow.conf.python`，后续更新配置只需 reload：
   ```nginx
   include /etc/nginx/conf.d/ragflow.conf.python;
   ```

**关联**: 与问题 #5（nginx 热更新需手动复制）是同一类问题的不同表现。问题 #5 是修改 `ragflow.conf.python` 后未生效，本问题是 `ragflow.conf.python` 从未被 nginx 加载过。

**关键原则**:
- `docker-compose.yml` 的 volume 挂载 ≠ nginx 实际加载。必须确认 `nginx.conf` 的 `include` 路径与挂载文件名一致。
- WebSocket 代理必须包含 `proxy_http_version 1.1` + `proxy_set_header Upgrade` + `proxy_set_header Connection "upgrade"`。
- WebSocket 连接失败时 close code 1006 = 异常关闭（握手未完成），1000/1001 = 正常关闭。

---

## 10. Quart WebSocket 中使用 `request` 导致 RuntimeError

**现象**: 协作文档 WebSocket 始终连接失败，浏览器控制台显示 `WebSocket connection to 'ws://...' failed` + close code 1006。

**排查过程**:

1. **nginx 层**（已排除）：nginx 配置正确设置了 `Upgrade` / `Connection "upgrade"` 头，curl 测试返回 HTTP 101。
2. **容器内直连 Quart**（定位问题）：直接 `websockets.connect('ws://localhost:9380/...')` 返回 `InvalidStatus: HTTP 200`。Quart 没有返回 101 Switching Protocols。
3. **查看 Quart 日志**：发现 `RuntimeError: Not within a request context`，异常被全局 `server_error_response` 捕获后返回 HTTP 200。

**根本原因**: Quart 的 `request` 是 HTTP 请求代理（`werkzeug.local.LocalProxy`），绑定到 `_cv_request` 上下文变量。WebSocket 上下文使用的是 `_cv_websocket`，`request` 不可用。

```python
# 错误代码
async def handle_ws(doc_id: str):
    from quart import websocket, request
    token = _get_token_from_request(request)  # ← RuntimeError!
```

**正确代码**: Quart 的 `websocket` 对象同样拥有 `args` 和 `headers` 属性：

```python
async def handle_ws(doc_id: str):
    from quart import websocket
    token = _get_token_from_request(websocket)  # ✓ websocket.args / websocket.headers
```

**为什么错误难以发现**:
- Quart 的全局异常处理器 `app.errorhandler(Exception)(server_error_response)` 将所有异常包装为 HTTP 200 + JSON `{"code":100,"message":"..."}`，掩盖了真实错误。
- 浏览器只看到 HTTP 200（非 101），WebSocket 握手失败，close code 1006。没有任何有用的错误信息传到前端。
- 必须从服务端日志或容器内直连才能看到真实的 `RuntimeError`。

**关键原则**:
- Quart 中 WebSocket handler 只能通过 `websocket` 对象访问请求数据（`websocket.args`、`websocket.headers`），不能用 `request`。
- 全局异常处理器不应将 WebSocket 异常包装为 HTTP 200，否则掩盖握手失败的真实原因。
- 排查 WebSocket 连接失败时，优先在容器内直连后端（绕过 nginx）+ 查看后端日志。

---

## 总结

| 编号 | 问题分类 | 一句话 |
|------|----------|--------|
| 1 | 换行符 | Windows→Linux SCP 的文件始终检查 CRLF |
| 2 | Docker Volume | Volume 目标路径必须在镜像中预创建并设权限 |
| 3 | Nginx proxy_redirect | proxy_pass 不用变量；匹配上游实际返回的 Location 格式 |
| 4 | SPA sub-path | JS 中 API 路径不能硬编码，必须动态计算 |
| 5 | Nginx 热更新 | 了解容器内文件链，热更新时手动复制中间文件 |
| 6 | Docker 缓存 | COPY 内容变更必然导致缓存失效，无法避免 |
| 7 | Shell 转义 | 复杂脚本写文件再 SCP，不用内联 `python -c` |
| 8 | Peewee migrate | migrator 只能改 schema，建表用 `Model.create_table(safe=True)` |
| 9 | Nginx WebSocket | docker-compose 挂载的配置文件必须与 nginx.conf include 路径一致 |
| 10 | Quart WebSocket request | WebSocket handler 中不能用 `request`，必须用 `websocket` 对象 |
| 11 | Quart WebSocket proxy | `from quart import websocket` 是 werkzeug LocalProxy，存入 room 必须用 `_get_current_object()` 解析为真实对象，否则广播时 proxy 解析为发送者自己的 WebSocket |

---

## 12. 协作保存不生效 + 无通知：服务端不广播保存事件

**现象**: A 和 B 同时编辑同一文档，B 点击保存后：① B 的内容没有被其他协作者看到；② A 完全不知道 B 保存了。

**原因**: 服务端 WebSocket `save` 消息处理只做了持久化（更新 `full_state` + 写入 MySQL + 清空 buffer），**没有广播通知给房间内其他客户端**。前端也没有处理远程保存通知的逻辑。

```python
# 问题代码 — 只持久化，不广播
elif msg_type == "save":
    if msg_data:
        save_bytes = base64.b64decode(msg_data)
        room["full_state"] = save_bytes
        await _persist_full_state(doc_id, save_bytes)
        room["buffer"].clear()
        # ← 没有 _broadcast！其他客户端完全不知道
```

**解决方案**: 服务端保存成功后广播 `{t: "saved", d: {name: "xxx"}}` 给房间内其他客户端；前端 `yjs-provider.ts` 新增 `saved` 事件类型，`document-editor.tsx` 监听后弹出 `notification.info`。

**关键原则**: 服务端作为"盲管道"时，`save` 持久化操作必须配合广播通知，否则协同用户无法感知保存状态。

---

## 13. 协作保存静默失败：provider 为 null 时无用户反馈

**现象**: 点击保存按钮后，文档头部状态栏没有变化，没有任何错误提示，用户以为保存成功了但实际上内容未持久化。

**原因**: `CollabSavePlugin.doSave()` 在 `providerRef.current` 为 null 时直接 `return`，仅输出 `console.log`，用户无感知。

```typescript
// 问题代码 — 静默跳过
if (!provider || savingRef.current) {
    console.log('[CollabSave] skip save: ...'); // ← 用户看不到
    return;
}
```

**解决方案**: 在手动保存入口（`handleSave`）中检查 provider 状态，为 null 时弹 `notification.warning`。定时保存（每30秒）保持静默，避免 toast 刷屏。

**关键原则**: 任何可能失败的写操作都必须向用户暴露错误状态，且区分手动触发（需要反馈）和自动触发（静默即可）。

---

## 14. useEffect 空依赖导致事件监听永远不注册

**现象**: 修复了服务端广播后，前端仍然收不到"xxx 已保存文档"的通知。

**原因**: 注册远程保存监听器的 `useEffect` 使用空依赖数组 `[]`，在组件挂载时执行。但此时 `collabProviderRef.current` 为 `null`（`CollaborationPlugin` 的 `providerFactory` 尚未执行），effect 直接 return，**listener 永远不会被注册**。

```typescript
// 问题代码 — 挂载时 provider 为 null
useEffect(() => {
    const provider = collabProviderRef.current; // ← null
    if (!provider) return;                      // ← 直接返回
    provider.on('saved', ...);
}, []); // ← 空依赖，不再重新执行
```

**解决方案**: 改用轮询机制（与 `CollabSavePlugin` 的 `onProviderReady` 相同模式），每 200ms 检查 `collabProviderRef.current`，provider 就绪后注册 listener。cleanup 时 off。

**关键原则**: 当需要等待异步初始化的 ref 值（如 Lexical 插件创建的 provider），不能用 `useEffect([], [])` + 直接读 ref。必须用轮询或 callback 机制。

---

## 15. WebSocket 断线缓冲发送过时更新

**现象**: 用户网络断开期间编辑文档，重连后其他协作者收到闪烁或错误内容。

**原因**: 断线期间 `_onDocUpdate` 将编辑 update 缓冲到 `offlineBuffer`。重连时 `ws.onopen` 先 flush 缓冲（发送基于旧状态的 update），然后才收到服务端的 `init` 消息（最新状态）。缓冲中的 update 是从旧 Yjs 文档状态生成的，与服务端当前状态可能冲突。

**解决方案**: 在 `_handleMessage` 收到 `init` 消息后清空 `offlineBuffer`。服务端 `init` 已包含完整最新状态 + buffer replay，无需再发送断线期间的旧增量。

```typescript
// 在 init 处理中添加
case 'init': {
    // ... apply init state ...
    this.offlineBuffer.length = 0; // ← 丢弃旧缓冲
}
```

**关键原则**: Yjs 虽然 CRDT 能合并冲突，但发送基于旧状态的增量更新是无意义的网络开销，且可能在极端边界条件下产生非预期行为。重连后应以服务端 `init` 为权威源，丢弃本地离线缓冲。

---

## 16. React StrictMode 导致协编辑器内容为空：binding stale children 阻止 bootstrap

**现象**: 开发环境下（React StrictMode 启用），协作文档编辑器打开后内容为空，用户输入的文字保存后刷新页面内容消失。浏览器控制台无报错，`save success` 日志正常。

**原因**: `@lexical/yjs` 的 `CollaborationPlugin` 使用 `isProviderInitialized` 和 `isBindingInitialized` 两个 ref guard 防止 StrictMode 重复创建 provider 和 binding。但 cleanup 时 `binding.root.destroy(binding)` 只清除 `collabNodeMap`，**不清空 `root._children` 数组**，也不清除子节点的 `_collabNode` 属性。

StrictMode 执行 mount → cleanup → remount 后：
1. binding 的 `root._children` 仍持有 **已销毁的 stale CollabNode**（key 指向旧 editor 的节点）
2. WS 重连，服务端发送 `init` → CollaborationPlugin 的 `onSync` 触发
3. `root.isEmpty()` 检查 `_children.length === 0` → **false**（有 stale children）
4. `root._xmlText._length === 0` 检查 → 可能 **false**（stale children 的 XmlText 仍嵌入在 root XmlText 中）
5. **bootstrap 被跳过**，编辑器保持默认空状态
6. 后续用户输入时，`syncLexicalUpdateToYjs` 用 `this._children[index]` 取到 **stale child**，文本写入错误的 XmlText

**排查方法**: 在 `doSave` 中打日志检查 `editor.getEditorState().toJSON()` 的 `root.children[0].children` 是否为空数组（无 text 节点）。

**解决方案**:

1. **yjs-provider.ts `connect()` 方法**: 在 WS 重连前清除 stale children
```typescript
connect(): void {
    this.closed = false;
    try {
      const rootXmlText = this.doc.get('root', Y.XmlText);
      const collabNode = rootXmlText._collabNode;
      if (collabNode && collabNode._children.length > 0) {
        collabNode._children.length = 0;
      }
    } catch { /* binding may not exist yet */ }
    this._connect();
}
```

2. **document-editor.tsx 新增 `BindingFixPlugin`**: 作为兜底，检测到编辑器为空且 `document.content` 有内容时，强制从 REST API 数据恢复编辑器状态。轮询 200ms，provider 就绪后检测，一次性修复后停止。

```tsx
function BindingFixPlugin({ document, providerRef }) {
  const [editor] = useLexicalComposerContext();
  useEffect(() => {
    const timer = setInterval(() => {
      // ... 检查编辑器根节点是否有文本内容
      // 若无内容但有 document.content，用 editor.setEditorState() 恢复
    }, 200);
    return () => clearInterval(timer);
  }, [editor, document, providerRef]);
}
```

**关键原则**: `@lexical/yjs` 的 `CollabElementNode.destroy()` 不清空 `_children` 数组和不清除 `_collabNode` 属性。在 StrictMode 下，这两个遗留会导致 binding 状态与实际 editor 状态不一致。必须在 provider 重连时手动清除 stale children。
