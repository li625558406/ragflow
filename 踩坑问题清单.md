# 踩坑问题清单

Crawl4AI 独立部署 + Docker / Nginx 配置过程中遇到的问题及解决方案。

---

## 1. Windows ↔ Linux CRLF 换行符问题

**现象**: 容器启动后立即退出，日志显示 `set: pipefail\r: invalid option name`

**原因**: Windows 下 SCP 到服务器的 shell 脚本保留 CRLF 换行符，bash 将 `\r` 视为命令名的一部分。

**解决**: 服务器端执行 `sed -i 's/\r$//' <script>` 移除回车符。涉及的脚本：
- `entrypoint.sh`
- `supervisord.conf`

**预防**: SCP 前在本地用 `dos2unix` 转换，或服务器端始终检查换行符。

---

## 2. Docker Volume 挂载权限问题

**现象**: 容器日志报 `PermissionError: [Errno 13] Permission denied: '/home/appuser/.crawl4ai/html_content'`

**原因**: Docker Compose 中声明了 `crawl4ai_data:/home/appuser/.crawl4ai` 卷，但镜像内该目录不存在。Docker 创建空卷时用 `root` 所有权，而容器以 `appuser` 用户运行。

**解决**: 在 Dockerfile 中 `USER appuser` 之前添加：
```dockerfile
RUN mkdir -p /home/appuser/.crawl4ai \
    && chown -R appuser:appuser /home/appuser/.crawl4ai
```
当卷首次挂载时，Docker 会将镜像中的目录内容（包括权限）复制到卷中。

**关键原则**: 任何声明为 Docker Volume 的路径，必须在镜像中预创建并设置正确的所有权和权限。

---

## 3. Nginx sub-path 反向代理重定向问题

**现象**: 通过 `http://47.98.102.55/5d41402abc4b2a76b9719d911017c592/crawl4ai/` 访问 crawl4ai 时，所有重定向（307）返回错误路径，缺少 sub-path 前缀。

**排查过程**:

### 3a. `proxy_redirect` 与变量 proxy_pass 不兼容
- 最初使用 `set $crawl4ai_upstream crawl4ai:11235; proxy_pass http://$crawl4ai_upstream/;` 实现动态 DNS 解析
- **发现**: Nginx 文档明确：`proxy_redirect` 指令在 `proxy_pass` 使用变量时**完全被忽略**
- **解决**: 改为直接 `proxy_pass http://crawl4ai:11235/;`（同一 Docker 网络的容器名稳定，不需要动态 DNS）

### 3b. `proxy_redirect` 匹配模式错误
- 最初尝试匹配相对路径 `proxy_redirect / /prefix/;` 和 `proxy_redirect ~^/(.*)$ /prefix/$1;`
- 但 crawl4ai 的 FastAPI 根据 `Host` 请求头生成**绝对 URL** 的 Location header：`http://47.98.102.55/playground/`（而非 `/playground`）
- **正确做法**: 匹配包含 host 的完整前缀：
  ```nginx
  proxy_redirect http://$host/ /5d41402abc4b2a76b9719d911017c592/crawl4ai/;
  ```

### 3c. `sub_filter` 与 `proxy_buffering off` 不兼容
- 尝试用 `sub_filter` 修复 playground HTML 中硬编码的 API 路径
- `proxy.conf` 中 `proxy_buffering off;` 导致 nginx 不缓存响应体，`sub_filter` 完全无效
- **最终方案**: 不依赖 `sub_filter`，直接修改 crawl4ai 源码中的 playground HTML

**最终 nginx 配置**:
```nginx
location ^~ /5d41402abc4b2a76b9719d911017c592/crawl4ai/ {
    proxy_pass http://crawl4ai:11235/;       # 必须不用变量
    include proxy.conf;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_redirect http://$host/ /5d41402abc4b2a76b9719d911017c592/crawl4ai/;
}
```

**关键原则**:
- `proxy_redirect` 需要 `proxy_pass` 不使用变量
- 检查上游返回的 Location 是相对路径还是绝对 URL，据此写匹配规则
- `sub_filter` 需要 `proxy_buffering on`，但流式 API 会受影响

---

## 4. SPA / 前端应用 sub-path 部署的 API 路径问题

**现象**: Playground 页面加载正常，但 API 调用 `POST /crawl` 返回 405（没有 sub-path 前缀），`POST /config/dump` 同样 405。

**原因**: crawl4ai 的 playground HTML 是纯静态 SPA，JavaScript 中硬编码了绝对 API 路径：
```javascript
const endpointMap = {
    crawl: '/crawl',
    crawl_stream: '/crawl/stream',
    md: '/md',
    llm: '/llm'
};
// ...
const res = await authFetch('/config/dump', {...});
```

当通过 sub-path 代理访问时，`/crawl` 指向 `http://host/crawl` 而非 `http://host/prefix/crawl`。

**解决方案**: 修改 playground HTML 源码，注入动态 base URL 计算逻辑：
```html
<head><script>window.CRAWL4AI_BASE=window.location.pathname.replace(/\/playground\/?$/,"");</script>
```
然后将所有硬编码路径改为 `CRAWL4AI_BASE + '/crawl'` 等形式。

**部署方式**: 通过 Docker Compose bind mount 将修改后的 HTML 挂载到容器（容器为 `read_only: true`，无法 `docker cp`）：
```yaml
volumes:
  - /home/bid-agent-konus/crawl4ai/deploy/docker/static/playground/index.html:/app/static/playground/index.html:ro
```

**关键原则**: 任何部署在 sub-path 下的 SPA，其 JS 代码中的 API 路径必须是相对路径或可动态计算，不能硬编码绝对路径。

---

## 5. Nginx 配置热更新注意事项

**现象**: 修改 `ragflow.conf.python` 后 `nginx -s reload`，但新配置未生效。

**原因**: RAGFlow 容器的 entrypoint.sh 在启动时将 `ragflow.conf.python` 复制到 `ragflow.conf`，但 nginx reload 不会重新执行 entrypoint。

**解决**: 每次更新配置后，手动执行：
```bash
docker exec docker-ragflow-cpu-1 cp -f /etc/nginx/conf.d/ragflow.conf.python /etc/nginx/conf.d/ragflow.conf
docker exec docker-ragflow-cpu-1 nginx -s reload
```

**关键原则**: 了解容器内 nginx 的配置文件链 — entrypoint 在启动时做一次性文件复制，热更新需手动复制。

---

## 6. Docker 构建层缓存与 COPY 失效

**现象**: 修复 `entrypoint.sh` 的 CRLF 后第二次构建，即使不加 `--no-cache`，`COPY . /tmp/project/` 之后的步骤全部重建。

**原因**: `COPY` 指令检查文件内容校验和，文件内容变更（CRLF→LF）会导致该层及所有后续层缓存失效。

**不是问题而是预期行为**: Docker BuildKit 的正确行为，无法避免。构建时间约 30-40 分钟（含 playwright 浏览器下载）。

---

## 7. Python `-c` 内联脚本在 SSH 中的转义问题

**现象**: 尝试 `ssh ... python3 -c "..."` 执行多行 Python 代码时，引号嵌套和反斜杠导致语法错误或字符串截断。

**原因**: Windows cmd/bash/ssh 三层的引号解析 + Python 字符串转义，四个层级叠加极度复杂。

**解决方案**:
1. 优先创建本地 `.py` 文件 → SCP 上传 → SSH 执行
2. SCP 路径使用正斜杠：`scp D:/AI/ragflow2/script.py root@host:/tmp/`
3. 避免 `python -c` 内联复杂逻辑

---

## 8. Peewee `MySQLMigrator` 不支持 `create_table()` 导致启动崩溃循环

**现象**: 容器启动后立即退出并无限重启，日志显示：
```
AttributeError: 'MySQLMigrator' object has no attribute 'create_table'. Did you mean: 'rename_table'?
```

**原因**: `migrate_db()` 中使用了 `migrator.create_table(Model, safe=True)`，但当前项目使用的 Peewee 版本中 `MySQLMigrator` 没有 `create_table` 方法。项目中其他地方建表使用的是 `Model.create_table(safe=True)` 模式。

**错误代码**:
```python
migrator.create_table(CollaborationDocumentACL, safe=True)
migrator.create_table(CollaborationComment, safe=True)
```

**正确代码**:
```python
CollaborationDocumentACL.create_table(safe=True)
CollaborationComment.create_table(safe=True)
```

**关键原则**: Peewee 的 `playhouse.migrate.MySQLMigrator` 仅提供 schema 变更操作（add_column、rename_column、alter_column_type 等），不含建表操作。新增表必须通过 Model 实例的 `create_table()` 方法，与 `init_database_tables()` 中的用法一致。

---

## 9. 协作 WebSocket 连接失败：nginx 未加载含 WebSocket 配置的文件

**现象**: 协作文档编辑器中，两个账号打开同一文档，右上角只显示"1人在线"（仅当前用户），无法看到其他在线协作者。浏览器控制台显示：
```
WebSocket connection to 'ws://.../api/v1/collaboration/ws/...' failed:
[Collab WS] CLOSE: 1006
```
WebSocket 从未成功建立（无 `[Collab WS] OPEN:` 日志），close code 1006 = 异常关闭。

**原因**: `docker-compose.yml` 挂载了 `ragflow.conf.python`（包含 collaboration WebSocket 的 `Upgrade` 头配置），但 `nginx.conf` 只 include 了基础镜像自带的 `ragflow.conf`（无 WebSocket 配置）：
```nginx
# nginx.conf（原始）
include /etc/nginx/conf.d/ragflow.conf;           # ← 基础镜像的配置，无 WebSocket
# ragflow.conf.python 虽已挂载到容器，但 nginx 未加载
```
WebSocket 请求命中的是 `location ~ ^/(v1|api)` 普通代理规则，缺少 `proxy_http_version 1.1` + `Upgrade` 头，WebSocket 握手失败。

**排查方法**: 浏览器控制台 F12 → Network → WS 标签页，查看 WebSocket 连接状态。或在 yjs-provider.ts 中添加 `console.warn` 在 `ws.onopen/onclose/onerror` 中定位。

**解决方案**:
1. **立即生效**（无需重启容器）：容器内手动复制 + reload：
   ```bash
   docker exec docker-ragflow-cpu-1 cp -f /etc/nginx/conf.d/ragflow.conf.python /etc/nginx/conf.d/ragflow.conf
   docker exec docker-ragflow-cpu-1 nginx -s reload
   ```
2. **永久修复**：修改 `nginx.conf` 直接 include `ragflow.conf.python`，后续更新配置只需 reload：
   ```nginx
   include /etc/nginx/conf.d/ragflow.conf.python;
   ```

**关联**: 与问题 #5（nginx 热更新需手动复制）是同一类问题的不同表现。问题 #5 是修改 `ragflow.conf.python` 后未生效，本问题是 `ragflow.conf.python` 从未被 nginx 加载过。

**关键原则**:
- `docker-compose.yml` 的 volume 挂载 ≠ nginx 实际加载。必须确认 `nginx.conf` 的 `include` 路径与挂载文件名一致。
- WebSocket 代理必须包含 `proxy_http_version 1.1` + `proxy_set_header Upgrade` + `proxy_set_header Connection "upgrade"`。
- WebSocket 连接失败时 close code 1006 = 异常关闭（握手未完成），1000/1001 = 正常关闭。

---

## 10. Quart WebSocket 中使用 `request` 导致 RuntimeError

**现象**: 协作文档 WebSocket 始终连接失败，浏览器控制台显示 `WebSocket connection to 'ws://...' failed` + close code 1006。

**排查过程**:

1. **nginx 层**（已排除）：nginx 配置正确设置了 `Upgrade` / `Connection "upgrade"` 头，curl 测试返回 HTTP 101。
2. **容器内直连 Quart**（定位问题）：直接 `websockets.connect('ws://localhost:9380/...')` 返回 `InvalidStatus: HTTP 200`。Quart 没有返回 101 Switching Protocols。
3. **查看 Quart 日志**：发现 `RuntimeError: Not within a request context`，异常被全局 `server_error_response` 捕获后返回 HTTP 200。

**根本原因**: Quart 的 `request` 是 HTTP 请求代理（`werkzeug.local.LocalProxy`），绑定到 `_cv_request` 上下文变量。WebSocket 上下文使用的是 `_cv_websocket`，`request` 不可用。

```python
# 错误代码
async def handle_ws(doc_id: str):
    from quart import websocket, request
    token = _get_token_from_request(request)  # ← RuntimeError!
```

**正确代码**: Quart 的 `websocket` 对象同样拥有 `args` 和 `headers` 属性：

```python
async def handle_ws(doc_id: str):
    from quart import websocket
    token = _get_token_from_request(websocket)  # ✓ websocket.args / websocket.headers
```

**为什么错误难以发现**:
- Quart 的全局异常处理器 `app.errorhandler(Exception)(server_error_response)` 将所有异常包装为 HTTP 200 + JSON `{"code":100,"message":"..."}`，掩盖了真实错误。
- 浏览器只看到 HTTP 200（非 101），WebSocket 握手失败，close code 1006。没有任何有用的错误信息传到前端。
- 必须从服务端日志或容器内直连才能看到真实的 `RuntimeError`。

**关键原则**:
- Quart 中 WebSocket handler 只能通过 `websocket` 对象访问请求数据（`websocket.args`、`websocket.headers`），不能用 `request`。
- 全局异常处理器不应将 WebSocket 异常包装为 HTTP 200，否则掩盖握手失败的真实原因。
- 排查 WebSocket 连接失败时，优先在容器内直连后端（绕过 nginx）+ 查看后端日志。

---

## 总结

| 编号 | 问题分类 | 一句话 |
|------|----------|--------|
| 1 | 换行符 | Windows→Linux SCP 的文件始终检查 CRLF |
| 2 | Docker Volume | Volume 目标路径必须在镜像中预创建并设权限 |
| 3 | Nginx proxy_redirect | proxy_pass 不用变量；匹配上游实际返回的 Location 格式 |
| 4 | SPA sub-path | JS 中 API 路径不能硬编码，必须动态计算 |
| 5 | Nginx 热更新 | 了解容器内文件链，热更新时手动复制中间文件 |
| 6 | Docker 缓存 | COPY 内容变更必然导致缓存失效，无法避免 |
| 7 | Shell 转义 | 复杂脚本写文件再 SCP，不用内联 `python -c` |
| 8 | Peewee migrate | migrator 只能改 schema，建表用 `Model.create_table(safe=True)` |
| 9 | Nginx WebSocket | docker-compose 挂载的配置文件必须与 nginx.conf include 路径一致 |
| 10 | Quart WebSocket request | WebSocket handler 中不能用 `request`，必须用 `websocket` 对象 |
| 11 | Quart WebSocket proxy | `from quart import websocket` 是 werkzeug LocalProxy，存入 room 必须用 `_get_current_object()` 解析为真实对象，否则广播时 proxy 解析为发送者自己的 WebSocket |
| 12 | 协作保存不广播 | 服务端 save 持久化后必须 broadcast `saved` 通知，否则协同用户无感知 |
| 13 | 协作保存静默失败 | provider 为 null 时手动保存必须 toast 提示，不能 console.log 完事 |
| 14 | useEffect 空依赖 | 等待异步初始化的 ref 不能用 `useEffect([])`，必须用轮询或 callback |
| 15 | WebSocket 断线缓冲 | 重连后收到 `init` 必须清空 offlineBuffer，避免发基于旧状态的 update |
| 16 | StrictMode + Lexical binding | `@lexical/yjs` 的 destroy 不清 `_children`，StrictMode 重连需手动清理 stale CollabNode |
| 17 | id(websocket) 碰撞 | Python `id()` 是内存地址，StrictMode 双挂载会复用，永远用 uuid 作连接标识 |
| 18 | applyEpochRef 永久阻塞 | 用 ref 做模式标记必须有退出路径，本地编辑也要触发 useEffect 时需区分来源 |
| 19 | 持久化窗口过大 | 协作应用需 4 层保存：编辑防抖 / visibilitychange / pagehide / 卸载 flush |
| 20 | Awareness 不重放 | Yjs awareness 是软状态，新加入者需服务端 replay `last_aw` + 客户端 presence 触发自广播 |
| 23 | SPA load 永不触发 | analytics/长连接站点必须用 `domcontentloaded` + goto 超时容错 |
| 24 | HeadlessChrome UA 泄漏 | BrowserPool 必须显式设 UA，YAML headers.User-Agent 不影响浏览器 navigator |
| 25 | CSS extractor 语法差异 | `::text`/`::attr` 是 Scrapy 风格本项目不支持，复杂 SPA 结构用 `js_extract` |
| 26 | 顶层 try/except 捆绑 import | 多个无关 import 共用一个 except 会互相拖累，必须独立降级 + 独立日志 |
| 27 | ORM DateField 未导入 | 新增字段类型必须同步更新 peewee 显式 import 白名单 |
| 28 | 智能采集成套部署 | 横跨多层功能改动需成套 SCP，部署后必须冒烟测试全部 import |
| 29 | 三个采集列表 API + tenant 过滤 | 前端"智能采集"读 crawler_task 表（不是 scheduled_task）；三个 API 各自按 tenant 过滤；DB INSERT 前先确认前端读哪张表 |

---

## 12. 协作保存不生效 + 无通知：服务端不广播保存事件

**现象**: A 和 B 同时编辑同一文档，B 点击保存后：① B 的内容没有被其他协作者看到；② A 完全不知道 B 保存了。

**原因**: 服务端 WebSocket `save` 消息处理只做了持久化（更新 `full_state` + 写入 MySQL + 清空 buffer），**没有广播通知给房间内其他客户端**。前端也没有处理远程保存通知的逻辑。

```python
# 问题代码 — 只持久化，不广播
elif msg_type == "save":
    if msg_data:
        save_bytes = base64.b64decode(msg_data)
        room["full_state"] = save_bytes
        await _persist_full_state(doc_id, save_bytes)
        room["buffer"].clear()
        # ← 没有 _broadcast！其他客户端完全不知道
```

**解决方案**: 服务端保存成功后广播 `{t: "saved", d: {name: "xxx"}}` 给房间内其他客户端；前端 `yjs-provider.ts` 新增 `saved` 事件类型，`document-editor.tsx` 监听后弹出 `notification.info`。

**关键原则**: 服务端作为"盲管道"时，`save` 持久化操作必须配合广播通知，否则协同用户无法感知保存状态。

---

## 13. 协作保存静默失败：provider 为 null 时无用户反馈

**现象**: 点击保存按钮后，文档头部状态栏没有变化，没有任何错误提示，用户以为保存成功了但实际上内容未持久化。

**原因**: `CollabSavePlugin.doSave()` 在 `providerRef.current` 为 null 时直接 `return`，仅输出 `console.log`，用户无感知。

```typescript
// 问题代码 — 静默跳过
if (!provider || savingRef.current) {
    console.log('[CollabSave] skip save: ...'); // ← 用户看不到
    return;
}
```

**解决方案**: 在手动保存入口（`handleSave`）中检查 provider 状态，为 null 时弹 `notification.warning`。定时保存（每30秒）保持静默，避免 toast 刷屏。

**关键原则**: 任何可能失败的写操作都必须向用户暴露错误状态，且区分手动触发（需要反馈）和自动触发（静默即可）。

---

## 14. useEffect 空依赖导致事件监听永远不注册

**现象**: 修复了服务端广播后，前端仍然收不到"xxx 已保存文档"的通知。

**原因**: 注册远程保存监听器的 `useEffect` 使用空依赖数组 `[]`，在组件挂载时执行。但此时 `collabProviderRef.current` 为 `null`（`CollaborationPlugin` 的 `providerFactory` 尚未执行），effect 直接 return，**listener 永远不会被注册**。

```typescript
// 问题代码 — 挂载时 provider 为 null
useEffect(() => {
    const provider = collabProviderRef.current; // ← null
    if (!provider) return;                      // ← 直接返回
    provider.on('saved', ...);
}, []); // ← 空依赖，不再重新执行
```

**解决方案**: 改用轮询机制（与 `CollabSavePlugin` 的 `onProviderReady` 相同模式），每 200ms 检查 `collabProviderRef.current`，provider 就绪后注册 listener。cleanup 时 off。

**关键原则**: 当需要等待异步初始化的 ref 值（如 Lexical 插件创建的 provider），不能用 `useEffect([], [])` + 直接读 ref。必须用轮询或 callback 机制。

---

## 15. WebSocket 断线缓冲发送过时更新

**现象**: 用户网络断开期间编辑文档，重连后其他协作者收到闪烁或错误内容。

**原因**: 断线期间 `_onDocUpdate` 将编辑 update 缓冲到 `offlineBuffer`。重连时 `ws.onopen` 先 flush 缓冲（发送基于旧状态的 update），然后才收到服务端的 `init` 消息（最新状态）。缓冲中的 update 是从旧 Yjs 文档状态生成的，与服务端当前状态可能冲突。

**解决方案**: 在 `_handleMessage` 收到 `init` 消息后清空 `offlineBuffer`。服务端 `init` 已包含完整最新状态 + buffer replay，无需再发送断线期间的旧增量。

```typescript
// 在 init 处理中添加
case 'init': {
    // ... apply init state ...
    this.offlineBuffer.length = 0; // ← 丢弃旧缓冲
}
```

**关键原则**: Yjs 虽然 CRDT 能合并冲突，但发送基于旧状态的增量更新是无意义的网络开销，且可能在极端边界条件下产生非预期行为。重连后应以服务端 `init` 为权威源，丢弃本地离线缓冲。

---

## 16. React StrictMode 导致协编辑器内容为空：binding stale children 阻止 bootstrap

**现象**: 开发环境下（React StrictMode 启用），协作文档编辑器打开后内容为空，用户输入的文字保存后刷新页面内容消失。浏览器控制台无报错，`save success` 日志正常。

**原因**: `@lexical/yjs` 的 `CollaborationPlugin` 使用 `isProviderInitialized` 和 `isBindingInitialized` 两个 ref guard 防止 StrictMode 重复创建 provider 和 binding。但 cleanup 时 `binding.root.destroy(binding)` 只清除 `collabNodeMap`，**不清空 `root._children` 数组**，也不清除子节点的 `_collabNode` 属性。

StrictMode 执行 mount → cleanup → remount 后：
1. binding 的 `root._children` 仍持有 **已销毁的 stale CollabNode**（key 指向旧 editor 的节点）
2. WS 重连，服务端发送 `init` → CollaborationPlugin 的 `onSync` 触发
3. `root.isEmpty()` 检查 `_children.length === 0` → **false**（有 stale children）
4. `root._xmlText._length === 0` 检查 → 可能 **false**（stale children 的 XmlText 仍嵌入在 root XmlText 中）
5. **bootstrap 被跳过**，编辑器保持默认空状态
6. 后续用户输入时，`syncLexicalUpdateToYjs` 用 `this._children[index]` 取到 **stale child**，文本写入错误的 XmlText

**排查方法**: 在 `doSave` 中打日志检查 `editor.getEditorState().toJSON()` 的 `root.children[0].children` 是否为空数组（无 text 节点）。

**解决方案**:

1. **yjs-provider.ts `connect()` 方法**: 在 WS 重连前清除 stale children
```typescript
connect(): void {
    this.closed = false;
    try {
      const rootXmlText = this.doc.get('root', Y.XmlText);
      const collabNode = rootXmlText._collabNode;
      if (collabNode && collabNode._children.length > 0) {
        collabNode._children.length = 0;
      }
    } catch { /* binding may not exist yet */ }
    this._connect();
}
```

2. **document-editor.tsx 新增 `BindingFixPlugin`**: 作为兜底，检测到编辑器为空且 `document.content` 有内容时，强制从 REST API 数据恢复编辑器状态。轮询 200ms，provider 就绪后检测，一次性修复后停止。

```tsx
function BindingFixPlugin({ document, providerRef }) {
  const [editor] = useLexicalComposerContext();
  useEffect(() => {
    const timer = setInterval(() => {
      // ... 检查编辑器根节点是否有文本内容
      // 若无内容但有 document.content，用 editor.setEditorState() 恢复
    }, 200);
    return () => clearInterval(timer);
  }, [editor, document, providerRef]);
}
```

**关键原则**: `@lexical/yjs` 的 `CollabElementNode.destroy()` 不清空 `_children` 数组和不清除 `_collabNode` 属性。在 StrictMode 下，这两个遗留会导致 binding 状态与实际 editor 状态不一致。必须在 provider 重连时手动清除 stale children。

---

## 17. Python `id(websocket)` 碰撞：StrictMode 双挂载导致 client_id 复用，room 提前清空

**现象**: 协作表格两个账号同时进入，在线人数一直显示 1，要等数秒后才偶尔显示 2；编辑也无法实时同步。服务端日志显示异常模式：
```
konus connected to doc 25abffce...
konus connected to doc 25abffce...    ← 同一用户 38ms 内连了两次
Room 25abffce... closed (no clients)   ← 但 11ms 后房间就空了
konus disconnected
```
两次 connect 后只有一次 disconnect 日志，房间就被认为空了。

**原因**: 服务端 `collaboration_ws.py` 用 `id(websocket)` 作为 ws 标识构造 client_id：
```python
def _make_client_id(user_id: str, ws_id: int) -> str:
    return f"{user_id}:{ws_id}"

client_id = _make_client_id(user["id"], id(websocket))
```

`id()` 在 CPython 中返回对象的**内存地址**，对象被 GC 后地址可立即被新对象复用（特别是同尺寸对象）。

React StrictMode 下组件挂载顺序为 mount → cleanup → re-mount：
1. mount: `ws1 = new WebSocket()`，Python 端 `id(ws1) = 126349458505792`
2. cleanup: `ws1.close()`，客户端发送 close
3. re-mount: `ws2 = new WebSocket()`，**Python 端 ws1 已被 GC，ws2 复用同一地址** → `id(ws2) = 126349458505792`
4. 服务端两次 accept 都生成相同 client_id `"konus:126349458505792"`
5. 第二次 `room["clients"][client_id] = ws2_info` **覆盖** ws1_info
6. ws1 的 close 到达 → finally 中 `room["clients"].pop(client_id)` 弹出的是 ws2_info（已被覆盖）→ 房间空 → "Room closed"
7. ws2 在客户端仍 OPEN，但**已不在服务端 room 字典中** → 所有 aw/update 消息无法 broadcast → 协作者永远看不到对方

**排查方法**: 看服务端日志的 connect/disconnect 序列。正常 StrictMode 应该看到 connect → disconnect → connect 三次；如果只有 connect → connect → disconnect 且紧接着 Room closed，就是 id() 碰撞。

**解决方案**: 用 UUID 替代 `id(websocket)`：
```python
import uuid

def _make_client_id(user_id: str, ws_uid: str) -> str:
    return f"{user_id}:{ws_uid}"

# 在 handle_ws 中
ws_uid = uuid.uuid4().hex
client_id = _make_client_id(user["id"], ws_uid)
```

**关键原则**: 任何 Python Web 服务中用作 map key 的"连接唯一标识"，**永远不要用 `id(obj)`**。CPython 的内存地址回收机制 + 异步框架的对象生命周期难以预测，几乎必然在边界场景下产生碰撞。改用 `uuid.uuid4().hex` 或单调递增计数器。

---

## 18. React `useEffect` 依赖 epoch 但每次都覆盖：本地编辑被永久静默丢弃

**现象**: A 编辑表格后，B 看不到任何变化；自动保存后清浏览器缓存，所有修改消失。

**原因**: `spreadsheet-editor.tsx` 用 `applyEpochRef` 区分"本地编辑触发的 CommandExecuted"和"应用远端数据时触发的 CommandExecuted"：
```typescript
// CommandExecuted handler
if (remoteEpoch.current === applyEpochRef.current) return; // skip remote-triggered

// useEffect[workbookData]
useEffect(() => {
  applyEpochRef.current = remoteEpoch.current; // ← 每次 useEffect 都覆盖
  // ... apply remote data to Univer ...
}, [workbookData]);
```

问题：`useEffect[workbookData]` 在**每次** workbookData 变更时运行 —— 包括本地 `pushSnapshot` 触发的 `setWorkbookData`。本地编辑流程：
1. 用户输入 → Univer 触发 CommandExecuted
2. handler: `remoteEpoch(0) === applyEpoch(-1)` → 不等 → 调 `pushSnapshot`
3. `pushSnapshot` 内部 `setWorkbookData(newData)` → 触发 useEffect
4. useEffect: `applyEpoch = remoteEpoch = 0`
5. （等等 —— 后续所有本地编辑的 CommandExecuted 都满足 `0 === 0` → 全部 skip）

第一次本地编辑后，applyEpoch 被永久"卡住"在当前 remoteEpoch 值。后续所有 CommandExecuted 被静默跳过 → `pushSnapshot` 不再被调用 → `yMap.set('data', ...)` 不再触发 → Yjs 文档保留初始状态 → 30s 自动保存持久化的是**初始空状态** → 清缓存后服务器加载空 ydoc → 用户数据消失。

**排查方法**: 编辑表格后在浏览器 console 中观察是否看到 `[SpreadsheetCollab]` 的 pushSnapshot 日志；服务端日志看是否有 ws `update` 消息。

**解决方案**: 引入 `lastSeenEpochRef` 跟踪上次处理的 epoch，仅远端变更（epoch 推进）才设置 applyEpoch：
```typescript
const lastSeenEpochRef = useRef<number>(0);

useEffect(() => {
  const currentEpoch = remoteEpoch.current;
  const isRemoteUpdate = currentEpoch !== lastSeenEpochRef.current;
  lastSeenEpochRef.current = currentEpoch;

  if (!isRemoteUpdate) return; // 本地 pushSnapshot 触发：Univer 已是数据源，无需 apply

  applyEpochRef.current = currentEpoch;
  try {
    // ... apply ...
  } finally {
    // ⚠️ 关键：apply 完成后必须重置，否则永久阻塞后续本地编辑
    setTimeout(() => { applyEpochRef.current = -1; }, 50);
  }
}, [workbookData]);
```

**关键原则**:
- 用 ref 做"模式标记"（如"正在应用远端数据"）时，**必须有明确的退出路径**，不能依赖"下次自然变化"。注释中"until the next local edit changes the epoch naturally" 是错误的 —— 本地编辑不会改变 remoteEpoch。
- `useEffect[workbookData]` 会在所有 workbookData 变更时运行，包括本地 setter 触发的。需要区分"远端推送"vs"本地写入"时，用一个独立的 epoch/计数器跟踪来源。

---

## 19. 持久化窗口过大：30s 自动保存导致清缓存丢数据

**现象**: 用户编辑表格后立即清浏览器缓存（或杀浏览器进程），重开后最近 N 秒的修改全部丢失。

**原因**: 协作表格的持久化只依赖两个触发点：
- 30s 间隔的 `setInterval` 调 `PUT /ydoc`
- 用户点击"保存"按钮

如果用户在自动保存完成前关闭/刷新页面，最近 30s 内的编辑全部丢失。Yjs 文档虽然在客户端内存中是最新的，但服务器 MySQL 中的 ydoc 列仍是上一次保存的旧状态。

**解决方案**: 加三层保险：

```typescript
// 1. 本地编辑后 5s 触发防抖保存（在 pushSnapshot 内）
if (yDocRef.current && providerRef.current?.doc) {
  if (saveDebounceRef.current) clearTimeout(saveDebounceRef.current);
  saveDebounceRef.current = setTimeout(() => {
    // PUT /ydoc with latest ydoc_state + content
  }, 5000);
}

// 2. visibilitychange：用户切标签页时立即 flush
const onVisibilityChange = () => {
  if (document.visibilityState === 'hidden') flushSave();
};

// 3. pagehide：用户关闭页面时尽力 flush（mobile Safari 比 beforeunload 可靠）
const onPageHide = () => flushSave();

// 4. 组件卸载（切换文档）时也 flush
return () => { flushSave(); ... };
```

`flushSave` 同时做两件事：HTTP `PUT /ydoc`（写 MySQL）+ WS `sendFullState`（更新服务端 room.full_state，房间清空时再 persist 一次）。

**关键原则**:
- 编辑型协作应用不能只依赖定时保存，必须有：① 编辑防抖保存（频繁但延迟可控）② 切换标签页 flush ③ 关闭页面 flush ④ 卸载 flush。
- `visibilitychange` 比 `beforeunload` 更可靠 —— mobile Safari 上 `beforeunload` 不一定触发；`pagehide` 是替代方案。
- 单纯依靠 `sendFullState` 的 WS 消息不够 —— 服务器 room 是内存状态，进程重启会丢。必须配合 HTTP PUT 写持久化存储。

---

## 20. 新加入者看不到既有用户：Awareness 不重放，需等服务端补 replay

**现象**: A 先打开文档，B 后加入。B 进入后看不到 A 的在线头像（要等约 5s 才出现）。A 也看不到 B 的实时出现。

**原因**: Awareness（光标/在线状态）走 Yjs 的 awareness 协议，前端用 5s 心跳定时广播自己。新加入者 B 连接 WebSocket 后：
1. 服务端给 B 发 `init`（Yjs 文档二进制状态）
2. 服务端广播 `presence`（房间成员列表，但只有 uid/name，没有 awareness 详情）
3. B 想看到 A 的 awareness，必须等 A 的下一次 5s 心跳

这是 awareness 协议的固有延迟，对在线人数显示特别不友好。

**解决方案**: 服务端在 client_dict 中缓存每个用户的最近一条 aw 消息（`last_aw`），新用户加入时立即 replay：
```python
room["clients"][client_id] = {
    "ws": actual_ws,
    "user_id": user["id"],
    "user_name": user["name"],
    "aw_client_id": None,
    "last_aw": None,  # ← 新增：缓存最近一条 aw 原始消息
}

# aw 消息处理时更新缓存
if msg_type == "aw":
    if client_id in room["clients"]:
        room["clients"][client_id]["last_aw"] = raw

# 新用户加入时 replay 既有用户的 aw
for other_cid, other_client in list(room["clients"].items()):
    if other_cid == client_id:
        continue
    last_aw = other_client.get("last_aw")
    if last_aw:
        try:
            await websocket.send(last_aw)  # replay to newcomer
        except Exception:
            pass
```

**前端补强**: 收到 `presence` 消息（房间有其他人）时，立即重播自己的 awareness，不等下一个 5s 心跳 —— 解决"我的 aw 还没缓存到服务端，对方就加入了"的竞态：
```typescript
case 'presence': {
  const onlineList = msg.d as Array<{uid, name}>;
  if (onlineList.length === 0) {
    // 清理所有远端 awareness
  } else if (this.ws?.readyState === WebSocket.OPEN) {
    // 房间里有其他人 → 立即重播自己的 aw
    const enc = this.awareness.encodeLocalState();
    if (enc) this.ws.send(JSON.stringify({ t: 'aw', d: enc }));
  }
}
```

**关键原则**: Yjs awareness 是"软状态"（soft state），依赖客户端心跳维持。新加入者要立即看到既有用户，必须有"状态重放"机制 —— 要么服务端缓存最近状态并主动 replay，要么客户端在感知到房间有变化时主动广播自身状态。

---

## 21. Univer 电子表格图片资源 401 + JWT 泄漏到数据库

**现象**:
1. 协作电子表格导入 xlsx 后，表格中的图片不展示
2. 浏览器 Network 面板里**根本看不到** `/api/v1/collaboration/documents/{id}/assets/{assetId}` 请求，或请求返回 401
3. 数据库 `collaboration_document.content` 字段里的 `SHEET_DRAWING_PLUGIN.resources[].data` JSON 中，图片 URL 形如 `.../assets/xxx?token=Bearer%20Ijg2Y2M1Zj...`（**JWT 永久写入 DB**）

**根因（三个独立 bug 叠加）**:

### Bug A — 后端不剥离 "Bearer " 前缀
前端用 `getAuthorization()` 取 token（含 `Bearer ` 前缀）→ `encodeURIComponent` 后变成 `Bearer%20xxx` → URL 携带进 `<img src>`。后端 `?token=` 拿到 URL decode 后的 `"Bearer Ijg2..."` → `itsdangerous.URLSafeTimedSerializer.loads()` 必然抛异常 → fallback 用原字符串 `access_token=Bearer Ijg2...` 查 DB → 匹配失败 → 401。

**修复**: `api/apps/restful_apis/collaboration_api.py` 的 `get_document_asset` 统一剥离 `Bearer ` 前缀：
```python
if token and token.lower().startswith("bearer "):
    token = token[7:]
```

### Bug B — token 泄漏到 DB
保存路径 `pushSnapshot / saveToServer / 30s 自动保存 / flushSave` 的 `content` 字段直接用了 Univer 的 `save()` 快照，而快照里的图片 URL 是**已注入 JWT 的**（前端 `injectAssetTokens` 注入用于渲染）。保存时虽然 `yMap.set('data', ...)` 调用了 `stripUIState`（内部 `stripAssetTokens`），但 PUT 请求体的 `content` 字段是另一份独立的 `data` 变量，没剥。结果 JWT 通过 `content` 字段持久化进 DB。

更糟：yMap 初始化时直接 `yMap.set('data', JSON.stringify(workbookDataRef.current))`，写入的是**已注入 JWT 的 React state**，JWT 通过 `ydoc_state` 也持久化进 DB。

**修复**: 4 个保存路径的 `content` 字段全部改为 `stripAssetTokens(data)`；yMap 初始化时也 `stripAssetTokens(workbookDataRef.current)`。原则：**yDoc 和 DB 永远存 stripped 版本，只有 React state（Univer 渲染用）保留 injected 版本**。

### Bug C — 加载时陈旧 token 不刷新
前端 `injectAssetTokens` 遇到 URL 已包含 `token=` 时直接 `continue` 跳过，导致旧 session 的 JWT（已过期）一直卡在 URL 中。即使修复 Bug A/B，旧文档里已经持久化的过期 token 还是会让图片 401。

**修复**: inject 模式改为"先剥后注"——无论 URL 是否已含 `token=`，都先正则移除，再注入当前 session 的 JWT。这样旧文档加载后会自愈。

**关键原则**:
- **JWT 不能写入共享状态**（DB / yDoc）—— JWT 是 per-session 的，写入后其他 session / 后续 session 都会拿到无效 token
- **协作数据双层模型**：共享层（stripped，给 yDoc / DB / 多人协作）+ 渲染层（injected，给 Univer 渲染），两层之间通过 `injectAssetTokens` / `stripAssetTokens` 转换
- **`<img src>` 不能携带 Authorization header**，所以必须用 query string token；但 query string 会进 URL → 进 DB → 进日志，必须严格管理生命周期

**测试方法**:
1. 上传含图片的 xlsx → 看响应里 `resources` 是否有 `SHEET_DRAWING_PLUGIN`
2. 看图片 URL 是否含 `?token=`（不应含，应该只在 React state 里含）
3. 浏览器 Network 看图片请求状态码（应 200）
4. 关闭浏览器重新打开同一文档 → 图片应正常展示（验证 token 刷新逻辑）

---

## 22. Univer `Object.keys(null)` 崩溃（空值兜底）

**现象**: 打开电子表格时浏览器报 `Uncaught TypeError: Cannot convert undefined or null to object at Object.keys`，定位不到具体源（在压缩的 Univer chunk 内部）。

**根因**: Univer 的快照（IWorkbookData）在初始化/销毁/资源加载阶段可能返回部分字段为 `undefined` 的对象。我们的代码多处对 `snapshot.sheets / sheet.columnData / sheet.rowData / sheet.cellData` 调用 `Object.keys/entries`，没做空值兜底。

**修复**:
- `stripUIState` 改为 `Object.entries(snapshot.sheets ?? {})`
- 初始 state resolver：`content` 存在但缺 `sheets` / `sheetOrder` 时降级为空白工作簿
- Yjs 远程更新观察者：`parsed?.sheets` 为空时跳过
- `JSON.parse(resource.data)` 可能返回 `null`，加 `typeof` 守卫
- 编辑器增量合并前：`!currentSnapshot?.sheets || !remoteSnapshot?.sheets` 时降级为全量重建

**关键原则**: 与第三方库（Univer）的数据结构交互时，所有 `Object.keys/entries/values` 调用必须做空值兜底。库内部可能在生命周期特定阶段返回不完整对象，TS 类型签名不代表运行时一定有值。

---

## 23. SPA 站点 `page.goto(wait_until="load")` 永不返回：长连接阻止 load 事件

**现象**: 用 Playwright 爬 `https://ggzyjd.fj.gov.cn/case/index/` 时，`page.goto(url, wait_until="load", timeout=60000)` 每次都超时 60s 抛 `TimeoutError`，即使把 `wait_until` 改成 `"networkidle"` 也一样超时。但用调试脚本 `page.query_selector_all("a.list-item")` 实测：**页面其实早就渲染好了（10 个列表项）**，只是 Playwright 一直在等 `load` 事件。

**原因**: 这类政府 SPA 站点（Vue 2.6 + Element UI）通常会加载百度统计 `hm.baidu.com/hm.js?xxx`，该脚本会建立一个**长连接**（持续向服务器上报数据），网络活动永不空闲 → `networkidle` 永远不触发；同时一些懒加载图片/polling 接口也会让 `load` 事件卡住。

**排查方法**:
1. 在容器里跑独立 `_debug_case.py`：`page.goto(url, wait_until="load", timeout=30000)` 外层套 try/except，超时后继续 `page.content()` 看 HTML 长度、`page.query_selector_all(items_path)` 看是否真的有数据。
2. 如果 HTML 长度合理（>30KB）且选择器命中 → 就是 `load` 事件被阻塞，DOM 已就绪。

**解决方案**: 在 `spa_render.py` 的三处 `page.goto`（listing、`fetch_detail` api_request、`_fetch_detail_css`）都改用 `domcontentloaded` + goto 超时容错：
```python
wait_until = "networkidle" if getattr(self._transport, "network_idle", True) else "domcontentloaded"
try:
    page.goto(url, wait_until=wait_until, timeout=self._transport.timeout * 1000)
except Exception as goto_err:
    logging.warning(
        "SpaRenderAdapter: page.goto(%s, wait_until=%s) failed: %s — continuing to check DOM",
        url, wait_until, goto_err,
    )
# 继续走 wait_for_selector + 抽取流程
```

YAML 里显式声明 `transport.network_idle: false`，adapter 切换到 `domcontentloaded`。

**关键原则**:
- `wait_until="load"` / `"networkidle"` 对带 analytics/长连接的站点不可靠，必须配合 try/except 容错，超时不代表页面没渲染好。
- 优先级：`commit`（几乎不用）> `domcontentloaded`（DOM 解析完）> `load`（所有资源加载完）> `networkidle`（500ms 无网络活动）。政府/电商类站点通常只能用 `domcontentloaded`。
- 调试 SPA 爬虫时，独立 debug 脚本（绕过完整 pipeline）是定位"是 goto 卡住还是抽取失败"的最快方法。

---

## 24. HeadlessChrome 默认 UA 泄漏：BrowserPool 必须显式覆盖 User-Agent

**现象**: 同一个 ggzyjd 站点，用 curl 能拿到 200，但 Playwright 渲染时即使 `load` 超时后 DOM 里也没有列表数据（或被反爬跳转到错误页）。

**原因**: `BrowserPool.new_context()` 没传 `user_agent`，Chromium headless 模式默认 UA 是 `Mozilla/5.0 ... HeadlessChrome/125.0.0.0 ...`。`HeadlessChrome` 这个标记被几乎所有反爬系统（包括最简单的 JS 特征检测）一眼识别。

**解决方案**: 在 `browser_pool.py` 的 `new_context()` 里强制设置真实 UA：
```python
self._context = self._browser.new_context(
    viewport={"width": 1920, "height": 1080},
    locale="zh-CN",
    user_agent="Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 "
               "(KHTML, like Gecko) Chrome/125.0.0.0 Safari/537.36",
)
```

**关键原则**:
- 永远不要用 Chromium headless 默认 UA。YAML `transport.headers.User-Agent` **只作用在 HTTP 请求头上，不会影响浏览器 navigator.userAgent**。两者必须分别设置。
- 进阶反爬还需要配合 `--disable-blink-features=AutomationControlled`、`page.add_init_script("Object.defineProperty(navigator, 'webdriver', {get: () => undefined})")` 等 stealth 措施。

---

## 25. CSS extractor 字段语法 `::text` / `::attr(href)` 是 Scrapy 风格，本项目不支持

**现象**: 在 YAML 里给 `spa_render` 站点配：
```yaml
extract:
  type: css_selector
  items_path: "a[href*='case/detail']"
  fields:
    title: "::text"
    url:   "::attr(href)"
    date:  "em::text"
```
运行后 items 全部是空字典，或字段值缺失。

**原因**: `::text` / `::attr(href)` 是 **Scrapy parsel** 的选择器语法（基于 lxml），不是 BeautifulSoup4 的 CSS 选择器。本项目 `extractors/css_selector.py` 用的是 BS4，`el.select_one("::text")` 会抛 "Unsupported selector" 或返回 None。

本项目 CSS extractor 实际支持的语法（看 `css_selector.py::_extract_field`）：
- `"a"` → 选后代 `<a>` 取文本
- `"a@href"` → 选后代 `<a>` 取 href 属性
- `"@href"` → 取**元素自身**的 href 属性
- `.title-col` / `td:last-child` → 标准 CSS 选择器

**另外的问题**: 当 `items_path` 选中的就是 `<a>` 元素本身时，**无法用上述任何语法取到 `<a>` 自身的 text**（BS4 的 `select_one` 只搜后代），只能取后代的 text/attr。

**解决方案**: 对这种"列表项 = `<a>` 元素，title 是 `<a>` 自身的 text，date 是 `<em>` 子元素"的 SPA 结构，改用 `js_extract` 直接在页面上跑 JS 提取（绕开 CSS extractor）：
```yaml
extract:
  type: css_selector
  items_path: "a.list-item"      # 仅用于 wait_for_selector 提示
  js_extract: |
    () => {
      const results = [];
      const links = document.querySelectorAll('a.list-item, a[href*="case/detail"]');
      for (const a of links) {
        const text = (a.textContent || '').trim();
        const em = a.querySelector('em, .time');
        results.push({
          title: text.replace(/\d{4}[-/]\d{1,2}[-/]\d{1,2}/g, '').trim(),
          url:   a.href,
          date:  em ? (em.textContent || '').trim() : '',
          id:    new URL(a.href).searchParams.get('MGUID') || '',
        });
      }
      return results;
    }
```

`spa_render.py` 在 `fetch_items` 中的优先级：API captures → `js_extract` → 通用 DOM 提取（`_extract_from_dom`）。

**关键原则**:
- 不要假设爬虫框架间的选择器语法通用。Scrapy、BeautifulSoup、PyQuery、lxml 各有自己的 DSL。
- 对于 `<a>` 自身 text + 子元素 date 的组合，BS4 CSS extractor 表达不了，必须用 JS（Playwright `page.evaluate`）或换 items_path 选父容器。
- JS 提取片段应参考**同域的老脚本**（`rag/svr/*_crawler.py`），通常已经有人逆向过 DOM 结构。

---

## 26. 顶层 `try/except ImportError` 把多个无关 import 捆绑，一个失败拖累全部

**现象**: 智能采集系统运行时日志打印 `CollectionWriter: CrawlerResultService not available, skipping`，导致 0 行写入 `crawler_result` 表。但直接 `python -c "from api.db.services.crawler_service import CrawlerResultService"` 又能正常 import。

**原因**: `collection_writer.py` 顶部这样写：
```python
try:
    from api.db.services.crawler_service import CrawlerResultService
    from api.db.services.collection_ext_service import (
        CollectionPolicyExtService,
        CollectionPersonnelExtService,
    )
except ImportError:
    CrawlerResultService = None
    CollectionPolicyExtService = None
    CollectionPersonnelExtService = None
```

**两个 import 用同一个 try 块**。当 `collection_ext_service.py` 文件没部署到服务器时（ImportError），`CrawlerResultService` 也被一起设为 `None` —— 即使 `crawler_service` 本身完全没问题。日志只打印 "CrawlerResultService not available"，**完全不提示真正的失败是 collection_ext_service**，排查时被严重误导。

**排查方法**: 单独 import 每个模块，定位真正的失败点：
```bash
docker exec docker-ragflow-cpu-1 python -c '
try:
    from api.db.services.crawler_service import CrawlerResultService
    print("direct OK")
except ImportError as e:
    print("direct ImportError:", e)
try:
    from api.db.services.collection_ext_service import CollectionPolicyExtService
    print("ext OK")
except ImportError as e:
    print("ext ImportError:", e)   # ← 这里会暴露真正的失败
'
```

**解决方案**: 拆成独立的 try/except，让每个服务独立降级；或在 except 里 `logging.exception` 打印完整 traceback：
```python
try:
    from api.db.services.crawler_service import CrawlerResultService
except ImportError:
    logging.exception("CollectionWriter: CrawlerResultService import failed")
    CrawlerResultService = None

try:
    from api.db.services.collection_ext_service import (
        CollectionPolicyExtService, CollectionPersonnelExtService,
    )
except ImportError:
    logging.exception("CollectionWriter: collection_ext_service import failed")
    CollectionPolicyExtService = None
    CollectionPersonnelExtService = None
```

**关键原则**:
- 多个无关 import 共用一个 `try/except ImportError` 是反模式 —— 排查时只看到"XX not available"，无法定位真正缺失的模块。
- 可选依赖降级模式（Optional Dependency Pattern）必须让每个依赖独立失败 + 独立日志，或直接 fail-fast 抛异常（如果是必需依赖）。

---

## 27. 新增 ORM 模型用到 `DateField` 但没加到 peewee import 列表

**现象**: 部署 `db_models.py` 后容器启动崩溃：
```
File "/ragflow/api/db/db_models.py", line 2052, in CollectionPolicyExt
    effective_date = DateField(null=True, help_text="实施日期")
NameError: name 'DateField' is not defined. Did you mean: 'DateTimeField'?
```

**原因**: `db_models.py` 顶部 `from peewee import (...)` 显式列出了 `BigIntegerField`、`CharField`、`DateTimeField`、`IntegerField`、`TextField` 等，但**没有 `DateField`**。老代码里所有模型都用 `DateTimeField`/`BigIntegerField` 存时间，没人用过 `DateField`。新增的 `CollectionPolicyExt.effective_date / expiry_date` 和 `CollectionPersonnelExt.valid_until` 是 `DATE` 类型字段，首次用 `DateField` 就崩了。

**解决方案**: 在 `from peewee import (...)` 列表里加 `DateField`：
```python
from peewee import (
    fn, InterfaceError, OperationalError, ProgrammingError,
    BigIntegerField, BlobField, BooleanField, CharField, CompositeKey,
    DateField,        # ← 新增
    DateTimeField, Field, FloatField, IntegerField, Metadata, Model,
    TextField, PrimaryKeyField,
)
```

**关键原则**:
- 项目用 "显式 import 白名单" 风格时（而非 `from peewee import *`），新增字段类型必须同步更新 import 列表 —— Python 不会自动补全。
- 建议 IDE/CI 加 `ruff` 或 pyright 检测 undefined name；本地 commit 前跑一次 `python -c "from api.db.db_models import *"` 验证模型文件能加载。

---

## 28. 智能采集成套文件部署清单（部署时必须核对）

**现象**: 智能采集系统（新）某次部署后，YAML 加载正常，但运行时要么报 "CrawlerResultService not available"，要么报 "cannot import name CollectionPolicyExt"，要么某个 writer 字段找不到。每次补一个文件又冒出下一个错误。

**原因**: 智能采集系统是横跨 8+ 个文件的成套功能，部署时只 SCP 了部分文件（比如只传了 writer 没传 ORM，或只传了 ORM 没传 service），导致各层代码版本不一致。bind mount 是热更新但**不会自动同步所有相关文件**。

**完整部署清单**（任一改动需一起部署）：

| 类型 | 路径 | 说明 |
|------|------|------|
| ORM 模型 | `api/db/db_models.py` | 末尾 CollectionPolicyExt / CollectionPersonnelExt + migrate_db 的 category 列 |
| 主表 Service | `api/db/services/crawler_service.py` | CrawlerResultService.upsert_result / gen_result_id |
| 扩展表 Service | `api/db/services/collection_ext_service.py` | policy_ext / personnel_ext 的 CRUD |
| REST API | `api/apps/restful_apis/collection_app.py` | trigger / scheduled / results / stats 端点 |
| Writer | `rag/svr/crawler_engine/collection_writer.py` | 写 crawler_result + category 路由扩展表 |
| Storage 管道 | `rag/svr/crawler_engine/storage_pipeline.py` | writer_mode / category / date_filter 参数 |
| Engine | `rag/svr/crawler_engine/engine.py` | run() 透传 writer_mode / category / date_filter |
| Config | `rag/svr/crawler_engine/config.py` | SiteConfig.category + ExtractConfig.js_extract |
| spa_render adapter | `rag/svr/crawler_engine/adapters/spa_render.py` | SPA 渲染 + js_extract + goto 容错 |
| BrowserPool | `rag/svr/crawler_engine/browser_pool.py` | 显式 UA + playwright 单例 |
| CLI 入口 | `rag/svr/unified_crawler.py` | `--writer --category --date-filter` 参数 |
| YAML 配置 | `rag/svr/crawler_sites.yaml` | 站点定义 + category 字段 |

**部署命令模板**（本地 → 服务器）：
```bash
# 1. 打包所有改动文件到 /tmp/crawler_patch/
scp -i "D:/AI/konus-key.pem" -o StrictHostKeyChecking=no \
  rag/svr/crawler_engine/adapters/spa_render.py \
  rag/svr/crawler_engine/config.py \
  rag/svr/crawler_engine/browser_pool.py \
  rag/svr/crawler_engine/collection_writer.py \
  rag/svr/crawler_engine/storage_pipeline.py \
  rag/svr/crawler_engine/engine.py \
  api/db/db_models.py \
  api/db/services/crawler_service.py \
  api/db/services/collection_ext_service.py \
  root@47.98.102.55:/tmp/crawler_patch/

# 2. 服务器上从 staging 复制到 bind mount 源目录
ssh ... "for f in /tmp/crawler_patch/*.py; do
  base=\$(basename \$f);
  case \$base in
    db_models.py|*service.py) cp \$f /home/bid-agent-konus/ragflow2/api/db/services/\$base 2>/dev/null || cp \$f /home/bid-agent-konus/ragflow2/api/db/\$base;;
    spa_render.py) cp \$f /home/bid-agent-konus/ragflow2/rag/svr/crawler_engine/adapters/\$base;;
    *) cp \$f /home/bid-agent-konus/ragflow2/rag/svr/crawler_engine/\$base;;
  esac
done"

# 3. 容器内冒烟测试所有 import
docker exec docker-ragflow-cpu-1 python -c '
from api.db.db_models import CollectionPolicyExt, CollectionPersonnelExt, CrawlerResult
from api.db.services.crawler_service import CrawlerResultService
from api.db.services.collection_ext_service import CollectionPolicyExtService, CollectionPersonnelExtService
from rag.svr.crawler_engine.collection_writer import CollectionWriter
print("all imports OK")
'
```

**关键原则**:
- 横跨多层的功能（如智能采集）改动时，**部署清单必须成套**，单文件 SCP 容易漏。
- 部署后必须用 `python -c "import ..."` 冒烟测试覆盖所有改动文件，不能只看单个文件能否加载。
- 建议在 worktree commit 前 `git diff master --stat` 看全部改动文件列表，据此生成 SCP 清单。

## 29. 前端"智能采集"看不到任务：三个独立列表 API + tenant 过滤连环坑

**现象**: 创建了 ggzyjd_cases 每日采集任务（直接 DB INSERT 到 `scheduled_task` 表），但前端"智能采集 > 采集任务"列表始终为空；改成放开 `scheduled_task_api` 的 tenant 过滤后仍然看不到。

**根本原因（三层坑叠加）**:

1. **前端实际调用的接口不是你以为的那个**。"智能采集"页的 tasks-tab.tsx 调用 `/api/v1/crawl4ai/tasks`（crawl4ai_app.py → `CrawlerTaskService` → **`crawler_task` 表**），**不是** `/api/v1/collection/scheduled`（collection_app.py → `ScheduledTaskService` → `scheduled_task` 表），也**不是** `/api/v1/scheduled-tasks`（scheduled_task_api.py → 同 `scheduled_task` 表）。三套接口三套语义：

   | 前端入口 | API 端点 | 数据表 | Service |
   |---|---|---|---|
   | 智能采集 > 采集任务 / 采集结果 | `/api/v1/crawl4ai/*` | `crawler_task` + `crawler_result` | `CrawlerTaskService` / `CrawlerResultService` |
   | 定时任务管理页 | `/api/v1/scheduled-tasks` | `scheduled_task` | `ScheduledTaskService` |
   | 新智能采集 API（后端设计文档里写的，前端未接） | `/api/v1/collection/*` | `scheduled_task`（writer=collection 标记区分） | `ScheduledTaskService` |

   我把任务插到 `scheduled_task` 表，前端读的是 `crawler_task` 表，永远查不到。

2. **每个列表 API 都按 `current_user.id` 过滤**。即使表对了，非任务创建者本人登录也看不到。三个文件三层过滤：
   - `scheduled_task_api.py::_get_authorized_tenant_ids()` + `list_scheduled_tasks(tenant_id=tenant_ids)`
   - `crawl4ai_app.py::list_tasks(tenant_id=current_user.id)`
   - `crawl4ai_app.py::list_results(tenant_id=current_user.id)` 及 `get/update/delete/trigger/status` 全部 `task.tenant_id != current_user.id` 拒绝
   - Service 层 `CrawlerTaskService.get_list` / `CrawlerResultService.get_list` / `site_options` 把 `tenant_id` 当必填参数，传空字符串会退化成 `WHERE tenant_id IS NULL` 返回 0 行

3. **`crawler_task` 表没有调度字段**（无 cron_expression / interval_seconds），即使插入记录也只能手动触发，每日定时仍要靠 `scheduled_task` 表 + `scheduled_task_executor.py` 真正执行。

**修复方案**（团队共享可见模式，2026-07-22）:

```python
# 1. scheduled_task_api.py — list_scheduled_tasks 不过滤 tenant
objs, total = ScheduledTaskService.get_list(
    page_number=page_number, items_per_page=items_per_page,
    name=name, enabled=enabled,
)  # 不传 tenant_id

def _check_task_ownership(task_id):
    e, obj = ScheduledTaskService.get_by_id(task_id)
    if not e:
        return False, None, "Task not found."
    return True, obj, None  # 不再校验 tenant

# 2. crawl4ai_app.py — 所有端点移除 tenant 校验
#    list_tasks / list_results / get_task / get_result / update / delete /
#    trigger / status / list_sites 全部改成只校验任务存在

# 3. crawler_service.py — Service 层支持 tenant_id="" 全局模式
def get_list(cls, tenant_id: str = "", ...):
    if tenant_id:
        query = cls.model.select().where(cls.model.tenant_id == tenant_id)
    else:
        query = cls.model.select()  # 全局可见
```

同时在 `crawler_task` 表插入 ggzyjd_cases 记录用于前端展示（实际每日调度仍由 `scheduled_task` 触发 unified_crawler.py）。

**关键原则**:
- 改前端列表数据不见与否，**第一步永远先查前端 service 调的哪个 API → 读哪张表**，不要看到 API 名字相似就假设。本项目至少有 3 个名字相近但完全独立的"采集任务"列表接口。
- **DB 直接 INSERT 时务必确认前端读的是不是这张表**。`scheduled_task` ≠ `crawler_task`，两张表 schema 完全不同。
- 内部团队工具的多账号可见性：与其每个 API 都写 `_get_authorized_tenant_ids()` 然后逐一 debug 看不到的权限问题，不如直接放开为"登录即可见"，把审计放在 create 时打 `tenant_id=current_user.id` 即可。
- Service 层 `get_list(tenant_id)` 必须支持 `tenant_id=""` 跳过过滤，否则上层放开过滤也没用。

## 30. 高频站点重复爬虫任务堆满队列：TTL 自愈是错的，必须"跑完才清"（2026-08-08）

**现象**: detector-meta 旁路（A2 方案）上线后主队列仍在膨胀（XLEN ~21000，lag ~8400）。排查发现队列里堆了 **503 条 `detect:<site>` 爬虫任务，68 个站点**，最严重的 `cebpubservice×65`、`ccgp_bxsearch×41`。每条爬虫 ~8min，单 worker 串行消费，重复任务把解析任务的吞吐吃光。

**根本原因**: detector 探测到站点内容变化就无条件入队一条爬虫，**没有"该站是否已有任务在排队/运行"的判断**。高频发标站点（全天持续更新）每个 detect_interval 都命中"有变化"，叠加队列积压（前一条还没轮到跑），就滚雪球式堆出几十条重复。爬虫本身幂等（DB upsert + processed_ids 去重），不会数据错乱，但每条白白烧 ~8min 单 worker 时间。

**第一版错误设计（被否决）**: Redis NX key + 30~60min TTL "自愈"。问题：**纯 TTL 只限制入队频率，不限制队列内数量** —— 队列积压时任务排队时长 >> TTL，key 过期后任务还没跑，下一轮 detector 又能入队一条，重复照旧。TTL 自愈对"排队中"的任务是无效防护。

**正确设计（已部署）**: `crawl:queued:{site}` 标记，生命周期 = "排队中 + 运行中"全程：

```
入队前 (crawler_detector._enqueue_full_crawl):
    SET crawl:queued:{site} NX EX 21600   # 抢不到 → skip 入队
入队失败/异常:
    DEL crawl:queued:{site}               # 回收, 不留孤儿锁
跑完 (task_executor.handle_scheduled_script_task 的 finally):
    DEL crawl:queued:{site}               # 成功/失败/超时三路全覆盖
```

- 标记靠**任务真跑完**才清，不靠短定时器 → 扛任意时长队列积压
- 6h `SAFETY_TTL` 只是孤儿 key 最后保险（容器崩溃消息丢失 / 手动 XTRIM），正常路径永不触发
- Redis NX 异常 fail-open（放行走入队）：宁偶发重复（爬虫幂等），不漏采整站
- 两端 key 格式必须一致：`crawler_detector.CRAWL_QUEUED_KEY` ↔ `task_executor._CRAWL_QUEUED_KEY`，两处注释互相引用

**部署后一次性清理**（关键，否则旧重复仍会被逐条跑掉）: 脚本扫全 stream，每站保留最早 1 条 `detect:` 消息，XDEL 其余 435 条，并给每个保留站点**补设标记**（NX），防止 detector 在积压消化期间又塞新的。清理脚本注意：

- **导入顺序**：`from common import settings` 必须最先，否则 `rag.utils.redis_conn` ↔ `common.settings`（L36 反向 import REDIS_CONN）循环导入 ImportError
- 分页 XRANGE 翻页游标：`next_min = f"{seq}-{int(ms)+1}"`（同毫秒内 seq 递增，不能 ms+1 否则丢同毫秒条目）
- 保留最早的而非最新的：consumer 按先进先出消费，留最早的最先跑完释放标记

**验证**: 部署后日志出现 `detector: crawl already queued/running for site=cebpubservice; skip duplicate enqueue`（该站又探测到变化但被正确拦下）；XLEN 21188 → 20753，剩余 detect: 恰好 68 条（每站 1 条）。

**关键原则**:
- 给"排队中任务"做去重/限流，**防护的生命周期必须覆盖排队全程**。任何短于"最坏排队时长"的 TTL 自愈都是假防护 —— 想清楚"key 过期时任务在哪"。
- 清理队列积压时，删重复消息只是第一步；**必须同时重建去重状态**（本例的标记），否则清理窗口内新任务又会进来。
- task_executor 有 `get_unacked_iterator`：重启后 pending 消息会重新投递，所以"跑完 DEL"路径在容器重启场景也能闭环（消息不会凭空消失）。
- 相关文件：`rag/svr/crawler_detector.py`、`rag/svr/task_executor.py`；归档文档 `D:\AI\ragflow2\crawl-dedup-爬虫排队去重方案.md`。

