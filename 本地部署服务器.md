---
name: reference_full_deploy
description: 一键部署前端+后端到服务器的完整流程和注意事项（给新LLM会话使用）
type: reference
---

# 完整部署指南

## 服务器信息

- **IP**: `47.98.102.55`
- **用户**: `root`
- **SSH 密钥**: `D:\AI\konus-key.pem`
- **项目路径**: `/home/bid-agent-konus/ragflow2/`
- **容器名**: `docker-ragflow-cpu-1`
- **SCP 命令**: `scp -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no <local> root@47.98.102.55:<remote>`
- **SSH 命令**: `ssh -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no root@47.98.102.55 "<command>"`

## Docker Bind Mount 映射

以下宿主机路径直接映射到容器内，SCP 到宿主机即可热更新（无需重启容器）：

| 宿主机 | 容器内 |
|--------|--------|
| `/home/bid-agent-konus/ragflow2/rag` | `/ragflow/rag` |
| `/home/bid-agent-konus/ragflow2/api` | `/ragflow/api` |
| `/home/bid-agent-konus/ragflow2/agent` | `/ragflow/agent` |
| `/home/bid-agent-konus/ragflow2/common` | `/ragflow/common` |
| `/home/bid-agent-konus/ragflow2/deepdoc` | `/ragflow/deepdoc` |
| `/home/bid-agent-konus/ragflow2/web/dist` | `/ragflow/web/dist` |

### ⚠️ 关键注意事项：Bind Mount Inode 陷阱

Docker bind mount 绑定的是**目录的 inode**，而非路径字符串。

**会破坏 inode 的误操作**：`mv dist dist.old && tar -xzf dist.tar.gz` — 创建了新的 dist 目录（新 inode），容器仍指向旧 inode（原 dist 现在是 dist.old），导致 403 Forbidden。

**不会破坏 inode 的正确操作**：`rm -rf dist/*` — 只删目录内容，目录本身的 inode 不变，无需重建容器。
```bash
rm -rf dist/* dist/.[!.]* dist/..?* 2>/dev/null
tar -xzf dist.tar.gz
```

**不要用 `docker compose up -d --force-recreate`**：此命令删除并重建容器，导致 entrypoint 全部重跑（包括安装 ddddocr/croniter 等依赖），耗时且容易卡住。`rm -rf dist/*` 不改变 inode，直接用 `docker exec ... nginx -s reload` 即可。

## 前端部署（web/dist/）

### 构建
```bash
cd "D:\AI\ragflow2\web" && npm run build
```

### 上传 + 原地解压 + nginx reload
```bash
# 1. 打包
cd "D:\AI\ragflow2\web" && tar -czf dist.tar.gz dist/

# 2. 上传到服务器
scp -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no "D:\AI\ragflow2\web\dist.tar.gz" "root@47.98.102.55:/home/bid-agent-konus/ragflow2/web/dist.tar.gz"

# 3. 清空旧 dist 内容 + 原地解压（rm -rf dist/* 删内容不删目录，inode 不变）
ssh -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no root@47.98.102.55 "cd /home/bid-agent-konus/ragflow2/web && rm -rf dist/* dist/.[!.]* dist/..?* 2>/dev/null; tar -xzf dist.tar.gz && rm -f dist.tar.gz"

# 4. nginx reload（dist 是 bind mount，解压后容器内直接可见，无需重启容器）
ssh -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no root@47.98.102.55 "docker exec docker-ragflow-cpu-1 cp -f /etc/nginx/conf.d/ragflow.conf.python /etc/nginx/conf.d/ragflow.conf && docker exec docker-ragflow-cpu-1 nginx -s reload"
```

### 验证
```bash
ssh -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no root@47.98.102.55 "docker exec docker-ragflow-cpu-1 ls /ragflow/web/dist/ | head -5"
```
如果显示 `total 0` 或无文件 → bind mount 断开了，检查是否 mv 创建了新目录。

## 后端部署（Python 文件）

由于 bind mount，直接 SCP 到宿主机对应路径即可。但如果修改了核心模块，需要重启容器让它生效。

### 上传后端文件
```bash
# agent/
scp -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no "D:\AI\ragflow2\agent\component\llm.py" "root@47.98.102.55:/home/bid-agent-konus/ragflow2/agent/component/llm.py"

# api/
scp -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no "D:\AI\ragflow2\api\apps\restful_apis\agent_api.py" "root@47.98.102.55:/home/bid-agent-konus/ragflow2/api/apps/restful_apis/agent_api.py"
# ... 其他 api 文件同理

# rag/
scp -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no "D:\AI\ragflow2\rag\llm\chat_model.py" "root@47.98.102.55:/home/bid-agent-konus/ragflow2/rag/llm/chat_model.py"
scp -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no "D:\AI\ragflow2\rag\svr\task_executor.py" "root@47.98.102.55:/home/bid-agent-konus/ragflow2/rag/svr/task_executor.py"
```

### docker/nginx 配置文件
这些 NOT bind mount，需要额外步骤：
```bash
scp -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no "D:\AI\ragflow2\docker\docker-compose.yml" "root@47.98.102.55:/home/bid-agent-konus/ragflow2/docker/docker-compose.yml"
scp -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no "D:\AI\ragflow2\docker\nginx\ragflow.conf.python" "root@47.98.102.55:/home/bid-agent-konus/ragflow2/docker/nginx/ragflow.conf.python"
```

### 重启容器使后端生效
```bash
ssh -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no root@47.98.102.55 "docker restart docker-ragflow-cpu-1"
```

## 完整部署命令（一键脚本）

以下是一次性执行全部部署的命令序列：

```bash
# === 前端 ===
# 1. 构建
cd "D:\AI\ragflow2\web" && npm run build

# 2. 打包上传
cd "D:\AI\ragflow2\web" && tar -czf dist.tar.gz dist/
scp -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no "D:\AI\ragflow2\web\dist.tar.gz" "root@47.98.102.55:/home/bid-agent-konus/ragflow2/web/dist.tar.gz"

# 3. 解压 + nginx reload（rm -rf dist/* 不改变 inode，无需重建容器）
ssh -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no root@47.98.102.55 "cd /home/bid-agent-konus/ragflow2/web && rm -rf dist/* dist/.[!.]* dist/..?* 2>/dev/null; tar -xzf dist.tar.gz && rm -f dist.tar.gz && docker exec docker-ragflow-cpu-1 cp -f /etc/nginx/conf.d/ragflow.conf.python /etc/nginx/conf.d/ragflow.conf && docker exec docker-ragflow-cpu-1 nginx -s reload"

# === 后端 ===
# 上传所有修改过的 Python 文件（SCP 多文件）
# ...根据实际 git status 决定上传哪些文件...

# 重启容器使 Python 变更生效
ssh -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no root@47.98.102.55 "docker restart docker-ragflow-cpu-1"

# === 清理 ===
rm -f "D:\AI\ragflow2\web\dist.tar.gz"
```

## Nginx 配置说明

- Nginx 配置位于容器内 `/etc/nginx/conf.d/ragflow.conf`
- 宿主机上的模板: `docker/nginx/ragflow.conf.python`
- 容器启动时 entrypoint 会自动复制 `.python` → `.conf`，但手动更新后需要：
  ```bash
  docker exec docker-ragflow-cpu-1 cp -f /etc/nginx/conf.d/ragflow.conf.python /etc/nginx/conf.d/ragflow.conf
  docker exec docker-ragflow-cpu-1 nginx -s reload
  ```
- `root /ragflow/web/dist;` — 前端静态文件由 nginx 直接 serve
- Admin 路径: `/5d41402abc4b2a76b9719d911017c592/`
- 有 hash 的静态资源有 10 年缓存（immutable）
- `try_files $uri $uri/ /index.html;` — SPA fallback

### Nginx 地址映射速查表

Crawl4AI 管理与 RAGFlow 后台使用**同一套 hash 前缀** `/5d41402abc4b2a76b9719d911017c592/`，无需额外认证。

| 外部 URL（IP + hash 前缀） | Nginx location | 代理到（Docker 内部） | 用途 |
|---|---|---|---|
| `/5d41402.../crawl4ai/` | `^~ /5d41402.../crawl4ai/` | `crawl4ai:11235` | ★ crawl4ai 引擎 Dashboard + Playground + API |
| `/5d41402.../crawler/` | `^~ /5d41402.../crawler/` | `crawl4ai-service:8001` | 爬虫调度管理 REST API（未来） |
| `/5d41402.../monitor/` | `^~ /5d41402.../monitor/` | `grafana:3000` | Grafana 监控面板（未来） |
| `/5d41402.../` 其他 | `^~ /5d41402.../` | `/ragflow/web/dist/` | RAGFlow Admin SPA（已有） |
| `/api/v1/*` | `~ ^/(v1\|api)` | `localhost:9380` | RAGFlow 后端 API（已有） |

### Crawl4AI Nginx 配置片段

在 `ragflow.conf.python` 的 `server` 块中，**放在 Admin SPA catch-all 之前**（Nginx 最长前缀优先匹配）。使用 Docker 内置 DNS resolver (`127.0.0.11`) + 变量 proxy_pass 实现运行时解析，nginx 启动时上游容器不必已经存在：

```nginx
    # Docker internal DNS resolver (allow upstreams to not exist at startup)
    resolver 127.0.0.11 valid=30s;

    # =====================================================================
    # Crawl4AI Service — 爬虫调度管理 (未来)
    # =====================================================================
    location ^~ /5d41402abc4b2a76b9719d911017c592/crawler/ {
        set $crawler_upstream crawl4ai-service:8001;
        proxy_pass http://$crawler_upstream/;
        include proxy.conf;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # =====================================================================
    # Crawl4AI Engine — 内置 Dashboard + Playground + REST API
    # =====================================================================
    location ^~ /5d41402abc4b2a76b9719d911017c592/crawl4ai/ {
        set $crawl4ai_upstream crawl4ai:11235;
        proxy_pass http://$crawl4ai_upstream/;
        include proxy.conf;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # =====================================================================
    # Grafana 监控 (未来)
    # =====================================================================
    location ^~ /5d41402abc4b2a76b9719d911017c592/monitor/ {
        set $monitor_upstream grafana:3000;
        proxy_pass http://$monitor_upstream/;
        include proxy.conf;
    }

    # =====================================================================
    # Admin SPA catch-all (已有，保持不变)
    # =====================================================================
    location ^~ /5d41402abc4b2a76b9719d911017c592/ {
        alias /ragflow/web/dist/;
        ...
    }
```

> **关键设计**：`resolver 127.0.0.11` 是 Docker 内置 DNS。`set $upstream ...` + `proxy_pass http://$upstream/` 强制 nginx 在**请求时**才解析主机名，而不是启动时。这样即使 crawl4ai 容器还没部署，nginx 也能正常启动。

### 初次部署 Nginx 配置步骤

```bash
# 1. 本地编辑 ragflow.conf.python，在 Admin SPA catch-all 之前追加上述两个 location block
#    注意: crawler 和 monitor 的 ^~ block 必须在 Admin SPA 的 ^~ block 之前
#    文件: D:\AI\ragflow2\docker\nginx\ragflow.conf.python

# 2. 上传 Nginx 配置
scp -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no "D:\AI\ragflow2\docker\nginx\ragflow.conf.python" "root@47.98.102.55:/home/bid-agent-konus/ragflow2/docker/nginx/ragflow.conf.python"

# 3. 容器内应用
ssh -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no root@47.98.102.55 "docker exec docker-ragflow-cpu-1 cp -f /etc/nginx/conf.d/ragflow.conf.python /etc/nginx/conf.d/ragflow.conf && docker exec docker-ragflow-cpu-1 nginx -s reload"

# 4. 验证
ssh -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no root@47.98.102.55 "curl -s http://localhost/5d41402abc4b2a76b9719d911017c592/crawler/api/v1/health && echo '' && curl -s http://localhost/5d41402abc4b2a76b9719d911017c592/monitor/api/health"
```

### 生产访问 URL

| 用途 | URL |
|------|-----|
| crawl4ai 引擎 Dashboard | `http://47.98.102.55/5d41402abc4b2a76b9719d911017c592/crawl4ai/dashboard` |
| crawl4ai 调试器 | `http://47.98.102.55/5d41402abc4b2a76b9719d911017c592/crawl4ai/playground` |
| crawl4ai 健康检查 | `http://47.98.102.55/5d41402abc4b2a76b9719d911017c592/crawl4ai/health` |
| crawl4ai API 文档 | `http://47.98.102.55/5d41402abc4b2a76b9719d911017c592/crawl4ai/schema` |
| 爬虫调度管理（未来） | `http://47.98.102.55/5d41402abc4b2a76b9719d911017c592/crawler/` |
| Swagger API 文档（未来） | `http://47.98.102.55/5d41402abc4b2a76b9719d911017c592/crawler/docs` |
| Grafana 监控（未来） | `http://47.98.102.55/5d41402abc4b2a76b9719d911017c592/monitor/` |

> 域名 `bidagent.konusmumu.com` 也可用，直接替换 IP 即可。

## MCP Server（Hermes 集成）

MCP Server 代码在 `rag/svr/mcp_server.py`，无外部依赖，随项目部署。迁移服务器时需要：

### 新服务器上配置 Hermes MCP

1. 确认 `python3` 可用（版本 ≥ 3.10）
2. 确认 RAGFlow API 端口已映射到宿主机（默认 9380）
3. 在 `/root/.hermes/config.yaml` 末尾添加：

```yaml
mcp_servers:
  ragflow:
    command: python3
    args:
      - /home/bid-agent-konus/ragflow2/rag/svr/mcp_server.py
```

4. 如需自定义 RAGFlow API 地址，加 env：
```yaml
mcp_servers:
  ragflow:
    command: python3
    args:
      - /home/bid-agent-konus/ragflow2/rag/svr/mcp_server.py
    env:
      RAGFLOW_BASE_URL: http://<ip>:9380/api/v1
```

5. 重启 Hermes：`hermes gateway restart`
6. 验证：`echo '{"jsonrpc":"2.0","id":1,"method":"tools/list"}' | python3 /path/to/mcp_server.py`

### 注意事项
- 密码/API key 不在 MCP 代码或配置中，由 LLM 调用时通过参数传入（`api_key` 字段）
- MCP server 用 stdio 模式，Hermes 自动管理进程生命周期
- 升级 Hermes 不会覆盖 `config.yaml`，但迁移时记得检查

## 常见问题排查

| 症状 | 原因 | 解决 |
|------|------|------|
| 403 Forbidden | bind mount 断开（inode 变了） | 原地解压 dist，不要 mv |
| 前端不更新 | nginx 缓存 / 浏览器缓存 | nginx reload + 强制刷新 |
| 后端代码不生效 | Flask 没重启 | `docker restart docker-ragflow-cpu-1` |
| 部署后页面加载慢 | 首次访问无缓存 | 正常现象，chunk 已优化为 vendor 合并 |

## 移动端 App 下载部署

### 项目信息

- **项目路径**: `D:\AI\ragflow2\bidding_app\`
- **框架**: Flutter 3.44.2 / Dart 3.12.2
- **架构**: Riverpod 状态管理 + Dio HTTP/SSE + go_router 路由
- **功能**: 3 个底部 Tab — 聊天(SSE 流式对话)、工具(费用计算器)、招标(标书搜索)
- **Android**: AGP 8.7.0, Gradle 8.10.2, Kotlin 2.1.0, NDK 28.2.13676358, compileSdk 36
- **JDK**: OpenJDK 17 (`C:/Program Files/ojdkbuild/java-17-openjdk-17.0.3.0.6-1`)
- **SDK 路径**: `F:\code` (Android SDK, 无 Android Studio)
- **gradle.properties 关键配置**:
  - `org.gradle.java.home=C:/Program Files/ojdkbuild/java-17-openjdk-17.0.3.0.6-1`
  - `kotlin.incremental=false`（跨盘符增量编译 bug）
  - `disable-abi-filtering=true`

### 服务器端初始化（仅需一次）

```bash
# 1. 在容器内创建 downloads 目录
ssh -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no root@47.98.102.55 "docker exec docker-ragflow-cpu-1 mkdir -p /ragflow/downloads"

# 2. 上传 nginx downloads 配置并应用
scp -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no "D:\AI\ragflow2\docker\nginx\ragflow.conf.python" "root@47.98.102.55:/home/bid-agent-konus/ragflow2/docker/nginx/ragflow.conf.python"
ssh -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no root@47.98.102.55 "docker exec docker-ragflow-cpu-1 cp -f /etc/nginx/conf.d/ragflow.conf.python /etc/nginx/conf.d/ragflow.conf && docker exec docker-ragflow-cpu-1 nginx -s reload"

# 3. 上传 iOS 安装说明页面到容器
scp -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no "D:\AI\ragflow2\bidding_app\server\ios-install.html" "root@47.98.102.55:/tmp/ios-install.html"
ssh -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no root@47.98.102.55 "docker cp /tmp/ios-install.html docker-ragflow-cpu-1:/ragflow/downloads/ios-install.html && rm -f /tmp/ios-install.html"

# 4. 上传 iOS OTA manifest（等 IPA 准备好后）
scp -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no "D:\AI\ragflow2\bidding_app\server\manifest.plist" "root@47.98.102.55:/tmp/manifest.plist"
ssh -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no root@47.98.102.55 "docker cp /tmp/manifest.plist docker-ragflow-cpu-1:/ragflow/downloads/manifest.plist && rm -f /tmp/manifest.plist"
```

### Flutter 构建环境（中国大陆特殊配置）

由于 `storage.googleapis.com` 和 `pub.dev` 在中国大陆被墙，构建时必须使用 Flutter 中国镜像，并处理以下问题：

**1. 设置环境变量（每次构建必须）**
```bash
export FLUTTER_STORAGE_BASE_URL="https://storage.flutter-io.cn"
export PUB_HOSTED_URL="https://pub.flutter-io.cn"
export JAVA_HOME="C:/Program Files/ojdkbuild/java-17-openjdk-17.0.3.0.6-1"
export PATH="$JAVA_HOME/bin:$PATH"
```

**2. NDK 安装**（AGP 8.7.0 + Flutter 3.44.2 需要 NDK 28.2.13676358）
```bash
# 使用 Java 17 安装 NDK
"F:/code/cmdline-tools/latest/cmdline-tools/bin/sdkmanager.bat" --sdk_root="F:/code" "ndk;28.2.13676358"
# NDK 安装在: F:\code\ndk\28.2.13676358\
```

**3. lucide_icons Dart 3.12 兼容性修复**（每次 `flutter pub get` 后需重新执行）
```bash
# icon_data.dart: 删除 extends IconData（Dart 3.12 不允许继承 final class）
echo "import 'package:flutter/widgets.dart';" > "$PUB_CACHE/lucide_icons-0.257.0/lib/src/icon_data.dart"

# lucide_icons.dart: 替换所有 LucideIconData(0xNNNN) → IconData(0xNNNN, ...)
sed -i 's/const LucideIconData(0x\([0-9a-fA-F]*\))/const IconData(0x\1, fontFamily: '\''Lucide'\'', fontPackage: '\''lucide_icons'\'')/g' \
  "$PUB_CACHE/lucide_icons-0.257.0/lib/lucide_icons.dart"

# 删除 import "src/icon_data.dart";
sed -i '/import "src\/icon_data.dart";/d' "$PUB_CACHE/lucide_icons-0.257.0/lib/lucide_icons.dart"
```
注意：`PUB_CACHE` 路径取决于使用的 Pub 镜像，中国镜像为 `C:/Users/lg186/AppData/Local/Pub/Cache/hosted/pub.flutter-io.cn/lucide_icons-0.257.0`。

**4. kotlin.incremental=false**（跨盘符增量编译 bug，已在 gradle.properties 中配置）
```properties
# D:\AI\ragflow2\bidding_app\android\gradle.properties
kotlin.incremental=false
```

**5. 预先下载 Flutter 引擎工件**
```bash
cd D:\AI\ragflow2\bidding_app
C:/Users/lg186/flutter/bin/flutter.bat precache --android
```

### 构建并部署 APK（完整流程）

```bash
# === 一键构建 + 部署 ===
export JAVA_HOME="C:/Program Files/ojdkbuild/java-17-openjdk-17.0.3.0.6-1"
export PATH="$JAVA_HOME/bin:$PATH"
export FLUTTER_STORAGE_BASE_URL="https://storage.flutter-io.cn"
export PUB_HOSTED_URL="https://pub.flutter-io.cn"

cd D:/AI/ragflow2/bidding_app

# 清理（可选，出问题时加上 android/.gradle android/build）
rm -rf build .dart_tool

# 构建 debug APK
C:/Users/lg186/flutter/bin/flutter.bat build apk --debug

# 上传到服务器（注意：文件名必须与 .env.production 中 VITE_ANDROID_DOWNLOAD_URL 一致）
scp -i "D:/AI/konus-key.pem" -o StrictHostKeyChecking=no \
  "build/app/outputs/flutter-apk/app-debug.apk" \
  "root@47.98.102.55:/tmp/bidding-app.apk"

# 放入容器 downloads 目录
ssh -i "D:/AI/konus-key.pem" -o StrictHostKeyChecking=no root@47.98.102.55 \
  "docker cp /tmp/bidding-app.apk docker-ragflow-cpu-1:/ragflow/downloads/bidding-app.apk && rm -f /tmp/bidding-app.apk"

# 验证下载
curl -m 10 -I "http://47.98.102.55/downloads/bidding-app.apk"
# 预期: HTTP/1.1 200 OK, Content-Type: application/vnd.android.package-archive
```

### iOS IPA 部署（需 Apple 开发者证书后）

```bash
# 1. 在 Mac 上构建 IPA（需 Xcode + 开发者证书）
cd bidding_app && flutter build ipa --export-method enterprise

# 2. 上传到服务器
scp build/ios/ipa/bidding_app.ipa root@47.98.102.55:/tmp/

# 3. 放入容器 downloads 目录
ssh root@47.98.102.55 "docker cp /tmp/bidding_app.ipa docker-ragflow-cpu-1:/ragflow/downloads/bidding-app.ipa"

# 4. 更新 manifest.plist 中的 IPA URL（如果需要）
```

### 下载地址

| 平台 | URL |
|------|-----|
| Android | `http://47.98.102.55/downloads/bidding-app.apk` (151MB, 2026-06-13 构建) |
| iOS 安装说明 | `http://47.98.102.55/downloads/ios-install.html` |
| iOS OTA 安装 | `itms-services://?action=download-manifest&url=https://47.98.102.55/downloads/manifest.plist`（待 IPA） |

### ⚠️ 注意事项

- 容器内 `/ragflow/downloads/` 目录**不在 bind mount 范围内**，容器重启后文件会丢失
- 容器重启后需重新 `docker cp` APK 文件进去
- **永久方案**: 在 `docker-compose.yml` 中添加 `- ./downloads:/ragflow/downloads` bind mount，然后重启容器
- iOS 企业签名分发需要 Apple Developer Enterprise Program ($299/年) 或第三方签名服务
- 环境变量 `VITE_ANDROID_DOWNLOAD_URL` / `VITE_IOS_DOWNLOAD_URL` 控制前端二维码 URL，迁移服务器时修改 `.env.production`

---

## Crawl4AI 独立爬虫服务部署

> 详细方案: `D:\AI\ragflow2\crawl4ai-service-独立部署方案.md`
> crawl4ai 源码: `D:\AI\ragflow2\crawl4ai\`
> 部署配置: `D:\AI\ragflow2\crawl4ai_service\` (docker-compose.yml + config.yml)

### 架构概览

```
服务器 47.98.102.55

┌──────────────────────────────────────────────────────┐
│                  Docker Network: docker_ragflow       │
│                                                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐   │
│  │  mysql   │  │  redis   │  │ ragflow-server    │   │
│  │  :3306   │  │  :6379   │  │ :9380             │   │
│  └────┬─────┘  └────┬─────┘  └────────┬─────────┘   │
│       │             │                │              │
│  ┌────┴─────────────┴────────────────┴──────────┐   │
│  │          NEW: Crawl4AI Stack                  │   │
│  │                                               │   │
│  │  ┌────────────┐  ┌─────────────────────┐      │   │
│  │  │ crawl4ai   │  │ crawl4ai-service     │      │   │
│  │  │ :11235     │←─│ :8001 (调度+API)     │      │   │
│  │  │ dashboard  │  │ /dashboard (业务监控) │      │   │
│  │  │ playground │  │ /docs (Swagger API)   │      │   │
│  │  └────────────┘  └─────────────────────┘      │   │
│  │                                               │   │
│  │  ┌──────────┐  ┌──────────┐                   │   │
│  │  │prometheus│  │ grafana  │                   │   │
│  │  │ :9090    │  │ :3000    │                   │   │
│  │  └──────────┘  └──────────┘                   │   │
│  └───────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────┘
```

### 关键设计

- **不拉云端镜像**：从本地 crawl4ai 源码构建 Docker 镜像，方便二次开发
- crawl4ai Redis 指向 RAGFlow 的 Valkey (`redis:6379`, 密码 `infini_rag_flow`, DB 1)
- crawl4ai 自己的 SQLite 缓存通过 Docker volume 持久化
- `CRAWL4AI_API_TOKEN` 设为固定 token → gunicorn 监听 0.0.0.0（Docker 网络可达）
- 容器不映射端口到宿主机，所有外部访问经过 Nginx hash 前缀路由

### 服务器路径

| 路径 | 内容 |
|------|------|
| `/home/bid-agent-konus/crawl4ai/` | crawl4ai 源码（Docker build context） |
| `/home/bid-agent-konus/crawl4ai_service/` | docker-compose.yml + 定制 config.yml |
| `/home/bid-agent-konus/ragflow2/docker/nginx/ragflow.conf.python` | Nginx 配置 |

---

### 首次部署

#### 1. 上传 crawl4ai 源码并构建镜像

```bash
# 1a. 本地压缩源码（排除 .git 等无关目录）
cd D:\AI\ragflow2
tar -czf crawl4ai_src.tar.gz --exclude='.git' --exclude='__pycache__' --exclude='*.pyc' --exclude='.pytest_cache' --exclude='node_modules' crawl4ai/

# 1b. 上传到服务器
scp -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no D:/AI/ragflow2/crawl4ai_src.tar.gz root@47.98.102.55:/home/bid-agent-konus/

# 1c. 解压
ssh -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no root@47.98.102.55 "cd /home/bid-agent-konus && rm -rf crawl4ai && tar -xzf crawl4ai_src.tar.gz && rm crawl4ai_src.tar.gz"

# 1d. 上传 docker-compose.yml + config.yml
ssh -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no root@47.98.102.55 "mkdir -p /home/bid-agent-konus/crawl4ai_service"
scp -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no "D:\AI\ragflow2\crawl4ai_service\docker-compose.yml" "D:\AI\ragflow2\crawl4ai_service\config.yml" root@47.98.102.55:/home/bid-agent-konus/crawl4ai_service/

# 1e. 构建镜像（首次 ~15-20 分钟，含 Playwright + Chromium）
ssh -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no root@47.98.102.55 "cd /home/bid-agent-konus/crawl4ai_service && docker compose build --no-cache"
```

#### 2. 启动

```bash
ssh -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no root@47.98.102.55 "cd /home/bid-agent-konus/crawl4ai_service && docker compose up -d"
```

#### 3. 验证

```bash
# 健康检查（通过 Nginx 外部访问）
curl -s http://47.98.102.55/5d41402abc4b2a76b9719d911017c592/crawl4ai/health

# 或直接容器内检查
ssh -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no root@47.98.102.55 "docker exec crawl4ai curl -s http://localhost:11235/health"

# 查看启动日志
ssh -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no root@47.98.102.55 "docker logs --tail=30 crawl4ai"
```

---

### 日常部署（源码更新）

Docker 有层缓存机制，不需要每次全量构建：

#### 场景 A：只改了 crawl4ai 源码（Dockerfile 和依赖不变）

```bash
# 上传新源码
cd D:\AI\ragflow2
tar -czf crawl4ai_src.tar.gz --exclude='.git' --exclude='__pycache__' --exclude='*.pyc' crawl4ai/
scp -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no D:/AI/ragflow2/crawl4ai_src.tar.gz root@47.98.102.55:/home/bid-agent-konus/
ssh -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no root@47.98.102.55 "cd /home/bid-agent-konus && rm -rf crawl4ai && tar -xzf crawl4ai_src.tar.gz && rm crawl4ai_src.tar.gz"

# 增量构建（只重建 COPY 及之后的层，约 2 分钟）
ssh -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no root@47.98.102.55 "cd /home/bid-agent-konus/crawl4ai_service && docker compose build"
# 然后重启
ssh -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no root@47.98.102.55 "cd /home/bid-agent-konus/crawl4ai_service && docker compose up -d"
```

#### 场景 B：源码没变，只是重启

```bash
# 直接用已有镜像启动（几秒）
ssh -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no root@47.98.102.55 "cd /home/bid-agent-konus/crawl4ai_service && docker compose up -d"
```

#### 场景 C：依赖变了（requirements.txt / pyproject.toml 修改）

```bash
# 需要 --no-cache 跳过 pip install 层缓存
ssh -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no root@47.98.102.55 "cd /home/bid-agent-konus/crawl4ai_service && docker compose build --no-cache"
ssh -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no root@47.98.102.55 "cd /home/bid-agent-konus/crawl4ai_service && docker compose up -d"
```

#### Docker 缓存总结

```
基础镜像 (python:3.12)     → 不变，永久缓存
├── apt 系统包              → 不变，永久缓存
├── pip install             → requirements.txt 变化才重建
├── COPY crawl4ai           → 源码变化才重建
└── Playwright browsers     → 不变，永久缓存
```

> **最佳实践**：日常爬虫业务逻辑开发放到 `crawl4ai_service/`（自研 FastAPI 调度服务），通过 bind mount 热更新。crawl4ai 引擎本身作为稳定的底层，只在需要升级时才 rebuild。

---

### 自研调度服务部署（crawl4ai-service, 未来）

> crawl4ai-service 是自研的 FastAPI + APScheduler 调度服务，负责定时任务、结果入库（MySQL）、KB 上传。这部分用 bind mount 热部署。

```bash
# 上传修改的文件
scp -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no "D:\AI\ragflow2\crawl4ai_service\main.py" "root@47.98.102.55:/home/bid-agent-konus/crawl4ai-service/main.py"

# 重启服务（无需 rebuild）
ssh -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no root@47.98.102.55 "docker restart crawl4ai-service"

# 查看日志确认
ssh -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no root@47.98.102.55 "docker logs --tail=30 crawl4ai-service"
```

---

### 监控入口

| 入口 | URL | 用途 |
|------|-----|------|
| crawl4ai 引擎 Dashboard | `http://47.98.102.55/5d41402abc4b2a76b9719d911017c592/crawl4ai/dashboard` | 浏览器池、请求跟踪、系统健康 |
| crawl4ai 调试器 | `http://47.98.102.55/5d41402abc4b2a76b9719d911017c592/crawl4ai/playground` | 可视化配置提取策略 |
| crawl4ai 健康检查 | `http://47.98.102.55/5d41402abc4b2a76b9719d911017c592/crawl4ai/health` | 服务存活检查 |
| crawl4ai API 文档 | `http://47.98.102.55/5d41402abc4b2a76b9719d911017c592/crawl4ai/schema` | OpenAPI Schema |
| 爬虫调度管理（未来） | `http://47.98.102.55/5d41402abc4b2a76b9719d911017c592/crawler/` | 任务管理 + Swagger API |
| Grafana（未来） | `http://47.98.102.55/5d41402abc4b2a76b9719d911017c592/monitor/` | 历史趋势 + 告警 |

> 所有入口均通过 Nginx hash 前缀路由，**不暴露额外端口**到公网。

---

### 数据库表（未来）

crawl4ai-service 在 RAGFlow 的 `rag_flow` 库中新建 5 张独立表：

| 表 | 用途 | 关键字段 |
|---|------|---------|
| `crawler_task` | 任务定义 | schedule_type, target_url, extraction_config |
| `crawler_task_log` | 执行日志 | status, duration, items_found, error_msg |
| `crawler_result` | 爬取结果 | markdown, extracted_json, kb_doc_id, bid_project_id |
| `crawler_site_config` | 站点提取配置 | list_base_selector, list_fields, detail_fields |
| `crawler_state` | 断点续爬状态 | processed_urls, last_page, last_offset |

> 建表 SQL → `D:\AI\ragflow2\crawl4ai_service\migrations\001_initial.sql`

---

### 网络配置

crawl4ai 容器必须加入 RAGFlow 的 Docker 网络才能访问 `redis` 和 `mysql`：

```bash
ssh -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no root@47.98.102.55 "docker network ls | grep ragflow"
# 输出示例: docker_ragflow  bridge  local
```

---

### 常见问题

| 症状 | 原因 | 解决 |
|------|------|------|
| 502 Bad Gateway via Nginx | crawl4ai 容器未启动或未就绪 | `docker logs -f crawl4ai` 查看启动进度，等 1-2 分钟 |
| `Connection refused` via Nginx | 容器已启动但 API 未就绪 | healthcheck 生效后自动恢复，等 30s |
| 首次构建卡住 | deb.debian.org 国内下载慢 | 正常，等 15-20 分钟（服务器 apt 已配国内加速） |
| Redis 认证失败 | Valkey 8 需要密码 | 确认 `config.yml` 中 `redis.password: "infini_rag_flow"` |
| crawl4ai 内存 OOM | 浏览器池占用过高 | `text_mode: true` + 降低 `max_pages` + 限制容器内存 |
| 构建后无法访问 dashboard | GUNICORN_BIND 绑定 127.0.0.1 | 检查 `CRAWL4AI_API_TOKEN` 环境变量是否设置 |
| `docker compose build` 很慢 | 层缓存未命中 | 检查是否误用 `--no-cache`，优先只用 `docker compose build` |
