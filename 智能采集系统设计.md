# 智能采集系统（新）设计文档

> **版本**：v1.3（2026-07-24 新增 §15.6 探测监控/采集任务列表数据源对齐通用规约）
> **日期**：2026-07-24
> **分支**：`feat/unified-crawler-framework`
> **状态**：实施完成，待部署验证

---

## 1. 背景与边界

项目中共存 **3 套采集系统**，新系统与其它两套完全解耦：

| 系统 | 触发方式 | 入口 | 存储表 | 用途 |
|------|---------|------|--------|------|
| A. 旧脚本体系 | 定时（`scheduled_task_executor.py`） | 80+ `*_crawler.py` 硬编码 | `bid_project` 族 | 老定时任务 |
| B. 统一爬虫 v2.1 | 定时 / 手动 | `unified_crawler.py` + YAML | `bid_project` 族 | YAML 驱动的招投标采集 |
| **C. 智能采集（新）** | **定时 + 手动** | `unified_crawler.py`（同 B） | **`crawler_result` + 扩展表** | **★ 支持招标/政策/人员/新闻/其他多类别** |

### 设计原则
- **完全解耦**：不修改 `bid_*` 任何表、不改 `bid_writer.py`、不动老 `*_crawler.py` 脚本
- **共用基础设施**：复用 `crawler_task` 表（前端智能采集页签）、`crawler_engine` 三层架构、`crawler_sites.yaml` 配置；`scheduled_task` 表为旧脚本体系（系统 A）所用，本文档仅作参考
- **向后兼容**：老站点采集行为零变化（默认 `writer_mode=bid`，不读 `category`）
- **手动触发能力**：通过 `/api/v1/crawl4ai/tasks/<id>/trigger` 一键执行

---

## 2. 数据库设计

### 2.1 复用现有表

- **`crawler_task`** — 任务定义（前端智能采集页签的任务列表即此表，CRUD 走 `/api/v1/crawl4ai/tasks`）
- **`crawler_result`** — 采集结果主表（已有 `category` 列，`id = md5(site_id|source_url)` 去重）

> **参考**：旧脚本体系（系统 A）用 `scheduled_task` + `scheduled_task_executor.py` 调度，新建任务**不**再使用该表。本文档其余章节提到 `scheduled_task` 处均为旧体系参考。

### 2.2 新增字段

```sql
ALTER TABLE crawler_result
  ADD COLUMN category VARCHAR(32) NOT NULL DEFAULT 'bid'
    COMMENT 'bid|policy|personnel|news|other',
  ADD INDEX idx_category (category);
```

### 2.3 新增扩展表

#### `collection_policy_ext`（政策法规类扩展）

| 字段 | 类型 | 说明 |
|------|------|------|
| `result_id` | VARCHAR(32) PK | FK → crawler_result.id |
| `doc_number` | VARCHAR(200) | 发文字号（国发〔2024〕12号） |
| `issuing_authority` | VARCHAR(200) | 发文机构 |
| `authority_level` | VARCHAR(50) | 法律/行政法规/部门规章/规范性文件 |
| `topic_category` | VARCHAR(100) | 主题分类 |
| `effective_date` | DATE | 实施日期 |
| `expiry_date` | DATE | 失效日期 |
| `status` | VARCHAR(20) | 有效/废止/修订中 |
| `legal_basis` | VARCHAR(500) | 依据上位法 |

#### `collection_personnel_ext`（人员信息类扩展）

| 字段 | 类型 | 说明 |
|------|------|------|
| `result_id` | VARCHAR(32) PK | FK → crawler_result.id |
| `person_name` | VARCHAR(100) | 姓名 |
| `id_card_masked` | VARCHAR(50) | 身份证号（脱敏） |
| `cert_no` | VARCHAR(100) | 证书编号 |
| `cert_type` | VARCHAR(100) | 证书类型（一级建造师/监理工程师等） |
| `employer` | VARCHAR(200) | 所属单位 |
| `specialty` | VARCHAR(200) | 注册专业 |
| `position` | VARCHAR(100) | 职务 |
| `valid_until` | DATE | 证书有效期 |
| `status` | VARCHAR(20) | 注册/注销/转注 |

### 2.4 类别路由策略

| category | 写入表 |
|----------|--------|
| `bid` | `crawler_result`（结构化字段放 `extracted_json`） |
| `policy` | `crawler_result` + `collection_policy_ext` |
| `personnel` | `crawler_result` + `collection_personnel_ext` |
| `news` / `other` | `crawler_result`（同 bid） |

### 2.5 迁移机制

- ORM 类定义于 `api/db/db_models.py` 末尾，`init_database_tables()` 自动发现并通过 `create_table(safe=True)` 创建（新库自动建表，老库 IF NOT EXISTS 跳过）
- `crawler_result.category` 列通过 `migrate_db()` 中的 `alter_db_add_column` 自动添加
- **不涉及手动 SQL 脚本**，符合 CLAUDE.md "新增数据库要放到初始化脚本中" 的要求

---

## 3. 代码结构

### 3.1 新增文件

```
api/db/services/collection_ext_service.py       Service 层（扩展表 CRUD）
rag/svr/crawler_engine/collection_writer.py     新 Writer（输出到 crawler_result）
api/apps/restful_apis/collection_app.py         REST API Blueprint
```

### 3.2 修改文件（追加，不改现有逻辑）

| 文件 | 改动 |
|------|------|
| `api/db/db_models.py` | +2 ORM 类（CollectionPolicyExt / CollectionPersonnelExt）<br>+migrate_db() 加 crawler_result.category 列 |
| `rag/svr/crawler_engine/storage_pipeline.py` | +`writer_mode` / `category` / `task_id` 参数<br>+`_write_to_collection` 方法<br>+collection 模式跳过 AttachmentHandler（避免污染 bid_project_file） |
| `rag/svr/crawler_engine/engine.py` | `run()` 加 `writer_mode` / `category` / `task_id` 参数并透传<br>+`date_filter` 非空时重置 `last_page=0`（§5.8） |
| `rag/svr/crawler_engine/config.py` | `SiteConfig` +`category` 字段（默认 `bid`），ConfigLoader 解析<br>+`DetailConfig.metadata_js` 字段（§5.7） |
| `rag/svr/unified_crawler.py` | CLI +`--writer` / `--category` 参数，script_args 透传 |
| `rag/svr/crawler_sites.yaml` | 顶部加 `category` 字段说明 + policy/personnel 示例 |
| `CLAUDE.md` | 参考文档表新增本文件链接 |

---

## 4. 触发机制

### 4.1 手动触发（前端「立即运行」按钮）

前端「智能采集 > 采集任务」列表的「立即运行」按钮调用：

```
POST /api/v1/crawl4ai/tasks/<task_id>/trigger
```

触发后后端线程执行（详见 §4.4.1 三路派发），前端可通过 WebSocket 实时查看进度（§4.6）。

**任务创建**（前端「新建任务」按钮）：

```
POST /api/v1/crawl4ai/tasks
Body: {
  "name": "福建省工程领域招投标在线监管平台-政策法规",
  "site_id": "ggzyjd_policies",
  "target_url": "https://ggzyjd.fj.gov.cn/policies/list/...",
  "max_pages": 3,
  "extraction_schema": { "baseSelector": "...", "fields": {...} },
  "output_targets": ["db", "kb"],
  "kb_id": "a35c93a0544811f1b4e835d43f94478d",
  "enabled": true
}
```

对于 YAML 站点，`extraction_schema` 仅为前端表单占位（触发时后端走 unified_crawler 子进程，不依赖此字段）；`target_url` 和 `max_pages` 同理。非 YAML 站点则走 crawl4ai_executor（LLM 抽取），此时 extraction_schema 必须有效。

**直接 DB 创建**（API 不可用时备选）：

```sql
INSERT INTO crawler_task (id, tenant_id, name, site_id, target_url,
  start_page, max_pages, extraction_schema, detail_config, headers,
  output_targets, kb_id, parser_id, enabled, last_run_summary)
VALUES (...);
```

关键字段：`site_id`、`kb_id`、`output_targets` = `["db","kb"]`、`enabled` = 1。

> **⚠️ 踩坑：直接 DB 写入中文必须显式指定 utf8mb4 字符集（2026-07-24）**
>
> 现象：通过 `docker exec -i docker-mysql-1 mysql -uroot -p... < file.sql` 管道执行 INSERT/UPDATE 时，如果 SQL 文件含中文（任务名 `name`、`description` 等），前端列表显示乱码（如 "ç¦å»ºçœ..."），但 mysql CLI `SELECT` 出来看却是正常中文。
>
> 根因：**mysql 客户端默认连接字符集是 `latin1`**（不是 utf8mb4）。SQL 文件是 UTF-8 编码的原始字节，被 client 当作 latin1 解码 → 再以 utf8mb4 编码存入 DB，造成 "UTF-8 → latin1 → utf8mb4" 双重编码（mojibake）。mysql CLI `SELECT` 时走同样反向路径所以看起来正常，但 API/前端通过 utf8mb4 连接读取时拿到的是乱码字节。
>
> 验证方法（用 `latin1` 客户端读 HEX，正常 UTF-8 应是 `E7A68F` 这种 3 字节汉字编码，双重编码后是 `C3A7C2A6` 这种 2+2 字节）：
> ```bash
> docker exec docker-mysql-1 mysql -uroot -pinfini_rag_flow --default-character-set=latin1 rag_flow \
>   -e "SELECT name, HEX(name) FROM crawler_task WHERE id='<ID>'"
> ```
>
> **正确做法**（二选一）：
> ```bash
> # 方式 A：命令行加 --default-character-set=utf8mb4（推荐）
> docker exec -i docker-mysql-1 mysql -uroot -pinfini_rag_flow \
>   --default-character-set=utf8mb4 rag_flow < /tmp/task.sql
>
> # 方式 B：SQL 文件首行加 SET NAMES
> # file.sql 开头写：SET NAMES utf8mb4;
> ```
>
> 修复已乱码的行：用任一正确方式重新 UPDATE 该行的中文列即可（HEX 校验通过后再算完成）。

### 4.2 定时调度（TBD）

定时执行复用 `crawler_task` 表，通过外部调度器（如 cron 调用 trigger API 或 CLI）实现。当前 `scheduled_task_executor.py` 为旧脚本体系调度器，暂不绑定智能采集任务。

如需定时执行，推荐在服务器 crontab 中配置：

```bash
# 每天凌晨 2 点触发
0 2 * * * curl -X POST http://localhost:9380/api/v1/crawl4ai/tasks/<task_id>/trigger \
  -H "Authorization: Bearer <token>"
```

或通过 CLI：

```bash
docker exec docker-ragflow-cpu-1 python /ragflow/rag/svr/unified_crawler.py \
  --tenant-id <TID> --kb-id <KID> --task-name daily_ggzyjd_policies \
  --writer collection --category policy \
  --date-filter today \
  --script-args '{"site_id":"ggzyjd_policies","task_id":"<TASK_ID>"}'
```

### 4.3 任务状态查询

```
GET  /api/v1/crawl4ai/tasks                    # 任务列表（智能采集页签绑定此接口）
GET  /api/v1/crawl4ai/tasks/<task_id>           # 单个任务详情
PUT  /api/v1/crawl4ai/tasks/<task_id>           # 更新任务
DELETE /api/v1/crawl4ai/tasks/<task_id>         # 删除任务
GET  /api/v1/crawl4ai/tasks/<task_id>/status    # 运行状态（running + last_run_status）
```

前端智能采集页签的「采集任务」Tab 调用 `GET /api/v1/crawl4ai/tasks`（后端：`crawl4ai_app.py::list_tasks()`），读 `crawler_task` 表。**不是** `/api/v1/scheduled-tasks`（那是旧脚本体系的定时任务列表页）。

### 4.4 前端集成与权限模型（CRITICAL）

**前端「智能采集」页签实际调用的接口**：

| Tab | 接口 | 后端文件 | 数据库表 |
|-----|------|---------|----------|
| 采集任务 | `GET /api/v1/crawl4ai/tasks` | `crawl4ai_app.py::list_tasks()` | `crawler_task` |
| 采集结果 | `GET /api/v1/collection/results` | `collection_app.py::list_results()` | `crawler_result` |

**不是** `/api/v1/scheduled-tasks` — 那是独立的「定时任务」页签（读 `scheduled_task` 表），与智能采集无关。

**权限模型（2026-07-22 调整）**：

| 操作 | 权限 |
|------|------|
| `GET /crawl4ai/tasks` (列表) | ✅ 所有登录用户可见全部任务，不按 tenant 过滤 |
| `GET /crawl4ai/tasks/<id>` (详情) | ✅ 所有登录用户可查 |
| `POST /crawl4ai/tasks` (创建) | 归属 `current_user.id`（任务列表里仍全局可见） |
| `PUT /crawl4ai/tasks/<id>` (更新) | 不强制校验所有权 |
| `DELETE /crawl4ai/tasks/<id>` (删除) | 同上 |
| `POST /crawl4ai/tasks/<id>/trigger` (立即运行) | ✅ 所有登录用户可触发 |

**理由**：这是内部团队工具，不是多租户 SaaS。所有采集任务对团队成员共享可见，便于协作和排查。任务的归属仅用于审计，不用于隔离。

### 4.4.1 前端「立即运行」的执行路由（CRITICAL）

**坑点**：前端「智能采集 > 采集任务」列表的「立即运行」按钮调用 `/api/v1/crawl4ai/tasks/<id>/trigger`，这个端点早期硬编码走 `Crawl4aiExecutor`（基于 crawl4ai Docker LLM 抽取，要求 `crawler_task.extraction_schema.fields` 非空）。但 YAML 配置的智能采集站点（如 `ggzyjd_dissent` / `ggzyjd_cases`）实际采集逻辑在 `unified_crawler.py` 里，`crawler_task.extraction_schema` 只是占位（`{"baseSelector": "a.list-item", "fields": []}`），导致触发时报：

```
Crawl4aiError: extraction_schema 缺少 baseSelector/fields
```

**修复（2026-07-23）**：`trigger_task()` 在派发前先查 YAML 判断路由：

```python
# api/apps/restful_apis/crawl4ai_app.py::trigger_task
is_yaml, yaml_category = _is_yaml_site(task.site_id)
if is_yaml and yaml_category and yaml_category != "bid":
    writer_mode = "collection"     # 新智能采集（objection/news/policy/personnel/other）
elif is_yaml:
    writer_mode = "bid"            # 老统一爬虫 v2.1（仍走 bid_* 表）
else:
    writer_mode = ""               # 非 YAML 站点 → 走 crawl4ai_executor
```

**三路派发**：

| 条件 | 派发目标 | 实现 |
|------|---------|------|
| YAML 站点 + category≠bid | `subprocess.run(["python", "unified_crawler.py", "--writer", "collection", "--category", <cat>, ...])` | YAML 配置驱动，写入 `crawler_result` + 扩展表 |
| YAML 站点 + category=bid | 同上，但 `--writer bid` | 走老 `bid_*` 表族（兼容统一爬虫 v2.1 站点） |
| 非 YAML 站点 | `api.utils.crawl4ai_executor.run_task(task_id)` | LLM 抽取，要求 `extraction_schema.fields` 非空 |

**`_is_yaml_site()` 辅助函数**（同文件）：
```python
def _is_yaml_site(site_id: str) -> tuple[bool, str]:
    """读 crawler_sites.yaml 判断 site_id 是否配置，返回 (is_yaml, category)。"""
    # 按 YAML mtime 隐式缓存（os.path.getmtime），无显式缓存
    # 找不到 site_id 返回 (False, "")
```

**子进程派发的参数透传**：
- `--tenant-id` ← `task.tenant_id`（保留任务创建者归属）
- `--kb-id` ← `task.kb_id`（若 YAML 站点 crawler_task 指定了 kb_id 则透传）
- `--writer` ← `writer_mode`
- `--script-args` ← JSON `{"site_id", "writer", "category", "task_id", "date_filter"}`

**`date_filter` 必传 `"today"`（2026-07-23 修复）**：

早期 trigger 派发的 `script_args` 漏了 `date_filter` 字段，导致 unified_crawler 收到空 date_filter → 不过滤日期 → 把前几天累积的数据一并抓回来。修复后 script_args 固定注入：

```python
script_args = json.dumps({
    "site_id": task.site_id,
    "writer": writer_mode,
    "category": yaml_category or "bid",
    "task_id": task_id,
    "date_filter": "today",   # ← 必传，否则会抓到历史数据
}, ensure_ascii=False)
```

`date_filter: "today"` 经 `CollectionWriter._normalize_date_filter()` 解析为运行当天日期，items 中日期字段（`date`/`publishDate`/`pubDate`/`publish_date` 等）不匹配当天的会被丢弃。无日期字段的 item 也视为不匹配（严格模式），避免脏数据混入。

**跨 run 去重（不需要 DedupChecker）**：

智能采集的跨次去重靠主键 upsert，不依赖 `DedupChecker`：

| 机制 | 层 | 作用 |
|------|---|------|
| `crawler_result.id = md5(site_id\|source_url)` | 写入层 | 同一站点同一 URL 永远映射到同一主键 |
| `CrawlerResultService.upsert_result(data)` | DB 层 | `id` 已存在 → UPDATE；不存在 → INSERT |
| stats 统计 | 日志层 | 新增计 `results_new`，更新计 `results_updated` |

因此重复触发同一任务**绝不会产生重复行**，只会刷新已有行。`DedupChecker._exists_in_bid_project()` 仅查 `bid_project` 表（老 bid 模式用），对 collection 模式无效，这是设计如此——collection 模式的去重责任在写入层。

**子进程完成后的状态回写**：
```python
status = "success" if proc.returncode == 0 else "fail"
summary = _pull_summary_from_redis(task_id)   # 从 Redis history 读最后一条 done
if not summary:
    # 子进程崩溃未发 done → 合成最小 fail summary
    summary = {"status": status, "pages": 0, "items_found": 0, "items_new": 0,
               "kb_uploaded": 0, "attachments_uploaded": 0,
               "errors": [] if status == "success" else [(proc.stderr or "crawler failed")[-300:]]}
CrawlerTaskService.update_by_id(task_id, {
    "last_run_status": status,
    "last_run_time":   int(time.time() * 1000),   # ★ ms epoch，必须显式写
    "last_run_summary": summary,
})
```
- `proc.returncode != 0` → 记录 `proc.stderr[-2000:]` 到日志
- `proc.stdout[-500:]` 成功时记录到日志
- 超时 3600s（`subprocess.run(timeout=3600)`）

**last_run_summary 扁平结构约定**（前端 TasksTab 直接读）：
```json
{
  "status": "success|fail",
  "pages": N,                // 扫描页数（对应 reporter 的 scanned_pages）
  "items_found": N,          // 扫描条数（scanned_items）
  "items_new": N,            // 新增条数（new_items）
  "kb_uploaded": N,          // 上传 KB 数（bid_stats.kb_uploaded）
  "attachments_uploaded": N, // 附件上传数（bid_stats.attachments_uploaded）
  "errors": []               // 错误信息列表
}
```
`_pull_summary_from_redis()` 负责把 ProgressReporter 发布的内层嵌套 summary 拍扁成这个结构。**不要**返回 `{"status":..., "summary":{嵌套}}`，前端会显示「-」。

**并发控制**：进程内 `_running_tasks: set` + `_running_lock = threading.Lock()`，同一 task_id 不能并发触发。

**踩坑教训**：
- 前端 trigger 端点的名字可能误导（`/crawl4ai/tasks/.../trigger` 不代表一定走 crawl4ai_executor），实际路由要看后端实现
- `crawler_task` 表的 `extraction_schema` 字段对 YAML 站点只是 UI 展示占位，不能作为执行参数
- 修复 trigger 时务必同步更新 `last_run_status`，否则前端列表状态停留在上一次的结果

### 4.5 完整爬虫接入流程（标准 SOP）

接入一个新爬虫站点，按以下步骤顺序执行：

```text
Step 0  调用 konus-crawler-dev 技能，了解 YAML 规范
   ↓
Step 1  搜索 rag/svr/*_crawler.py 同域老脚本，复用其签名/加密/字段映射
   ↓
Step 2  在 crawler_sites.yaml 添加站点配置
        - 必填: name(中文名<机构>-<栏目>) + site_url(含域名) + category
        - 选择适配器: rest_api / encrypted_api / spa_render / playwright_http
        - 选择分页策略 + 提取器
        - 参考反爬能力等级文档选择反爬等级
   ↓
Step 3  容器内单站测试（CLI）
        docker exec docker-ragflow-cpu-1 python /ragflow/rag/svr/unified_crawler.py \
          --tenant-id <TID> --kb-id <KID> --task-name test_xxx \
          --writer collection --category <cat> \
          --date-filter today \
          --script-args '{"site_id":"xxx"}'
        验证: [CRAWLER] Done: N new items + DB 行数 + KB 上传数
   ↓
Step 4  调用 konus-code-review 审查 YAML + 代码改动
   ↓
Step 5  成套 SCP 部署（按部署清单，至少包含 YAML + db_models + 相关 service）
   ↓
Step 6  创建采集任务（二选一）
        方式 A: 前端 UI 推荐
          智能采集页签 → 采集任务 → 新建任务 → 填写表单
          （site_id / target_url / max_pages / extraction_schema / output_targets / kb_id）
          对 YAML 站点 extraction_schema 只作占位，实际执行不依赖它
        方式 B: 直接 DB INSERT（API 不可用时备选）
          表: crawler_task（不是 scheduled_task！）
          关键字段: site_id / kb_id / output_targets=["db","kb"] / enabled=1
          extraction_schema 至少含 baseSelector + fields（满足 DB NOT NULL）
          示例见 §4.1 底部 SQL
   ↓
Step 7  前端验证
        登录任意账号 → 智能采集页签 → 列表中可见新任务
        → 点"立即运行"测试 → 查看 logs 确认状态
   ↓
Step 8  次日检查采集结果
        GET /api/v1/collection/results?site_id=xxx
        响应自动附带 site_name(中文名) + site_domain(域名)
```

**关键检查点**：
- YAML 站点 `name` 必须是中文可读名（前端列表展示用），不要用英文 site_id
- 触发时 `date_filter` 自动为 `"today"`（`crawl4ai_app.py::trigger_task()` 硬编码注入），每日只采当天数据
- 任务列表不按 tenant 隔离，任意账号登录即可见可操作（§4.4）
- 结果查询 API 自动从 YAML 派生 `site_name` + `site_domain`（§5.1）
- **任务表是 `crawler_task`，不是 `scheduled_task`**。前端「智能采集」页签绑定 `crawl4ai/tasks` 接口

---

## 4.6 实时进度上报（ProgressReporter + WebSocket）

> **版本**：v1.1（2026-07-23）
> **目的**：采集任务执行期间对用户可见 — 看到爬到第几页、产出几条、错误日志。执行前只有 `last_run_status` 末态，过程是黑盒。

### 4.6.1 架构

```
unified_crawler.py (子进程)
  ├─ CrawlerEngine.__init__(progress_reporter=ProgressReporter(task_id))
  ├─ _log_header/_log_site_info/_log_footer() → reporter.publish_log()
  ├─ _log_progress() 每页 → reporter.publish_progress(page, total_pages, new, scanned)
  └─ finally / except / before_8am_skip → reporter.publish_done(status, summary)
         ↓
   Redis pubsub channel: crawler:task:{task_id}           (实时推送)
   Redis list:          crawler:task:{task_id}:history    (RPUSH+LTRIM 保留最近 500 条, 24h TTL)
         ↓
crawl4ai_ws.py @app.websocket("/api/v1/crawl4ai/tasks/<task_id>/ws")
  ├─ token 鉴权（URLSafeTimedSerializer 解 JWT）
  ├─ 连接时 LRANGE history 0 499 → backfill {type:"history", messages:[...]}
  ├─ pubsub.subscribe(channel) → 实时转发 {type:"progress"|"log"|"done"}
  └─ 30s 空闲心跳 ping，收到 done 后 0.5s 关闭
         ↓
前端 useCrawlerProgress(taskId) hook
  └─ onMessage → 进度条 + 日志面板（最近 200 条）
```

**核心约束**：
- 进度走 Redis 直传，**不依赖父进程 stdout 中转**（`subprocess.run(capture_output=True)` 保持不变）
- ProgressReporter 所有 publish **静默失败**（Redis 不可用时 no-op），绝不影响爬虫主流程
- DB schema 不动 — 进度走 Redis/内存，结束才落 `last_run_summary`

### 4.6.2 关键文件清单

| 类型 | 路径 | 说明 |
|------|------|------|
| 新建 | `rag/svr/crawler_engine/progress_reporter.py` | ProgressReporter 类（Redis pubsub+history 双写） |
| 修改 | `rag/svr/crawler_engine/engine.py` | `__init__` 接 reporter；`_log_*` + `run()` 调用 reporter |
| 修改 | `rag/svr/unified_crawler.py` | 初始化 `ProgressReporter(task_id)` 传给 engine |
| 修改 | `api/apps/restful_apis/crawl4ai_app.py` | `_pull_summary_from_redis()` + trigger 后回写 last_run_summary |
| 新建 | `api/apps/restful_apis/crawl4ai_ws.py` | WebSocket handler |
| 修改 | `api/apps/__init__.py` | `register_crawler_ws_routes(app)` |
| 新建 | `web/src/services/crawler-progress.ts` | WS 客户端 + 类型 |
| 新建 | `web/src/hooks/use-crawler-progress.ts` | React hook |
| 新建 | `web/src/pages/crawl4ai/progress-dialog.tsx` | 进度面板 Dialog |
| 修改 | `web/src/pages/crawl4ai/tasks-tab.tsx` | 「查看进度」按钮 |
| 修改 | `docker/nginx/ragflow.conf.python` | 新增 WS 反代规则 |

### 4.6.3 nginx WebSocket 反代（踩坑 CRITICAL）

**坑 1：前缀过宽吞掉 REST 请求**

错误写法（会 break）：
```nginx
# 错误：前缀匹配 + 硬编码 "upgrade" 头
location ^~ /api/v1/crawl4ai/tasks/ {
    proxy_set_header Connection "upgrade";   # ← 所有 HTTP 请求都强制 upgrade
}
```
这会把 `POST /api/v1/crawl4ai/tasks/{id}/trigger` 等 REST 请求也强制加 `Connection: upgrade` 头，后端拒绝 → 触发前端全局 401 拦截器 → **用户被登出**。

正确写法（精确正则只匹配 WS URL）：
```nginx
# 正确：只在 .../ws 后缀路径上启用 upgrade
location ~ ^/api/v1/crawl4ai/tasks/[^/]+/ws$ {
    proxy_pass http://localhost:9380;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 86400s;
    proxy_send_timeout 86400s;
}
```
注意 location 顺序：此正则 block 必须在 `location ~ ^/(v1|api)` 之前，否则会被通用 API 规则先匹配走（无 upgrade 头，WS 握手失败）。

**坑 2：active 配置文件不是 `ragflow.conf`**

容器内 `/etc/nginx/conf.d/` 有 4 个候选：`ragflow.conf` / `ragflow.conf.python` / `ragflow.conf.golang` / `ragflow.conf.hybrid`。**实际激活的是 `ragflow.conf.python`**（通过 `nginx.conf` 的 `include` 指令）。

修改 nginx 配置后：
1. 改本地 `docker/nginx/ragflow.conf.python`
2. SCP 到 `/home/bid-agent-konus/ragflow2/docker/nginx/ragflow.conf.python`（bind mount 目标）
3. `docker exec docker-ragflow-cpu-1 nginx -t && nginx -s reload`

### 4.6.4 WebSocket token 鉴权（踩坑 CRITICAL）

**坑：用错 Serializer 导致签名验证必然失败**

错误写法：
```python
# crawl4ai_ws.py — 错误
from itsdangerous import Serializer           # ← 基类，签名格式不同
jwt = Serializer(secret_key=settings.SECRET_KEY)
access_token = str(jwt.loads(token))
```

正确写法（与登录、collaboration_ws 一致）：
```python
from itsdangerous.url_safe import URLSafeTimedSerializer as Serializer
```

**根因**：登录流程 `user_api.py:144` 用 `URLSafeTimedSerializer` 签发 token，浏览器存的 cookie/Authorization 是 URLSafe 格式。WS handler 用基类 `Serializer` 验签，签名算法不同 → `jwt.loads()` 抛异常 → 落到 `except` 分支用原 token 字符串查 DB → 查不到 → 返回 `{"type":"error","message":"Invalid token"}` + close(4003)。

**症状**：浏览器点「查看进度」→ nginx 日志显示 `GET /ws 101 50`（101 切换协议成功，50 字节错误响应）→ 前端 onclose 触发自动重连 → 每 2-4 秒重连一次 → 弹窗一直显示「连接中」。

**经验**：所有需要解 JWT 的 WS handler / API 都必须用 `URLSafeTimedSerializer`，不能图简写用基类。`collaboration_ws.py:15` 就是标准范本。

### 4.6.5 trigger 状态回写坑（last_run_time + summary 扁平化）

**坑 1：忘记写 `last_run_time`**

trigger `_run()` 函数完成后调用 `CrawlerTaskService.update_by_id()`，payload 里只放 `last_run_status` → 列表「上次运行时间」永远停留在上一次值。

修复：payload 必须显式加 `"last_run_time": int(time.time() * 1000)`（ms epoch，与 `create_time` / `update_time` 一致）。

**坑 2：summary 嵌套 vs 扁平**

`_pull_summary_from_redis` 初版返回 `{"status":..., "summary":{内层 reporter summary}}`，但前端 TasksTab 读的是 `summary.items_new / summary.pages / summary.items_found`（扁平）。结果：DB 写了 summary 但前端显示「-」。

修复：`_pull_summary_from_redis` 必须把 reporter 的内层 summary 拍扁：
```python
return {
    "status": msg.get("status"),
    "pages":              inner.get("scanned_pages"),
    "items_found":        inner.get("scanned_items"),
    "items_new":          inner.get("new_items"),
    "kb_uploaded":        bid_stats.get("kb_uploaded"),
    "attachments_uploaded": bid_stats.get("attachments_uploaded"),
    "errors": [],
}
```

### 4.6.6 ProgressReporter 消息协议

后端 → 前端（JSON）：
```typescript
{type: "progress", page, total_pages, new, scanned, ts}     // 每爬一页一条
{type: "log", text, level, ts}                               // level: info|warning|error
{type: "done", status, summary, ts}                          // status: success|fail|skipped
{type: "history", messages: [...]}                           // 连接时一次性 backfill
{type: "ping"}                                               // 30s 空闲心跳
{type: "error", message: str}                                // 鉴权/内部错误
```

前端 → 后端：无（只读推送）。

**特殊路径**：before_8am 跳过（站点限制 8 点前不爬）也必须 publish_done("skipped", {...})，否则前端 WS 永远等不到结束消息。

### 4.6.7 调试验证方法

WS 链路 4 个层面，自底向上排查：

```bash
# 1. Redis pubsub 是否有数据（最底层）
docker exec -d docker-redis-1 sh -c 'redis-cli -a infini_rag_flow -n 1 \
  subscribe crawler:task:<TASK_ID> > /tmp/ws.log 2>&1'
# 触发任务后 cat /tmp/ws.log，应看到 progress/log/done 流

# 2. Redis history list 是否有数据（注意是 db1 不是 db0）
docker exec docker-redis-1 redis-cli -a infini_rag_flow -n 1 \
  llen crawler:task:<TASK_ID>:history
docker exec docker-redis-1 redis-cli -a infini_rag_flow -n 1 \
  lrange crawler:task:<TASK_ID>:history 0 2

# 3. WS 端点是否工作（容器内直连，绕过 nginx）
docker exec docker-ragflow-cpu-1 python -c '
import asyncio, websockets
async def t():
    async with websockets.connect(
        "ws://localhost:9380/api/v1/crawl4ai/tasks/<TASK_ID>/ws?token=<RAW_TOKEN>",
        additional_headers={"Origin":"http://47.98.102.55"}
    ) as ws:
        print(await ws.recv())   # 应收到 history backfill
asyncio.run(t())
'
# 注意：raw token 直查 user_token.token 字段（32 位 hex），不用 JWT 签名格式

# 4. nginx 是否转发 WS upgrade
docker exec docker-ragflow-cpu-1 tail -50 /var/log/nginx/access.log | grep "/ws"
# 期望：HTTP/1.1" 101 ...（101 = upgrade 成功）
# 异常：HTTP/1.1" 200 ... + 前端疯狂重连 = 协议握手失败
```

**4 层排查顺序**：Redis pubsub → Redis history → 后端 WS 端点 → nginx。出问题先看最底层有没有数据，逐层往上定位。

---

## 5. 数据查询 API

```
GET /api/v1/collection/results
  ?category=policy
  &site_id=xxx
  &task_id=xxx
  &status=raw|kb_uploaded|failed
  &keyword=xxx
  &start_date=2026-01-01
  &end_date=2026-07-22
  &page=1&page_size=20
  &with_ext=true|false              # 是否关联扩展表（默认 true）

GET /api/v1/collection/results/{result_id}      # 详情（含 markdown 正文 + 扩展字段）

GET /api/v1/collection/stats                    # 按 category 分组统计

GET /api/v1/collection/sites?category=policy    # 列出 YAML 中可用站点
```

**扩展表批量查询**：`get_by_result_ids(result_ids)` 一次性 IN 查询，避免 N+1。

### 5.1 站点元数据约定（CRITICAL）

每条 `results` 列表项 / 详情响应**自动附带**以下两个字段（来自 YAML，前端无需自行解析）：

| 字段 | 来源 | 示例 |
|------|------|------|
| `site_name` | YAML `sites.<id>.name` | `福建省工程领域招投标在线监管平台-有关案例` |
| `site_domain` | YAML `sites.<id>.site_url` 的 netloc | `ggzyjd.fj.gov.cn` |

**约定（所有站点强制遵循）**：

1. YAML 每个站点**必须**配置 `name`（中文显示名，能让人一眼看懂是哪个机构的哪个栏目）和 `site_url`（站点根 URL，含域名）。
2. `name` 规范：`<机构简称>-<栏目名>`，例如 `福建省工程领域招投标在线监管平台-有关案例`、`住建部-政策文件`。不要用英文 site_id 兜底。
3. `site_url` 规范：必须是真实可访问的站点根 URL，形如 `https://www.example.gov.cn`，API 会用 `urlparse().netloc` 提取域名。
4. 后端 `_build_site_metadata_map()` 按 YAML mtime 缓存 `site_id → {site_name, site_domain}` 映射，所有 results 查询复用。
5. 前端列表展示站点时，统一显示 `site_name`（中文名），hover/详情可附带 `site_domain`（域名），不再显示英文 `site_id`。

### 5.2 date_filter 参数（CLI / 手动触发）

`unified_crawler.py --date-filter` 的 `date_filter` 字段语义：

| 取值 | 含义 | 适用场景 |
|------|------|----------|
| `""`（默认） | 不做日期过滤，全量入库 | 首次接入 / 历史回溯 |
| `"today"` | 只保当天发布的数据，每次运行时解析为当天 | **trigger API 默认注入** |
| `"YYYY-MM-DD"` | 只保指定日期 | 历史补采 / 回归测试 |

`CollectionWriter._normalize_date_filter()` 在 writer 初始化时把 `"today"` 解析为运行当天日期。前端「立即运行」按钮经 `trigger_task()` 固定注入 `"date_filter": "today"`（见 §4.4.1），CLI 手动运行时需自行传 `--date-filter today`。

### 5.3 category 字段写入规则（CRITICAL）

> **踩坑教训**：早期版本只在 `extracted_json["_category"]` 里存了 category，没写入 `crawler_result.category` 列，导致按 category 过滤/统计时全表扫空，前端列表也显示不出类型。

**规则**：`CollectionWriter._write_result()` 写主表时，data dict **必须显式包含 `"category": category` 字段**。

```python
# rag/svr/crawler_engine/collection_writer.py::_write_result
data = {
    "id": result_id,
    "task_id": task_id,
    "tenant_id": self._tenant_id,
    "site_id": site_id,
    "site_display": site_display,
    "category": category,          # ★ 必须显式写入主表列，不能只存 extracted_json
    "title": ...,
    ...
}
```

**Why**：
- `extracted_json` 是 JSON blob，SQL 层 `WHERE category='objection'` 查不到
- `/collection/stats` 分组统计依赖 `crawler_result.category` 实体列
- 前端 category 过滤下拉走 `/collection/results?category=xxx`，同样依赖实体列

**幂等保证**：`CrawlerResultService.upsert_result(data)` 以 `id` 为主键 upsert，重复跑同一 item 不会产生重复行，category 列会被新值覆盖（值相同时无变化）。

### 5.4 category 双字段返回约定（英文 code + 中文 label）

后端 API 返回采集结果时**同时返回两个字段**：

| 字段 | 用途 | 示例 |
|------|------|------|
| `category` | 英文 code，用于过滤/统计/前端下拉 value | `"objection"` |
| `category_label` | 中文展示名，用于 UI 显示 | `"异议结果"` |

**前端不维护 code → 中文映射**，统一由后端 `CATEGORY_LABELS` 常量提供：

```python
# api/apps/restful_apis/collection_app.py 顶部
CATEGORY_LABELS: Dict[str, str] = {
    "bid": "标讯",
    "policy": "政策法规",
    "personnel": "人员",
    "news": "新闻资讯",
    "other": "其他",
    "objection": "异议结果",
}

def _category_label(code: str) -> str:
    if not code:
        return ""
    return CATEGORY_LABELS.get(code, code)  # 未知 code 原样返回
```

**三个返回路径**都要附 `category_label`：
- `list_results()` — 列表循环里附 `it["category_label"]`
- `get_result()` — 详情返回前附 `d["category_label"]`
- `collection_stats()` — 统计行附 `"category_label": _category_label(r.category or "")`

**新增 category 时的同步清单**（三处必改）：
1. `CATEGORY_LABELS` 字典加映射
2. `web/src/pages/collection-results/field-labels.ts` 的 `EXT_FIELD_LABELS` 加该 category 扩展字段标签
3. `web/src/pages/collection-results/field-labels.ts` 的 `CATEGORY_COLORS` 加 Badge 染色

### 5.5 前端采集结果展示规范

**路由 + 菜单**：`/collection-results` 独立页面，不复用 crawl4ai 结果页。在 `routes.tsx` + `global-navbar.tsx` 同步注册。

**列表页布局**（`web/src/pages/collection-results/index.tsx`）：
- 过滤器：搜索框（防抖 500ms）+ 类型下拉（从 `/collection/stats` 拉取带 count）+ 日期范围
- 表格列：`类型(Badge)` / `标题` / `站点(site_display)` / `发布日期` / `采集时间`
- 行点击 → 打开详情弹窗
- React Query queryKey：`['collectionResults', { keyword, category, startDate, endDate, page, pageSize }]`（所有过滤参数进 key 保证唯一性）
- 列表请求 `with_ext=false`（不返回大字段）；详情请求自动含 markdown + ext

**详情弹窗布局**（`result-detail-dialog.tsx`）4 区：
1. **头部**：标题 H2 + 类型 Badge（`category_label` + 染色）+ 站点 + 发布日期 + 原文链接(新窗) + KB Badge
2. **基本信息**：发布日期 / 站点 / 采集时间（2 列表格）
3. **结构化扩展字段**：按 category 取 `EXT_FIELD_LABELS[category]` 映射中文标签，2 列表格渲染，**空值和内部字段跳过**
4. **正文**：`<pre>` 渲染 markdown

**扩展字段过滤规则**（空值不显示）：
```typescript
const extEntries = Object.entries(result?.ext ?? {})
  .filter(([k, v]) => {
    // 跳过内部字段
    if (['result_id', 'create_time', 'create_date',
         'update_time', 'update_date', 'id'].includes(k)) {
      return false;
    }
    // 跳过空值
    return v !== null && v !== '' && v !== undefined;
  })
  .map(([k, v]) => [extLabels[k] ?? k, v as string]);  // 英文 key → 中文 label，未知 key 原样显示
```

**详情弹窗 queryKey**：`['collectionResult', resultId]`（每个弹窗独立缓存，关闭后保留缓存避免重复请求）。

### 5.6 历史 category 数据回填

修复 category 写入 bug 或新接入 category 列后，历史数据的 `crawler_result.category` 列可能为空，需要手动回填一次：

```sql
-- 按站点回填（推荐，粒度细）
UPDATE crawler_result SET category='objection'
WHERE site_id='ggzyjd_dissent' AND category='';

-- 按时间段回填（备选）
UPDATE crawler_result SET category='policy'
WHERE create_time BETWEEN '2026-07-01' AND '2026-07-22'
  AND category='';
```

验证回填结果：
```sql
SELECT site_id, category, COUNT(*)
FROM crawler_result
GROUP BY site_id, category;
```

### 5.7 详情页结构化数据提取（metadata_js）

> **版本**：v1.2（2026-07-23）
> **问题**：SPA 站点详情页除正文外常有结构化元数据（信息来源、发布机构、文号等），但这些信息仅在渲染后的 DOM 中出现，不在列表 API 响应里。`content_field` 只抓正文容器，不解析元数据 → 扩展表字段全空。

**通用机制**：`DetailConfig.metadata_js` — 一段在 Playwright 渲染的详情页上 `page.evaluate()` 执行的 JS，返回 `{内部字段名: 值}` 字典，结果直接 merge 到 item 中。

**数据流**：
```
YAML detail.metadata_js (JS snippet)
  → spa_render._fetch_detail_css() 中 page.evaluate(metadata_js)
  → 返回 dict merge 到 item (e.g. item["issuing_authority"] = "国家住建部")
  → CollectionWriter._write_policy_ext() 的 _first_of(item, "issuing_authority", ...) 自动拾取
  → extracted_json 自动收录所有非排除字段
  → collection_policy_ext / collection_personnel_ext 写入结构化字段
```

**YAML 配置示例**（`ggzyjd_policies` 详情页）：
```yaml
detail:
  type: css_selector
  content_field: ".new-detail .content"   # 只抓正文 div（不含 header 元数据）
  metadata_js: |
    () => {
      const result = {};
      const spans = document.querySelectorAll('.new-detail .info span');
      for (const span of spans) {
        const label = span.querySelector('label');
        if (!label) continue;
        const key = label.textContent.replace(/[:：]/g, '').trim();
        const value = span.textContent.replace(label.textContent, '').trim();
        if (key === '信息来源') result.issuing_authority = value;
        if (key === '发布日期') result.publish_datetime = value;
      }
      return result;
    }
```

**关键约定**：
- `metadata_js` 返回的 key 必须匹配 `_write_policy_ext` / `_write_personnel_ext` 中 `_first_of()` 查找的标准名（如 `issuing_authority` / `doc_number` / `person_name` 等），否则不会进扩展表
- 即使 key 不匹配扩展表字段，也会进入 `extracted_json`（前端/Agent 可读）
- `page.evaluate()` 在 `_fetch_detail_css` 内部执行，页面已完整渲染，无需额外等待
- `metadata_js` 为空或执行异常时静默跳过（logging.warning），不影响正文提取

**适用范围**：仅 `spa_render` adapter 的 `detail.type: css_selector` 路径。

### 5.8 date_filter 与 crawler_state 交互（CRITICAL）

> **版本**：v1.2（2026-07-23）
> **问题**：`crawler_state.last_page` 跨次运行持续递增。`click_next` 分页模式下 `start_page = last_page + 1`，第二次运行从第 2 页开始。但 `date_filter=today` 时当天数据始终在第 1 页 → 后续运行跳过第 1 页 → **什么都抓不到**。

**根因分析（第一性原理）**：
- `last_page` 设计用于**增量分页**（"从上次中断处继续"），适合全量扫描历史数据
- `date_filter` 语义是**按日期重置数据集**（"每次只抓当天"），数据集从第 1 页重新开始
- 两者组合时语义冲突：date_filter 改变了数据集，但 last_page 仍记住旧位置

**修复**（`engine.py::run_internal`）：当 `date_filter` 非空时，每次运行重置 `last_page = 0`，但保留 `processed_ids` 去重：

```python
if full:
    self._state.last_page = 0
    self._state.last_offset = 0
elif getattr(self, "_date_filter", ""):
    # date_filter 改变数据集 — 当天数据始终从第 1 页开始
    self._state.last_page = 0
    self._state.last_offset = 0
    logging.info(
        "Engine: date_filter='%s' — resetting to page 1 "
        "(keeping %d processed IDs for dedup)",
        self._date_filter, self._state.processed_count,
    )
```

**行为矩阵**：

| `full` | `date_filter` | `last_page` | 行为 |
|--------|--------------|-------------|------|
| True | 任意 | 重置为 0 | 从第 1 页全量重扫，保留 processed_ids 去重 |
| False | 非空 | 重置为 0 | 从第 1 页扫当天数据，保留 processed_ids 去重 |
| False | 空 | 不重置 | 增量模式，从 `last_page + 1` 继续 |

**去重保持**：重置 `last_page` 但**不**清 `processed_ids`。同一 item 在同一天内重复运行只入库一次（主键 upsert），后续运行靠 `processed_ids` 跳过，不浪费 detail 抓取。

**适用范围**：所有 adapter（rest_api / encrypted_api / spa_render），所有分页类型（page_no / click_next / offset 等）。不限于 `click_next` — `page_no` 分页也有同样问题。

---

## 6. YAML 配置示例

### 6.1 政策法规站点

```yaml
sites:
  ggcy_zcwj:
    category: policy                    # ★ 关键字段
    name: "某某政策法规栏目"
    site_url: "https://www.example.gov.cn"
    transport:
      type: rest_api
      verify_ssl: false
    listing:
      url: "https://www.example.gov.cn/api/policies"
      method: GET
    pagination:
      type: page_no
      page_param: page
      page_size_param: pageSize
      page_size: 20
      max_pages: 50
    extract:
      type: json_path
      items_path: "data.rows"
      fields:
        # ── 公共字段 ──
        title: "title"
        url:   "url"
        date:  "publishDate"
        # ── 政策扩展字段（→ collection_policy_ext）──
        doc_number:          "wenhao"
        issuing_authority:   "fawenjigou"
        authority_level:     "xiaoli"
        topic_category:      "zhuti"
        effective_date:      "shixingri"
        expiry_date:         "shixiaori"
        status:              "zhuangtai"
        legal_basis:         "yiju"
    anti_crawler:
      delay_min: 1.5
      delay_max: 3.0
      max_consecutive_empty: 3
```

### 6.2 人员信息站点

```yaml
sites:
  builder_registry:
    category: personnel
    name: "注册建造师查询"
    transport: { type: rest_api }
    listing: { url: "...", method: GET }
    pagination: { type: page_no, page_size: 20 }
    extract:
      type: json_path
      items_path: "data.list"
      fields:
        person_name: "xm"
        cert_no:     "zsh"
        cert_type:   "zslx"
        employer:    "danwei"
        specialty:   "zhuanye"
        valid_until: "yxq"
        url:         "url"
```

### 6.3 现有 bid 站点（无需改动）

老站点不写 `category` 字段，默认 `bid`，继续走老 `bid_*` 表族（`writer_mode=bid`）。

---

## 7. 调试命令

```bash
# 1. YAML 语法校验
docker exec docker-ragflow-cpu-1 python -c "
from rag.svr.crawler_engine.config import ConfigLoader
cfg = ConfigLoader('rag/svr/crawler_sites.yaml').load()
print('Sites:', len(cfg))
print('First policy site:', next((s for s in cfg.values() if s.category=='policy'), None))
"

# 2. 查任务列表
curl http://localhost:9380/api/v1/crawl4ai/tasks

# 3. 手动触发任务（前端「立即运行」同款）
curl -X POST http://localhost:9380/api/v1/crawl4ai/tasks/<task_id>/trigger \
  -H "Authorization: Bearer <token>"

# 4. 直接 CLI 运行（绕过 API）
docker exec docker-ragflow-cpu-1 python rag/svr/unified_crawler.py \
  --tenant-id <TID> --kb-id <KID> --task-name test_policy \
  --writer collection --category policy \
  --date-filter today \
  --script-args '{"site_id":"ggzyjd_policies","task_id":"<TASK_ID>"}'

# 5. 查询采集结果
docker exec docker-mysql-1 mysql -u root -pinfini_rag_flow rag_flow -e "
  SELECT id, title, category, site_id, create_time
  FROM crawler_result
  WHERE category='policy'
  ORDER BY create_time DESC LIMIT 10;
"

# 6. 查询扩展表
docker exec docker-mysql-1 mysql -u root -pinfini_rag_flow rag_flow -e "
  SELECT p.result_id, p.doc_number, p.issuing_authority, r.title
  FROM collection_policy_ext p
  JOIN crawler_result r ON r.id = p.result_id
  ORDER BY p.update_time DESC LIMIT 10;
"

# 7. 查 crawler_task 表（智能采集任务列表）
docker exec docker-mysql-1 mysql -u root -pinfini_rag_flow rag_flow --default-character-set=utf8mb4 -e "
  SELECT id, name, site_id, kb_id, enabled, last_run_status, last_run_time
  FROM crawler_task
  WHERE site_id='ggzyjd_policies';
"
```

---

## 8. 审查记录

已通过 `konus-code-review`，修复 4 个问题：

| 级别 | 问题 | 修复 |
|------|------|------|
| HIGH | collection 模式附件处理会写入 bid_project_file 破坏解耦 | storage_pipeline.py 加 writer_mode 判断，collection 模式跳过 AttachmentHandler |
| MEDIUM | list_scheduled 的 total 与实际列表数不一致 | SQL 层用 LIKE '%"writer": "collection"%' 一起过滤 |
| MEDIUM | 一次性任务描述与定时任务混淆 | description 加 `[one-shot]` 标识 |
| MEDIUM | crawled_at 用本地时间戳与其他写入器不一致 | 统一用 `common.time_utils.current_timestamp()` |

---

## 8.1 数据处理硬性规范（CRITICAL）

> **所有采集脚本/站点配置的输出端，只允许两个动作**：

| # | 动作 | 实现 |
|---|------|------|
| 1 | **存入数据库** | `CollectionWriter._write_result()` → `crawler_result` 主表 + category 对应扩展表（policy_ext / personnel_ext） |
| 2 | **上传知识库并触发解析** | `StoragePipeline._upload_content_to_kb()` → `KBUploader.upload_content()` 触发 RAGFlow KB 解析 |

**❌ 禁止的动作**（一律不操作）：

- 发邮件 / 钉钉 / 企微推送
- 写本地文件（除爬虫临时缓存）
- 调用第三方业务 API（除采集源本身）
- 修改其他业务表（`bid_*` / 用户表 / 任务表 / Agent 相关表）
- 触发 Agent / Webhook / 消息队列广播

**Why**：采集系统的职责边界要清晰，避免与业务系统耦合。所有"后续业务处理"都由其他模块从 KB / DB 读取数据后自行触发，不归采集系统负责。

**适用范围**：
- ✅ 新系统：`CollectionWriter` + 智能采集所有 YAML 站点
- ✅ 旧系统：`BidWriter` + 统一爬虫 v2.1 所有 YAML 站点（已经符合此规范）

---

## 9. 后续工作（TBD）

- [ ] 为现有 78 个 bid 站点中实际属于政策类的标注 `category: policy`
- [ ] 新接入政策法规/人员信息站点的 YAML 配置
- [x] **Collection 模式下的附件下载 + KB 上传**（2026-07-24 已实现，详见 §11）
- [ ] 前端采集管理页面（手动触发/结果浏览/定时任务 CRUD）
- [ ] 结果去重策略（同 URL 不同站点 / 同标题不同时间）
- [ ] KB 解析状态同步（CrawlerResult.status 与 bid_project_parse 类似机制）

---

## 11. 多栏目站点采集规约（2026-07-24 补）

> 来源：ggzyfw_fujian_guide（福建公共资源交易-示范文本/行业规范/培训资料）开发踩坑。
> 本节内容对所有 YAML 多栏目站点通用，后续脚本照此编写即可。

### 11.1 首次回溯 vs 后续增量的自动判定

**问题**：设计文档原 §4.4.1 规定 trigger 总是注入 `date_filter=today`，导致新任务首次运行也是 0 条（历史文章日期早于今日，被 CollectionWriter 全部过滤）。

**修复**（`api/apps/restful_apis/crawl4ai_app.py::trigger_task._run`）：
```python
is_first_run = not (task.last_run_status or "").strip()
script_args_dict = {"site_id": ..., "writer": ..., "category": ..., "task_id": ...}
if not is_first_run:
    script_args_dict["date_filter"] = "today"   # 仅非首次注入
```

**语义**：
- `last_run_status IS NULL` → 首次回溯，全量抓取
- `last_run_status = 'success'/'fail'` → 后续增量，只抓当日

**注意**：修改了 `crawl4ai_app.py` 后**必须重启 Docker 容器**（主进程缓存模块）。
其他子进程文件（`engine.py` / `storage_pipeline.py` / `collection_writer.py` 等）由
`unified_crawler.py` 每次 `subprocess.run` 新起，不需要重启即生效。

### 11.2 Section 名称自动流入前端「类型」列

**问题**：多栏目站点共享同一个 `category`（如 `other`），前端「类型」列只能显示一个
"其他"，无法区分"示范文本/行业规范/培训资料"。

**方案**：engine 注入 `section_name` → CollectionWriter 流入 `extracted_json` → API
暴露为顶层字段 → 前端 Badge 优先显示。

**改动链**（通用，任何多栏目站点只要 YAML 写 `name` 就自动生效）：

1. **`rag/svr/crawler_engine/config.py`** — `SectionConfig` 新增 `name: str = ""`：
   ```python
   @dataclass
   class SectionConfig:
       label: str = ""
       name: str = ""    # Chinese display name, surfaced to frontend 类型 column
       ...
   ```
   `_parse_section` 读 `data.get("name", "")`。

2. **`rag/svr/crawler_engine/engine.py`** — `_crawl_one_section` 循环内 storage 前注入：
   ```python
   if section:
       item["section_label"] = section.label
       item["section_name"] = section.name or section.label
   ```

3. **`api/apps/restful_apis/collection_app.py`** — list/detail 接口返回 `section_name`：
   ```python
   def _extract_section_name(extracted_json):
       # 兼容 dict / JSON string / None
       ...
   for it in items:
       it["section_name"] = _extract_section_name(it.get("extracted_json"))
   ```

4. **前端**（`web/src/pages/crawl4ai/results-tab.tsx` + `result-detail-dialog.tsx`）：
   ```tsx
   <Badge className={badgeColorFor(r.category, r.section_name)}>
     {r.section_name || r.category_label}
   </Badge>
   ```

### 11.3 Section Badge 配色（哈希自动取色）

**问题**：多栏目 Badge 原本都用 `CATEGORY_COLORS[category]`，同站点不同栏目颜色相同，
视觉上无法区分。

**方案**（`web/src/pages/crawl4ai/field-labels.ts`）：按 `section_name` 字符串哈希
稳定映射到 10 色调色板：
```ts
const SECTION_COLOR_PALETTE = [
  'bg-blue-500/15 text-blue-600',
  'bg-emerald-500/15 text-emerald-600',
  'bg-amber-500/15 text-amber-600',
  // ... 共 10 色
];

export function badgeColorFor(category, sectionName) {
  if (sectionName) {
    return PALETTE[hash(sectionName) % PALETTE.length];
  }
  return CATEGORY_COLORS[category];  // 无 section 回退到 category 色
}
```

**特性**：
- 同名栏目稳定同色（哈希确定）
- 不同站点同名栏目可能撞色（不影响视觉区分）
- 无需为每个站点配颜色映射

### 11.4 Collection 模式附件下载链路（已实现）

原 §9 TBD 已解决。4 步链路，缺一环 = 0 附件：

```
detail API (encrypted_api) → item["content"] = HTML
  ↓ engine._extract_files_from_item 扫 <a href> 正则
item["files"] = [{file_name, file_url}, ...]
  ↓ item_from_dict → NormalizedItem.attachments
  ↓ CollectionWriter 把 attachments 落 CrawlerResult.attachments JSON（DB）
  ↓ StoragePipeline._handle_attachments(link_to_bid=False)
AttachmentHandler 下载 + ZIP 解压 + 上传 KB
```

**关键 bug 修复**（均已合并）：

| Bug | 文件 | 修复 |
|---|---|---|
| `_extract_files_from_item` 不扫 `content` 键 | `engine.py` | HTML fallback 列表加 `item.get("content")` |
| `encrypted_api.fetch_detail` 丢非 content_field 字段 | `adapters/encrypted_api.py` | 把 inner JSON 其他 key merge 进 item |
| Collection 模式附件强耦合 `bid_project_file` | `attachment_handler.py` | `handle(link_to_bid=False)` 跳过 |
| Writer 过滤掉的 item 仍上传附件 → KB 孤儿 | `storage_pipeline.py` | 附件分支加 `project_id` 真值检查 |

### 11.5 YAML 模板（多栏目 encrypted_api 站点照抄）

```yaml
site_id:
  name: "站点中文名"
  site_url: "https://..."
  category: other              # 或 news/policy/...
  transport:
    type: encrypted_api         # AES/SM4 + MD5 sign
    encryption: { algorithm, key, iv, field }
    signing:    { algorithm, secret, header_name }
  pagination: { type: page_no, total_field, items_field }
  extract:
    fields: { id: ID, title: TITLE, date: TM, news_type: TYPE, source_org: SOURCES }
    url_template: "https://.../detail?id={ID}"   # 列表无 url 时拼接
  detail:
    type: api_request
    content_field: CONTENTS                    # HTML 正文，含 <a href> 文件链接
  sections:
    - label: xxx
      name: "示范文本"                          # ← 必填，前端「类型」列显示用
      listing: { params: { type: "53" } }      # 仅 params 不同
    - label: yyy
      name: "行业规范"
      listing: { params: { type: "54,55" } }
```

### 11.6 存量数据回填（SQL 一次性）

如果站点已经跑过一次但没写 `section_name`，按 `extracted_json.TYPE` 映射回填：
```sql
UPDATE crawler_result
SET extracted_json = JSON_SET(extracted_json, '$.section_name',
      CASE
        WHEN JSON_UNQUOTE(JSON_EXTRACT(extracted_json, '$.TYPE')) LIKE '%53%' THEN '示范文本'
        WHEN JSON_UNQUOTE(JSON_EXTRACT(extracted_json, '$.TYPE')) LIKE '%54%'
          OR JSON_UNQUOTE(JSON_EXTRACT(extracted_json, '$.TYPE')) LIKE '%55%' THEN '行业规范'
        WHEN JSON_UNQUOTE(JSON_EXTRACT(extracted_json, '$.TYPE')) LIKE '%59%' THEN '培训资料'
        ELSE '其他'
      END)
WHERE site_id='xxx';
```

### 11.7 部署清单（改 section_name 链路时）

| 文件 | 是否需重启容器 |
|---|---|
| `rag/svr/crawler_engine/config.py` | 否（子进程加载） |
| `rag/svr/crawler_engine/engine.py` | 否 |
| `rag/svr/crawler_engine/storage_pipeline.py` | 否 |
| `rag/svr/crawler_engine/attachment_handler.py` | 否 |
| `rag/svr/crawler_engine/adapters/encrypted_api.py` | 否 |
| `api/apps/restful_apis/crawl4ai_app.py` | **是**（主进程缓存） |
| `api/apps/restful_apis/collection_app.py` | **是** |
| 前端 `web/dist/` | SCP + `nginx -s reload` |

---

## 10. 相关文件索引

| 类型 | 路径 |
|------|------|
| ORM 定义 | `api/db/db_models.py` (末尾 CollectionPolicyExt / CollectionPersonnelExt) |
| Service 层 | `api/db/services/collection_ext_service.py` |
| Writer | `rag/svr/crawler_engine/collection_writer.py` |
| 存储管道 | `rag/svr/crawler_engine/storage_pipeline.py` |
| 引擎 | `rag/svr/crawler_engine/engine.py` |
| 配置加载 | `rag/svr/crawler_engine/config.py` |
| CLI 入口 | `rag/svr/unified_crawler.py` |
| REST API | `api/apps/restful_apis/collection_app.py` |
| YAML 配置 | `rag/svr/crawler_sites.yaml` |
| 本文档 | `智能采集系统设计.md` |

---

## 12. 增量采集与监控面板（2026-07-24 补）

> 来源：本次会话解决的"如何让智能采集近实时感知新内容"问题。
> 目标：后续接入任何新站点，**只写 YAML**，无需新建 cron 任务、无需改后端代码、无需改前端，即可自动获得：增量爬取 + 近实时监控 + 失败告警。

### 12.1 问题背景

旧模式下用户痛点：智能采集任务通过 cron 拉取"当天数据"，但不是实时的 —— 站点上午 10 点发新内容，cron 要到晚上才跑，延迟数小时。
朴素方案是"10 分钟轮询所有脚本"，但站点规模扩到 200+ 后压力很大且浪费（多数时候没新内容）。

### 12.2 方案：签名探测 + 自适应退避

核心思路：**"先轻探，再深爬"**。

```
detector meta-task (60s 轮询)
   ↓ 只抓列表第 1 页
   ↓ md5(sorted(id|publish_date)[:30])[:8]  →  签名
   ↓ 对比 Redis 里上次签名
 changed? ──是──▶ 入队 unified_crawler 全量采集（writer=collection, date_filter=today）
            └─ miss_count = 0, interval = base
 unchanged? ──否──▶ miss_count += 1, interval = min(base * 2^miss, cap)
```

关键特性：
- **签名而非全量比对**：探测器只抓列表页前 30 条，不算 DB、不写表，毫秒级
- **指数退避**：站点 5 次没更新 → 探测间隔翻 5 番（上限 1 小时），有更新立刻回到 base
- **静默时间**：`detect_quiet_hours: "22-6"` 跳过夜间
- **断路器**：连续 5 次探测异常 → `auto_disabled=True`，不再消耗资源
- **强制触发**：前端按钮可设 `FORCE_KEY` 跳过退避立即探测

### 12.3 新增文件

| 类型 | 路径 | 职责 |
|------|------|------|
| 探测器入口 | `rag/svr/crawler_detector.py` | meta-task 调度，Redis 状态机，入队全量任务 |
| 签名计算 | `rag/svr/crawler_engine/detector.py` | `SiteDetector.detect()`，抓列表页 + md5[:8] |
| 任务注册 | `rag/svr/crawler_engine/register_detector_task.py` | 幂等 upsert 探测器 meta-task 到 `scheduled_task` 表 |
| REST API | `api/apps/restful_apis/collection_app.py` 的 `/collection/detect/*` | state / stats / activity / trigger / reset / enable / disable / install 8 个端点 |
| 前端面板 | `web/src/pages/crawl4ai/detect-tab.tsx` | 监控面板（统计/活动流/站点表/操作按钮） |
| 前端服务 | `web/src/services/collection-service.ts` | 类型 + 服务函数 |

### 12.4 YAML 新增字段（SiteConfig）

所有字段都有默认值，**老 YAML 不需要回填**：

```yaml
sites:
  - site_id: example_new_site
    name: "示例新站"
    # ... 其他原有字段 ...
    detect_enabled: true          # 是否参与探测器轮询（默认 false）
    detect_interval: 300          # 基础探测间隔秒数（默认 300）
    detect_max_interval: 3600     # 退避上限（默认 3600）
    detect_min_interval: 0        # 退避下限，0 = 用 detect_interval（默认 0）
    detect_quiet_hours: ""        # 静默时间 "0-7" / "22-6"，跨天支持（默认空）
```

### 12.5 后续增量脚本通用 SOP（★ 重点）

接入新站点只需 **3 步**：

#### Step 1：写 YAML（沿用 §11 多栏目规约）

```yaml
- site_id: my_new_site
  name: "我的新站"
  site_url: "https://example.gov.cn"
  category: policy                # bid|policy|personnel|news|other|objection
  enabled: true
  detect_enabled: true            # ★ 关键：开启探测器自动增量
  detect_interval: 300
  # sections / listing / pagination / extract / anti_crawler / detail
  # 按老规则写，探测器自动复用
```

#### Step 2：本地/容器内单站验证（沿用 §7 调试命令）

```bash
docker exec docker-ragflow-cpu-1 python /ragflow/rag/svr/unified_crawler.py \
  --tenant-id <TID> --kb-id <KID> --task-name test_my_new_site \
  --writer collection --category policy \
  --date-filter 2026-07-24 \
  --script-args '{"site_id":"my_new_site"}'
```

验证：① `Done: N new items` ② `SELECT COUNT(*) FROM crawler_result WHERE site_id='my_new_site'` 对得上。

#### Step 3：部署 YAML 即生效

```bash
scp ... crawler_sites.yaml root@47.98.102.55:/home/bid-agent-konus/ragflow2/rag/svr/
```

**无需重启 Docker**。下次探测器 meta-task 跑（默认 60s 一次）会自动：
1. 读取新 YAML → 发现 `my_new_site` 且 `detect_enabled: true`
2. 加入轮询队列
3. 首次探测签名入库
4. 后续每次签名变化 → 入队 `unified_crawler` 全量采集（writer=collection, date_filter=today）

前端「探测监控」面板 10 秒内自动出现新站点行。

### 12.6 前端监控面板功能

访问：B端 → 智能采集 → **探测** Tab。功能：

| 区域 | 说明 |
|------|------|
| 顶部统计 | total / due / active / cold / error / auto_disabled / manual_disabled / never_probed + avg interval。error/disabled 计数 >0 时变红 + pulse 动画 |
| 活动流横条 | 近 1 小时有新数据的站点（绿底徽章），新出现的 site 绿色闪烁 2s；hover 显示该站最新一条 title |
| 站点表 | 异常优先排序：auto_disabled → error → manual_disabled → due → active → cold。行背景：auto_disabled 红 / error 琥珀 |
| 操作按钮 | ⚡ 立即探测（FORCE_KEY 跳过退避）/ 🔄 重置状态 / ⏻ 启用/禁用 |

数据接口：
- `GET /collection/detect/state` - 站点列表（YAML + Redis state 合并）
- `GET /collection/detect/stats` - 桶计数
- `GET /collection/detect/activity?window=3600&limit=20` - 近 N 秒活动流（`crawler_result` 表聚合）
- `POST /collection/detect/{trigger|reset|enable|disable}` - 单站操作
- `POST /collection/detect/install` - 注册/更新探测器 meta-task（body: `{kb_id, interval_seconds}`）

### 12.7 增量数据流（图示）

```
┌───────────────────────┐
│ scheduled_task 表      │  detector-meta-{tenant} meta-task (interval=60s)
│ detector-meta-xxx     │  由 /collection/detect/install 幂等注册
└──────────┬────────────┘
           │ scheduled_task_executor 拉取
           ▼
┌───────────────────────┐
│ crawler_detector.py   │  for site in enabled+detect_enabled:
│ (subprocess)          │   - 检查 not_due / quiet_hours / auto_disabled / force
│                       │   - SiteDetector.detect() 抓列表 1 页 + 签名
│                       │   - 签名变 → _enqueue_full_crawl()
└──────────┬────────────┘
           │ Redis queue (svr_queue_name)
           ▼
┌───────────────────────┐
│ task_executor 消费    │  unified_crawler.py subprocess:
│                       │   --writer collection --category <YAML>
│                       │   --date-filter today
│                       │   --script-args {"site_id":"..."}
└──────────┬────────────┘
           │ CollectionWriter
           ▼
┌───────────────────────┐
│ crawler_result 表     │  ← 前端 activity 流的数据源
│ + 扩展表 (policy/...) │
│ + KB 上传             │
└───────────────────────┘
```

### 12.8 老定时任务清理指引

| 任务类型 | 处理 |
|---------|------|
| `bid_sync.py` 类老脚本 | **保留** —— 走 bid_project 表，与 crawler_result 是两条独立数据流 |
| 智能采集 per-site cron（`/collection/scheduled` 创建的） | 若该站点 `detect_enabled: true` 且探测器稳定 → **删除**；签名不稳定（SPA 列表乱序）则保留 cron 兜底 |
| `detector-meta-{tenant}` | **必须保留** —— 探测器 meta-task，删了系统就停了 |

**推荐清理流程**：
1. 在「探测监控」面板观察 1-2 天
2. 确认某站点 `miss_count` 稳定、`has_new` 能及时触发
3. 到「定时任务」页面删除该站点的 cron
4. **不要**一次清空所有 cron，逐站点验证

### 12.9 排查清单（探测不工作？）

| 现象 | 检查 |
|------|------|
| 站点 `never_probed` | 探测器 meta-task 未安装 → 点面板右上「安装探测器」按钮 |
| 站点 `auto_disabled` | 看 `last_error` 字段（hover 黄色感叹号），修 YAML 后点「启用」 |
| 签名每次都变（`last_reason=changed` 但实际没新内容） | 列表顺序不稳定 → 检查 `extract.fields` 里有没有稳定 id（`compute_signature` 会 fallback 用 url/href，但若都没有就乱） |
| 全量任务入队但没执行 | 看 task_executor 日志 / `scheduled_task_log` 表 / Redis 队列长度 |
| 活动流为空 | `SELECT MAX(crawled_at) FROM crawler_result WHERE site_id='xxx'` —— 确认 unified_crawler 真的写了数据 |

### 12.10 关键约束

1. **探测层不做关键词过滤** —— 签名只看 id+publish_date，过滤逻辑放到 CollectionWriter
2. **签名基于列表页前 30 条** —— 若站点新内容出现在第 31+ 条之后，探测器看不到（极少见，政府站都是新在上）
3. **~~`kb_id` 必须在 install 时传~~**（2026-07-24 重构,见 §14）—— 探测器不再消费 kb_id,由 unified_crawler 按 site_id 查 crawler_task 表自动解析
4. **探测器复用站点配置** —— `site.anti_crawler.max_retries=1`、`site.transport.timeout=10` 强制收紧，保证探测快
5. **state TTL = 30 天** —— 长期不活跃的站点状态会过期，重新加入时按 `never_probed` 处理

### 12.11 部署清单（本次会话涉及）

| 类型 | 路径 | 是否需重启 Docker |
|------|------|------------------|
| 探测器入口 | `rag/svr/crawler_detector.py` | 否（subprocess） |
| 签名计算 | `rag/svr/crawler_engine/detector.py` | 否 |
| 任务注册 | `rag/svr/crawler_engine/register_detector_task.py` | 否 |
| SiteConfig 扩展 | `rag/svr/crawler_engine/config.py` | 否 |
| REST API | `api/apps/restful_apis/collection_app.py`（+ `detect_activity` 端点） | **是**（主进程缓存） |
| 前端面板 | `web/src/pages/crawl4ai/detect-tab.tsx` 等 | 前端构建 + SCP + nginx reload |

---

## 13. 后续工作（TBD，替代原 §9）

- [ ] 现有 84 个 YAML 站点逐步补 `detect_enabled: true`（按业务优先级，一次一批）
- [ ] 验证签名稳定性：SPA 站点列表顺序若乱，需在 `compute_signature` 加更稳定的 key
- [x] **探测器 + 自适应退避 + 监控面板**（2026-07-24 已实现，详见 §12）
- [x] **多栏目站点采集规约**（2026-07-24 已补，详见 §11）
- [x] **Collection 模式附件下载 + KB 上传**（2026-07-24 已实现）
- [x] **触发源/业务参数解耦**（2026-07-24 已实现，详见 §14 —— 探测器不再消费 kb_id）
- [ ] 结果去重策略（同 URL 不同站点 / 同标题不同时间）
- [ ] KB 解析状态同步（CrawlerResult.status 与 bid_project_parse 类似机制）

---

## 14. 触发源 / 业务参数解耦（2026-07-24 补）

### 14.1 问题背景

早期设计中探测器入队时携带 `kb_id`,导致两个本不相关的关注点耦合:

- **触发源**（探测器/scheduled_task/手动 CLI）：应该只回答"什么时候爬"
- **业务参数**（kb_id/category/writer）：应该只回答"爬下来的数据往哪写"

后果:每换一个 KB 就要重建一次 detector meta-task;YAML 没法承载多租户/多 KB;前端安装按钮必须 prompt 用户输 kb_id,体验差。

### 14.2 设计原则（一句话）

> **触发源只知道 site_id;kb_id 是 crawler_task 表的属性,由爬虫脚本自己查.**

- 探测器: 只负责"侦察 + 触发",不碰任何业务参数
- 爬虫脚本: 按 site_id 查 `crawler_task` 表获取 kb_id / parser_id / output_targets 等
- `crawler_task` 表: 唯一的"业务参数"事实表,enabled=1 的行就是生效配置

### 14.3 具体改动（6 个文件）

| 文件 | 改动 |
|------|------|
| `rag/svr/crawler_detector.py` | `_enqueue_full_crawl(site_id, category, tenant_id)`,Redis msg 不含 kb_id |
| `rag/svr/crawler_engine/register_detector_task.py` | `kb_id` 参数保留但强制写空;CLI `--kb-id` 标记 deprecated |
| `rag/svr/unified_crawler.py` | `--kb-id` 改 optional;新增 `_resolve_kb_id(site_id, tenant_id, explicit)` 按 site_id 查 crawler_task |
| `api/apps/restful_apis/collection_app.py` | `/detect/install` 移除 kb_id 必填,只需 `interval_seconds` |
| `web/src/services/collection-service.ts` | `installDetect(interval_seconds)` |
| `web/src/pages/crawl4ai/detect-tab.tsx` | 安装按钮去掉 kb_id `window.prompt` |

### 14.4 kb_id 解析顺序（`_resolve_kb_id`）

```
1. CLI 显式 --kb-id 非空        → 直接用（手动测试场景保留）
2. crawler_task 表 site_id+tenant_id+enabled=1 → 取 create_time 最新一行的 kb_id
3. 都没命中                      → 返回 ""（crawler 会跳过 KB 上传,仅写 crawler_result 表）
```

容错: 查询异常/无匹配任务 → 返回空 + warning 日志,**不中断爬虫**,保证至少 DB 写入成功.

### 14.5 为什么 task_executor 不用改

`task_executor.py:1094` 已有守卫 `if task.get("kb_id"):` —— Redis msg 中没有 kb_id 字段时,子进程调用自动省略 `--kb-id` 参数,unified_crawler 收到后走自动解析路径. 无需改 task_executor.

### 14.6 通用 SOP（后续所有爬虫脚本照此做）

新增/改造爬虫站点时,**禁止**在触发链路任何位置硬编码或 prompt kb_id. 标准流程:

1. **在 `crawler_task` 表建一行配置**:
   ```sql
   INSERT INTO crawler_task (id, tenant_id, name, site_id, target_url, kb_id,
                             parser_id, output_targets, enabled, ...)
   VALUES (UUID(), '<TID>', '<显示名>', '<site_id>', '<url>', '<KB_ID>',
           'naive', '["db","kb"]', 1, ...);
   ```
2. **YAML 站点**: 只配采集相关字段（transport/extract/anti_crawler）,**不配 kb_id**
3. **YAML `defaults.detect_enabled: true`**: 默认参与探测器轮询,无需逐站配置
4. **启动探测器**（租户级,一次性）: 前端"安装探测器"按钮 / `POST /collection/detect/install` body 只需 `{"interval_seconds": 60}`
5. **手动测试单站**: 仍可显式传 `--kb-id <KID>` 覆盖自动解析（便于临时切换 KB）

### 14.7 兼容性

- 老的 `--kb-id` CLI 参数保留,显式传入时优先使用
- 老的 `register_detector_task.py --kb-id` CLI 参数保留,标记 deprecated
- 存量 `scheduled_task` 行中的 kb_id 字段保留不删,但 detector 运行时不再读取

### 14.8 部署清单（本次会话涉及）

| 类型 | 路径 | 是否需重启 Docker |
|------|------|------------------|
| 探测器入口 | `rag/svr/crawler_detector.py` | 否（subprocess） |
| 任务注册 | `rag/svr/crawler_engine/register_detector_task.py` | 否 |
| 爬虫 CLI | `rag/svr/unified_crawler.py` | 否 |
| REST API | `api/apps/restful_apis/collection_app.py`（`/detect/install` 去 kb_id） | **是** |
| 前端 | `web/src/pages/crawl4ai/detect-tab.tsx` + `collection-service.ts` | 前端构建 + SCP |

### 14.9 验收要点

- [ ] 前端"安装探测器"按钮一键成功,无 prompt
- [ ] 探测器发现新内容 → 入队 msg 不含 kb_id → task_executor 不传 `--kb-id`
- [ ] unified_crawler 日志出现 `resolved kb_id=xxx from crawler_task (site_id=yyy)`
- [ ] 无 crawler_task 匹配的站点 → warning 日志 + 仅写 DB,不写 KB
- [ ] 手动 `--kb-id <KID>` 显式传参仍能覆盖

---

## 15. 踩坑速记（2026-07-24 当日排查记录）

> 本节记录当日上线后出现的 3 个真实问题 + 根因 + 修复 + 预防要点。后续脚本开发和上线前必读,避免重复踩坑。

### 15.1 服务器存量代码与 worktree 不同步 → 运行时 AttributeError

**现象**: 探测器一上线就报
```
AttributeError: 'SiteConfig' object has no attribute 'detect_max_interval'
```

**根因**: `config.py` 在 §12 里就加了 `detect_max_interval` 字段,但当时只改了 worktree,**忘了 SCP 到服务器**。本机 import 正常,服务器跑的还是老版本。

**修复**: `scp rag/svr/crawler_engine/config.py root@...:/home/.../rag/svr/crawler_engine/config.py`

**预防要点**:
- 任何 SiteConfig / 模型 / 公共配置改动,**必须和业务文件一起 SCP**,不能只 SCP 业务文件
- 参考 §11.6 智能采集系统部署清单 — 横跨 8+ 文件必须成套 SCP
- **冒烟测试时除了 import 业务模块,也要 `import SiteConfig; print(SiteConfig.detect_max_interval)` 验证字段可见**

---

### 15.2 多 section 站点的「类型」字段语义（重要设计决策）

**现象**: 用户反馈 `ggzyfw_fujian_trade` 列表"类型列全是其他交易"。

**错误判断 1**: 一开始以为 `category` 字段错了,把 `category: other` 改成 `category: bid`,并把 DB 里 932 行全 UPDATE 成 `bid`。用户澄清后才回滚。

**错误判断 2**: 又以为前端排序有问题,把 list 接口排序从 `crawled_at DESC` 改成 `publish_date DESC, crawled_at DESC`。这只是缓解视觉错觉,没解决根因。

**真正的根因（设计语义）**:
- `category` (英文 code): 粗粒度业务分类 — `bid / policy / personnel / news / other / objection`
- `section_name` (中文): 多栏目站点的子分类展示名 — 例如 ggzyfw_fujian_trade 的 "工程建设/政府采购/土地使用权/矿业权/国有产权/医疗药品/其他交易"
- **前端类型列渲染优先级**: `r.section_name || r.category_label`（见 `results-tab.tsx`）

**结论 / 设计原则**:
1. `category` 是粗分类,用于跨站点聚合统计,**不要试图用 category 去表达子主题**
2. 站点有多个子栏目时,通过 YAML `sections:` 配置 + engine 注入 `section_name` 到 extracted_json,前端类型列优先读它
3. **用户原始需求 #10 "类型字段按列表主题为主"** = 用 section_name 作为子主题展示,不是改 category

**预防要点**:
- 新接入多栏目站点前,先想清楚「类型列应该显示什么」
- section 定义参考 §11 《多栏目站点采集规约》
- 单栏目站点不需要 section_name,前端自动回退到 category_label

---

### 15.3 多 section 站点的「列表排序」视觉陷阱

**现象**: 用户在 15.2 的基础上仍然觉得"首页全是同一类型"。

**根因**: 多 section 站点串行抓取 (工程建设→政府采购→...→其他交易),每个 section 的所有 item `crawled_at` 落在该 section 的抓取窗口内。前端默认 `crawled_at DESC` 排序时,首页正好是**最后抓的那个 section 的全部数据**。

**数据实测** (2026-07-24 一次抓取):
```
13:55-13:57  工程建设   177 条
13:57-13:58  政府采购   200 条
13:58        土地使用权 200 条
13:59        矿业权       6 条
13:59-14:00  国有产权   200 条
14:01-14:03  其他交易   149 条   ← 最后爬
```
首页按 `crawled_at DESC` 取的就是 14:01-14:03 这批,全是"其他交易"。

**修复**: `collection_app.py` list 接口排序改为:
```python
.order_by(CrawlerResult.publish_date.desc(),
          CrawlerResult.crawled_at.desc())
```
用发布时间作主排序,不同 section 的数据按实际发布时间交错显示。

**预防要点**:
- 多 section 站点上线前,**必须**确认前端列表排序包含 `publish_date DESC`
- 否则用户会误以为"数据只有一种类型"
- 如果站点本身没有可靠的 publish_date,考虑在 engine 里给同一批抓取的 items 做 time jitter,避免 crawled_at 全部挤在一起

---

### 15.4 url_template 不做 URL quote + 字段映射错误 → 详情链接 404

**现象**: 用户反馈部分详情原链接打开 404,URL 形如:
```
✗ https://ggzyfw.fujian.gov.cn/business/detail?cid=313158&type=ZFCG
✓ https://ggzyfw.fujian.gov.cn/business/detail?name=%E7%A6%8F%E5%B7%9E...&cid=D01-12350100MB02709751-20260622-069523-0&type=ZFCG
```

**根因 1**: `extractors/base.py` 渲染 url_template 用的是朴素 `tpl.format(**item)`,**对中文不做 URL quote**,生成的 URL 直接插入中文,浏览器无法识别。

**根因 2**: YAML 模板字段选错了 —— 用了 `cid={M_ID}` (短数字列表 ID),但目标网站实际需要的是 `cid={PROCODE}` (结构化项目编号,形如 `D01-XXXX-20260721-000002-3`),并且必须带 `name={NAME}` (urlencoded 标题) 才能打开。

**修复**:

1. **`extractors/base.py`** —— url_template 渲染时对每个 `{field}` 做 URL quote:
   ```python
   import re
   from urllib.parse import quote

   tpl = getattr(self._config, "url_template", "")
   if tpl and not mapped.get("url"):
       def _replace(m):
           key = m.group(1)
           val = item.get(key, "")
           return quote(str(val), safe="")
       mapped["url"] = re.sub(r"\{(\w+)\}", _replace, tpl)
   ```
   不再用 `tpl.format(**item)`,改用 regex 替换 + `urllib.parse.quote(safe="")`。

2. **YAML url_template** —— 改用正确字段:
   ```yaml
   # 旧 (错):
   url_template: "https://ggzyfw.fujian.gov.cn/business/detail?cid={M_ID}&type={KIND}"
   # 新 (对):
   url_template: "https://ggzyfw.fujian.gov.cn/business/detail?name={NAME}&cid={PROCODE}&type={KIND}"
   ```

3. **存量数据 backfill** —— 容器内跑一次性脚本重建 source_url:
   ```python
   #!/usr/bin/env python
   """fix_source_url_ggzyfw_trade.py — 回填 ggzyfw_fujian_trade 的 source_url"""
   import sys, json
   from urllib.parse import quote
   sys.path.insert(0, "/ragflow")
   from api.db.db_models import DB
   from api.db.services.crawler_service import CrawlerResultService

   @DB.connection_context()
   def fix():
       rows = list(CrawlerResultService.model
           .select(CrawlerResultService.model.id, CrawlerResultService.model.extracted_json)
           .where(CrawlerResultService.model.site_id == "ggzyfw_fujian_trade"))
       updated = 0
       for r in rows:
           ej = r.extracted_json
           d = json.loads(ej) if isinstance(ej, str) else ej
           if not isinstance(d, dict):
               continue
           name, procode, kind = d.get("NAME", ""), d.get("PROCODE", ""), d.get("KIND", "")
           if not procode:  # 旧数据可能没 PROCODE,跳过
               continue
           new_url = (f"https://ggzyfw.fujian.gov.cn/business/detail"
                      f"?name={quote(str(name), safe='')}"
                      f"&cid={quote(str(procode), safe='')}"
                      f"&type={quote(str(kind), safe='')}")
           CrawlerResultService.update_by_id(r.id, {"source_url": new_url})
           updated += 1
       print("updated:", updated)

   fix()
   ```
   932 行中 755 行有 PROCODE 已更新; 177 行 PROCODE 为空保留旧 URL。

**预防要点（url_template 配置清单）**:
- [ ] 字段名来自 API 响应的真实字段,不要用列表序号 (`M_ID`) 冒充详情 ID
- [ ] 中文 / 含特殊字符的字段必须 URL quote —— 渲染层统一处理,**不要**在 YAML 里手动加 quote
- [ ] 上线前**手动打开 3 条不同 section 的原链接**验证可访问
- [ ] 存量数据迁移时优先查"目标 URL 真正需要哪些字段",而非沿用列表接口字段

---

### 15.5 今日涉及文件清单（部署用）

| 文件 | 改动 | 是否需重启 Docker |
|------|------|------------------|
| `rag/svr/crawler_engine/config.py` | 补 `detect_*` 字段 | 否 (subprocess) |
| `api/apps/restful_apis/collection_app.py` | list 排序改 `publish_date DESC` | **是** |
| `rag/svr/crawler_engine/extractors/base.py` | url_template 加 URL quote | 否 (subprocess) |
| `rag/svr/crawler_sites.yaml` | ggzyfw_fujian_trade url_template 改字段 | 否 (subprocess) |
| (DB) | `crawler_result.source_url` 批量 backfill 755 行 | — |

---

### 15.6 探测监控与采集任务列表数据源不一致（★ 通用规约）

**现象**: 用户反馈「B 端智能采集 → 探测监控」面板列出的站点与「采集任务列表」页签不一致 —— 监控面板会出现任务列表里根本没有配置过的站点。

**根因（3 处数据源策略不一致）**:

| 组件 | 原过滤逻辑 | 问题 |
|------|----------|------|
| 采集任务列表 `/crawl4ai/tasks` | `crawler_task` 表，**全局共享**（不按 tenant 过滤，见 `crawl4ai_app.py:168`） | 基准 |
| 监控面板 `/collection/detect/state` | `_active_site_ids(tenant_id)` 按 `tenant_id=current_user.id` 过滤 | 与基准策略不符 |
| 探测器脚本 `crawler_detector.py` | `loader.get_enabled()` + `[s for s in sites if s.detect_enabled]`，**完全不查 `crawler_task`** | 对 YAML 84 个站点无差别探测，state 漂移 |

任一组件与其他两者策略不同步就会出现"列表里有但监控没"或反向的偏差。

**修复（统一以 `crawler_task` 表为唯一真相源）**:

1. `api/apps/restful_apis/collection_app.py::_active_site_ids()` —— 去掉 tenant_id 参数，全局查 `WHERE enabled=True`，与 `CrawlerTaskService.get_list` 共享模式对齐。
2. `rag/svr/crawler_detector.py` —— 新增模块级辅助函数 `_get_crawler_task_site_ids()`，`main()` 把 YAML `detect_enabled` 过滤替换为查 `crawler_task` 表。YAML 只提供站点元数据（URL/selector/interval），不再决定"探不探"。
3. DB 异常 fail-safe —— 返回 None 让调用方跳过过滤（监控面板）或跳过本轮探测（detector），不静默吞错。

**通用规约（后续脚本/服务照做）**:

> **凡是需要列举"当前在采的站点集合"的代码路径（前端 API / 后端脚本 / 定时任务），必须查 `crawler_task WHERE enabled=True`，且不按 tenant 过滤。** YAML 只提供 per-site 的元数据（URL、选择器、探测间隔），不是站点清单的真相源。

**自检清单**:

- [ ] 新增任何"列站点"的接口或脚本前，先确认 `CrawlerTaskService.get_list` 的过滤策略，保持一致
- [ ] DB 查询用 Peewee 参数化 + `@DB.connection_context()`，异常返回哨兵值（None 或空集）并打 ERROR 日志
- [ ] `crawler_task.site_id` 在 YAML 里缺失时打 WARN 跳过，不崩（允许 DB 有 YAML 未收录的预发布站点）
- [ ] 跨组件数据源策略变更必须三处同步：`collection_app._active_site_ids`、`crawler_detector._get_crawler_task_site_ids`、`crawl4ai_app.list_tasks`
- [ ] 上线后用以下 diff 验证三处返回值一致：
  ```bash
  docker exec docker-ragflow-cpu-1 python -c '
  from api.apps.restful_apis.collection_app import _active_site_ids
  from rag.svr.crawler_detector import _get_crawler_task_site_ids
  print("equal:", _active_site_ids() == _get_crawler_task_site_ids())
  '
  ```

**注意（不在本次范围）**:

- 探测器 Redis state key 仍按 `--tenant-id` 存（`detector:state:{tenant}:{site}`），与 `ensure_detector_task` 按 tenant 注册对称。跨租户共享 state 是后续诉求，本次不处理。
- YAML `detect_enabled` 字段保留但**不再作硬过滤**；未来若需 per-site 关探测，直接 `crawler_task.enabled=false`，语义比 YAML flag 更一致。

---

### 15.7 CLI/手动写入 tenant_id 选错导致前端列表看不到数据（★ 通用规约）

**现象**: 新接入 `fujian_zfcg_ningde_zcfg` 站点，CLI 单站测试日志显示 `Done: 18 new items`、DB 查 `crawler_result` 也有 18 行，但前端「智能采集 → 采集结果」Tab 列表为空，看不到这个站点的数据。

**根因（tenant_id 错位）**:

| 数据流 | tenant_id 取值 |
|--------|---------------|
| 前端登录用户 `lg18629285296@163.com` | `7ab771d4dec84f23b2c1fb5f4e453ff9`（user.id == tenant.id，项目约定） |
| KB `3b4f619c85c211f198269135a1db216c` | `7ab771d4dec84f23b2c1fb5f4e453ff9`（与登录用户匹配） |
| CLI 测试时 `--tenant-id` 传值 | `bdfe565472c911f1802f998384c9836a`（**误从 `SELECT id FROM tenant LIMIT 3` 拿了第一条**，是个不相干的老租户） |
| 18 条 `crawler_result.tenant_id` 落库 | `bdfe565472c911f1802f998384c9836a`（错） |

`collection_app.py::list_results()` 默认按 `CrawlerResult.tenant_id == current_user.id` 过滤（line 538），登录用户的 `current_user.id` 是 `7ab771d4dec84f23b2c1fb5f4e453ff9`，与数据行的 `bdfe5654...` 不匹配 → 列表查不到。

**修复（一次性 SQL 回填，不动代码）**:

```sql
-- 把错位 tenant_id 的行改回 KB 所属用户
UPDATE crawler_result
SET tenant_id = '7ab771d4dec84f23b2c1fb5f4e453ff9'
WHERE site_id = 'fujian_zfcg_ningde_zcfg'
  AND tenant_id = 'bdfe565472c911f1802f998384c9836a';

-- crawler_task 同步修
UPDATE crawler_task
SET tenant_id = '7ab771d4dec84f23b2c1fb5f4e453ff9'
WHERE id = '<task_id>';
```

KB 上传走 KBUploader 时使用 KB 自己的 tenant（与 CLI 传的 `--tenant-id` 解耦），所以 KB 文档归属本来就对，只是 `crawler_result` 行错位。

**通用规约（CLI 手动跑测试 / DB 直接 INSERT 时必读）**:

> **CLI 跑采集脚本时，`--tenant-id` 必须传「KB 所属用户的 user.id」，不能从 `SELECT id FROM tenant LIMIT N` 拿。**

1. **取 tenant_id 的正确姿势**（二选一）：
   ```bash
   # 方式 A：从 KB 反查（推荐，永远对得上）
   docker exec docker-mysql-1 mysql -uroot -pinfini_rag_flow rag_flow -e \
     "SELECT tenant_id FROM knowledgebase WHERE id='<KB_ID>';"

   # 方式 B：从已知登录邮箱反查
   docker exec docker-mysql-1 mysql -uroot -pinfini_rag_flow rag_flow -e \
     "SELECT id FROM user WHERE email='lg18629285296@163.com';"
   ```

2. **错误姿势（必避）**:
   - ❌ `SELECT id FROM tenant LIMIT 1` — 取到的是任意老租户，与登录用户无关
   - ❌ 用其它站点的 tenant_id 套用（除非确认同一用户）

3. **生产部署用 trigger 端点不会踩这个坑**：`crawl4ai_app.py::trigger_task()` 自动从 `crawler_task.tenant_id`（建任务时填的 `current_user.id`）透传给子进程，不需要手动指定。

4. **创建 `crawler_task` 行时 tenant_id 也要对**：CLI/SQL INSERT 任务时，tenant_id 字段必须是目标用户的 user.id，否则任务列表能见到（list_tasks 不过滤 tenant，见 §4.4），但 trigger 后产出的数据看不到。

**自检清单**:

- [ ] CLI 跑测试前先 `SELECT tenant_id FROM knowledgebase WHERE id='<KB_ID>'` 拿到正确值
- [ ] 写 `crawler_task` 行时 `tenant_id` 字段同步用上述值
- [ ] 跑完后用 `SELECT tenant_id, COUNT(*) FROM crawler_result WHERE site_id='<SID>' GROUP BY tenant_id` 校验全部行归属正确
- [ ] 前端登录后能在「采集结果」Tab 看到本次抓取的数据 → 才算端到端通过

**与 §15.6 的关系**:

§15.6 解决的是"列站点清单时跨组件过滤策略不一致"（YAML vs crawler_task），本节解决的是"写入与查询的 tenant_id 不一致"（CLI vs list_results）。两者都是数据归属/过滤不匹配导致前端看不到数据，但层面不同：

| 节 | 维度 | 不一致的两端 |
|----|------|-------------|
| §15.6 | 站点集合 | YAML `detect_enabled` ↔ `crawler_task WHERE enabled=True` |
| §15.7 | 数据行归属 | CLI `--tenant-id` ↔ KB `tenant_id` / 登录用户 `current_user.id` |

---

## 16. 站点配置体系全景（2026-07-27 统计）

> 统计对象：`rag/svr/crawler_sites.yaml` 当前 95 个 YAML 站点（智能采集任务列表的来源）。
> 老脚本（§17）已废除，所有"采集任务"统一指 YAML 节点。

### 16.1 维度分布速查

| 维度 | 取值 | 数量 |
|------|------|------|
| **适配器 transport.type** | `rest_api` | 52 |
| | `spa_render` | 31 |
| | `encrypted_api` | 14 |
| | `playwright_http` | 1 |
| **分页 pagination.type** | `page_no` | 58 |
| | `single_page` | 27 |
| | `html_regex` | 42（含旧站点未迁移到 page_no 的）|
| | `click_next` | 11 |
| | `offset` | 6 |
| **提取器 extract.type** | `css_selector` | 99（多栏目站点重复计入）|
| | `json_path` | 64 |
| | `js_extract`（CSS 选器 + JS evaluate）| 5 |
| | `ai_extractor` | 0（兜底，未启用）|
| **category** | `announcement` | 10 |
| | `policy` | 8 |
| | `other` | 5 |
| | `tender` | 4 |
| | `bid` | 4 |
| | `news` | 3 |
| | `objection` | 1 |
| **page_size_param 取值** | `pageSize` / `pageSize` | 44 |
| | `rows` | 4（默认值，必须显式覆盖）|
| | `limit` / `rn` / `page.size` / `pagesize` | 11 |
| **pagination.start** | 1（1-based API）| 101 |
| | 0（0-based API，如 EpointWebBuilder）| 9 |
| **anti_crawler.delay_min** | 1.0s（默认）| 49 |
| | 2.0s（敏感站点）| 26 |
| | 1.5s | 11 |
| | ≤ 0.5s（高频 API）| 11 |
| **verify_ssl: false** | 政府自签名证书 | 72 / 95 |
| **sections 多栏目** | 配置了 sections 的站点 | 45 / 95 |
| **session_init_url** | 需要预请求获取 Cookie | 9 |
| **network_idle: false** | SPA 但禁用 networkidle 等待 | 21 / 31 |

### 16.2 通用功能点（80%+ 站点共有）

1. **HTTP 请求头自定义**：86 站点显式写 `User-Agent`（覆盖 Chromium 桌面版），17 站点加 `Referer`。新加站点必加 UA，政府站点常加 Referer。
2. **SSL 关闭**：72 站点 `verify_ssl: false`（政府自签名证书普遍）。**默认就关**，除非确认是对外 CA。
3. **page_no 分页 + pageSize 参数**：58 站点用 `page_no` + `page_size_param: "pageSize"`。这是最稳的搭配。
4. **1-based 页码**：101 处 `start: 1`。**默认 1**；0-based 站点（EpointWebBuilder 类）必须显式写 `start: 0`，否则首屏数据丢失（见 §15 踩坑）。
5. **anti_crawler 延迟**：1.0s 是甜点值；敏感站点（ggzyfw、zycg）拉到 2.0s 以上。
6. **detail.css_selector + content_field**：详情页正文 80% 用 CSS 选择器。常见容器：`#infoContentM`（EpointWebBuilder）/ `.TRS_Editor`（TRS CMS）/ `#zoom` / `.article-content`。
7. **extract.fields 显式映射**：标准字段 `title` / `url` / `date` / `id` 必须映射，否则 CollectionWriter 拒收。

### 16.3 差异化模式（少数站点特有）

| 模式 | 代表站点 | 关键配置 |
|------|---------|---------|
| **SM4/AES 加密** | `ggzyfw_fujian`、`ggzyjd_*`、`ggzyfw_*` | `transport.type: encrypted_api` + `encryption: {type, key, iv}` |
| **SPA + Playwright** | `zfcg`、`ggzyjd_cases`、`etrading` | `transport.type: spa_render` + `transport.network_idle` + 可选 `js_extract` |
| **MD5 签名** | `ggzyfw_fujian` 系列 | `signing: {type: md5, fields: [...]}` |
| **session_init 预请求** | 9 个站点（`cebpubservice`、`etrading` 等）| `session_init_url` 拿 Cookie 后才请求列表 |
| **HTML 正则分页** | 42 个老站点 | `pagination.type: html_regex` + `total_regex` + `page_link_regex`（新站点建议迁 `page_no`）|
| **0-based 页码** | `gcjyzx_*`、`gcjyzx_jyxx`、`zz_*` | `pagination.start: 0`（EpointWebBuilder 特征）|
| **点击下一页** | 11 个 SPA | `pagination.type: click_next` + `next_button_selector` |
| **js_extract 自定义 JS** | `ggzyjd_*`、`zfcg` | `extract.js_extract: | () => {...}` 浏览器内 evaluate，列表项是 `<a>` 自身的场景 |

### 16.4 痛点（重复出现的样板 / 易踩坑）

1. **page_size_param 默认是 `"rows"`** — 至少 4 个站点因为没显式覆盖导致 500 错误。**所有新站点必须显式写**。
2. **0-based 站点首屏丢失** — paginator 默认从 `last_page+1=1` 开始，0-based 站点第一页（pageIndex=0）跳过。已在 `engine.py:389-395` 修复（`if state.last_page == 0: start_page = pag_cfg.start`）。
3. **SPA networkidle 永不空闲** — 政府站点常挂百度统计/hm.baidu.com 长连接，`networkidle` 必超时。解决：YAML `transport.network_idle: false` + adapter 用 `domcontentloaded`。
4. **政府自签名 SSL** — 72 站点必须 `verify_ssl: false`，否则 urllib 抛错。
5. **附件 onclick 隐藏** — EpointWebBuilder 用 `<a onclick="ztbfjyz('/...')">`，无 href。`engine.py:635-700` 已加 onclick fallback（ztbfjyz / window.open / location.href / downloadFile）。
6. **加密 key/iv 是 base64 字符串** — 必须用项目的 `encrypted_api` 适配器，不能直接写裸 requests。
7. **CSS 选器语法差异** — 本项目用 BS4（`a@href`），不是 Scrapy（`a::attr(href)`）。
8. **section 覆盖父级配置** — section 只覆盖 listing URL 不够，必须同时覆盖 `extract.fields` 和 `items_path` 才生效；只覆盖 params 也得合并（`engine.py:336` dataclasses.replace）。

### 16.5 难点（实现复杂 / 需要逆向）

1. **加密响应解密** — 14 站点。SM4-ECB / AES-CBC + MD5 签名，老脚本里有完整逆向（如 `ggzyfw_fujian_crawler.py`）。新 YAML 直接复用 `encrypted_api` 适配器，不需要重新实现。
2. **SPA 列表项提取** — 列表项是 `<a class="list-item">` 自身（不是 `<tr>`/`<li>` 容器），需取自身 text + 子元素 date。CSS 提取器搞不定，必须 `js_extract` 写 JS evaluate。
3. **EpointWebBuilder 附件 onclick** — 详见 §15.5 / `engine.py:635-700`。难点：嵌套 `<div>` + 双引号属性内嵌单引号 → 普通 regex 截断。需要 backreference。
4. **date_filter 注入到 listing params** — 不带日期窗口的 API 返回近期全量（如 gcjyzx 87972 条），CollectionWriter 按 item 日期过滤后 0 命中。`engine.py:705-739` 加了 opt-in 注入（站点声明 `startdate`/`enddate` 键才触发）。
5. **tenant_id 错位** — §15.7。CLI `--tenant-id` 必须等于 KB.tenant_id，否则数据写进去前端看不到。
6. **HTML 正则分页** — 42 个老站点用，列表底部 `<a href="?page=2">` 这种模式。新站点不建议用，优先 `page_no`。

### 16.6 核心点（每个站点不可省略）

1. **site_id 全局唯一**（小写+下划线），与 `crawler_task.site_id` / `crawler_result.site_id` 对齐。
2. **name（中文名）+ site_url** — API 自动派生 `site_name` + `site_domain` 返回前端（参考 memory `feedback_site_metadata_convention.md`）。
3. **transport.type** — 4 选 1，决定 adapter。
4. **listing.url + method + body_type/params** — POST 站点必须写 `body_type: form` 或 `body_type: json`。
5. **pagination.page_size_param** — 必须显式写真实参数名，不写就用默认 `"rows"`（踩坑）。
6. **extract.fields** — title/url/date/id 四个标准字段必须映射到 API 实际字段名（不匹配 → NO_ITEMS）。
7. **category** — 决定路由（bid → bid_*；其他 → crawler_result + 扩展表）。
8. **format.parser_id** — KB 解析器，默认 `naive`。
9. **anti_crawler.delay_min/delay_max** — 默认 1.0/2.5 即可。
10. **detail.content_field**（如有详情页）— 必须是详情页正文容器的 CSS 选择器。

### 16.7 经验点（最佳实践，新站点继承）

1. **先看同域名老脚本再写 YAML** — 90% 的政府站点有同名老 `*_crawler.py`，复用其字段名/加密/选择器，不重新逆向（memory `feedback_reference_old_crawler.md`）。
2. **section.name 决定前端"类型"列** — 单栏目站点也建议配 1 个 section，name 写中文（如「漳州市工程项目-工程信息」）。`section_name` 优先于 `category_label`。
3. **category=other 是万能兜底** — 不确定时用 `other`，路由到 `crawler_result` 表不带扩展表。需要结构化字段（如政策文号、统一社会信用代码）才升级到 `policy`/`personnel`。
4. **transport.headers.User-Agent 用真实 Chrome** — 不要用 Python-urllib 默认 UA。headless Chromium 也必须显式覆盖（`BrowserPool` 已硬编码）。
5. **REST API 站点优先选 json_path 提取器** — 比 css_selector 稳，字段重命名直接走 extract.fields 映射。
6. **SPA 站点先写 debug 脚本验证渲染** — 容器内跑 `_debug_xxx.py` 用 sync_playwright 看 HTML 长度，>30KB 且选择器命中再写 YAML（CLAUDE.md「SPA 调试流程」）。
7. **max_pages 默认 30~50** — 不要写 200，避免被 IP block；首次回溯走 CLI 多次（每次 date-filter 一天）而不是一次性大分页。
8. **enabled: true 但 detect_enabled 走 defaults** — 默认开探测监控；不希望上探测面板的站点显式 `detect_enabled: false`。
9. **附件白名单路径特征** — engine.py onclick fallback 用 `/zzggzy/`、`/attach/download`、`attachguid=`、文件扩展名四重判断，避免误把 JS 跳转当附件。
10. **YAML 改动 + engine 改动成套 SCP** — 见 CLAUDE.md「智能采集系统部署清单」，单文件 SCP 会连环报错。

### 16.8 接入新站点 SOP（8 步）

参考 §4.5 / memory `feedback_scheduled_tasks_open_access.md`：

1. 看 `rag/svr/<site_id>_crawler.py` 老脚本（如有），抄字段映射和加密
2. 容器内写 `_debug_xxx.py` 验证请求能通（UA / SSL / 加密 / Cookie）
3. YAML 加节点（按 §16.6 核心点 10 项）
4. SCP YAML 到服务器
5. 容器内 import 冒烟：`ConfigLoader(...).load().get("<site_id>")` 不抛错
6. 单日 CLI 测试：`--date-filter YYYY-MM-DD`，验证 `[CRAWLER] Done: N new items`
7. DB 校验：`SELECT COUNT(*), GROUP_CONCAT(DISTINCT tenant_id) FROM crawler_result WHERE site_id='<SID>'`
8. 前端「智能采集 → 采集结果」Tab 出现数据，类型列显示 section.name

---

## 17. 老脚本废除标记（2026-07-27）

> **重要约定**：`rag/svr/*_crawler.py`（86 个老脚本）已废除，**不再维护**。新对话不要把它们当作"当前在用的爬虫"。所有"采集任务"统一指 `crawler_sites.yaml` 中的 YAML 节点。

### 17.1 废除清单

`rag/svr/` 下以下脚本全部已废除（列表截至 2026-07-27）：

```
ccgp_crawler.py              ccgp_jdcf_gg_cr_crawler.py    ccgp_zcfg_crawler.py
ccgp_zjlwbcbz_crawler.py     cebpubservice_consult_crawler.py
cebpubservice_crawler.py     chinabidding_crawler.py       czt_crawler.py
easy_prt_bidding_crawler.py  easy_prt_crawler.py           easy_prt_policy_crawler.py
easy_prt_trading_crawler.py  enjoy5191_crawler.py          enjoy5191_policy_crawler.py
enjoy5191_trade_crawler.py   etrading_crawler.py           etrading_statute_crawler.py
fjcia_edu_crawler.py         fjnj_crawler.py               fjnj_jdhy_crawler.py
fjtba_crawler.py             fjtba_pxzx_crawler.py         fjtba_wfwg_crawler.py
fycbid_crawler.py            gcjyzx_crawler.py             gcjyzx_jyxx_crawler.py
gcjyzx_wgtb_crawler.py       gcjyzx_zcfg_crawler.py        ggzy_crawler.py
ggzy_policy_crawler.py       ggzyfw_business_crawler.py    ggzyfw_crawler.py
ggzyfw_fujian_business_crawler.py  ggzyfw_fujian_crawler.py
ggzyfw_guide_crawler.py      ggzyfw_guide_trade_crawler.py ggzyfw_policies_crawler.py
ggzyjd_crawler.py            ggzyjd_policies_crawler.py    ggzyjy_crawler.py
hwdms_policy_crawler.py      jdhy_crawler.py               jsj_crawler.py
jsj_zhangzhou_crawler.py     jtyst_crawler.py              jtyst_jdhy_crawler.py
longyan_crawler.py           mohurd_crawler.py             mwr_crawler.py
ncha_crawler.py              ndrc_crawler.py               ndrc_fzggwl_crawler.py
ningde_gcjs_crawler.py       nj_crawler.py                 pingtan_fjsz_crawler.py
putian_fwzx_crawler.py       putian_zcwj_crawler.py        quanzhou_crawler.py
quanzhou_zcfg_crawler.py     slbgb_crawler.py              slt_crawler.py
slt_xxgk_crawler.py          slt_zcjd_crawler.py           smjy_crawler.py
smzcfg_crawler.py            smzx_notice_crawler.py        ygcg_crawler.py
ygcg_engineering_crawler.py  zfcg_crawler.py               zfcg_jdgl_crawler.py
zfcg_xmgg_crawler.py         zfcg_zcfg_crawler.py          zhangzhou_crawler.py
zhangzhou_fgw_crawler.py     zjfw_crawler.py               zjfw_score_sort_crawler.py
zjk_notice_crawler.py        zjk_zffg_crawler.py           zjt_crawler.py
zjt_jdhy_crawler.py          zjt_xxgk_crawler.py           zycg_crawler.py
zycg_notice_crawler.py       zycg_zdfg_crawler.py          zz_crawler.py
zz_zhangzhou_crawler.py
```

### 17.2 保留使用的框架文件（不算老脚本）

| 文件 | 用途 |
|------|------|
| `rag/svr/unified_crawler.py` | ★ 统一爬虫 CLI 入口（YAML 站点的执行器） |
| `rag/svr/crawler_engine/` | ★ 爬虫引擎 v2.1（adapter / paginator / extractor / writer）|
| `rag/svr/crawler_sites.yaml` | ★ 95 个站点配置（当前在用）|
| `rag/svr/crawler_detector.py` | ★ 探测监控脚本 |
| `rag/svr/wechat_mp_crawler.py` + `wechat_mp/` | 微信公众号采集（独立模块） |
| `rag/svr/bid_sync.py` | 标讯定时同步（独立逻辑） |

### 17.3 老脚本的归档价值

老脚本虽然不再维护，但**仍需保留在仓库**：

1. **逆向资料库** — 加密 key/iv、字段名映射、CSS 选择器都是从老脚本里逆向出来的，写新 YAML 时复用（memory `feedback_reference_old_crawler.md`）。
2. **回滚兜底** — YAML adapter 出问题时，老脚本可临时顶上。
3. **不要再修改** — 发现 bug 直接改 YAML + engine.py，不要回到老脚本改。

### 17.4 判定新老脚本的快捷方式

新对话不知道哪些是新体系时：

```bash
# 看站点列表（新体系，95 项）
grep -E "^  [a-z_]+:$" rag/svr/crawler_sites.yaml

# 看一个站点是否在 YAML（新体系）里
grep -E "^  <site_id>:$" rag/svr/crawler_sites.yaml

# 老脚本（废除中，仅归档）
ls rag/svr/*_crawler.py
```

「智能采集任务列表」== YAML 节点（`crawler_sites.yaml`），≠ `*_crawler.py` 文件。

---

## 18. tenant_id 一致性（CRITICAL 踩坑）

### 18.1 现象

手动触发采集脚本后，DB 里 96 条 `crawler_result` 都在，但前端「智能采集 → 采集结果」列表为空。

### 18.2 根因

`/api/v1/collection/results` API 用 `CrawlerResult.tenant_id == current_user.id` 过滤（`collection_app.py:540`），但写入 DB 时的 `tenant_id` 是采集脚本 CLI `--tenant-id` 传入的值。若两者不是同一个 user id，列表就为空。

**典型场景:** 开发者用 admin (`5a4a082e51cc11f192cdaddbbf4d29e9`, RAGFlow 默认 owner) 的 id 跑脚本，但用户日常用 `lg18629285296@163.com` (id `7ab771d4dec84f23b2c1fb5f4e453ff9`) 登录 → 数据归属 admin，列表按 konus 过滤 → 空。

### 18.3 正确做法

**触发脚本前先确认登录账号的 user id**，用该 id 作为 `--tenant-id`：

```bash
docker exec docker-mysql-1 mysql -uroot -pinfini_rag_flow rag_flow \
  -e "SELECT id, email FROM user WHERE email='<你的登录邮箱>'"
```

```bash
# 用上面查到的 id（不是 admin 的默认 id！）
docker exec docker-ragflow-cpu-1 python /ragflow/rag/svr/unified_crawler.py \
  --tenant-id <你的user_id> --kb-id <KB_ID> \
  --task-name ggzy_policy \
  --writer collection --category policy \
  --script-args '{"site_id":"ggzy_policy"}'
```

### 18.4 已有数据修复 SQL

若已经用错误 tenant_id 跑过，可批量迁移：

```sql
UPDATE crawler_result SET tenant_id='<正确user_id>'
  WHERE site_id='<site_id>' AND tenant_id='<错误user_id>';
UPDATE crawler_task SET tenant_id='<正确user_id>'
  WHERE site_id='<site_id>' AND tenant_id='<错误user_id>';
-- collection_policy_ext / collection_personnel_ext / collection_objection_ext 无 tenant_id 字段
-- （通过 result_id 关联），无需更新
```

### 18.5 验证

迁移后前端「采集结果」（按 category 过滤 policy）应立即可见；`SELECT tenant_id, COUNT(*) FROM crawler_result WHERE site_id='<site_id>' GROUP BY tenant_id` 应只剩一个 tenant。

### 18.6 后续触发

探测器 / 定时调度都通过 `crawler_task.id` 关联，会自动用 task 行上的 `tenant_id`（迁移后已正确），新数据不会再错位。

---

## 19. zhangzhou_zwgk 实践总结

第一个 custom_runner 模式站点上线全流程踩坑记录。

### 19.1 采集列表「类型」列填充规则（前端）

前端 `results-tab.tsx` 的类型列优先级：

```tsx
// line 268-270
{r.section_name || r.category_label}
```

| 字段 | 来源 | 说明 |
|------|------|------|
| `section_name` | `crawler_result.section_name` | Writer 写入时从 data 中取 `section_name` 字段 |
| `category_label` | `crawler_result.category_label` | Writer 写入时从 data 中取 `category_label` 字段 |

**如果两个字段都为空，前端显示空 Badge。** 写入时必须至少填其中一个。

### 19.2 category 端到端链路

```
YAML category 字段
  → unified_crawler 读取 site_cfg.get("category")
  → 传给 custom_runner.run(category=...)
  → custom_runner 传给 CollectionWriter.write_all(..., category=category)
  → CollectionWriter._write_one() 写入 crawler_result.category (VARCHAR(32))
  → 前端 type 列不直接显示 category，显示 section_name || category_label
```

**关键点**：
- `category` 是实体字段（VARCHAR(32)），存储站点级分类如 `漳州市人民政府-政务公开`
- `category_label` 和 `section_name` 是 JSON 扩展字段，存栏目级标签
- category 值必须在 32 字符以内（中文约 10 个字）
- YAML 中 category 不填时默认为 `"news"`，前端会显示为「新闻资讯」

### 19.3 site_display 填充规则

`site_display` 格式：`"中文名 域名"`，如 `"漳州市人民政府 www.zhangzhou.gov.cn"`。

```python
site_display = f"{_SITE_NAME} {_BASE.replace('https://', '')}"
```

API 端 `collection_app.py:572-587` 会自动拆分：
```python
parts = site_display.rsplit(" ", 1)
site_name = parts[0]    # "漳州市人民政府"
site_domain = parts[1]  # "www.zhangzhou.gov.cn"
```

前端 source 列：`{r.site_display || r.site_id}`。

### 19.4 tenant_id 错位问题

**根因**：crawler_detector 使用 admin 权限（`Authorization` header），但 detector 传给 unified_crawler 的 `tenant_id` 来自 CLI 参数。若 CLI 参数传入的是 admin 的 tenant_id，而用户自己的 id 不同，写入 `crawler_result.tenant_id` 就是错误的。

**症状**：脚本运行成功有数据，但前端查不到（API 强制 `WHERE tenant_id = current_user.id`）。

**修复**：
1. 确保 detector task 和 crawler_task 的 `tenant_id` 都设为目标用户
2. 已错位数据通过 SQL UPDATE 迁移

### 19.5 custom_runner 的 date_filter 规范

`unified_crawler` 传给 custom_runner 的 `date_filter` 可能是 `"today"` 字符串，**不会自动转为日期**。custom runner 必须在入口处调用归一化：

```python
def crawl(tenant_id, kb_id, task_id="", date_filter="", max_runtime=3600):
    date_filter = _normalize_date_filter(date_filter)  # "today" → "2026-07-29"
```

`_normalize_date_filter()` 实现：
```python
def _normalize_date_filter(df: str) -> str:
    if not df:
        return ""
    now = datetime.now()
    if df == "today":
        return now.strftime("%Y-%m-%d")
    if df == "yesterday":
        return (now - timedelta(days=1)).strftime("%Y-%m-%d")
    return df  # 已经是 YYYY-MM-DD
```

### 19.6 custom_runner 文件结构模板

```python
#!/usr/bin/env python3
"""<站点中文名> 采集脚本 — custom_runner 模式"""

# === 常量 ===
_BASE = "https://www.example.gov.cn"
_SITE_NAME = "站点中文名"
_SITE_ID = "site_id"
_TYPE_NAME = "站点中文名-栏目名"

# === 列表页抓取 ===
def _fetch_list_pages(...) -> list:
    """遍历分页返回 items 列表"""

# === 详情页抓取 ===
def _fetch_detail(url) -> dict:
    """抓取单篇详情，返回 {title, pub_date, source, content_text, attachments}"""

# === 写入 DB+KB ===
def _write_to_db(items, tenant_id, kb_id, task_id, category, section_name=""):
    writer = CollectionWriter(tenant_id, kb_id)
    site_display = f"{_SITE_NAME} {_BASE.replace('https://', '')}"
    for data in items:
        if section_name:
            data["section_name"] = section_name
        writer.write_all(item=data, site_id=_SITE_ID, category=category,
                         task_id=task_id, site_display=site_display)

# === 入口 ===
def crawl(tenant_id, kb_id, task_id="", date_filter="", max_runtime=3600):
    date_filter = _normalize_date_filter(date_filter)
    # ... 遍历 tabs → _fetch_list_pages → _fetch_detail → _write_to_db
```

### 19.7 上线检查清单

- [ ] YAML `category` 字段 = 站点中文名（非 `news`）
- [ ] YAML `detect_enabled: true` + `detect_interval: 3600`
- [ ] `crawler_task` 表中存在对应行（`site_id` 匹配，`enabled=1`，`tenant_id` 正确）
- [ ] `crawler_sites.yaml` 中 `name` 字段有中文名，`site_url` 有域名
- [ ] custom_runner 入口归一化 `date_filter`
- [ ] `CollectionWriter.write_all()` 传了 `site_display` 参数
- [ ] 分页循环有 `date_filter` 提前终止逻辑
- [ ] 单站测试通过（`--writer collection --category <中文名>`）
- [ ] 前端「采集监控」可见该站点（`detect_enabled=true`）
- [ ] 前端「采集结果」列表类型列正确（非「新闻资讯」）

---

