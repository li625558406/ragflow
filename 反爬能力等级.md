# 爬虫反防爬能力等级

> **版本**：v1.0
> **日期**：2026-07-22
> **分支**：`feat/unified-crawler-framework`
> **适用范围**：统一爬虫 v2.1 + 智能采集（新）系统，所有 YAML 驱动站点

---

## 1. 能力分级总览

| 等级 | 强度 | 适用场景 | 适配器 | 浏览器依赖 |
|------|------|---------|--------|-----------|
| 🟢 一级 | 基础 | 开放政府/行业列表站，无登录、无 Cloudflare、无 SPA | `rest_api` | 否 |
| 🟡 二级 | 中级 | 加密接口 / TLS 指纹检测站 | `encrypted_api` / `playwright_http` | playwright_http 需要 |
| 🔴 三级 | 强反爬 | SPA 渲染站 / Cloudflare Turnstile 防护 / 动态 DOM | `spa_render` / `scrapling_stealth` | 必需 |

**选型决策**：

```
站点返回 JSON API?
  ├── 是 → 响应加密?
  │        ├── 是 → 二级 (encrypted_api)
  │        └── 否 → 一级 (rest_api)
  └── 否 → 需要 JavaScript 渲染?
           ├── 是 → 有 Cloudflare?
           │        ├── 是 → 三级 (scrapling_stealth)
           │        └── 否 → 三级 (spa_render)
           └── 否 → 一级 (rest_api HTML 模式)
```

---

## 2. 🟢 一级（基础反爬）

**默认即具备**，由 `anti_crawler.py` + `session_manager.py` + `rest_api` 适配器提供。

### 2.1 能力清单

| 能力 | 实现位置 | 配置项 | 默认值 |
|------|---------|--------|--------|
| **UA 轮换** | `anti_crawler._UA_POOL` (8 个真实浏览器指纹) | 自动 | 自动 |
| **随机延迟** | `AntiCrawlerManager.delay()` | `anti_crawler.delay_min/max` | 1.0–2.5s |
| **指数退避重试** | `AntiCrawlerManager.backoff_delay()` | `anti_crawler.exponential_backoff` | true |
| **最大重试次数** | `AntiCrawlerManager.should_retry()` | `anti_crawler.max_retries` | 3 |
| **429 限流自愈** | `session_reset_on_429` → 清 cookie + 重访问 `session_init_url` | `anti_crawler.session_reset_on_429` | false |
| **连续空页停止** | `AntiCrawlerManager.record_empty_page()` | `anti_crawler.max_consecutive_empty` | 3 |
| **Cookie 预热** | `SessionManager.create()` → GET `session_init_url` | `transport.session_init_url` | 空 |
| **强制 UTF-8** | `_force_utf8` hook | 自动（政府站常见乱码兜底） | 自动 |
| **HTTP/SOCKS5 代理** | `SessionManager._configure_proxy()` | `transport.proxy.url/username/password` | 空 |
| **验证码 OCR** | `ddddocr` 离线识别 | import 失败自动跳过 | 可选 |
| **8 点前不爬** | `AntiCrawlerManager.check_eight_am()` | `anti_crawler.eight_am_check` | false |

### 2.2 YAML 配置模板

```yaml
sites:
  example_site:
    category: policy                          # bid|policy|personnel|news|other
    transport:
      type: rest_api                          # ← 一级核心标志
      verify_ssl: false                       # 政府站证书常过期
      timeout: 30
      headers:                                # 可选：自定义请求头
        Referer: "https://www.example.gov.cn"
      # session_init_url: "https://..."       # 可选：预热 Cookie (JSESSIONID 等)
      # proxy:
      #   url: "socks5://127.0.0.1:1080"
      #   username: ""
      #   password: ""
    anti_crawler:
      delay_min: 1.5
      delay_max: 3.0
      max_retries: 3
      max_consecutive_empty: 3
      exponential_backoff: true
      # session_reset_on_429: true            # 被限流时开启
      # eight_am_check: true                  # 政府站（凌晨更新）可开启
    listing: { ... }
    pagination: { ... }
    extract: { ... }
```

### 2.3 适用场景

- 国务院政策文件库、各部委公开栏目
- 省市政府信息公开网
- 行业协会公告列表
- 不需要登录的招投标公示页
- 所有返回标准 JSON 或静态 HTML 的开放接口

---

## 3. 🟡 二级（中级反爬）

### 3.1 加密接口 — `encrypted_api`

**应对**：响应体 SM4/AES 加密 + MD5 签名验证（常见于招投标交易平台）

```yaml
transport:
  type: encrypted_api
  verify_ssl: false
encryption:
  type: aes_cbc                               # aes_cbc | sm4_ecb
  key: "BASE64_ENCODED_KEY"
  iv: "BASE64_ENCODED_IV"
signing:
  type: md5
  fields: ["appKey", "timestamp", "nonce"]
  secret: "SIGN_SECRET"
```

**已接入示例站点**：`ggzyfw_fujian`, `ggzyjd`

### 3.2 浏览器指纹 HTTP — `playwright_http`

**应对**：JA3/TLS 指纹检测，需要浏览器环境但无需 SPA 渲染

```yaml
transport:
  type: playwright_http
  headless: true
  impersonate: "chrome124"                    # 浏览器指纹版本
  block_resources: true                       # 屏蔽图片/字体加速
  timeout: 30
```

**已接入示例站点**：`etrading`

---

## 4. 🔴 三级（强反爬）

### 4.1 SPA 渲染 — `spa_render`

**应对**：Vue/React/Angular 单页应用，列表通过 XHR 动态加载

```yaml
transport:
  type: spa_render
  headless: true
  network_idle: true                          # 等待网络空闲
  wait_for_selector: ".list-item"             # 等待列表渲染完成
  timeout: 60
```

**已接入示例站点**：`ggzyjd`, `zfcg`

### 4.2 Stealth 旁路 — `scrapling_stealth`

**应对**：Cloudflare Turnstile / 数据盾 / 极验等浏览器指纹+行为检测

```yaml
transport:
  type: scrapling_stealth
  headless: false                             # 有时非 headless 更不易被识别
  solve_cloudflare: true                      # ★ Cloudflare Turnstile 旁路
  impersonate: "chrome125"
  network_idle: true
  block_resources: false                      # 完整资源加载，更像真人
  adaptive: true                              # DOM 自愈选择器（应对布局变更）
  timeout: 60
```

**能力要点**：
- 浏览器 TLS 指纹伪装（camoufox）
- Cloudflare 5 秒盾、Turnstile 自动求解
- DOM 元素追踪 + 自愈选择器（`auto_save=True` → `adaptive=True`）
- 网络空闲等待（确保 SPA 完全渲染）

**依赖安装**：
```bash
pip install 'scrapling[fetchers]' && scrapling install
```

---

## 5. 共通能力（所有等级都有）

无论选哪个适配器，以下能力由 `anti_crawler.py` 统一提供：

- ✅ UA 池轮换（8 个真实浏览器指纹）
- ✅ 请求间随机延迟
- ✅ 指数退避重试
- ✅ 连续空页停止
- ✅ 429 限流检测 + Session 重置
- ✅ 验证码 OCR（ddddocr，按需）
- ✅ 8 点更新检查
- ✅ 代理支持（HTTP/HTTPS/SOCKS5 with 认证）

---

## 6. 选型排查流程

### 6.0 接入新站点必做的第一件事（CRITICAL）

**接入任何新站点之前，必须先查找并阅读现有同名/同域老脚本**：

1. 查找：`Glob rag/svr/*<keyword>*_crawler.py`（项目有 80+ 老脚本）
2. 阅读：请求方式 / 签名算法 / 加密 key / 字段映射 / 详情 URL 拼接
3. 复用：老脚本验证过的签名算法、加密 key、字段映射**直接复用**到 YAML，不要重新逆向

**Why**：老脚本经过实际运行验证，逆向资产可靠；重新逆向费时易错。

**示例**：
- 接入 `ggzyjd.fj.gov.cn` → 先看 `rag/svr/ggzyjd_crawler.py` / `ggzyjd_policies_crawler.py`
- 接入 `ggzyfw.fj.gov.cn` → 先看 `rag/svr/ggzyfw_*_crawler.py`

### 6.1 错误现象 → 升级路径

当新站点接入失败时，按以下顺序排查：

| 错误现象 | 原因 | 升级到 |
|---------|------|--------|
| HTTP 403 / 502 | 基础 UA + 延迟被识别 | 检查 headers，加 Referer；仍失败 → 二级 playwright_http |
| HTTP 200 但响应乱码 | 加密接口 | 二级 encrypted_api |
| HTTP 200 但列表为空 | SPA 动态渲染 | 三级 spa_render |
| HTTP 403 + Cloudflare 页面 | Cloudflare 防护 | 三级 scrapling_stealth |
| 列表正常但详情空白 | 详情页 SPA 渲染 | 三级 spa_render 或 detail.type=api_request |
| 长期稳定后突然失败 | 站点 DOM 改版 | 三级 scrapling_stealth（adaptive=true 自愈） |

---

## 7. 配置规范要求

1. **不要过度升级**：一级能解决的站点不要用三级（浏览器开销大 10x+）
2. **delay 设置克制**：`delay_min ≥ 1.0s`，`delay_max ≤ 5.0s`，避免压垮政府站
3. **max_pages ≤ 50**：防止意外拉取全站
4. **代理按需配置**：只有被封才启用，避免无谓成本
5. **verify_ssl: false 仅限政府站**：商业站点应保留 true
6. **scrapling_stealth 的 solve_cloudflare 仅在确有 Cloudflare 时开启**：误开会降低速度
7. **★ 输出端只做两件事**：存数据库 + 上传知识库并解析。禁止推送/通知/写文件/调外部业务 API（详见 `智能采集系统设计.md` § 8.1）

---

## 8. 相关文件索引

| 能力 | 实现文件 |
|------|---------|
| 反爬总调度 | `rag/svr/crawler_engine/anti_crawler.py` |
| Session 管理 | `rag/svr/crawler_engine/session_manager.py` |
| rest_api 适配器 | `rag/svr/crawler_engine/adapters/rest_api.py` |
| encrypted_api 适配器 | `rag/svr/crawler_engine/adapters/encrypted_api.py` |
| playwright_http 适配器 | `rag/svr/crawler_engine/adapters/playwright_http.py` |
| spa_render 适配器 | `rag/svr/crawler_engine/adapters/spa_render.py` |
| scrapling_stealth 适配器 | `rag/svr/crawler_engine/adapters/scrapling_stealth.py` |
| 配置定义 | `rag/svr/crawler_engine/config.py` |
| 全部站点配置 | `rag/svr/crawler_sites.yaml` |
