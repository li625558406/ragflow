# RAGFlow 二次开发功能汇总

> 基于 [RAGFlow](https://github.com/infiniflow/ragflow) v0.25.1 深度二次开发
> 聚焦标讯（招投标）数据采集、存储、检索、知识库解析和投标书自动写作

---

## 1. 标讯系统 (Bid System)

### 数据库 (10+2=12 张表)

| 表 | 用途 | TTL |
|---|---|---|
| `bid_project` | 标讯/中标/合同搜索缓存 | 1h |
| `bid_project_detail` | 项目详情 + content_html | 30天 |
| `bid_project_structure` | 结构化数据 (JSON) | 30天 |
| `bid_project_file` | 附件元数据 | 永久(覆盖) |
| `bid_project_parse` | 知识库解析状态 | 永久 |
| `bid_enterprise_cache` | 企业画像/联系人/客户/供应商缓存 | 7天/3天/1天 |
| `bid_enterprise_parse` | 企业知识库解析状态 | 永久 |
| `bid_enterprise_business` | ★企业工商信息全量缓存（新） | 7天 |
| `bid_tender_search` | ★标讯搜索 v4 缓存（新） | 24h |
| `bid_sync_log` | API同步日志 | 永久 |
| `bid_construction_project` | 拟在建项目 | 永久 |
| `bid_construction_parse` | 拟在建项目解析状态 | 永久 |
| `bid_contract_parse` | 合同解析状态 | 永久 |

### 缓存优先策略

第三方 API 按次收费，所有端点必须走 `DB 缓存 → API 降级`：

```
搜索请求 → DB 查询 (过滤未过期)
  ├── 命中 → 直接返回 (免API费)
  └── 未命中 → 调第三方API → upsert → 设TTL → 返回DB数据
       └── API失败 → stale fallback (返回过期缓存)
```

### REST API 端点 (20+)

详见 `CLAUDE.md` 标讯系统章节。

---

## 2. 企业查询 (Enterprise Query) ★新

**日期**: 2026-07-15

**接口**: 阿里云API市场 — `/enterprise/business/all`
- Host: `https://juccvvn.market.alicloudapi.com`
- 鉴权: `Authorization: APPCODE <code>`
- 入参: `keyword` (公司名或统一社会信用代码)
- 缓存TTL=7天

**返回数据**:
- 工商基本信息 (Base)、行业分类 (Industry)、联系方式 (ContactInfo)
- 股东信息 (Partners)、企业高管 (Employees)
- 企业变更 (Changes)、曾用名 (OriginalName)、分支机构 (Branches)
- 行政处罚 (Penalties)、经营异常 (Exceptions)、失信 (ShiXinItems)、被执行 (ZhiXingItems)
- 抽查检查 (SpotChecks)、行政许可 (Permissions)、纳税信用 (TaxCreditItems)
- 股权出质 (Pledges)、动产抵押 (MPledges)

**后端**:
- 模型: `BidEnterpriseBusiness` (`api/db/db_models.py:1707`)
- 服务: `BidEnterpriseBusinessService` (`api/db/services/bid_service.py:454`)
- 缓存函数: `get_enterprise_business_cached` (`api/utils/bid_tool_service.py`)
- API 客户端: `BidApiClient.get_enterprise_business_all` (`api/utils/bid_api_client.py:450`)
- 端点: `GET /api/v1/bid/enterprises/business?keyword=xxx` (`api/apps/restful_apis/bid_app.py`)

**前端**:
- 组件: `web/src/components/bid/enterprise-search.tsx` (完全重写)
- 5 个 Tab: 工商信息 | 股东高管 | 变更记录 | 经营风险 | 资质信息
- 页面初始化只显示搜索表单，不加载数据

**旧端点已移除**: `/bid/enterprises/profile`, `/contacts`, `/customers`, `/suppliers`

---

## 3. 标讯搜索 v4 (Tender Search) ★新

**日期**: 2026-07-15

**接口**: 阿里云API市场 — `/enterprise/search/bid/v4`
- Host: `https://jmzbxxjs.market.alicloudapi.com`
- 鉴权: `Authorization: APPCODE fba98d6069f94ca4948387cfbe0265ac`
- 仅 **10 次 API 调用额度**，必须激进 DB 缓存
- 请求方式: form-encoded POST (application/x-www-form-urlencoded)

**缓存策略**:
- 查询 `bid_tender_search` 表（keyword_hash 匹配 + 24h TTL）
- DB 结果 >= 5 → 直接返回（`from_cache: true`）
- DB 结果 < 5 → 调第三方 API → 逐条 upsert（sha256(projectNumber|title) 去重）→ 重查 DB → 返回
- API 失败 → 降级返回 DB 已有数据（`stale: true`）

**后端**:
- 模型: `BidTenderSearch` (`api/db/db_models.py:1718`) — ~40 字段，覆盖 API 全量响应
- 服务: `BidTenderSearchService` (`api/db/services/bid_service.py:646`)
- API 客户端: `BidApiClient.search_tender_v4` (`api/utils/bid_api_client.py:485`)
- Mock 模式: `_mock_tender_search_response` (`api/utils/bid_api_client.py:647`) — `TENDER_API_MOCK=true` 免配额测试
- 端点: `POST /api/v1/bid/tender-search` (`api/apps/restful_apis/bid_app.py:2083`)

**前端**:
- 组件: `web/src/components/bid/tender-search.tsx` (~660行，全新)
- 两态模式: 搜索表单 ↔ 结果列表（含无限滚动）
- 集成位置: `web/src/components/bid/index.tsx` 的 "标讯搜索" 模块
- i18n: `zh.ts` + `en.ts` 各 33 个 key

**关键设计决策**:
- Item-level 缓存（非 query-hash），不同搜索词可共享同一 item 池
- 独立 AppCode（与 BidApiClient 默认 app_code 分离）
- 搜索结果 card 展示: 公告类型 badge + 标题 + 项目编号 + 预算/中标金额 + 区域 + 行业 + 中标候选 + 代理机构 + 开标/投标时间

---

## 4. Agent 画布系统

### 核心组件 (21个)

Begin, Agent, LLM, Generate, FanOut, Iteration, Switch, Categorize, Loop, Message, VariableAssigner, VariableAggregator, DataOperations 等

### 功能特性
- JSON DSL 画布 DAG + 动态组件发现
- asyncio 并发执行 (FanOut)
- Function Calling 工具调用 (Agent 节点迭代推理)
- Loop 循环节点 (子节点输出覆盖写)
- 结构化输出 JSON 强制模式 + 持久化
- StreamMD 增量流式渲染

### Agent 工具 (30个，标讯12个)

标讯工具 (`agent/tools/bid.py`): 🔧 旧版企业画像工具仍保留供 Agent 画布使用

---

## 5. 爬虫引擎 (Crawler v2.1)

三层架构: **Crawl → Dedup → Storage**，YAML 配置驱动，78+ 站点

```
Crawl Layer: Adapter → Paginator → Extractor → AntiCrawler → List[Dict]
Dedup Layer: DedupChecker (内存+DB双层去重, O(1))
Storage Layer: bid_writer + kb_uploader + attachment_handler
```

4种HTTP适配器 + 6种分页策略 + 3种数据提取器 (JSONPath, CSS, AI)

详见: `rag/svr/crawler_engine/`

---

## 6. MCP Server

`rag/svr/mcp_server.py` — JSON-RPC over stdio，11个 Tools

与 Hermes 桌面应用集成。Tools: `ask_agent`, `lookup_bid_code`, `search_bid_projects`, `get_bid_detail`, `import_bid_to_kb`, `check_bid_import_status`, `search_contracts`, `get_bid_detail_v2`, `enterprise_contacts`, `enterprise_customers`, `enterprise_suppliers`

---

## 7. 微信公众号采集

`rag/svr/wechat_mp_crawler.py` + `rag/svr/wechat_mp/` — 微信公众号离线批处理管道

支持: 文章列表采集、内容提取、去重入库

---

## 8. 用户管理

- 用户登录活动审计 (`user_login_log` 表)
- 用户管理页面 (管理员侧边栏)
- 多设备登录支持 (`user_token` 表自动迁移)
- 登录/注册/密码重置全流程

---

## 9. 文档处理增强

- `.doc` (OLE2) 文件解析支持
- `.docx` Word 批注注入 + 前端审核面板
- LibreOffice `.doc→.docx` 转换 (Docker 依赖)
- 文件解析异常隔离
- Annotation 归一化 + 模糊匹配
- 多文件审核系统 (结构化输出)

---

## 10. 前端架构

### 路由分割
- C端: `/` → c-landing, `/login`, `/home` (投标助手对话页)
- B端: Admin prefix 下 → 标讯管理, Agent画布, 知识库

### 关键组件
- C端标讯页: `web/src/components/bid/index.tsx` (9个模块)
- 企业查询: `web/src/components/bid/enterprise-search.tsx`
- 标讯列表: `web/src/pages/home/bid-list`
- 中标/合同: `web/src/components/bid/contract-list.tsx`
- 虚拟滚动: Virtuoso 消息列表 + 后台标签页优化

---

## 11. 移动端

Flutter 3.44 + Dart 3.12 — `bidding_app/`

标书分析助手 App，支持 Android APK 构建

---

## 关键约束

1. **禁止自动部署** — 部署/Docker重启需用户明确指示
2. **API 按次收费** — 必须走 DB缓存→API→fallback
3. **上游文件保护** — `ragflow_server.py`, `db_models.py`, `pipeline.py` 谨慎修改
4. **C端≠B端** — 两套独立代码，修改前确认路由
5. **字段名映射** — 缓存返回必须用DB数据(snake_case)，不能透传API raw data(camelCase)
6. **新增数据库表** — 必须放 `db_models.py` 初始化脚本中，考虑迁移兼容
