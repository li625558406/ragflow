# RAGFlow 二次开发项目 — 完整架构与开发指南

> 本文档供 LLM/AI 编程助手在新对话中快速理解项目框架、自定义功能、开发模式和部署流程。
> 最后更新: 2026-06-13 (API缓存持久化 + LLM工具系统 + 企业钻取工具)

---

## 1. 项目概要

基于 [RAGFlow](https://github.com/infiniflow/ragflow) (v0.25.1) 的深度二次开发版本，聚焦**标讯（招投标）数据的采集、存储、检索、知识库解析**和**投标书自动写作**。

| 项目 | 说明 |
|---|---|
| **GitHub** | `https://github.com/li625558406/ragflow.git` |
| **上游** | `infiniflow/ragflow` v0.25.1 |
| **本地路径** | `D:\AI\ragflow2` |
| **服务器** | `root@47.98.102.55`，SSH密钥 `D:\AI\konus-key.pem` |
| **部署方式** | Docker Compose，代码目录 Bind Mount 到容器 |

---

## 2. 技术栈

| 层 | 技术 |
|---|---|
| 后端框架 | Python Quart (async Flask-compatible) |
| ORM | Peewee |
| 数据库 | MySQL 8.0 (业务数据) |
| 搜索引擎 | Elasticsearch 8.11 (向量 + 全文检索) |
| 对象存储 | MinIO (文件/文档) |
| 缓存/队列 | Redis (Valkey 8) |
| LLM 调用 | 自研 LLMBundle 抽象层，支持 OpenAI/Qwen/DeepSeek 等 30+ 模型 |
| 前端框架 | React 18 + Vite 7 + TypeScript 5 |
| UI 库 | Radix UI + Tailwind CSS + shadcn/ui 风格组件 |
| 路由 | react-router v7 (createBrowserRouter) |
| 状态管理 | Zustand + TanStack React Query |
| HTTP 客户端 | Axios (新) + umi-request (旧) |
| Agent 编排 | JSON DSL 画布 + 动态组件发现 + asyncio 并发 |
| 文档解析 | DeepDOC (自研) + MinerU + PaddleOCR + TCDP |
| 移动端 | Flutter 3.44 + Dart 3.12 + Riverpod + Dio + go_router |
| 部署 | Docker Compose，Bind Mount 热更新 |

---

## 3. 目录结构与职责

```
ragflow2/
├── api/                    # Python 后端 (Quart async Flask)
│   ├── apps/
│   │   ├── restful_apis/   # 25+ REST API 蓝图 (自动注册)
│   │   ├── sdk/            # SDK API (对外开放)
│   │   └── services/       # 共享服务层 (collaboration等)
│   ├── db/
│   │   ├── db_models.py    # 所有 Peewee ORM 模型 (~1600行)
│   │   └── services/       # 31个领域 Service 文件
│   ├── utils/              # 工具函数 + bid_api_client + bid_tool_service
│   └── ragflow_server.py   # Quart 应用入口
│
├── agent/                  # Agent 画布编排引擎
│   ├── canvas.py           # Canvas 核心 (Graph子类, ~800行)
│   ├── component/          # 21个画布节点组件
│   │   ├── base.py         # ComponentBase + ComponentParamBase 基类
│   │   ├── llm.py          # LLM/Generate 核心组件
│   │   ├── agent_with_tools.py  # Agent (LLM + 工具调用)
│   │   ├── fanout.py       # ★ FanOut 并行迭代器 (新)
│   │   ├── iteration.py    # 串行迭代器
│   │   ├── message.py      # Message 输出组件
│   │   ├── switch.py       # 条件分支
│   │   ├── categorize.py   # 分类器
│   │   └── ...             # 其他: begin, invoke, excel, string等
│   ├── tools/              # 28个工具插件 (Agent可调用)
│   │   ├── base.py         # ToolBase + LLMToolPluginCallSession
│   │   ├── bid.py          # ★ BidSearch, BidImport, BidGetDetail, BidEnterprise*系列
│   │   ├── retrieval.py    # 知识库检索
│   │   ├── code_exec.py    # 沙箱代码执行
│   │   ├── tavily.py       # Tavily 网络搜索
│   │   └── ...             # email, crawler, github, arxiv 等
│   └── templates/          # 预置 Agent 模板 (JSON DSL)
│
├── rag/                    # RAG 核心引擎
│   ├── llm/                # LLM 抽象层
│   │   ├── chat_model.py   # Chat 模型 (stream + async)
│   │   ├── embedding_model.py  # Embedding 模型
│   │   ├── rerank_model.py # Rerank 模型
│   │   ├── cv_model.py     # 视觉模型
│   │   └── ocr_model.py    # OCR 模型
│   ├── flow/               # 文档处理流水线
│   │   ├── pipeline.py     # Pipeline (Graph子类)
│   │   ├── parser/         # 文档解析器 (PDF/DOCX/Image/Excel)
│   │   ├── chunker/        # 文本切分器 (TokenChunker, SemanticChunker)
│   │   ├── extractor/      # 信息提取器
│   │   └── tokenizer/      # Token 计数器
│   ├── nlp/                # NLP: 搜索/分词/同义词/词权重
│   ├── graphrag/           # GraphRAG: 知识图谱构建与查询
│   ├── svr/                # 后台服务 + 爬虫
│   │   ├── mcp_server.py              # ★ MCP JSON-RPC Server
│   │   ├── bid_sync.py                # 标讯定时同步
│   │   ├── task_executor.py           # 定时任务 Worker
│   │   ├── scheduled_task_executor.py # 定时任务调度器
│   │   ├── unified_crawler.py         # ★ 统一爬虫入口 (替代80+脚本)
│   │   ├── wechat_mp_crawler.py       # ★ 微信文章采集 (服务器端，离线批处理)
│   │   ├── crawler_sites.yaml         # ★ 站点配置文件 (78个站点)
│   │   ├── crawler_engine/            # ★ 爬虫引擎核心包
│   │   │   ├── engine.py              # 主引擎 (CrawlerEngine)
│   │   │   ├── config.py              # YAML 配置加载 + 站点配置 dataclass
│   │   │   ├── paginator.py           # 6种分页策略
│   │   │   ├── anti_crawler.py        # 统一反爬管理器
│   │   │   ├── formatter.py           # Markdown 模板格式化
│   │   │   ├── kb_uploader.py         # KB 上传封装
│   │   │   ├── bid_writer.py          # ★ 标讯数据库写入器
│   │   │   ├── models.py              # ★ NormalizedItem + AttachmentMeta 数据类
│   │   │   ├── dedup_checker.py       # ★ 双层去重 (内存+DB)
│   │   │   ├── storage_pipeline.py    # ★ 存储管线 (bid+KB+附件)
│   │   │   ├── attachment_handler.py  # ★ 附件下载/解析/KB上传/关联
│   │   │   ├── content_converter.py   # Markdown→HTML + 日期解析 + 结构化提取
│   │   │   ├── state_manager.py       # DB 爬虫状态管理
│   │   │   ├── session_manager.py     # HTTP Session 工厂
│   │   │   ├── crypto_utils.py        # SM4/AES/MD5 加密工具
│   │   │   ├── browser_pool.py        # Playwright 浏览器池
│   │   │   ├── date_parser.py         # 中文日期解析
│   │   │   ├── migrate_state.py       # 状态迁移脚本 (JSON→DB)
│   │   │   ├── migrate_scheduled_tasks.py # 定时任务迁移脚本
│   │   │   ├── adapters/              # 4种HTTP适配器
│   │   │   │   ├── base.py            # 抽象基类 + AdapterFactory
│   │   │   │   ├── rest_api.py        # requests.Session 适配器
│   │   │   │   ├── encrypted_api.py   # SM4/AES 加密API适配器
│   │   │   │   ├── spa_render.py      # Playwright SPA渲染适配器
│   │   │   │   └── playwright_http.py # PlaywrightHttpClient 封装
│   │   │   └── extractors/            # 3种数据提取器
│   │   │       ├── base.py            # 抽象基类 + ExtractorFactory
│   │   │       ├── json_path.py       # JSON 路径提取
│   │   │       ├── css_selector.py    # CSS 选择器提取 (含@attr语法)
│   │   │       └── ai_extractor.py    # LLM 智能提取 (兜底)
│   │   ├── crawler_utils.py          # PlaywrightHttpClient (旧版，仍被引用)
│   │   ├── wechat_mp/               # 微信公众号采集模块
│   │   │   ├── gather.py            # WxGather 基类 (cookies/token管理+fill_back)
│   │   │   ├── mps_api.py           # MpsApi — WeChat appmsg API 文章列表
│   │   │   ├── token.py             # WeChat 认证 token (⚠️ 会shadow Python stdlib)
│   │   │   ├── cloudflare_proxy.js  # Cloudflare Worker 代理中继
│   │   │   └── local_content_fetcher.py # ★ Windows 本地正文抓取 (Playwright+反检测)
│   │   └── *_crawler.py              # 80个旧爬虫脚本 (已被统一框架替代)
│   └── utils/              # 存储连接器 (ES, MinIO, Redis, Infinity)
│
├── web/                    # React + Vite + TypeScript 前端
│   └── src/
│       ├── pages/
│       │   ├── home/       # ★ B端标讯管理 (bid-list, bid-card等)
│       │   ├── c-chat/     # ★ C端对话主页 (投标助手)
│       │   ├── c-landing/  # C端着陆页
│       │   ├── c-login/    # C端登录页
│       │   ├── agent/      # B端 Agent 画布编辑器
│       │   ├── datasets/   # B端知识库管理
│       │   ├── files/      # B端文件管理
│       │   ├── next-chats/ # B端对话
│       │   └── ...         # login-next, user-setting, admin等
│       ├── components/     # 共享UI组件库
│       ├── services/       # API 调用层 (xxx-service.ts)
│       ├── hooks/          # 自定义 React Hooks
│       ├── stores/         # Zustand 状态管理
│       ├── constants/      # 常量 + Agent算子注册
│       ├── locales/        # 国际化 (zh.ts, en.ts)
│       ├── utils/          # 工具函数 + HTTP 客户端
│       ├── routes.tsx      # B端路由表
│       ├── c-routes.tsx    # C端路由表
│       └── app.tsx         # 根组件 (C/B路由分割)
│
├── bidding_app/            # ★ Flutter 移动端 (标书分析助手 App)
│   └── lib/
│       ├── main.dart       # App 入口 + ProviderScope
│       ├── app.dart        # MaterialApp.router + 主题 + 自动登录
│       ├── core/
│       │   ├── api/        # Dio HTTP + JWT 拦截器 + SSE 流客户端
│       │   ├── router/     # GoRouter + ShellRoute (底部3Tab)
│       │   ├── theme/      # 设计Token (颜色/排版/间距/组件主题)
│       │   ├── storage/    # flutter_secure_storage 封装
│       │   └── utils/      # 密码 base64 编码 (与Web一致)
│       ├── features/
│       │   ├── auth/       # 登录/注册 (StateNotifier + Riverpod)
│       │   ├── chat/       # ★ 对话主页 (SSE流式 + Markdown渲染 + 文件上传)
│       │   ├── tools/      # 费用计算器 (差额定率分档累进法)
│       │   └── bid/        # 标讯搜索 (分页 + 下拉刷新)
│       ├── models/         # User/Message/Agent/BidItem/ToolItem
│       └── shared/widgets/ # AppButton/AppCard/EmptyState/ShimmerLoading/MainScaffold
│
├── deepdoc/                # 文档深度解析 (PDF/DOCX/OCR/布局分析)
├── common/                 # 共享工具库 (日志/加密/配置/异常)
├── docker/                 # Docker 部署
│   ├── docker-compose.yml       # 主服务编排
│   ├── docker-compose-base.yml  # 基础服务 (MySQL/ES/Redis/MinIO)
│   ├── nginx/                   # Nginx 配置
│   └── entrypoint.sh            # 容器入口脚本
└── sdk/                    # Python SDK (pip install)
```

---

## 4. 后端架构

### 4.1 API 层

Quart (async Flask) 框架，Blueprint 模式。关键文件 `api/apps/__init__.py`：

- **自动路由发现**: 扫描 `api/apps/restful_apis/` 下所有 `.py` 文件，动态 import 并注册 Blueprint
- **统一前缀**: `/api/v1/`
- **认证**: `@login_required` 装饰器校验 Bearer Token

**25 个 REST API 蓝图:**

| 蓝图文件 | 职责 |
|---|---|
| `agent_api.py` | Agent 画布 CRUD + Chat 流式补全 (SSE) |
| `bid_app.py` | ★ 标讯项目列表/详情/附件/解析/统计 |
| `chat_api.py` | 对话管理 |
| `dataset_api.py` | 知识库 CRUD + 配置 |
| `document_api.py` | 文档上传/解析/切分 |
| `file_api.py` | 文件管理 |
| `collaboration_api.py` | ★ 协作编辑 + Word/Markdown 导出 |
| `chunk_api.py` | Chunk 检索与管理 |
| `search_api.py` | 全网搜索/知识库搜索 |
| `mcp_api.py` | MCP Server 配置管理 |
| `memory_api.py` | Agent Memory 管理 |
| `scheduled_task_api.py` | 定时任务 CRUD + 启停 |
| `connector_api.py` | 外部数据源连接器 |
| `tenant_api.py` | 租户/用户管理 |
| `user_api.py` | 用户设置 |
| `plugin_api.py` | 插件管理 |
| `openai_api.py` | OpenAI 兼容 API |
| `system_api.py` | 系统状态/配置 |
| `stats_api.py` | 统计分析 |
| `task_api.py` | 任务管理 |
| `analysis_template_api.py` | 分析模板 |
| `document_analysis_api.py` | 文档分析 |
| `file2document_api.py` | 文件转文档 |
| `langfuse_api.py` | Langfuse 可观测性 |
| `wechat_mp_api.py` | 微信公众号集成 |

### 4.2 数据库层

**DB Models** (`api/db/db_models.py`, ~1600 行 Peewee ORM):

- 系统表: `Tenant`, `User`, `TenantLLM` (LLM模型配置)
- 知识库表: `Knowledgebase`, `Document`, `File`
- Agent 表: `Canvas`, `CanvasTemplate`
- 对话表: `Dialog`, `Conversation`
- MCP 表: `MCPServer`
- 标讯表: ★ `BidProject`, `BidProjectDetail`, `BidProjectStructure`, `BidProjectFile`, `BidProjectParse`, `BidSyncLog`, `AreaCode` (7张新增)
- 定时任务: `ScheduledTask`, `ScheduledTaskLog`

**31 个 Service 文件** (`api/db/services/`)，每文件一个领域，如:
- `bid_service.py` — 标讯 CRUD + 搜索/筛选/统计
- `canvas_service.py` — Agent 画布 DSL 存储
- `document_service.py` — 文档生命周期管理
- `knowledgebase_service.py` — 知识库配置
- `tenant_llm_service.py` — LLM 模型配置与校验
- `mcp_server_service.py` — MCP Server 配置管理
- `collaboration_service.py` — 协作编辑

### 4.3 Agent 画布系统

Agent 系统是项目的核心编排引擎，用 JSON DSL 定义有向无环图 (DAG) 工作流。

#### Graph 基类 (`agent/graph.py`)

- **DSL 结构**: `{"components": {...}, "path": [...], "globals": {...}, "variables": {...}}`
- **组件发现**: 扫描 `agent/component/` 和 `agent/tools/`，`component_class()` 按名称动态获取类
- **路径执行**: `path` 数组定义执行顺序，`_run_batch()` 对同层组件实现 `asyncio.gather` 并行
- **变量引用**: `{ComponentName@output_key}` 语法，运行时解析为上游组件的 output

#### Canvas (`agent/canvas.py`, ~800 行)

- 继承 `Graph`，是 Agent 画布的运行实例
- **Globals**: `sys.query`, `sys.user_id`, `sys.conversation_turns`, `sys.files`, `sys.history`, `sys.date`
- **Entry Point**: `async run(query=...)` → SSE 事件生成器
- **执行流程**:
  1. `load()` DSL → 构建 components dict 和 graph edges
  2. `_run_batch(path)` → asyncio.gather 执行当前层所有组件
  3. 每层完成后 `_append_path()` → 将 downstream 加入下一批 path
  4. 心跳循环 (100ms) → 监控完成状态 + 排空 FanOut 事件队列
  5. 完成后 yield `"workflow_finished"` 事件

- **SSE 事件类型**: `"message"` (LLM token), `"node_logs"` (组件状态), `"heartbeat"`, `"workflow_finished"`
- **FanOut 事件桥接**: 心跳循环中 `if eq and not eq.empty()` (每次 tick 只取一个事件，限流)

#### 21 个 Canvas 组件 (`agent/component/`)

| 组件 | 类型 | 用途 |
|---|---|---|
| `Begin` | 入口 | 接收用户输入，注入 `sys.query` |
| `LLM` / `Generate` | 核心 | 调用 LLM Chat API，支持 system/user prompt |
| `Agent` | 核心 | LLM + 工具调用 (Function Calling)，迭代式推理 |
| `FanOut` | ★ 新 | 并行迭代器，asyncio.gather 多 lane 并发 |
| `Iteration` | 控制流 | 串行迭代器，通过 canvas path 循环 |
| `Switch` | 控制流 | 条件分支，根据条件选不同下游 |
| `Categorize` | 分类 | LLM 分类器，输出类别标签 |
| `Message` | 输出 | 最终输出格式化，支持 Markdown |
| `Invoke` | 集成 | 调用外部 Agent/子画布 |
| `DataOperations` | 数据 | JSON 数据增删改查 |
| `ExcelProcessor` | 数据 | Excel 文件读写 |
| `VariableAssigner` | 变量 | 变量赋值 |
| `VariableAggregator` | 变量 | 多输入聚合 |
| `Fillup` | 填充 | 模板填充 |
| `StringTransform` | 文本 | 字符串替换/拼接 |
| `DocsGenerator` | 文档 | 文档生成器 |
| `ListOperations` | 数据 | 列表操作 (filter/sort/map) |
| `ExitLoop` | 控制流 | 条件退出循环 |
| `Loop` | 控制流 | 条件循环 (while)，支持子节点输出透传 (覆盖写) |

#### 30 个 Agent 工具 (`agent/tools/`)

| 工具 | 说明 |
|---|---|
| `BidLookupCode` | 行政区划/行业编码查询 (本地 CSV) |
| `BidSearch` | ★ 标讯搜索 (调用外部API) |
| `BidSearchAI` | ★ AI 轻量搜索 (自然语言友好，含结构化甲方/乙方/开标信息) |
| `BidGetDetail` | ★ 标讯详情获取 + 自动导入 KB |
| `BidGetSource` | ★ 获取标讯原始采集源网址 |
| `BidImportToKb` | ★ 标讯手动导入知识库 |
| `BidCheckImportStatus` | 查询 KB 导入解析进度 |
| `BidSearchContract` | ★ 合同/中标结果搜索 (含合同周期、起止日期) |
| `BidRewriteQuery` | ★ AI 搜索条件重写 (自然语言→结构化搜索条件) |
| `BidIndustryTag` | ★ AI 行业标签推理 (关键词→行业编码候选) |
| `BidEnterpriseProfile` | ★ 企业画像 (基础信息+项目统计+关系网络) |
| `BidConstructionSearch` | ★ 拟在建项目搜索 |
| `Retrieval` | 知识库检索 (向量+全文) |
| `CodeExec` | 沙箱 Python 代码执行 |
| `Tavily` | Tavily 网络搜索 |
| `DuckDuckGo` | DuckDuckGo 搜索 |
| `Wikipedia` | Wikipedia 查询 |
| `PubMed` | PubMed 学术搜索 |
| `Google` | Google 搜索 |
| `GitHub` | GitHub API 调用 |
| `ArXiv` | ArXiv 论文搜索 |
| `Email` | 邮件发送 |
| `Crawler` | 网页爬取 |
| `ExeSQL` | SQL 执行 |
| `AKShare` | A股/财经数据 |
| `TuShare` | 金融数据 |
| `Jin10` | 金十数据 |
| `QWeather` | 天气查询 |
| `Deepl` | DeepL 翻译 |
| `WenCai` | 问财财经搜索 |
| `YahooFinance` | 雅虎财经 |
| `GoogleScholar` | Google Scholar |

**工具调用流程** (`agent_with_tools.py`):
1. Agent 初始化时加载 `_param.tools` 列表 → 实例化 Tool 对象
2. 构建 OpenAI Function Calling 格式的 `tool_meta`
3. 绑定到 `LLMBundle` via `LLMToolPluginCallSession`
4. LLM 返回 `tool_calls` → `tool_use_callback()` 执行 → 结果注入消息历史
5. 循环直到 `max_rounds` 或用尽

### 4.4 FanOut 并行迭代器 (`agent/component/fanout.py`)

**是本次二次开发的标志性新功能**。解决串行迭代器处理 10 章标书需 10 分钟的问题。

#### 架构设计

```
FanOut(items_ref="outline@result", max_concurrency=5)
  ↓ asyncio.gather + Semaphore
  ├── Lane 0: render prompt → LLB async_chat_streamly → result
  ├── Lane 1: render prompt → LLB async_chat_streamly → result
  ├── Lane 2: render prompt → LLB async_chat_streamly → result
  └── ...
  ↓ 收集
results = [{item_index: 0, content: "..."}, ...]
results (flat text) → 下游组件
results_raw (structured) → 供代码组件使用
```

#### 与 Iteration 的本质区别

| 维度 | Iteration | FanOut |
|---|---|---|
| 并行模式 | 串行 (一个接一个) | 并行 (asyncio.gather + semaphore) |
| 路径机制 | 走 canvas path 循环 | 绕过 canvas，直接调 LLM |
| 子组件 | 迭代 IterationItem 子 DAG | 不需要子 DAG |
| 输出方式 | 逐 item 输出 | 所有 lane 完成后统一输出 |
| 适用场景 | 依赖顺序的结果链 | 独立的批处理任务 |

#### 关键参数

- `items_ref`: 上游变量引用，提供 items JSON 数组
- `llm_id`: LLM 模型选择
- `system_prompt` / `prompt_template`: 模板中 `{item}` 和 `{index}` 替换
- `max_concurrency`: 并发数 (Semaphore 控制)
- `error_strategy`: `"skip"` (跳过错误项) / `"stop"` (出错即停止)
- `tools` / `mcp`: 可选工具绑定

#### SSE 事件桥接

- 每 lane 完成后 → 一个 `"message"` 事件 (完整章节内容，带 `lane_index`/`lane_label`/`lane_total` 元数据)
- Canvas 心跳循环: 每 100ms 排空所有待处理事件 (不限速)
- 前端 `ChapteredMarkdown` 组件: 按 `---` 分隔符拆分为独立 memo 段，新增章节只渲染新段
- 前端 `findMessageFromList`: 检测 lane 元数据，自动插入章节标题分隔
- 防浏览器冻结: React.memo + content-visibility: auto

#### 保存逻辑

- `completion()` 用手动构建 `conv_data` dict 代替 `conv.to_dict()` (peewee setattr 不影响 `__dict__["__data__"]`)
- `_iter_session_completion_events` 只过滤 event 类型，不影响 data 内容
- 保存兼容: lane 元数据字段不影响 `txt += ans["data"]["content"]` 的拼接

### 4.5 Loop 条件循环 — 子节点输出透传 (二次开发增强)

Loop 是"while"型条件循环，与 Iteration（for-each）不同。**二次开发新增了子节点输出透传能力**，解决下游节点无法获取循环体内子节点输出的问题。

#### 执行流程

```
Loop 容器 _invoke() → 初始化循环变量
  → LoopItem _invoke() → 检查最大循环次数
    → 子节点执行 (LLM 等)
    → LoopItem output_collation() → 用子节点输出 覆盖写 Loop 的 output
    → LoopItem end() → 检查终止条件
      → 条件满足 → 退出循环 → 下游节点读取 {loop:0@output_name}
      → 条件不满足 → 回到 LoopItem 继续循环
```

#### 与 Iteration 输出的本质区别

| 维度 | Iteration output_collation | Loop output_collation (新) |
|---|---|---|
| 写入方式 | `res.append(obj.output(var))` → 追加到列表 | `p.set_output(k, val)` → 覆盖写 |
| 下游拿到 | `[结果1, 结果2, 结果3]` | 最后一轮的结果 |
| 适用场景 | 需要收集所有轮次结果 | 只需要最终结果 (如 LLM 重试取非空输出) |

#### 配置方式

1. Loop 容器表单 → **输出** 区域 → 添加输出映射 → 选子节点输出变量
2. Loop 容器 → **终止条件** → 检查变量是否满足退出条件
3. 下游节点 → 变量下拉框可见 Loop 的输出

#### 涉及文件

| 文件 | 改动 |
|---|---|
| `agent/component/loop.py` | `LoopParam` 加 `outputs = {}` |
| `agent/component/loopitem.py` | 新增 `output_collation()` — 覆盖写子节点输出 |
| `web/.../loop-form/schema.ts` | 加 `outputs` 数组字段 |
| `web/.../loop-form/dynamic-output.tsx` | ★ 新建 — Loop 输出映射 UI |
| `web/.../loop-form/index.tsx` | 表单加入 DynamicOutput 组件 |
| `web/.../loop-form/use-watch-form-change.ts` | outputs 合并循环变量 + ref 映射 |
| `web/.../loop-form/use-values.ts` | 加载时 ref outputs 转回数组格式 |

### 4.6 MCP Server (`rag/svr/mcp_server.py`)

JSON-RPC over stdio 的 MCP 服务器，暴露 11 个 Tool:

| Tool | 说明 | 缓存策略 |
|---|---|---|
| `ask_agent` | 调用 RAGFlow Agent/画布 | — |
| `lookup_bid_code` | 行政区划/行业代码查询 | 本地数据 |
| `search_bid_projects` | 搜索标讯 | 缓存优先 (DB→API) |
| `get_bid_detail` | 获取标讯详情 | 缓存优先 (30天TTL) |
| `import_bid_to_kb` | 导入标讯到知识库 | 去重检查 |
| `check_bid_import_status` | 查询导入状态 | — |
| `search_contracts` | ★ 合同/中标搜索 | 缓存优先 (1h TTL) |
| `get_bid_detail_v2` | ★ v2详情 (合同正文+结构化) | 缓存优先 (30天TTL) |
| `enterprise_contacts` | ★ 企业联系人 | API透传 |
| `enterprise_customers` | ★ 企业客户项目 | API透传 |
| `enterprise_suppliers` | ★ 企业供应商项目 | API透传 |

通信方式: HTTP `http://127.0.0.1:9380/api/v1`，与 Hermes 桌面应用集成。

### 4.7 RAG 文档处理流水线

**核心管道** (`rag/flow/pipeline.py`):

```
文档上传 → Parser (解析) → Chunker (切分) → Embedding → ES 索引
```

- **Parser**: 支持 PDF/Word/Excel/PPT/图片/HTML/Markdown/Email/Audio
  - PDF 引擎: DeepDOC (自研，布局分析) / MinerU / PaddleOCR / TCDP VLM
  - 输出格式: JSON / Markdown / HTML / Text
- **Chunker**: TokenChunker (按 token 数切) / SemanticChunker (语义切分)
- **Embedding**: 通过 `embedding_model.py` 调用嵌入模型生成向量
- **存储**: ES 存储 chunk (含向量)，MinIO 存储原始文件

**GraphRAG** (`rag/graphrag/`): 从文档中提取实体/关系 → 构建知识图谱 → 支持图检索增强生成。

### 4.8 定时任务系统

| 组件 | 文件 | 说明 |
|---|---|---|
| DB 模型 | `db_models.py` | `ScheduledTask` + `ScheduledTaskLog` |
| Service | `scheduled_task_service.py` | CRUD + 调度逻辑 |
| API | `scheduled_task_api.py` | REST 端点 |
| 调度器 | `scheduled_task_executor.py` | 15s 轮询 → Redis 队列 |
| Worker | `task_executor.py` | 消费队列，执行任务 |

支持 cron 表达式 (`croniter`) 和间隔调度。

---

## 5. 前端架构

### 5.1 路由分割 (关键!)

`app.tsx` 通过 `window.location.pathname.startsWith(ADMIN_PREFIX)` 做路由分割:

```
根路径 "/"
├── C端 (未匹配 ADMIN_PREFIX /5d41402abc4b2a76b9719d911017c592/)
│   ├── /           → c-landing (着陆页)
│   ├── /login      → c-login (登录)
│   └── /home       → c-chat (对话首页 ★ 用户主要使用)
│
└── B端 (匹配 ADMIN_PREFIX)
    ├── /5d41402abc4b2a76b9719d911017c592/...
    ├── home         → 标讯管理后台
    ├── datasets     → 知识库管理
    ├── agent        → Agent 画布编辑器 ★
    ├── files        → 文件管理
    ├── login-next   → 管理后台登录
    ├── user-setting → 用户设置
    └── ...
```

**⚠️ 重要教训**: C端页面 (`pages/c-*`) 和 B端页面 (`pages/home/`, `pages/login-next/`) 是完全独立的两套代码。修改前务必确认路由归属。

### 5.2 主要页面

**C端 (C-end, 用户端)**:
| 页面 | 路径 | 说明 |
|---|---|---|
| `c-landing/` | `/` | 着陆页 |
| `c-login/` | `/login` | 登录页 |
| `c-chat/` | `/home` | ★ 投标助手对话页，核心C端交互 |
| `next-chats/` | — | 新版对话组件 (开发中) |

**B端 (B-end, 管理端)**:
| 页面 | 说明 |
|---|---|
| `home/` | ★ 标讯管理: 列表搜索筛选 + 详情 + 配置解析 |
| `agent/` | ★ Agent 画布编辑器: 可视化拖拽 DAG |
| `datasets/` | 知识库列表管理 |
| `dataset/` | 单知识库详情 (文档/配置/检索测试) |
| `files/` | 文件管理器 (上传/预览) |
| `chunk/` | Chunk 检索与编辑 |
| `login-next/` | 管理端登录 |
| `user-setting/` | 用户设置 (API Key, 模型配置等) |
| `scheduled-tasks/` | 定时任务管理 |
| `memories/` | Agent Memory 管理 |
| `flow/` | 文档处理流编辑器 |
| `document-viewer/` | 文档预览 |
| `document-analysis/` | 文档分析 |

### 5.3 Agent 画布编辑器 (`pages/agent/`)

可视化拖拽 DAG 编辑器，是 Agent 系统的前端入口:

- **画布渲染**: ReactFlow + 自定义节点 + 贝塞尔边
- **节点面板**: `OperatorIcon` → 拖入画布 → 生成组件实例
- **配置表单**: `form-sheet/` 下的动态表单，按组件类型渲染不同配置面板
  - `FormConfigMap` → 每种 Operator 映射对应表单组件
  - 支持 LLM 选择、Prompt 编辑、Tools 选择、MCP 配置
- **状态管理**: `use-agent-form.ts` + `use-add-node.ts` + `use-agent-tool-initial-values.ts`
- **流式调试**: `dataflow-result/` — 实时展示 Agent 运行的 SSE 结果

### 5.4 状态管理

- **Zustand**: 全局状态 (用户信息、权限、UI 状态)
- **TanStack React Query**: 服务端状态 (标讯列表、知识库列表、对话历史)
- **URL Search Params**: 页面导航状态 (选中的知识库ID、文档ID等)

### 5.5 国际化和主题

- **i18n**: `locales/zh.ts` + `locales/en.ts`，通过 `react-i18next` 切换
- **主题**: `theme/` + Tailwind CSS 变量，支持暗色模式

---

## 6. 自定义功能详情

### 6.1 标讯系统 (Bid System)

**核心业务**: 中国招投标标讯信息的采集、搜索、详情查看、附件管理、导入知识库解析。

#### 数据流

```
外部 API (世舶科技/阿里云) → bid_api_client.py → 缓存到 MySQL
  → bid_app.py REST API → 前端 bid-list.tsx 展示
  → 用户点"配置" → bid_tool_service.py 下载拼接 → 导入知识库 → RAG解析
```

#### 新增数据库表 (7 张)

| 表 | 模型 | 主键 | 用途 |
|---|---|---|---|
| `bid_project` | `BidProject` | BigInteger `id` | 标讯项目列表 |
| `bid_project_detail` | `BidProjectDetail` | BigInteger `id` | 项目详情 (content_html) |
| `bid_project_structure` | `BidProjectStructure` | BigInteger `id` | 结构化数据 (JSON) |
| `bid_project_file` | `BidProjectFile` | auto id | 附件信息 (含 kb_document_id) |
| `bid_project_parse` | `BidProjectParse` | BigInteger `project_id` | 知识库解析状态 |
| `bid_sync_log` | `BidSyncLog` | BigInteger `id` | 同步日志 |
| `area_code` | `AreaCode` | — | 中国行政区划编码 |

#### API 端点

```
GET    /api/v1/bid/projects                          # 列表 (缓存优先)
GET    /api/v1/bid/projects/{id}/detail              # 详情
GET    /api/v1/bid/projects/{id}/structure           # 结构化
GET    /api/v1/bid/projects/{id}/files               # 附件
GET    /api/v1/bid/projects/{id}/collect-url         # ★ 采集源网址
POST   /api/v1/bid/projects/{id}/parse               # 触发解析 → KB
GET    /api/v1/bid/projects/{id}/parse-status        # 解析进度
GET    /api/v1/bid/projects/by-number               # ★ 项目编号查询
GET    /api/v1/bid/stats                             # 统计
GET    /api/v1/bid/sync-logs                         # 同步日志
POST   /api/v1/bid/trigger-sync                      # 手动同步
GET    /api/v1/bid/areas?parent_code=0&level=1       # 省市联动
GET    /api/v1/bid/industries                        # 行业分类
GET    /api/v1/bid/contracts                         # ★ 合同/中标结果搜索
GET    /api/v1/bid/enterprises/profile               # ★ 企业画像汇总
GET    /api/v1/bid/enterprises/contacts               # ★ 企业联系人
GET    /api/v1/bid/enterprises/customers              # ★ 企业客户
GET    /api/v1/bid/enterprises/suppliers              # ★ 企业供应商
GET    /api/v1/bid/construction/projects              # ★ 拟在建项目搜索
GET    /api/v1/bid/construction/projects/{id}/detail  # ★ 拟在建项目详情
```

#### 外部 API 调用策略 — 缓存持久化 (2026-06-13)

标讯第三方API按次收费，为降低费用支出，实现**缓存优先 (Cache-First)** 持久化策略：

**核心原理:**
```
第1次搜索 "keyword=安防" page=1:
  API → 10条(id:1~10) → upsert到DB(TTL=1h) → 返回

第2次搜索 (1小时内):
  DB命中10条，缓存未过期 → 直接返回 (免API费)

第2次搜索 (超过1小时):
  DB过期 → API → 20条(id:1~20) → id 1~10更新, id 11~20新增 → 返回

API故障:
  DB过期数据降级返回 (stale=true)
```

**TTL 策略:**
| 数据类型 | TTL | 原因 |
|---|---|---|
| 搜索/合同列表 | 1 小时 (`fetched_at` + `cache_expires_at`) | 搜索结果变化较快，1h内相同搜索有效 |
| 项目详情 (detail) | 30 天 | 详情内容基本不变，长缓存省费用 |
| 结构化数据 (structure) | 30 天 | 同详情 |
| 附件信息 (files) | 30 天 | 文件URL变化少 |

**ID 去重机制:**
- 合同/搜索数据以第三方API的 `id` 为主键 upsert
- 详情/结构化数据以 `project_id` 为主键 upsert
- API 数据始终覆盖 DB（API 为权威源）
- 同条件重复搜索 → 同ID命中 → 返回缓存（免API费）
- 同条件搜索新增数据 → 新ID不在DB → 写入DB

**降级策略:**
- API调用失败 → 返回DB已有数据（标记 `stale=true`）
- DB无数据且API失败 → 抛出异常

**BidProject 新增字段:**
```python
# db_models.py — BidProject
fetched_at = DateTimeField(null=True)       # 搜索缓存写入时间
cache_expires_at = DateTimeField(null=True)  # 缓存过期时间
```

**服务层:**
- `bid_service.py`: `upsert_contract()`, `filter_valid_cache()` — 合同数据upsert + 过期过滤
- `bid_tool_service.py`: `search_contracts_cached()`, `get_bid_detail_v2_cached()` — 缓存优先封装
- `bid_app.py`: `list_bid_contracts` 和 `get_bid_project_detail_v2` 端点改造为缓存优先

#### API 端点 (完整)

```
GET    /api/v1/bid/projects                          # 列表 (缓存优先)
GET    /api/v1/bid/projects/{id}/detail              # 详情 (v1, 缓存优先)
GET    /api/v1/bid/projects/{id}/detail-v2           # ★ 详情v2 (合同正文+结构化, 缓存30天)
GET    /api/v1/bid/projects/{id}/structure           # 结构化
GET    /api/v1/bid/projects/{id}/files               # 附件
GET    /api/v1/bid/projects/{id}/collect-url         # ★ 采集源网址
POST   /api/v1/bid/projects/{id}/parse               # 触发解析 → KB
GET    /api/v1/bid/projects/{id}/parse-status        # 解析进度
GET    /api/v1/bid/projects/by-number               # ★ 项目编号查询
GET    /api/v1/bid/stats                             # 统计
GET    /api/v1/bid/sync-logs                         # 同步日志
POST   /api/v1/bid/trigger-sync                      # 手动同步
GET    /api/v1/bid/areas?parent_code=0&level=1       # 省市联动
GET    /api/v1/bid/industries                        # 行业分类
GET    /api/v1/bid/contracts                         # ★ 合同/中标结果搜索 (缓存1h)
GET    /api/v1/bid/enterprises/profile               # ★ 企业画像汇总
GET    /api/v1/bid/enterprises/contacts               # ★ 企业联系人
GET    /api/v1/bid/enterprises/customers              # ★ 企业客户
GET    /api/v1/bid/enterprises/suppliers              # ★ 企业供应商
GET    /api/v1/bid/construction/projects              # ★ 拟在建项目搜索
GET    /api/v1/bid/construction/projects/{id}/detail  # ★ 拟在建项目详情
```

### 6.2 协作编辑与导出 (`collaboration_api_service.py`)

- **Lexical Editor**: React 前端富文本编辑器，JSON 格式存储 (`root.children`)
- **Markdown ↔ Lexical**: 双向转换 (`_markdown_to_lexical_json()`)
  - 支持 inline 格式化: `**bold**`, `*italic*`, `` `code` ``, `~~strike~~`
  - 支持列表 (ul/ol)、标题 (h1-h6)、段落
  - ★ 支持表格: `| cell |` 语法 → Lexical `table`/`tablerow`/`tablecell` 节点
  - Lexical format bitmask: `IS_BOLD=1, IS_ITALIC=2, IS_STRIKETHROUGH=4, IS_UNDERLINE=8, IS_CODE=16`
- **前端表格渲染** (二次开发增强):
  - 安装 `@lexical/table@0.23.1`，注册 `TableNode`/`TableCellNode`/`TableRowNode`
  - `TablePlugin` 启用 Tab 键导航和水平滚动
  - 表格主题样式: 边框、内边距、表头灰底
  - `AutoSavePlugin` 表格→Markdown 序列化（`| cell |` 格式）
  - `FormatApplyPlugin` 跳过表格节点保护
- **Word 导出**: Lexical 路径 + Markdown 回退路径均支持表格
  - Lexical 路径: `_generate_docx()` 识别 `table` block → python-docx `add_table()`
  - Markdown 路径: `_generate_docx_markdown()` 表格状态机检测
  - `_parse_inline_markdown()` — 递归解析器，支持嵌套格式
  - `_add_markdown_paragraph()` — 多 Run 格式化段落
  - `_set_cell_markdown()` — 表格单元格内联格式化
- **PDF 导出**: 同 Word，Lexical 路径和 Markdown 路径均支持表格 (reportlab)
- **HTML→Markdown**: `bid_tool_service.py` 使用 `markdownify` 库替代 `_strip_html()`，正确转换 HTML 表格

#### 协作完整数据流

```
对话页签 "协作"按钮 → markdown_content (含 | cell | 表格)
  → POST /api/v1/collaboration/documents
  → _markdown_to_lexical_json(): 检测表格行 → _build_lexical_table() → Lexical JSON
  → 存 doc.content (Lexical JSON) + doc.markdown_content
  ↓
协作页签编辑器: SetInitialStatePlugin 加载 doc.content
  → Lexical Editor 渲染 (表格节点已注册 → 正确显示)
  → 编辑后 AutoSavePlugin 保存 (表格→Markdown 序列化)
  ↓
下载 DOCX/PDF: download_document()
  → 优先取 doc.content (Lexical JSON)
  → _generate_docx() / _generate_pdf() Lexical 路径
  → 表格 block → Word Table / PDF Table 渲染
```

### 6.3 标书自动写作 (Tender Writing Agent)

利用 Agent 画布的完整流程:

```
Begin (用户输入"某项目招标")
  → BidSearch (搜索相关标讯)
  → BidGetDetail (获取标讯详情)
  → Outline (LLM生成大纲 → 多章节列表)
  → FanOut (并行处理每章: 每 lane 直接调用 LLM)
  → Aggregator (合并所有章节)
  → Message (输出完整投标书)
```

### 6.4 统一爬虫框架 (Crawler Engine)

> **v2.1 三层架构重构** — 用 YAML 配置驱动的统一引擎替代 80+ 独立爬虫脚本。
> v2.1 将引擎拆分为 **Crawl → Dedup → Storage** 三层，解耦数据采集、去重判断和存储入库。

#### 6.4.1 三层架构概述

```
┌──────────────────────────────────────────────────────────┐
│  Layer 1: CRAWL LAYER (只负责"取数据")                    │
│                                                          │
│  Adapter → Paginator → Extractor → AntiCrawler          │
│  输出: List[Dict] (原始采集数据)                          │
│  职责: HTTP请求、页面解析、字段提取、分页、反爬             │
└───────────────────────┬──────────────────────────────────┘
                        │  NormalizedItem
                        ▼
┌──────────────────────────────────────────────────────────┐
│  Layer 2: DEDUP LAYER (只负责"判重")                       │
│                                                          │
│  DedupChecker: StateManager(内存) + bid_project表(DB)     │
│  输出: 过滤后的 List[NormalizedItem]                      │
│  职责: 内存ID集合O(1)检查 + DB存量历史数据检查              │
└───────────────────────┬──────────────────────────────────┘
                        │  NormalizedItem
                        ▼
┌──────────────────────────────────────────────────────────┐
│  Layer 3: STORAGE LAYER (只负责"存数据")                    │
│                                                          │
│  ┌──────────────┐  ┌──────────────┐  ┌────────────────┐  │
│  │ StructStore  │  │ ContentStore │  │ AttachmentStore │  │
│  │ (bid_*表)    │  │ (KB上传+解析)│  │ (下载+KB+关联)  │  │
│  └──────────────┘  └──────────────┘  └────────────────┘  │
│                                                          │
│  职责: 结构化入库、KB文档上传解析、附件下载解析关联           │
└──────────────────────────────────────────────────────────┘
```

**关键原则**: 三层之间通过 `NormalizedItem` 数据结构传递，任何一层可独立测试/替换。

#### 6.4.2 v2.1 新增/增强模块

**4 个新增模块:**

| 模块 | 文件 | 职责 |
|------|------|------|
| 数据模型 | `models.py` | NormalizedItem + AttachmentMeta 数据类，统一77种脚本字段格式 |
| 双层去重 | `dedup_checker.py` | 内存 StateManager 快速检查 + bid_project 表存量检查，DB结果缓存 |
| 存储管线 | `storage_pipeline.py` | 编排三种存储目标：bid表写入 + KB上传 + 附件处理 |
| 附件处理 | `attachment_handler.py` | 下载→ZIP解压→KB上传→关联 bid_project_file 记录 |

**3 个增强模块:**

| 模块 | 变更 |
|------|------|
| `engine.py` | 重构为三层分离；Phase 1 CRAWL → Phase 2 DEDUP → Phase 3 STORAGE |
| `kb_uploader.py` | 新增 `upload_content()` 内存上传 + `_upload_blob()` 公共方法 |
| `state_manager.py` | 新增 `MAX_PROCESSED_IDS=10000` 上限，save() 自动清理最旧记录 |

#### 6.4.3 核心组件总览 (26+ 文件)

| 组件 | 文件 | 职责 |
|------|------|------|
| 入口 | `unified_crawler.py` | 参数解析、日志初始化、引擎启动 |
| 配置 | `crawler_sites.yaml` | 78 个站点配置 (YAML 驱动) |
| 配置 | `config.py` | YAML 加载 + 14 个 SiteConfig dataclass |
| 引擎 | `engine.py` | ★ 三层分离主编排器 (Crawl→Dedup→Storage) |
| 数据模型 | `models.py` | ★ NormalizedItem + AttachmentMeta + item_from_dict() |
| 去重 | `dedup_checker.py` | ★ 双层去重 (内存 O(1) + DB 缓存) |
| 存储管线 | `storage_pipeline.py` | ★ 编排 bid写入 + KB上传 + 附件处理 |
| 附件处理 | `attachment_handler.py` | ★ 下载→ZIP解压→KB上传→关联记录 |
| 适配器 | `adapters/rest_api.py` | requests.Session + API JSON / HTML 解析 |
| 适配器 | `adapters/encrypted_api.py` | urllib/PlaywrightHttpClient + SM4/AES 加密 + MD5 签名 |
| 适配器 | `adapters/spa_render.py` | Playwright sync_playwright 浏览器渲染 SPA |
| 适配器 | `adapters/playwright_http.py` | PlaywrightHttpClient 封装 (浏览器指纹) |
| 提取器 | `extractors/json_path.py` | JSONPath 提取 (API 响应) |
| 提取器 | `extractors/css_selector.py` | CSS 选择器提取 (HTML), 支持 `a@href` 属性提取 |
| 提取器 | `extractors/ai_extractor.py` | LLM 智能提取 (兜底+监控) |
| 分页 | `paginator.py` | 6 种策略: page_no / offset / total_count / html_regex / click_next / single_page |
| 反爬 | `anti_crawler.py` | 延迟随机化、429 自动退避、空页检测、连续空页停止 |
| 格式化 | `formatter.py` | Markdown 模板格式化，批次写入 |
| 上传 | `kb_uploader.py` | ★ KB 上传 (文件+内存) + 解析队列 |
| 入库 | `bid_writer.py` | 标讯数据库写入 (bid_project 等表，idempotent upsert) |
| 状态 | `state_manager.py` | DB 爬虫状态管理 (容量上限+自动清理) |
| 浏览器池 | `browser_pool.py` | Playwright 浏览器实例复用 |
| 加密 | `crypto_utils.py` | SM4-ECB / AES-256-CBC / MD5 签名 / hex 转换 |
| 工具 | `content_converter.py` | Markdown→HTML、日期解析、结构化字段提取 |
| 工具 | `session_manager.py` | HTTP Session 工厂 (SSL忽略、Cookie初始化) |
| 工具 | `date_parser.py` | 中文日期解析 (支持多种格式) |

#### 6.4.4 NormalizedItem 数据模型

`models.py` 定义了三层之间的数据传递单元:

```python
@dataclass
class NormalizedItem:
    item_id: str           # 唯一ID (URL / API id / title+date hash)
    title: str             # 标题
    url: str               # 原始链接 (绝对URL)
    date: str              # 发布日期 (YYYY-MM-DD)
    content: str           # 正文纯文本
    content_html: str      # 正文HTML (保留格式)
    source_site: str       # 来源站点ID
    section: str           # 栏目名称
    news_type: str         # 新闻/公告类型
    region_code: str       # 地区编码
    attachments: List[AttachmentMeta]  # 附件列表
    raw_data: Dict[str, Any]          # 原始API响应/HTML提取字段

@dataclass
class AttachmentMeta:
    file_name: str         # 文件名
    file_url: str          # 下载地址 (绝对URL)
    file_suffix: str       # .pdf/.doc/.xls/.zip/.rar
    file_size: int         # 字节数 (0=未知)
```

`item_from_dict(data, site_id, section)` — 从 77 种脚本格式（JSON API / HTML DOM / CSS 选择器）统一转换为 NormalizedItem。自动处理各种字段名变体（如 `publishDate`/`pubDate`/`pub_date` → `date`）。

#### 6.4.5 双层去重 (DedupChecker)

`dedup_checker.py` 实现内存+数据库双层去重策略:

| 层 | 检查方式 | 性能 | 覆盖范围 |
|---|---|---|---|
| Layer 1 | `StateManager.is_processed(item_id)` | O(1) 内存 Set | 本次会话已处理的条目 |
| Layer 2 | `BidProjectService.get_by_project_id(project_id)` | DB查询 (带缓存) | bid_project 表中 81,574+ 条历史数据 |

- **DB 缓存**: 同一个 `project_id` 只查一次，结果缓存在 `_db_checked` 集合中
- **同步回写**: DB 命中时自动 `mark_processed()` 同步到 StateManager，后续检查走 O(1) 内存
- **容量保护**: `_db_checked` 最大 50000 条，超限自动清空

#### 6.4.6 存储管线 (StoragePipeline)

`storage_pipeline.py` 编排三种存储目标的写入，每步错误隔离:

```
NormalizedItem
  → 1. BidWriter.write_all(raw_data, site_id)     # 写入 bid_project 等表 (幂等 upsert)
  → 2. KBUploader.upload_content(md, title)        # 正文转 Markdown 上传 KB (单条模式)
  → 3. AttachmentHandler.handle(attachments, id)    # 下载附件→ZIP解压→KB上传→关联记录
```

**单条KB上传 (新行为)**: v2.1 改为每条爬取数据单独上传为一个 KB 文档（以文章标题命名），替代 v2.0 的批量写入模式（10 条合并为一个 .md 文件）。

**附件生命周期 (新功能)**:
1. **下载**: `requests.get()` 流式下载，支持 200MB 大小上限、120s 超时
2. **ZIP 解压**: 自动识别 ZIP 文件，解压后逐文件上传 KB
3. **KB 上传**: 调用 `KBUploader.upload_file()` 上传
4. **关联记录**: 通过 `BidProjectFileService.upsert_file()` 更新 `bid_project_file` 表的 `kb_document_id`
5. **临时文件清理**: 下载完成后自动删除临时文件和目录

#### 6.4.7 四种 HTTP 适配器覆盖

| 适配器 | 传输引擎 | 适用场景 | 覆盖站点数 |
|--------|---------|---------|-----------|
| `rest_api` | requests.Session | 纯 API JSON / HTML 网页解析 | ~30 |
| `encrypted_api` | urllib / PlaywrightHttpClient | SM4/AES 加密响应 + MD5 签名 | ~8 |
| `spa_render` | Playwright sync_playwright | Vue/React SPA 全浏览器渲染 | ~5 |
| `playwright_http` | PlaywrightHttpClient | 浏览器指纹 (无 SPA 渲染) | ~4 |

#### 6.4.8 分页策略

| 策略 | 原理 | 适用站点 |
|------|------|---------|
| `page_no` | 递增页码参数 (如 `page=1,2,3...`) | 大多数政府网站 |
| `offset` | 递增偏移量参数 (如 `start=0,20,40...`) | 分页 API |
| `total_count` | 首次请求获取 total，计算总页数 | 含 `total` 字段的 JSON API |
| `html_regex` | 正则从 HTML 匹配总条数 + 总页数 | HTML 分页栏 |
| `click_next` | Playwright 点击"下一页"按钮 | SPA 站点 |
| `single_page` | 单页抓取 | 静态公告页 |

#### 6.4.9 YAML 配置示例 (`crawler_sites.yaml`)

```yaml
version: "1.0"
sites:
  fgw_zwgk:
    name: "发改委政务公开"
    site_url: "https://www.ndrc.gov.cn"
    transport:
      type: rest_api
      headers:
        User-Agent: "Mozilla/5.0 ..."
    listing:
      url: "https://www.ndrc.gov.cn/xxx/list.html"
      params:
        page: "{{ page }}"
        pageSize: "{{ page_size }}"
    pagination:
      type: page_no
      page_param: "page"
      page_size: 20
    extract:
      type: css_selector
      items_path: "ul.list li"
      fields:
        title: "a"
        url: "a@href"
        date: ".date"
    format:
      template: |
        # {{ title }}
        **日期:** {{ date }}
        {{ content }}
      upload_batch_size: 10
    anti_crawler:
      delay_min: 1.0
      delay_max: 2.5
      max_retries: 4
```

#### 6.4.10 并发安全机制

- **分布式锁**: `RedisDistributedLock(lock_key, timeout=1800, blocking_timeout=0)`
  - Lock key 格式: `crawler_engine:{tenant_id}:{site_id}`
  - 非阻塞模式: 锁被占用时立即返回 `skipped` 而非等待
  - 30 分钟自动过期: 防止崩溃进程永久阻塞
  - **Force 模式**: `--force` 先删除旧锁再获取新锁，确保能覆盖卡死任务
- **Section 继承**: Sections 可覆盖父级 listing URL、分页策略、提取配置；未覆盖则透明继承
- **状态管理**: `CrawlerState` DB 表持久化，支持断点续爬
- **容量保护**: `StateManager` 的 `processed_ids` 超过 10000 条时自动清理最旧记录
- **并发写入安全**: BidWriter 使用 `INSERT ... ON DUPLICATE KEY UPDATE` 幂等 upsert

#### 6.4.11 向后兼容

`task_executor.py` 和 `scheduled_task_executor.py` **无需任何修改**。DB 中 `ScheduledTask` 行只需改两字段:
```
script_path: rag/svr/unified_crawler.py
script_args: {"site_id": "fgw_zwgk"}
```

#### 6.4.12 关键修复记录

| 阶段 | 问题 | 修复 |
|------|------|------|
| v2.0 | 提取器未创建 (json_path 类型) | `_crawl_one_section` 始终创建 `section_extractor` |
| v2.0 | Section listing URL 未继承 | 检查 `section.listing.url` 非空才覆盖 |
| v2.0 | `state.processed_ids` 返回副本导致丢失 | 返回内部集合引用，添加 public setter |
| v2.0 | 重复爬取 (无并发锁) | Engine 入口加 `RedisDistributedLock` |
| v2.0 | 循环导入 `redis_conn` | try/except ImportError 回退 |
| v2.1 | `task_name` 未定义变量 | 改用 `self._task_name` 引用实例属性 |
| v2.1 | `all_raw_items` 死代码 | 删除未使用的列表 |
| v2.1 | Force 模式无锁保护 | 删除旧锁后重新获取新锁 |
| v2.1 | Section 提取器 base_url 错误 | 传入 `listing_cfg.url` 而非全局 config URL |
| v2.1 | 附件统计重复计数 | 使用 delta 计算 (前后差值) |
| v2.1 | `_db_checked` 无限增长 | 添加 50000 上限自动清空 |
| v2.1 | ZIP 清理用 os.rmdir | 改用 `shutil.rmtree(ignore_errors=True)` |

#### 6.4.2 四种 HTTP 适配器覆盖

| 适配器 | 传输引擎 | 适用场景 | 覆盖站点数 |
|--------|---------|---------|-----------|
| `rest_api` | requests.Session | 纯 API JSON / HTML 网页解析 | ~30 |
| `encrypted_api` | urllib / PlaywrightHttpClient | SM4/AES 加密响应 + MD5 签名 | ~8 |
| `spa_render` | Playwright sync_playwright | Vue/React SPA 全浏览器渲染 | ~5 |
| `playwright_http` | PlaywrightHttpClient | 浏览器指纹 (无 SPA 渲染) | ~4 |

#### 6.4.3 分页策略

| 策略 | 原理 | 适用站点 |
|------|------|---------|
| `page_no` | 递增页码参数 (如 `page=1,2,3...`) | 大多数政府网站 |
| `offset` | 递增偏移量参数 (如 `start=0,20,40...`) | 分页 API |
| `total_count` | 首次请求获取 total，计算总页数 | 含 `total` 字段的 JSON API |
| `html_regex` | 正则从 HTML 匹配总条数 + 总页数 | HTML 分页栏 |
| `click_next` | Playwright 点击"下一页"按钮 | SPA 站点 |
| `single_page` | 单页抓取 | 静态公告页 |

#### 6.4.4 YAML 配置示例 (`crawler_sites.yaml`)

```yaml
version: "1.0"
sites:
  fgw_zwgk:
    name: "发改委政务公开"
    site_url: "https://www.ndrc.gov.cn"
    transport:
      type: rest_api
      headers:
        User-Agent: "Mozilla/5.0 ..."
    listing:
      url: "https://www.ndrc.gov.cn/xxx/list.html"
      params:
        page: "{{ page }}"
        pageSize: "{{ page_size }}"
    pagination:
      type: page_no
      page_param: "page"
      page_size: 20
    extract:
      type: css_selector
      items_path: "ul.list li"
      fields:
        title: "a"
        url: "a@href"
        date: ".date"
    format:
      template: |
        # {{ title }}
        **日期:** {{ date }}
        {{ content }}
      upload_batch_size: 10
    anti_crawler:
      delay_min: 1.0
      delay_max: 2.5
      max_retries: 4
```

#### 6.4.5 并发安全机制

- **分布式锁**: `RedisDistributedLock(lock_key, timeout=7200, blocking_timeout=0)`
  - Lock key 格式: `crawler_engine:{tenant_id}:{site_id}`
  - 非阻塞模式: 锁被占用时立即返回 `skipped` 而非等待
  - 2 小时自动过期: 防止崩溃进程永久阻塞
- **Section 继承**: Sections 可覆盖父级 listing URL、分页策略、提取配置；未覆盖则透明继承
- **状态管理**: `CrawlerState` DB 表持久化，支持断点续爬
- **并发写入安全**: BidWriter 使用 `INSERT ... ON DUPLICATE KEY UPDATE` 幂等 upsert

#### 6.4.6 向后兼容

`task_executor.py` 无需修改。DB 中 `ScheduledTask` 行只需改两字段:
```
script_path: rag/svr/unified_crawler.py
script_args: {"site_id": "fgw_zwgk"}
```

#### 6.4.7 关键修复记录 (v2.0 开发阶段)

| 问题 | 修复 |
|------|------|
| 提取器未创建 (json_path 类型) | `_crawl_one_section` 始终创建 `section_extractor` |
| Section listing URL 未继承 | 检查 `section.listing.url` 非空才覆盖 |
| `state.processed_ids` 返回副本导致丢失 | 返回内部集合引用，添加 public setter |
| 重复爬取 (无并发锁) | Engine 入口加 `RedisDistributedLock` |
| 循环导入 `redis_conn` | try/except ImportError 回退 |

### 6.5 微信公众号离线批处理管道 (WeChat MP Batch Pipeline)

> **核心问题**: 微信公众平台对数据中心 IP (阿里云/Cloudflare) 实施严格反爬，服务器侧 `gather_content=true` 直接抓取正文会被 CAPTCHA 拦截。解决方案：将**元数据采集**和**正文抓取**分离，正文由 Windows 住宅 IP 机器离线完成。

#### 6.5.1 架构概述

```
┌──────────────────────────┐      ┌──────────────────────────┐      ┌──────────────────────────┐
│  Phase 1: 服务器           │      │  Phase 2: 本地Windows      │      │  Phase 3: 服务器           │
│  wechat_mp_crawler.py     │ SCP  │  local_content_fetcher.py│ SCP  │  wechat_mp_crawler.py     │
│  --output-json            │ ───→ │  --input                 │ ───→ │  --input-json             │
│                           │      │                          │      │                           │
│  ① 调用 MpsApi 获取元数据   │      │  ③ Playwright Firefox     │      │  ⑤ 过滤有效文章             │
│  ② 去重 → 导出 JSON 文件    │      │     + add_init_script()  │      │  ⑥ 上传 KB + 触发解析       │
│  ③ 写入 pending_aids       │      │     + 反检测 JS 注入       │      │  ⑦ 更新 processed_aids     │
│                           │      │  ④ 检测CAPTCHA/已删除      │      │                           │
└──────────────────────────┘      └──────────────────────────┘      └──────────────────────────┘
```

#### 6.5.2 核心文件

| 文件 | 位置 | 职责 |
|------|------|------|
| `wechat_mp_crawler.py` | `rag/svr/` (服务器) | Phase 1 (导出元数据) + Phase 3 (上传KB) |
| `local_content_fetcher.py` | `rag/svr/wechat_mp/` (Windows) | Phase 2 (Playwright 正文抓取) |
| `gather.py` | `rag/svr/wechat_mp/` (服务器) | WxGather 基类 — cookies/token 管理 |
| `mps_api.py` | `rag/svr/wechat_mp/` (服务器) | MpsApi — `action=list_ex` API 调用 |
| `cloudflare_proxy.js` | `rag/svr/wechat_mp/` | CF Worker 代理中继 (备用，已被直连替代) |

#### 6.5.3 状态管理 (关键修复)

两阶段状态追踪，防止数据丢失和重复爬取：

```
state = {
    "processed_aids": [...],  # 已上传 KB 的文章ID (永久标记)
    "pending_aids": [...],    # 已导出元数据但尚未上传的文章ID
    "mp_articles": {          # 每个 MP 的统计信息
        "<mp_id>": {"last_aid": "...", "count": N}
    }
}
```

**状态流转:**
- Phase 1 (`--output-json`): 新文章 ID 写入 `pending_aids`
- Phase 2: 不改变状态
- Phase 3 (`--input-json`): 从 `pending_aids` 移到 `processed_aids`
- 去重检查: 同时检查 `processed_aids` + `pending_aids`

**⚠️ v1.0 bug 已修复**: Phase 1 原本直接写入 `processed_aids`（此时内容还没抓取）；若 Phase 2 崩溃则文章永久丢失。v1.1 引入 `pending_aids` 两阶段机制解决。

#### 6.5.4 反检测机制

Windows 侧 `local_content_fetcher.py` 使用 we-mp-rss 项目的反检测模块：

| 技术 | 说明 |
|------|------|
| **Playwright Firefox** | Firefox 的自动化标记比 Chromium 少 |
| **add_init_script()** | 在页面 JS 执行前注入 ~400 行反检测脚本 |
| **navigator.webdriver 隐藏** | `Object.getOwnPropertyDescriptor` 拦截 |
| **伪造 plugins/mimeTypes** | PDF Viewer + Chrome PDF Plugin |
| **window.chrome 伪造** | 绕过 Sannysoft 检测 |
| **WebGL 指纹伪装** | NVIDIA GeForce RTX 3060 指纹 |
| **Canvas 噪声注入** | 每次读取加随机噪声 |
| **sec-fetch-* headers** | Chrome 131 标准请求头 |
| **住宅 IP** | 最关键的绕过因素——数据中心 IP 被微信永久标记 |

#### 6.5.5 文章内容提取

`LocalContentFetcher.fetch(url)` 完整流程：

1. 启动 Playwright Firefox (lazy init, 全局复用)
2. 创建 browser context (随机 UA + 移动端 viewport + zh-CN)
3. `page.add_init_script()` 注入反检测脚本
4. `page.goto(url)` → 等待 `domcontentloaded` + `networkidle`
5. 检测 CAPTCHA: `"当前环境异常，完成验证后即可继续访问"`
6. 检测已删除: 5 种删除状态文字
7. 提取: `og:title` → `document.title` (fallback)
8. 提取: author / description / mp_name / publish_time
9. 滚动加载懒图片
10. 提取: `#js_content` → `#js_article` → `body` (fallback)
11. `_clean_html()`: 移除 `display:none` 元素、script 标签、底部交互组件
12. BeautifulSoup 提取纯文本

**边缘情况处理:**

| 情况 | 检测方式 | 标记 |
|------|---------|------|
| 纯图片文章 | `#js_content` 只有 `<section><img>` 无文本 | `EMPTY_TEXT` |
| CAPTCHA | body_text 包含特定文字 | `CAPTCHA` |
| 已删除 | body_text 匹配 5 种模式 | `DELETED` / `DELETED_AUDIT` / ... |
| 无正文 | 所有选择器都返回空 | `NO_CONTENT` |

#### 6.5.6 操作流程

**前置条件:**
- 服务器 `wechat_mp_auth` 表有有效的 cookie + token
- Windows 安装 Playwright Firefox: `playwright install firefox`
- Windows 有 `D:\AI\we-mp-rss` 项目 (反检测模块)

**Phase 1 — 服务器导出元数据:**
```bash
docker exec docker-ragflow-cpu-1 python /ragflow/rag/svr/wechat_mp_crawler.py \
  --tenant-id 7ab771d4dec84f23b2c1fb5f4e453ff9 \
  --task-name "【微信公众号】福建省招标投标协会" \
  --kb-id 4bc37dfa5f5311f195c25b228b2f345c \
  --mp-ids "MzUzNjE2Nzk4Mw==" \
  --output-json /ragflow/rag/data/wechat_mp_states/articles.json \
  --gather-content false \
  --max-page 1
```

**Phase 2 — Windows 抓取正文:**
```powershell
# ① 下载元数据
scp -i D:\AI\konus-key.pem root@47.98.102.55:/home/bid-agent-konus/ragflow2/rag/data/wechat_mp_states/articles.json D:\AI\ragflow2\data\wechat_mp_states\

# ② 抓取正文 (从 we-mp-rss 目录运行以避免 token.py 冲突)
cd D:\AI\we-mp-rss
python D:\AI\ragflow2\rag\svr\wechat_mp\local_content_fetcher.py \
  --input D:\AI\ragflow2\data\wechat_mp_states\articles.json \
  --output D:\AI\ragflow2\data\wechat_mp_states\articles_with_content.json \
  --interval 8
```

**Phase 3 — 服务器上传 KB:**
```bash
# ① 上传正文 JSON
scp -i D:\AI\konus-key.pem D:\AI\ragflow2\data\wechat_mp_states\articles_with_content.json root@47.98.102.55:/home/bid-agent-konus/ragflow2/rag/data/wechat_mp_states/

# ② 上传 KB
docker exec docker-ragflow-cpu-1 python /ragflow/rag/svr/wechat_mp_crawler.py \
  --tenant-id 7ab771d4dec84f23b2c1fb5f4e453ff9 \
  --task-name "【微信公众号】福建省招标投标协会" \
  --kb-id 4bc37dfa5f5311f195c25b228b2f345c \
  --input-json /ragflow/rag/data/wechat_mp_states/articles_with_content.json
```

**自动化: 服务器 Scheduled Task + Windows 计划任务:**
```json
// scheduled_task 配置
{
  "script_path": "rag/svr/wechat_mp_crawler.py",
  "schedule_type": "cron",
  "cron_expression": "0 8,18 * * *",
  "script_args": {
    "mp_ids": ["MzUzNjE2Nzk4Mw=="],
    "gather_content": false,
    "first_max_page": 10,
    "output_json": "/ragflow/rag/data/wechat_mp_states/articles.json"
  }
}
```

Windows 计划任务脚本 (`wechat_fetch.ps1`) 自动执行 Phase 2 + Phase 3 的 SCP 传输。

#### 6.5.7 关键边界行为

| 场景 | 处理方式 |
|------|---------|
| **图片型文章** | 检测为 `EMPTY_TEXT`，保存 HTML 但不做纯文本解析，Phase 3 自动跳过 |
| **CAPTCHA** | 标记 `fetch_error=CAPTCHA`，Phase 3 自动跳过 |
| **已删除文章** | 标记 `DELETED/DELETED_AUDIT` 等，Phase 3 自动跳过 |
| **Phase 2 崩溃** | `pending_aids` 保留，下次 Phase 1 不会重新导出同一批 |
| **Phase 3 崩溃** | `pending_aids` 仍在，重新上传即可 |
| **重复上传** | `--input-json` 不检查 KB 重复（依赖流程保证只调一次） |
| **异常退出** | 浏览器 context 在 `finally` 块中关闭，无泄漏 |
| **空文章列表** | 浏览器在 `return` 前关闭，无泄漏 |
| **token.py 模块冲突** | `local_content_fetcher.py` 启动时移除 wechat_mp/ 目录从 sys.path |
| **首次运行 vs 增量** | `is_first_run` 检查 pending + processed 总数，首次用 `first_max_page` (大)，后续用 `max_page` (小) |

#### 6.5.8 已知限制

- 当前仅配置 1 个 MP (福建省招标投标协会)，需通过 `wechat_mp_account` 表和 `script_args.mp_ids` 新增
- 福建招标投标协会的公众号主要发布**图片型通知**，文字型内容占比较低 (~17%)
- `cloudflare_proxy.js` 代理中继已废弃——CF Worker 出站 IP 被微信永久标记

#### 6.5.9 v1.1 代码 Review 修复清单

| # | 问题 | 文件 | 修复 |
|---|------|------|------|
| 1 | Phase 1 提前更新 `processed_aids`，内容未抓取就标为已处理 | `wechat_mp_crawler.py` | 引入 `pending_aids` 两阶段状态 |
| 2 | Phase 3 不更新状态 | `wechat_mp_crawler.py` | `--input-json` 后从 pending → processed |
| 3 | `mp_articles` 只追踪第一个 MP | `wechat_mp_crawler.py` | 改为遍历所有 MP 统计 |
| 4 | `faker_id` 字段丢失 (ext_data 不包含) | `wechat_mp_crawler.py` | 从 `mp_info_list` 构建 lookup 映射 |
| 5 | 空文章列表时浏览器未关闭 | `local_content_fetcher.py` | 所有 return 路径统一 `finally: close()` |
| 6 | 异常时 context 泄漏 | `local_content_fetcher.py` | context 提升到 try 外，finally 中关闭 |

#### 6.5.10 Claude Code Skill — 一键触发

`konus-wechat-mp` skill 封装了完整的三阶段操作流程，新对话中直接说 "运行微信公众号任务" 即可自动执行：

- 自动查询服务器 `scheduled_task` 表，列出启用的微信任务
- 确认参数后依次执行 Phase 1 (服务器) → Phase 2 (Windows) → Phase 3 (服务器)
- 自动验证 KB 文档解析状态并报告结果
- 详见 §14.6 及 `C:\Users\lg186\.claude\skills\konus-wechat-mp\SKILL.md`

---

## 7. 核心调用链路

### 7.1 用户对话流 (C端)

```
用户输入 Query (c-chat/index.tsx)
  → POST /api/v1/agent/chat/{agent_id}/completions (agent_api.py)
  → Canvas.run(query=...)
  → Canvas.load() DSL → 构建 DAG
  → Canvas._run_batch(["begin"]) → Begin._invoke_async()
  → _append_path() → 下一层节点
  → ... (LLM/Agent/Retrieval/Tools 依次执行)
  → Canvas SSE yield events
    → 心跳循环: 每 100ms → drain FanOut 事件队列 (一个/tick)
    → post-batch: 0.3s 间隔 drain 完
  → yield "workflow_finished"
  → 前端 EventSourceParserStream → 渲染 Markdown
```

### 7.2 标讯数据流

```
定时同步 (bid_sync.py / 手动触发)
  → bid_api_client.py → 外部 API
  → bid_service.py → MySQL (bid_project 等表)
  ↓
前端列表 (bid-list.tsx)
  → GET /api/v1/bid/projects → bid_app.py → bid_service.py
  → TanStack React Query 缓存 → 渲染
  ↓
解析入库 (用户点击"配置")
  → bid_tool_service.py:
    1. 获取详情 + 附件
    2. 下载附件 (zip/rar → 解压)
    3. _build_combined_text() 拼接正文
    4. 上传到知识库 (DocumentService)
    5. 触发 KB 解析 (Parser → Chunker → Embedding → ES)
    6. 更新 bid_project_parse 状态
```

### 7.3 Agent 工具调用流

```
Agent 组件收到用户 Query
  → 构建 messages: [system_prompt, ...history, user_query]
  → LLMBundle.async_chat_streamly(messages, tools=tool_meta)
  → LLM 返回 delta (含 tool_calls)
  → tool_use_callback(tool_name, args):
    → 查找 tools_map[tool_name]
    → Tool._invoke(**args) → 执行业务逻辑
    → 返回结果文本
  → 将 {role: "tool", content: result} 追加到 messages
  → 继续 LLM 调用 (直到无 tool_calls 或 max_rounds 用尽)
  → 最终生成文本 → yield SSE message events
```

### 7.4 文档处理流

```
文件上传 (file_api.py)
  → MinIO 存储原始文件
  → DocumentService.add_document()
  ↓
解析触发 (document_api.py / bid_tool_service.py)
  → Pipeline.run()
  → Parser (PDF/Word/Image → 结构化文本)
  → Chunker (切分为 chunk)
  → Embedding (向量化)
  → ES.bulk_index (存储 chunk + vector)
  ↓
检索 (retrieval.py)
  → Query Embedding
  → ES KNN/ANN 检索
  → Rerank (可选)
  → 返回 top_k chunks
```

---

## 8. 关键开发模式

### 8.1 新增 REST API 端点

```
1. 在 api/apps/restful_apis/ 新建 xxx_api.py
2. 创建 Blueprint("rest_xxx", __name__, url_prefix="/api/v1")
3. 用 @manager.route + @login_required 定义端点
4. 路由会自动注册 (扫描 api/apps/restful_apis/)
```

### 8.2 新增数据库表

```
1. 在 api/db/db_models.py 添加 Peewee Model 类
2. 在 api/db/services/ 创建对应的 Service 类
3. 服务器上执行 docker exec mysql 建表
```

主键约定:
- 自定义业务表: `BigIntegerField(primary_key=True)` (雪花ID)
- RAGFlow 内置表: `CharField(max_length=32, primary_key=True)` (UUID)

### 8.3 新增 Agent 组件

```
1. 在 agent/component/ 新建 .py 文件
2. 继承 ComponentParamBase (参数定义)
3. 继承 ComponentBase (执行逻辑)
4. 实现 _invoke_async(self, **kwargs) 方法
5. 实现 get_input_form() → 定义上游输入
6. 实现 set_output() → 定义下游可用输出
7. 前端: 在 web/src/constants/agent.tsx 注册 Operator 枚举
8. 前端: 在 web/src/pages/agent/constant/index.tsx 注册 NodeMap
9. 前端: 添加配置表单 + 图标
```

### 8.4 新增 Agent 工具

```
1. 在 agent/tools/ 新建 .py 文件
2. 继承 ToolParamBase (参数定义) + ToolBase (执行逻辑)
3. 实现 _invoke(self, **kwargs) 方法
4. 工具会被自动发现 (component_class 扫描 agent/tools/)
5. 工具函数应委托给 api/utils/ 下的共享服务层
```

### 8.5 前端新增页面

```
1. 在 web/src/pages/ 新建目录和组件
2. 在 web/src/utils/api.ts 添加 API URL 常量
3. 在 web/src/services/ 添加 service 文件
4. 在 web/src/routes.tsx (B端) 或 c-routes.tsx (C端) 添加路由
```

**HTTP 客户端**: 新代码用 `next-request.ts` (axios)，旧代码用 `request.ts` (umi-request)。

### 8.6 前端新增 Agent 工具卡片

```
1. 在 web/src/assets/svg/ 添加工具图标 SVG
2. 在 web/src/constants/agent.tsx 注册 Tool 枚举
3. 在 web/src/pages/agent/constant/index.tsx 注册提供工具的逻辑
4. 在 web/src/pages/agent/form/tool-form/ 可添加专属配置面板
```

---

## 9. 部署

### 9.1 目标服务器

| 项目 | 值 |
|---|---|
| IP | `47.98.102.55` |
| SSH | `root@47.98.102.55` |
| 密钥 | `D:\AI\konus-key.pem` |
| 项目路径 | `/home/bid-agent-konus/ragflow2/` |
| Docker 配置 | `/home/bid-agent-konus/ragflow2/docker/` |
| 容器名 | `docker-ragflow-cpu-1` (带 `docker-` 前缀!) |

### 9.2 Bind Mount 映射

| 宿主机路径 | 容器内路径 | 部署方式 |
|---|---|---|
| `.../ragflow2/api` | `/ragflow/api` | 直接覆盖 → `docker restart` |
| `.../ragflow2/rag` | `/ragflow/rag` | 直接覆盖 → `docker restart` |
| `.../ragflow2/agent` | `/ragflow/agent` | 直接覆盖 → `docker restart` |
| `.../ragflow2/common` | `/ragflow/common` | 直接覆盖 → `docker restart` |
| `.../ragflow2/deepdoc` | `/ragflow/deepdoc` | 直接覆盖 → `docker restart` |
| `.../ragflow2/web/dist` | `/ragflow/web/dist` | npm build → scp tar.gz → 解压 → nginx reload |

### 9.3 ⚠️ Bind Mount Inode 陷阱

Docker bind mount 绑定的是**目录的 inode**，而非路径字符串。

- ❌ `mv dist dist.old && tar -xzf dist.tar.gz` — 创建新 dist (新 inode)，容器仍指向旧 inode → 403
- ✅ `rm -rf dist/* && tar -xzf dist.tar.gz` — 只删内容，目录 inode 不变
- ❌ `docker compose up -d --force-recreate` — 删除重建容器，entrypoint 重跑 (下载依赖耗时)

### 9.4 部署命令

```bash
# === 前端完整部署 ===
cd "D:\AI\ragflow2\web" && npm run build
cd "D:\AI\ragflow2\web" && tar -czf dist.tar.gz dist/
scp -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no "D:\AI\ragflow2\web\dist.tar.gz" "root@47.98.102.55:/home/bid-agent-konus/ragflow2/web/dist.tar.gz"
ssh -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no root@47.98.102.55 "cd /home/bid-agent-konus/ragflow2/web && rm -rf dist/* dist/.[!.]* dist/..?* 2>/dev/null; tar -xzf dist.tar.gz && rm -f dist.tar.gz && docker exec docker-ragflow-cpu-1 cp -f /etc/nginx/conf.d/ragflow.conf.python /etc/nginx/conf.d/ragflow.conf && docker exec docker-ragflow-cpu-1 nginx -s reload"

# === 后端文件上传 ===
scp -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no "D:\AI\ragflow2\agent\component\fanout.py" "root@47.98.102.55:/home/bid-agent-konus/ragflow2/agent/component/fanout.py"
# ... 同理上传其他修改的文件

# === 后端重启 ===
ssh -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no root@47.98.102.55 "docker restart docker-ragflow-cpu-1"

# === 验证 ===
ssh -i "D:\AI\konus-key.pem" -o StrictHostKeyChecking=no root@47.98.102.55 "docker exec docker-ragflow-cpu-1 ls /ragflow/web/dist/ | head -5"
```

### 9.5 常见问题排查

| 症状 | 原因 | 解决 |
|---|---|---|
| 403 Forbidden | bind mount 断开 (inode 变了) | 原地解压 dist，不要 mv |
| 前端不更新 | nginx/浏览器缓存 | nginx reload + 强制刷新 |
| 后端不生效 | Flask 没重启 | `docker restart` |
| 容器启动慢卡在下载依赖 | 用了 `--force-recreate` | 等它完成，下次用 restart |
| dist 解压后容器内无文件 | MV 操作改变了目录 inode | 必须用 rm -rf dist/* |

---

## 10. 目录文件快速索引

```
# === 标讯系统 ===
标讯 API:             api/apps/restful_apis/bid_app.py
标讯 Service:         api/db/services/bid_service.py
标讯工具层:           api/utils/bid_tool_service.py
标讯 API 客户端:      api/utils/bid_api_client.py (v1+v2 双网关)
标讯文件工具:         api/utils/bid_file_utils.py
行业分类数据:         api/utils/industry_data.py
标讯 Agent 工具:      agent/tools/bid.py
标讯同步脚本:         rag/svr/bid_sync.py

# === Agent 系统 ===
Canvas 核心:          agent/canvas.py
组件基类:             agent/component/base.py
LLM 组件:             agent/component/llm.py
Agent (LLM+工具):     agent/component/agent_with_tools.py
FanOut 并行迭代:      agent/component/fanout.py
串行迭代:             agent/component/iteration.py
条件循环 (Loop):       agent/component/loop.py
条件循环体 (LoopItem): agent/component/loopitem.py
Message 输出:         agent/component/message.py
工具基类:             agent/tools/base.py

# === RAG 引擎 ===
管道:                 rag/flow/pipeline.py
解析器:               rag/flow/parser/parser.py
切分器:               rag/flow/chunker/
嵌入模型:             rag/llm/embedding_model.py
Chat 模型:            rag/llm/chat_model.py

# === API 核心 ===
Quart 入口:           api/ragflow_server.py
Agent Chat API:       api/apps/restful_apis/agent_api.py
自动路由发现:         api/apps/__init__.py
DB 模型全集:          api/db/db_models.py
向后兼容层:           api/apps/backward_compat.py

# === 服务层 ===
协作编辑/导出:        api/apps/services/collaboration_api_service.py
定时任务 API:         api/apps/restful_apis/scheduled_task_api.py
MCP API:              api/apps/restful_apis/mcp_api.py

# === 后台服务 ===
MCP Server:           rag/svr/mcp_server.py
定时调度器:           rag/svr/scheduled_task_executor.py
Worker:               rag/svr/task_executor.py

# === 微信公众号采集 (离线批处理) ===
服务器端:             rag/svr/wechat_mp_crawler.py
Windows 本地抓取:     rag/svr/wechat_mp/local_content_fetcher.py
WxGather 基类:        rag/svr/wechat_mp/gather.py
MpsApi 文章列表:      rag/svr/wechat_mp/mps_api.py
CF Worker 代理:       rag/svr/wechat_mp/cloudflare_proxy.js
一键触发 Skill:       ~/.claude/skills/konus-wechat-mp/SKILL.md

# === Docker ===
Docker Compose:       docker/docker-compose.yml
基础服务:             docker/docker-compose-base.yml
Nginx 配置:           docker/nginx/ragflow.conf.python
服务配置模板:         docker/service_conf.yaml.template

# === Loop 表单 (前端) ===
Loop 输出映射组件:    web/src/pages/agent/form/loop-form/dynamic-output.tsx
Loop 表单:             web/src/pages/agent/form/loop-form/index.tsx
Loop 表单 schema:      web/src/pages/agent/form/loop-form/schema.ts
Loop 表单同步:         web/src/pages/agent/form/loop-form/use-watch-form-change.ts
Loop 加载还原:         web/src/pages/agent/form/loop-form/use-values.ts

# === 协作编辑器 (前端) ===
Lexical 编辑器:        web/src/components/collaboration/document-editor.tsx
协作工具栏:            web/src/components/collaboration/toolbar-plugin.tsx

# === 前端核心 ===
根组件 (C/B分割):     web/src/app.tsx
B端路由:              web/src/routes.tsx
C端路由:              web/src/c-routes.tsx
C端对话页:            web/src/pages/c-chat/index.tsx
B端标讯管理:          web/src/pages/home/bid-list.tsx
B端标讯详情:          web/src/pages/home/bid-detail-view.tsx
Agent 画布编辑器:     web/src/pages/agent/
Agent 算子注册:       web/src/constants/agent.tsx
Agent 节点映射:       web/src/pages/agent/constant/index.tsx
Agent 表单配置:       web/src/pages/agent/form-sheet/form-config-map.tsx
标讯服务:             web/src/services/bid-service.ts
HTTP 客户端 (新):     web/src/utils/next-request.ts
API URL 常量:         web/src/utils/api.ts

# === 爬虫 (统一框架 v2.1 三层架构) ===
统一入口:             rag/svr/unified_crawler.py
站点配置 (78个站点):  rag/svr/crawler_sites.yaml
引擎核心 (三层分离):  rag/svr/crawler_engine/engine.py
数据模型:             rag/svr/crawler_engine/models.py
双层去重:             rag/svr/crawler_engine/dedup_checker.py
存储管线:             rag/svr/crawler_engine/storage_pipeline.py
附件处理:             rag/svr/crawler_engine/attachment_handler.py
YAML 加载:            rag/svr/crawler_engine/config.py
REST API 适配器:      rag/svr/crawler_engine/adapters/rest_api.py
加密 API 适配器:      rag/svr/crawler_engine/adapters/encrypted_api.py
SPA 浏览器适配器:     rag/svr/crawler_engine/adapters/spa_render.py
浏览器 HTTP 适配器:   rag/svr/crawler_engine/adapters/playwright_http.py
JSON 提取器:          rag/svr/crawler_engine/extractors/json_path.py
CSS 提取器:           rag/svr/crawler_engine/extractors/css_selector.py
AI 提取器:            rag/svr/crawler_engine/extractors/ai_extractor.py
分页策略:             rag/svr/crawler_engine/paginator.py
反爬管理器:           rag/svr/crawler_engine/anti_crawler.py
格式化器:             rag/svr/crawler_engine/formatter.py
KB 上传器:            rag/svr/crawler_engine/kb_uploader.py
标讯入库:             rag/svr/crawler_engine/bid_writer.py
状态管理:             rag/svr/crawler_engine/state_manager.py
浏览器池:             rag/svr/crawler_engine/browser_pool.py
加密工具:             rag/svr/crawler_engine/crypto_utils.py
旧版工具 (仍引用):    rag/svr/crawler_utils.py
旧爬虫脚本 (已替代):  rag/svr/*.py (80+ 个，不再维护)
```

---

## 11. 数据库直连

```bash
# MySQL
docker exec -it docker-mysql-1 mysql -uroot -pinfini_rag_flow rag_flow

# MinIO Console
# http://47.98.102.55:9001  user=rag_flow  password=infini_rag_flow

# Redis
docker exec -it docker-redis-1 redis-cli
```

---

## 12. 世舶科技 API 扩展集成 (2026-06-04)

### 12.1 概述

基于世舶科技接口文档 v1.4 (21个接口)，在原有 5 个标讯搜索接口基础上，新增对接 12 个接口，覆盖**合同搜索、企业画像、拟在建项目、AI 辅助**四大能力域。同时 C 端 BidPanel 从 3 个标签扩展为 6 个标签。

### 12.2 双网关架构

世舶科技提供两个 API 网关，本项目同时对接：

| 网关 | 基础地址 | 认证方式 | 覆盖接口 |
|---|---|---|---|
| v1 阿里云 | `https://gov.market.alicloudapi.com` | Header: `APPCODE {key}` | searchProjectApi2025, getProject2025, getZTBStructreDetail2025, getProjectFiles2025 (4个) |
| v2 世舶直连 | `https://gate.gov-bid.com/outer-gateway` | Query: `?key={key}` | SearchProjectForAI, searchProjectContactApi, companyProfile*, searchNZJProjectApi, getCollectUrl, aiSearchSubmitPolling, industryReasoning 等 (12个) |

**环境变量:**

```env
# 必填 — 两个网关共用同一个 key
BID_APP_CODE=你的世舶科技key

# 可选 — v2 网关独立配置（不设则复用 BID_APP_CODE）
BID_API_KEY=

# 可选 — v2 网关基础地址（默认以下值）
BID_API_BASE_URL_V2=https://gate.gov-bid.com/outer-gateway
```

### 12.3 后端改动

#### `api/utils/bid_api_client.py` — API 客户端 (双网关)

新增 v2 网关支持：
- `BID_API_BASE_URL_V2` / `BID_API_KEY` 环境变量（`BID_API_KEY` 默认复用 `BID_APP_CODE`）
- `_v2_post_json(endpoint, payload)` — JSON POST，key query param 认证
- `_v2_post_form(endpoint, data)` — Form POST，key query param 认证
- 统一错误处理：`code != 200` / `state != 1` / `subCode != "0000000000"` 均抛 `BidApiError`

新增 12 个 API 方法：

| 方法 | 端点 | 用途 |
|---|---|---|
| `get_collect_url()` | `/bid/getCollectUrl` | 获取原始采集源网址 |
| `search_project_ai()` | `/bid/SearchProjectForAI` | AI 轻量搜索（自然语言友好） |
| `search_contract()` | `/bid/searchProjectContactApi` | 合同数据搜索 |
| `ai_search_rewrite()` | `/bid/aiSearchSubmitPolling` | AI 搜索条件重写 |
| `ai_industry_reasoning()` | `/bid/industryReasoning` | AI 行业标签推理 |
| `get_company_profile_summary()` | `/bid/companyProfileSummary` | 企业画像汇总 |
| `get_company_profile_contacts()` | `/bid/companyProfileContacts` | 企业联系人 (max 5/page) |
| `get_company_profile_customers()` | `/bid/companyProfileCustomers` | 企业客户 (max 20/page) |
| `get_company_profile_suppliers()` | `/bid/companyProfileSuppliers` | 企业供应商 (max 20/page) |
| `search_nzj_project()` | `/bid/searchNZJProjectApi` | 拟在建项目搜索 |
| `get_nzj_project_detail()` | `/bid/getNZJProjectDetail` | 拟在建项目详情 |
| `get_project_by_number()` | `/bid/getProjectByProjectNumber` | 项目编号查询 |

#### `api/apps/restful_apis/bid_app.py` — REST API 端点

新增 9 个 REST 端点：

| 端点 | 方法 | 用途 | 数据来源 |
|---|---|---|---|
| `/bid/projects/{id}/collect-url` | GET | 采集源网址 | getCollectUrl |
| `/bid/projects/by-number` | GET | 编号查询 | getProjectByProjectNumber |
| `/bid/contracts` | GET | 合同/中标搜索 | searchProjectContactApi |
| `/bid/enterprises/profile` | GET | 企业画像 | companyProfileSummary |
| `/bid/enterprises/contacts` | GET | 企业联系人 | companyProfileContacts |
| `/bid/enterprises/customers` | GET | 企业客户 | companyProfileCustomers |
| `/bid/enterprises/suppliers` | GET | 企业供应商 | companyProfileSuppliers |
| `/bid/construction/projects` | GET | 拟在建搜索 | searchNZJProjectApi |
| `/bid/construction/projects/{id}/detail` | GET | 拟在建详情 | getNZJProjectDetail |

所有端点均为 `@login_required`，直接调用 `BidApiClient` v2 方法（除企业钻取类为API透传，其余走缓存优先）。

#### `api/utils/bid_tool_service.py` — 共享服务层 (★ 缓存优先核心)

Agent 工具和 MCP 工具共享的缓存优先服务函数。每个函数遵循: DB查询 → TTL检查 → API调用 → upsert → 返回。

| 函数 | 用途 | 缓存TTL |
|---|---|---|
| `lookup_bid_code()` | 行政区划/行业代码查询 | 本地数据 |
| `search_bid_projects()` | 标讯搜索 (DB→API fallback) | — |
| `get_bid_detail()` | 获取详情+结构化+附件 (v1) | 30天 |
| `import_bid_to_kb()` | 导入知识库 (后台异步) | 去重检查 |
| `check_import_status()` | 查询解析进度 | — |
| `search_contracts_cached()` | ★ 合同搜索 (缓存优先) | 1h |
| `get_bid_detail_v2_cached()` | ★ v2详情 (缓存优先) | 30天 |
| `get_enterprise_contacts()` | ★ 企业联系人 | API透传 |
| `get_enterprise_customers()` | ★ 企业客户 | API透传 |
| `get_enterprise_suppliers()` | ★ 企业供应商 | API透传 |

`search_contracts_cached()` 完整逻辑:
1. `BidProjectService.get_list(news_type_id=3)` → 过滤过期缓存
2. 有效结果数 ≥ page_number → 直接返回 (`from_cache: true`)
3. 不足 → `BidApiClient.search_contract()` → `upsert_contract()` by ID
4. API异常 → 降级返回DB过期数据 (`stale: true`)

`get_bid_detail_v2_cached()` 完整逻辑:
1. 查 `BidProjectDetailService` + `BidProjectStructureService` 的 TTL
2. 两者都有效 → 返回缓存 + `BidProjectFileService.get_by_project()` 文件列表
3. 任一过期 → `BidApiClient.get_detail_v2()` + `get_structure_v2()` → upsert
4. API异常 → 降级返回过期缓存

#### `agent/tools/bid.py` — Agent 工具

新增/更新 11 个工具类（每个工具 = Param 类 + Tool 类）：

| 工具 | 说明 | 适用场景 | 数据来源 |
|---|---|---|---|
| `BidLookupCode` | 行政区划/行业代码查询 | 搜索前先查代码 | 本地数据 |
| `BidSearch` | 标讯搜索 (缓存优先) | 查招标/采购/中标 | bid_tool_service |
| `BidGetDetail` | 标讯详情+导入KB | 查详情并自动解析 | bid_tool_service |
| `BidSearchAI` | AI 轻量搜索 | 需要甲方/乙方/开标信息 | API直连 |
| `BidGetSource` | 获取标讯原始采集网址 | 用户想看官网原文 | API直连 |
| `BidImportToKb` | 手动导入知识库 | 专项导入操作 | bid_tool_service |
| `BidCheckImportStatus` | 查询导入进度 | 轮询解析状态 | bid_tool_service |
| `BidRewriteQuery` | AI 搜索条件重写 | 自然语言→结构化条件 | API直连 |
| `BidIndustryTag` | AI 行业标签推理 | 关键词→行业编码 | API直连 |
| `BidEnterpriseProfile` | 企业画像 | 查企业基础信息+投标统计 | API直连 |
| `BidSearchContract` | ★ 合同搜索 (缓存优先) | 查合同、中标结果 | bid_tool_service |
| `BidGetContractDetail` | ★ v2合同详情 (缓存优先) | 合同正文+结构化 | bid_tool_service |
| `BidEnterpriseContacts` | ★ 企业联系人 | 查联系人信息 | bid_tool_service |
| `BidEnterpriseCustomers` | ★ 企业客户 | 查客户项目列表 | bid_tool_service |
| `BidEnterpriseSuppliers` | ★ 企业供应商 | 查供应商项目列表 | bid_tool_service |
| `BidConstructionSearch` | 拟在建搜索 | 查规划审批中的项目 | API直连 |

工具设计模式（★ 缓存优先工具）:
```
_invoke() → bid_tool_service.xxx_cached() → DB查询(免费) → TTL检查
  → 有效 → 返回缓存
  → 过期 → BidApiClient(付费) → upsert到DB → 返回
  → API故障 → 降级返回过期缓存(stale=true)
```

工具设计模式（API直连工具）:
```
_invoke() → BidApiClient → json.dumps → self.set_output("formalized_content", ...)
```

### 12.4 前端改动

#### C端 BidPanel 扩展 (`web/src/components/bid/index.tsx`)

从 3 个标签扩展为 6 个标签：

| 标签 | 组件 | 新增文件 | 调用端点 |
|---|---|---|---|
| 标讯搜索 | `BidList` (原有) | — | `/api/v1/bid/projects` |
| 中标/合同 | `ContractList` | `contract-list.tsx` ★ | `/api/v1/bid/contracts` |
| 企业查询 | `EnterpriseSearch` | `enterprise-search.tsx` ★ | `/api/v1/bid/enterprises/profile` |
| 拟在建项目 | `ConstructionList` | `construction-list.tsx` ★ | `/api/v1/bid/construction/projects` |
| 失信查询 | `ShixinSearch` (原有) | — | 外部API |
| 信用中国 | `CreditChinaSearch` (原有) | — | 外部API |

#### 新组件特性

- `ContractList`: 关键词 + 日期 + 甲方/乙方筛选，结果含合同周期、甲方/乙方信息、合同起止日期
- `EnterpriseSearch`: 企业名查询 → 展示基础信息(类型/法人/成立日期/注册资本/信用代码/经营范围) + 联系方式 + 关系网络(联系人/客户项目/供应商项目) + 投标统计(按行业)
- `ConstructionList`: 关键词 + 日期筛选，结果含项目摘要(HTML)、发布时间、附件标识

### 12.5 接口覆盖总览

| 能力域 | 接口 | REST端点 | Agent工具 | MCP工具 | C端标签 |
|---|---|---|---|---|---|
| 代码查询 | — | `/bid/areas` + `/bid/industries` | BidLookupCode | lookup_bid_code | — |
| 标讯搜索 | searchProjectApi2025 | `/bid/projects` | BidSearch | search_bid_projects | 标讯搜索 |
| 标讯详情 | getProject2025 + getZTBStructreDetail + getZTBProjectFiles | `/bid/projects/{id}/detail` + structure + files | BidGetDetail | get_bid_detail | — |
| v2详情 | getDetailV2 + getStructureV2 (缓存30天) | `/bid/projects/{id}/detail-v2` | BidGetContractDetail | get_bid_detail_v2 | — |
| 采集源网址 | getCollectUrl | `/bid/projects/{id}/collect-url` | BidGetSource | — | — |
| 编号查询 | getProjectByProjectNumber | `/bid/projects/by-number` | — | — | — |
| 合同搜索 | searchProjectContactApi (缓存1h) | `/bid/contracts` | BidSearchContract | search_contracts | 中标/合同 |
| AI搜索 | SearchProjectForAI | — | BidSearchAI | — | — |
| AI重写 | aiSearchSubmitPolling | — | BidRewriteQuery | — | — |
| 行业推理 | industryReasoning | — | BidIndustryTag | — | — |
| 企业画像 | companyProfileSummary | `/bid/enterprises/profile` | BidEnterpriseProfile | — | 企业查询 |
| 企业联系人 | companyProfileContacts | `/bid/enterprises/contacts` | BidEnterpriseContacts | enterprise_contacts | — |
| 企业客户 | companyProfileCustomers | `/bid/enterprises/customers` | BidEnterpriseCustomers | enterprise_customers | — |
| 企业供应商 | companyProfileSuppliers | `/bid/enterprises/suppliers` | BidEnterpriseSuppliers | enterprise_suppliers | — |
| 拟在建 | searchNZJProjectApi + getNZJProjectDetail | `/bid/construction/*` | BidConstructionSearch | — | 拟在建项目 |
| KB导入 | — | `/bid/projects/{id}/parse` | BidImportToKb | import_bid_to_kb | — |
| 导入状态 | — | `/bid/projects/{id}/parse-status` | BidCheckImportStatus | check_bid_import_status | — |

### 12.6 涉及文件索引

```
# === 世舶科技 API 对接 (2026-06-04) ===

# 后端 — API 客户端 (修改)
api/utils/bid_api_client.py                    # 双网关 + 12个新方法

# 后端 — REST API (修改)
api/apps/restful_apis/bid_app.py             # 20+ REST端点

# 后端 — Agent 工具 (修改)
agent/tools/bid.py                              # 15+工具类 (含缓存优先)

# 后端 — 共享服务层
api/utils/bid_tool_service.py                   # 缓存优先服务函数 (Agent + MCP通用)

# 后端 — MCP Server
rag/svr/mcp_server.py                           # 11个MCP工具

# 后端 — DB (新增字段)
api/db/db_models.py                             # BidProject加fetched_at/cache_expires_at
api/db/services/bid_service.py                  # upsert_contract/filter_valid_cache

# 前端 — C端组件 (修改 + 新建)
web/src/components/bid/index.tsx                # 6标签扩展
web/src/components/bid/contract-list.tsx          # ★ 新建
web/src/components/bid/enterprise-search.tsx      # ★ 新建
web/src/components/bid/construction-list.tsx       # ★ 新建
```

### 12.7 缓存持久化架构 (2026-06-13)

#### 架构总览

```
                        ┌─────────────────────┐
                        │   LLM / MCP Client   │
                        └──────┬──────┬────────┘
                               │      │
                   ┌───────────┘      └───────────┐
                   ▼                               ▼
        ┌──────────────────┐           ┌──────────────────┐
        │  Agent Tools      │           │  MCP Server      │
        │  (bid.py)         │           │  (mcp_server.py) │
        │  _invoke() →      │           │  tool_xxx() →    │
        └────────┬──────────┘           └────────┬─────────┘
                 │                               │
                 └───────────┬───────────────────┘
                             ▼
              ┌──────────────────────────┐
              │  bid_tool_service.py     │  ← 共享服务层
              │  search_contracts_cached │
              │  get_bid_detail_v2_cached│
              │  get_enterprise_*()      │
              └──────┬──────────┬────────┘
                     │          │
         ┌───────────┘          └───────────┐
         ▼                                   ▼
  ┌─────────────┐                   ┌──────────────┐
  │  bid_service │                   │ BidApiClient  │
  │  (MySQL CRUD)│                   │ (付费API)     │
  └──────┬───────┘                   └──────┬───────┘
         │                                  │
         ▼                                  ▼
  ┌──────────┐                     ┌───────────────┐
  │  MySQL   │ ← 免费              │  世舶科技API  │ ← 按次付费
  │  (缓存)  │                     │  (权威数据源) │
  └──────────┘                     └───────────────┘
```

#### 数据流向

**缓存命中 (免费路径):**
```
搜索请求 → bid_tool_service → BidProjectService.get_list()
  → filter_valid_cache() → 有效结果 ≥ page_number
  → 返回 (from_cache: true, 0次API调用)
```

**缓存未命中 (付费路径):**
```
搜索请求 → bid_tool_service → DB查询 → 结果不足
  → BidApiClient.search_contract() [付费]
  → upsert_contract() by ID → 更新/新增DB记录
  → 返回 (from_cache: false, 1次API调用)
```

**降级路径 (API故障):**
```
搜索请求 → bid_tool_service → DB查询 → API调用失败
  → 返回DB过期数据 (stale: true, 0次API调用, 数据可能过时)
```

#### 两级工具入口对比

| 维度 | Agent Tools (bid.py) | MCP Server (mcp_server.py) |
|---|---|---|
| 调用方式 | LLM Agent画布内调用 | JSON-RPC over stdio |
| 集成目标 | RAGFlow画布编排 | Hermes/Claude Code外部工具 |
| 服务层 | bid_tool_service.py | REST API (bid_app.py) → bid_service.py |
| 缓存策略 | 一致 (共享同一服务层) | 一致 (REST端点走同一DB缓存) |
| 工具数量 | 15+ | 11 |
| 发现机制 | 自动扫描 agent/tools/*.py | TOOLS列表手动注册 |

---

## 13. 知识库动态采集 (KB Web Clip)

> 用户可在任意网页上一键采集内容到 RAGFlow 知识库，支持三种采集方式，无需手动复制粘贴、保存文件、上传。

### 13.1 功能概览

| 采集方式 | 适用场景 | 安装要求 | 便捷度 |
|---|---|---|---|
| **浏览器插件** | 日常高频采集 | 需安装 Chrome 扩展 | ★★★★★ |
| **Bookmarklet 书签工具** | 快速试用、简单页面 | 无需安装，拖拽到书签栏 | ★★★★☆ |
| **cURL / API** | 自动化脚本、批量采集 | 无，直接调用 HTTP | ★★★☆☆ |
| **B端"动态采集"按钮** | 获取安装指南和代码示例 | 无 | — |

### 13.2 架构与数据流

```
浏览器页面 ──[提取内容]──▶ 浏览器插件 Popup ──[POST]──▶ POST /api/v1/kb/{id}/clip
                              │                              │
                              ▼                              ▼
                   ① content script 注入            ① 鉴权 (JWT / API Token)
                   ② 移除 nav/footer/广告           ② KB 归属校验
                   ③ 提取 article/main 语义标签      ③ parse_mode 分流:
                   ④ 返回 title + html + content        - naive: HTML→纯文本
                                                        - llm: LLM 结构化提取 Markdown
                                                        - (空): 已预处理文本直接上传
                                                     ④ KBUploader.upload_content()
                                                     ⑤ 进入解析队列 → 知识库可检索
```

### 13.3 使用方式

#### 方式一: 浏览器插件 (推荐)

**安装步骤:**

1. 打开 Chrome，地址栏输入 `chrome://extensions/`
2. 开启右上角 **"开发者模式"**
3. 点击 **"加载已解压的扩展程序"**
4. 选择目录: `ragflow2/browser-extension/`
5. 扩展图标出现在工具栏，点击图钉固定

**配置:**

1. 右键扩展图标 → **"选项"**（或点击 Popup 中的设置链接）
2. 填入服务器地址（如 `http://47.98.102.55:9380`）
3. 填入 API Key（在 B端 → 个人设置 → API Token 获取）
4. 保存

**采集流程:**

1. 访问任意网页（文章、公告、文档等）
2. 点击扩展图标，弹出 Popup
3. 自动加载你的知识库列表（5分钟缓存），选择目标 KB
4. 选择解析模式: **Naive**（纯文本，快速）或 **LLM**（结构化 Markdown，需配置聊天模型）
5. 点击 **"采集到知识库"**
6. 几秒后显示 `采集成功 | doc_id: xxx`，文档进入解析队列

**工作原理:**

- `popup.js`: UI 逻辑，响应用户操作
- `background.js`: Service Worker，代理 API 请求 + KB 列表缓存
- `extractPageContent()`: 通过 `chrome.scripting.executeScript` 注入页面提取内容
  - 移除 script/style/nav/footer/广告/cookie弹窗
  - 优先提取 `<article>` → `<main>` → `.post-content` → `.article-content` 等语义标签
  - 返回原始 HTML (供 LLM 模式) + 纯文本 (供 Naive 模式)

#### 方式二: Bookmarklet 书签工具

**安装:**

1. B端 → 知识库详情 → 点击 **"动态采集"** 按钮
2. 在弹出的 Modal 中找到 **"📎 采集到 KB"** 链接
3. 将该链接 **拖拽到浏览器书签栏**
4. **重要:** 右键书签 → 编辑 → 将代码中的 `YOUR_API_TOKEN` 替换为你的 API Key

**使用:**

- 访问任意网页 → 点击书签栏的 "采集到 KB"
- 自动提取页面内容，上传到对应知识库
- 成功/失败会弹出 alert 提示

**限制:**

- 内容截取前 8000 字符
- 仅支持 Naive 模式（纯文本）
- KB ID 硬编码在书签中，切换知识库需重新拖拽

#### 方式三: cURL / API 调用

```bash
# 获取知识库列表
curl -H 'Authorization: YOUR_API_TOKEN' \
  'http://47.98.102.55:9380/api/v1/kb_list'

# 采集网页内容
curl -X POST 'http://47.98.102.55:9380/api/v1/kb/{kb_id}/clip' \
  -H 'Authorization: YOUR_API_TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{
    "title": "页面标题",
    "url": "https://example.com/page",
    "html": "<html>原始HTML</html>",
    "content": "预处理的文本(可选,与html二选一)",
    "parse_mode": "naive"
  }'
```

### 13.4 API 接口参考

| 方法 | 路径 | 说明 |
|---|---|---|
| `GET` | `/api/v1/kb_list` | 获取当前用户的知识库列表 |
| `POST` | `/api/v1/kb/{kb_id}/clip` | 上传采集内容到指定知识库 |

**`POST /api/v1/kb/{kb_id}/clip` 请求参数:**

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `title` | string | 是 | 页面标题 |
| `url` | string | 否 | 来源 URL（上传后会附加到内容顶部） |
| `html` | string | 否* | 原始 HTML |
| `content` | string | 否* | 预处理文本/Markdown |
| `parse_mode` | string | 否 | `"naive"` (默认) / `"llm"` |

> *`html` 和 `content` 至少提供一个。`parse_mode=llm` 时优先使用 `html`（原始 HTML 给 LLM 更多上下文）。

**返回:**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "doc_id": "abc123def456",
    "status": "parsing"
  }
}
```

**鉴权:**

- 支持 JWT Token 和 API Token（`Authorization` header 以 `Bearer ` 开头则为 JWT，否则为 API Token）
- API Token 在 B端 → 个人设置 → API Token 获取

**安全机制:**

- 需登录认证（`@login_required`）
- KB 归属校验：只能上传到自己的知识库，跨用户返回 403
- 文件名安全化处理：去除路径分隔符、限制 80 字符、加时间戳防重名

### 13.5 三种 Parse Mode 对比

| 模式 | 输入 | 处理方式 | 输出质量 | 适用场景 |
|---|---|---|---|---|
| `naive` | HTML | 正则剥离标签 → 纯文本 | 一般 | 纯文字文章 |
| `llm` | HTML/文本 | 调用租户默认聊天模型 → Markdown | 高 | 复杂页面、表格、招标公告 |
| (直接传 content) | Markdown/文本 | 不做处理，直接存储 | 取决于输入 | 已预处理的内容 |

**LLM 模式工作原理:**

1. 加载 `rag/prompts/web_clip_parser.md` 作为 system prompt
2. 传入页面标题、URL、原始 HTML（截取前 6144 字符）
3. 调用租户配置的默认聊天模型
4. LLM 提取正文、保留表格/列表/代码块、输出结构化 Markdown
5. 若 LLM 调用失败，自动降级为原始文本

### 13.6 涉及文件索引

```
# === 本次新增文件 ===

# 后端 — API
api/apps/restful_apis/kb_clip_app.py           # 采集 API (GET kb_list + POST clip)

# LLM 解析 Prompt
rag/prompts/web_clip_parser.md                  # LLM 结构化提取的 system prompt

# 前端 — B端 Modal
web/src/pages/dataset/dataset/clip-dialog/      # "动态采集"弹窗组件
  index.tsx                                      #   - KB信息 + Bookmarklet + 扩展说明 + cURL示例

# 浏览器插件
browser-extension/
  manifest.json                                  # Chrome Extension Manifest V3
  background.js                                  # Service Worker (API代理 + 缓存)
  popup.html / popup.js / popup.css              # Popup UI (深色主题)
  options.html / options.js                      # 设置页面 (服务器地址 + API Key)

# 前端 — i18n
web/src/locales/zh.ts                           # 新增 knowledgeDetails.clip.* (11个键)
web/src/locales/en.ts                           # 新增 knowledgeDetails.clip.* (11个键)

# === 本次修改文件 ===

web/src/pages/dataset/dataset/index.tsx          # KB详情页操作栏: 加"动态采集"按钮
```

### 13.7 部署清单

```bash
# 1. 后端 — 上传 Python 文件
scp -i D:\AI\konus-key.pem api/apps/restful_apis/kb_clip_app.py \
  root@47.98.102.55:/data/ragflow/api/apps/restful_apis/

scp -i D:\AI\konus-key.pem rag/prompts/web_clip_parser.md \
  root@47.98.102.55:/data/ragflow/rag/prompts/

# 2. 前端 — 构建 + 上传
# docker exec ragflow-server npm run build (在服务器容器内)
# 然后上传 dist.tar.gz 解压到 nginx 静态目录

# 3. 重启服务 (需要时)
docker restart ragflow-server
# nginx reload (静态文件)
docker exec ragflow-nginx nginx -s reload
```

### 13.8 调试与验证

```bash
# 在服务器容器内验证 API
docker exec ragflow-server curl -s \
  -H "Authorization: YOUR_API_TOKEN" \
  "http://localhost:9380/api/v1/kb_list" | python3 -m json.tool

# 测试采集
docker exec ragflow-server curl -s -X POST \
  "http://localhost:9380/api/v1/kb/{kb_id}/clip" \
  -H "Authorization: YOUR_API_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"title":"测试页面","content":"这是测试内容"}' | python3 -m json.tool
```

### 13.9 边界情况与限制

| 场景 | 行为 |
|---|---|
| 内容为空 | 返回错误 `content or html is required` |
| 标题为空 | 返回错误 `title is required` |
| KB 不存在 | 返回错误 `KB {id} not found` |
| 跨用户上传 | 返回 403 `Access denied` |
| LLM 调用失败 | 降级为原始文本，不阻塞上传 |
| 插件访问受限页面 (chrome://等) | 提示 `Cannot access this page` |
| 页面无 article/main 标签 | 降级使用 `document.body` |
| Bookmarklet 未替换 Token | 返回 401 认证失败 |
| 重复标题 | 文件名加时间戳 `{title}_{YYYYMMDD_HHMMSS}.md` |
| 超大内容 | 插件截取前 100000 字符; LLM 模式截取前 6144 字符 |

---

## 14. Claude Code Skills 体系

> 7 个 konus-* 技能覆盖本项目全开发周期：项目理解 → 后端开发 → 前端开发 → 爬虫开发 → 公众号采集 → 代码审查 → 部署。
> 技能目录: `C:\Users\lg186\.claude\skills\`

### 14.1 Skill 总览

| Skill | 触发词 | 职责 |
|---|---|---|
| `konus-project-context` | ragflow 项目/架构/目录 | 项目整体理解、技术栈、目录结构 |
| `konus-backend-dev` | 后端/API/Peewee/ORM | API端点、ORM模型、Service层开发规范 |
| `konus-frontend-dev` | 前端/页面/组件/React | 页面、组件、路由前端开发规范 |
| `konus-crawler-dev` | 爬虫/站点/YAML/适配器 | 爬虫站点配置、适配器选择、字段映射 |
| `konus-wechat-mp` | 微信公众号/公众号采集/MP爬取 | ★ 一键触发微信公众号三阶段离线批处理管道 |
| `konus-code-review` | 代码审查/review/检查 | 逻辑检查、安全检查、代码质量审查 |
| `konus-deploy-guide` | 部署/上传/重启 | 热更新 vs 重启、SCP上传、前端构建部署 |

### 14.2 konus-project-context

**路径**: `C:\Users\lg186\.claude\skills\konus-project-context\SKILL.md`

新对话快速建立项目上下文：技术栈 (Quart + Peewee + MySQL + ES + React)、目录结构 (7大模块)、核心自定义功能 (标讯/协作/投标书/爬虫/MCP)、关键文件快速索引。

### 14.3 konus-backend-dev

**路径**: `C:\Users\lg186\.claude\skills\konus-backend-dev\SKILL.md`

后端开发代码模板与规范：
- REST API 端点模板 (Blueprint + `@login_required` + 统一响应格式)
- Peewee ORM 模型模板 (BigIntegerField主键 + `to_dict()`)
- Service 层模板 (`CommonService` 子类 + `@DB.connection_context()`)
- Agent 工具模板 (`ToolBase` 子类 + `_invoke()`)
- 禁止事项: 不修改 ragflow_server.py、不直接写 SQL、不引入新 pip 依赖

### 14.4 konus-frontend-dev

**路径**: `C:\Users\lg186\.claude\skills\konus-frontend-dev\SKILL.md`

前端开发规范：React 18 + TypeScript 5 + Tailwind CSS + shadcn/ui 风格组件，TanStack Query 数据获取，Zustand 状态管理，react-router v7 路由。

### 14.5 konus-crawler-dev

**路径**: `C:\Users\lg186\.claude\skills\konus-crawler-dev\SKILL.md`

爬虫开发规范，覆盖 v2.1 统一爬虫框架三层架构 (CRAWL → DEDUP → STORAGE)：
- YAML 站点配置模板 (transport/extract/pagination/anti_crawler)
- 四种 HTTP 适配器选择指南 (rest_api / encrypted_api / spa_render / playwright_http)
- 三种提取器用法 (json_path / css_selector / ai_extractor)
- 调试流程: 先 `validate_all_crawlers.py` → 单站点试运行 → 看错误日志定位

### 14.6 konus-wechat-mp (★ 重点)

**路径**: `C:\Users\lg186\.claude\skills\konus-wechat-mp\SKILL.md`

**一键触发微信公众号完整采集管道**。新对话中直接说 "运行微信公众号任务" 激活：

**自动执行流程:**

```
Step 1: 查询 scheduled_task 表 → 列出启用的微信任务
Step 2: 用户确认任务 + 参数 (全量/增量)
Step 3: Phase 1 — 服务器元数据采集 (docker exec wechat_mp_crawler.py --output-json)
Step 4: Phase 2 — Windows 本机内容抓取 (Playwright Firefox + 反检测)
Step 5: Phase 3 — 服务器 KB 入库+解析 (docker exec wechat_mp_crawler.py --input-json)
Step 6: 验证 KB 文档解析状态
Step 7: 恢复状态文件 (测试跑完后)
```

**技术要点 (skill 内置知识):**
- 反检测 7 项技术 (Playwright Firefox / 移动端UA / add_init_script / canvas噪声 / viewport / 住宅IP / 随机延迟)
- 状态管理: pending_aids → processed_aids 两阶段防数据丢失
- token.py 模块冲突自动修复
- 纯图片文章 (~35%) 标记 EMPTY_TEXT 自动跳过
- 频率限制 200013 自动停止

### 14.7 konus-code-review

**路径**: `C:\Users\lg186\.claude\skills\konus-code-review\SKILL.md`

代码审查检查清单：逻辑连通性、边界条件、异常处理、资源泄漏、SQL 注入/XSS 安全、N+1 查询、空值处理。

### 14.8 konus-deploy-guide

**路径**: `C:\Users\lg186\.claude\skills\konus-deploy-guide\SKILL.md`

部署操作规范：
- **热更新 (无需重启)**: 独立脚本 (.py 入口)、crawler_sites.yaml、前端 dist、配置文件
- **需重启容器**: API 端点代码、Service 层、Agent 组件、db_models.py 表结构变更
- **禁止操作**: `mv dist dist.old` (bind mount inode 断开)、`--force-recreate` (重下载依赖)
- 前端完整部署 5 步流程、后端文件 SCP 上传、验证命令

### 14.9 Skill 交互关系

```
新对话启动
  │
  ├─ "帮我理解项目" → konus-project-context (项目上下文)
  │
  ├─ "开发新功能/修Bug"
  │   ├─ 后端 → konus-backend-dev → konus-code-review → konus-deploy-guide
  │   └─ 前端 → konus-frontend-dev → konus-code-review → konus-deploy-guide
  │
  ├─ "添加/修复爬虫" → konus-crawler-dev → konus-code-review → konus-deploy-guide
  │
  └─ "运行微信公众号" → konus-wechat-mp (独立全流程，内置验证)
```

---

## 15. Flutter 移动端 (标书分析助手 App)

> **独立 Dart/Flutter 代码库**，零 Web 代码复用，复用现有 RAGFlow 后端 API（零后端改动）。
> 路径: `D:\AI\ragflow2\bidding_app\` | 34 Dart 文件 | 9 git commits

### 15.1 技术栈

| 层 | 技术 |
|---|---|
| 框架 | Flutter 3.44 + Dart 3.12 |
| 状态管理 | Riverpod (StateNotifierProvider 模式) |
| HTTP | Dio (JWT 拦截器 + SSE 流) |
| 路由 | go_router (ShellRoute 底部3Tab) |
| 安全存储 | flutter_secure_storage (Token 持久化) |
| Markdown | flutter_markdown (AI 消息渲染) |
| 图标 | lucide_icons (与 Web 端统一) |
| 文件选择 | file_picker |
| 动画 | flutter_animate |
| 字体 | Google Fonts (Plus Jakarta Sans) |
| 主题 | Material 3 (Light/Dark) |

### 15.2 架构

```
ProviderScope
└── BiddingApp (MaterialApp.router)
    ├── LoginScreen (/login)
    └── MainScaffold (ShellRoute 底部导航)
        ├── ChatScreen    (/chat)  — Tab 1: SSE对话
        ├── ToolsListScreen (/tools) — Tab 2: 费用计算
        └── BidScreen     (/bid)   — Tab 3: 标讯搜索
```

**状态管理链路**: `StateNotifier → StateNotifierProvider → ConsumerWidget/ConsumerStatefulWidget`

**数据流**:
```
用户输入 → ChatNotifier.sendMessage()
  → SseClient.connect() (async* yield SSE事件)
  → ChatState.messages 更新 (含 isStreaming 标记)
  → MessageBubble 渲染 (Markdown + 打字机效果)
```

### 15.3 核心功能模块

#### 15.3.1 对话模块 (Chat)

| 文件 | 职责 |
|---|---|
| `chat_provider.dart` | ChatNotifier — sendMessage() / cancelStream() / clearMessages() |
| `chat_screen.dart` | 对话 UI — ListView + QuickActionsBar + ChatInputBar |
| `message_bubble.dart` | 消息气泡 — AI/用户区分 + MarkdownBody 渲染 |
| `chat_input_bar.dart` | 输入栏 — 文件上传 + 发送/取消按钮 |
| `quick_actions.dart` | 快捷操作 — 横向滚动卡片 (招标解析/竞争分析/资质审查/风险审查) |

**SSE 流处理** (`sse_client.dart`):
```
HTTP POST → ResponseType.stream → utf8.decoder → LineSplitter
  → "data: ..." → buffer 累积
  → "event: ..." → eventType
  → 空行 → yield SseEvent(data, event)
  → "[DONE]" → 结束
```

#### 15.3.2 工具模块 (Tools)

| 工具 | 说明 |
|---|---|
| 代理费用计算 | 差额定率分档累进法 (6档: 1.5%→0.1%) |
| 造价咨询服务费 | 建设工程14类项目 |
| 工程勘察设计费 | 2002收费标准10号 |
| 建设工程监理费 | 发改价格[2007]670号 |

#### 15.3.3 招标模块 (Bid)

- 关键词搜索 (SearchBar + 分类)
- 分页列表 (BidCard + 下拉刷新)
- ShimmerLoading 骨架屏

### 15.4 认证系统

与 Web 端完全兼容的认证流程：
```
LoginScreen → encryptPassword(base64Encode)
  → POST /api/v1/auth/login {email, password}
  → Bearer Token → SecureStorage.saveToken()
  → AuthNotifier 状态: initial → loading → authenticated
  → GoRouter redirect → /chat
```

**自动登录**: `App.initState()` → `AuthNotifier.tryAutoLogin()` → 读取 SecureStorage token/userInfo → 跳过登录页

### 15.5 设计系统

**颜色 Token** (`app_colors.dart`):
| Token | 值 | 用途 |
|---|---|---|
| primary | #0F172A | 主色 (深蓝黑) |
| accent | #0369A1 | 强调色 (蓝) |
| background | #F8FAFC | 背景 |
| surface | #FFFFFF | 卡片/组件表面 |
| border | #E2E8F0 | 边框 |
| muted | #64748B | 次要文字 |
| success | #10B981 | 成功 |
| destructive | #DC2626 | 错误/删除 |

**间距系统** (8dp 网格): `xs=4, sm=8, md=12, lg=16, xl=24, xxl=32, xxxl=48`

**组件圆角**: cardRadius=16, buttonRadius=12, inputRadius=12, sheetTopRadius=24

**触摸目标**: buttonHeight=52, inputMinHeight=48, touchTargetMin=44

**暗色模式**: darkBackground=#0F172A, darkSurface=#1E293B, darkBorder=rgba(255,255,255,0.08)

### 15.6 CI/CD 构建

**Dockerfile**: 使用 `cirrusci/flutter:3.44.2` 镜像，三步构建:
1. `flutter pub get` — 依赖安装
2. `flutter analyze` — 静态分析 (0 issues)
3. `flutter build apk --debug` — 调试 APK 输出

**APK 输出**: `build/app/outputs/flutter-apk/app-debug.apk`

### 15.7 与 Web 端的对接

**Web 端下载入口** (`web/src/components/app-download-dialog.tsx`):
- QR 码弹窗组件，使用 `qrcode.react` 生成 Android/iOS 下载二维码
- 集成位置：
  - `c-landing` 着陆页 — 桌面导航栏 + 移动端菜单 "下载App" 按钮
  - `c-chat` 对话页 — 顶部 Header 右侧 (用户头像与退出登录之间)
- 占位 URL: `https://www.ragflow.com/app/android` / `https://apps.apple.com/cn/app/id0000000000`
- 待 App 发布后替换为实际下载链接

### 15.8 本地开发环境 (Windows + Android 模拟器)

> 配置日期: 2026-06-18

#### 环境变量 (`~/.bashrc`)

```bash
# Android SDK & Flutter
export ANDROID_HOME="F:/code"
export JAVA_HOME="F:/androidstudio/jbr"
# 移除旧 Java 8 (Oracle)，使用 Android Studio 自带的 JDK 21
PATH=$(echo "$PATH" | sed 's#/c/Program Files (x86)/Common Files/Oracle/Java/javapath:##')
export PATH="$JAVA_HOME/bin:$ANDROID_HOME/platform-tools:$ANDROID_HOME/cmdline-tools/latest/bin:/c/Users/lg186/flutter/bin:$PATH"
```

| 组件 | 版本 | 路径 |
|---|---|---|
| Flutter | 3.44.2 | `C:/Users/lg186/flutter` |
| Java | JDK 21 (Android Studio 自带) | `F:/androidstudio/jbr` |
| Android SDK | API 34/35/36 | `F:/code` |
| cmdline-tools | latest | `F:/code/cmdline-tools/latest/bin/` |
| 模拟器 | `flutter_emulator` (Pixel 6, API 36, x86_64) | — |

#### 快速启动

```bash
# 1. 启动模拟器（或从 Android Studio AVD Manager 启动）
emulator -avd flutter_emulator -no-snapshot-load -no-boot-anim &

# 2. 安装依赖（首次）
cd D:/AI/ragflow2/bidding_app
flutter pub get

# 3. 运行
flutter run -d emulator-5554
```

#### API 连接

| 场景 | `api_client.dart` 中的 baseUrl |
|---|---|
| 本机无后端 | `http://47.98.102.55:9380` (远程服务器) |
| 本机有 Docker | `http://10.0.2.2:9380` (模拟器→宿主机) |
| iOS 模拟器 | `http://localhost:9380` |

#### 已知问题与修复

| 问题 | 原因 | 修复 |
|---|---|---|
| Java 版本太旧 (JDK 8) | 系统 PATH 有 Oracle Java 8 | 用 sed 移除，改用 AS 自带的 JDK 21 |
| `cmdline-tools` 嵌套过深 | SDK 安装在 `cmdline-tools/latest/cmdline-tools/` 而非标准位置 | `mv` 到 `cmdline-tools/latest/` |
| `flutter doctor --android-licenses` 失败 | sdkmanager 找不到 | 修复 cmdline-tools 目录结构后重跑 |
| 登录报 Connection refused | 后端未在本机运行 | 改 baseUrl 为远程服务器 `47.98.102.55:9380` |

---

## 16. C端视觉升级 (2026-06-11~13)

> 对 C 端进行全面视觉升级：内联 SVG 替换为 lucide-react、动态图标系统、Premium 设计风格。

### 16.1 图标系统升级

**内联 SVG → lucide-react**: 49 个手写 `<svg>` 替换为等价的 lucide 图标组件，涉及 8 个文件：

| 文件 | 替换数 | 涉及图标 |
|---|---|---|
| `c-chat/index.tsx` | ~31 | Loader2, Check, FileText, Copy, Paperclip, Send, Square, ArrowRight, ChevronDown, ChevronRight, MessageSquare, Star, Menu, X, Plus, LogOut, Trash2, Download, RefreshCw, Smartphone |
| `c-login/index.tsx` | 2 | Eye, EyeOff |
| `c-landing/index.tsx` | 4 | Menu, X, Smartphone |
| `bid/contract-list.tsx` | 1 | FileText |
| `bid/construction-list.tsx` | 1 | HardHat |
| `bid/enterprise-search.tsx` | 1 | Building2 |
| `bid/credit-china-search.tsx` | 1 | ShieldCheck |
| `favorite-panel/index.tsx` | 1 | Star |

**DynamicIcon 组件** (`web/src/components/dynamic-icon.tsx`):
- 32 个 lucide 图标的名称→组件映射表
- 用于数据驱动的图标渲染 (如 bid/tools/c-landing 模块图标)
- 统一 strokeWidth=1.5，支持 color prop

### 16.2 Premium 设计注入

对 C 端 页面的设计升级（Enterprise SaaS Mobile 风格 + B2B Service 色彩）：

| 页面 | 升级内容 |
|---|---|
| `c-landing` | Hero渐变背景、Feature/Solution/Advantage卡片、CTA按钮动效 |
| `c-chat` | 消息气泡圆角优化、输入栏视觉层次、侧边栏折叠交互 |
| `c-login` | 表单卡片投影、Tab切换动效、错误状态容器 |
| `bid-home` | 搜索栏焦点态、卡片hover投影、骨架屏动画 |
| `favorite-panel` | 收藏卡片选中态、批量操作视觉反馈 |

### 16.3 视觉 Token 注入

通过 `useEffect` 注入全局 CSS 变量：
- `--primary: #0F172A` / `--accent: #0369A1` / `--bg: #F8FAFC`
- `--radius-card: 16px` / `--radius-button: 12px` / `--radius-input: 12px`
- 字体: Plus Jakarta Sans (标题) + system-ui (正文)
- 卡片样式: `border: 1px solid #E2E8F0; border-radius: 16px; box-shadow: none`