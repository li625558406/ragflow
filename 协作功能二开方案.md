# 协作功能二开方案

> 基于现有 `collaboration` 模块的现状分析，规划 3 大类 25 个功能的适配方案。

---

## 现状概要

| 组件 | 位置 | 说明 |
|---|---|---|
| 前端 | `web/src/components/collaboration/` (6文件) | CollaborationPanel → DocumentList + DocumentEditor(ToolbarPlugin) + FormatRulePanel + CreateDocumentDialog |
| 后端 API | `api/apps/restful_apis/collaboration_api.py` (11端点) | 文档CRUD + 格式规则CRUD + 下载 + 应用规则 |
| 后端 Service | `api/apps/services/collaboration_api_service.py` | Markdown↔Lexical JSON转换 + docx/PDF生成 |
| 数据库 | `api/db/db_models.py` (2表) | `collaboration_document` + `collaboration_format_rule` |
| Service层 | `api/db/services/collaboration_service.py` | Peewee ORM 封装 |
| 路由入口 | C端 `web/src/pages/c-chat/index.tsx` | Tab `collaboration` → `<CollaborationPanel>` |

**现有能力**：Lexical 富文本编辑器、Markdown导入、docx/PDF导出、格式规则(正则匹配段落样式)、团队共享(me/team)、自动保存(2s防抖)

---

## 参考开源项目

> 每个功能点均有对标的开源项目实现，参考其架构模式和关键代码片段。

| 开源项目 | 技术栈 | 核心参考价值 | 映射功能 |
|---|---|---|---|
| **La Suite Docs** ([suitenumerique/docs](https://github.com/suitenumerique/docs)) — 法国政府开源的 Notion 替代 (14k+ stars) | Django + React, BlockNote.js (ProseMirror) + Yjs + HocusPocus | 自定义 Block 系统 (`createReactBlockSpec`)、CRDT 实时协作、权限模型、docx/odt 导入导出、离线编辑 | 2.2 高级插入, 2.3 内容块, 2.4 导入导出, 3.1 权限 |
| **Docmost** ([docmost/docmost](https://github.com/docmost/docmost)) — Confluence/Notion 替代 | NestJS + React, TipTap (ProseMirror) + CASL + PostgreSQL | CASL 能力工厂权限系统 (WorkspaceAbilityFactory + SpaceAbilityFactory)、评论系统、附件管理、页面历史/版本 | 3.1 精细化 ACL, 2.6 附件管理, 1.7 版本历史 |
| **ONLYOFFICE DocumentServer** ([ONLYOFFICE/DocumentServer](https://github.com/ONLYOFFICE/DocumentServer)) — 在线 Office 套件 | C++ (x2t 引擎) + Node.js | OOXML 格式转换引擎、.docx/.odt/.pdf 双向转换 pipeline、`COfficeFileFormatChecker` 格式检测 | 2.4 docx 导入/导出 |
| **DzzOffice** ([zyx0814/dzzoffice](https://github.com/zyx0814/dzzoffice)) — 协同办公平台 | PHP + 多办公应用集成 | 模块化办公套件架构，Google Workspace/Office365 替代品 | 架构参考 |

### La Suite Docs 架构要点

```
┌──────────────────────────────────────────────────────┐
│  Frontend (Next.js / React)                          │
│  ┌──────────────────────┐  ┌──────────────────────┐  │
│  │  BlockNote.js Editor │  │  Service Worker      │  │
│  │  (ProseMirror + Yjs) │  │  (Offline Support)   │  │
│  └──────────────────────┘  └──────────────────────┘  │
└──────────┬───────────────────────┬───────────────────┘
           │ WebSocket (Yjs sync)  │ REST API
           ▼                       ▼
┌──────────────────┐    ┌──────────────────┐
│  HocusPocus      │    │  Django (DRF)    │
│  (Collab Server) │    │  + PostgreSQL    │
└──────────────────┘    │  + S3 Storage    │
                        └──────────────────┘
```

**核心设计模式**:
- **CRDT 无冲突合并**: 每个客户端维护独立的 Yjs CRDT 文档，无需中心化冲突仲裁（Google Docs 使用的 OT 方案需要复杂服务端）
- **Block 系统**: 文档由 Block 数组组成，每个 Block 支持 `/` 斜杠命令插入，内联内容(contentRef)支持富文本
- **离线编辑**: Service Worker 缓存，恢复网络后 Yjs 自动合并

### Docmost CASL 权限模型

```
WorkspaceAbilityFactory(user, workspace)
  → 生成 CASL Ability (can/cannot 规则集)
    → 规则: (Action, Subject) 元组
      Action: Read | Create | Edit | Manage | Delete
      Subject: Member | Settings | Space | Page | Comment | Attachment

SpaceAbilityFactory(user, space)
  → 空间级权限 (ADMIN | WRITER | READER)
    → 用户直接指派 或 通过 Group 继承
```

**关键模式**:
- `WorkspaceAbilityFactory.createForUser(user, workspace)` — 根据用户+上下文生成完整权限集
- `@CheckPolicies((ability) => ability.can(Actions.Manage, Subject.Settings))` — 控制器级权限守卫
- `useUserRole()` Hook — 前端按角色条件渲染 UI

### ONLYOFFICE 格式转换 Pipeline

```
源格式 (.doc/.odt/.rtf)
  → COfficeFileFormatChecker (检测真实文件类型)
    → x2t 引擎 (C++ 格式库)
      → 中间格式 (DOCT/XLST/PPTT 二进制)
        → 目标格式 (.docx)
          → 渲染引擎 / 下载
```

**格式库矩阵**:
| 类别 | 库 | 格式 |
|---|---|---|
| OOXML (原生编辑格式) | DocxFormatLib, XlsbFormatLib, PPTXFormatLib | .docx, .xlsx, .pptx |
| 旧版二进制 | DocFormatLib, XlsFormatLib, PptFormatLib | .doc, .xls, .ppt |
| 跨平台 | PdfFile, DjVuFile, HtmlFile2 | .pdf, .djvu, .html |

---

## 一、核心协作功能

### 1.1 实时协同编辑 — 多人同时编辑

**差距**：当前为单用户编辑，permission 仅控制列表可见性。

**适配方案**：

| 层次 | 方案 |
|---|---|
| 同步算法 | **Yjs (CRDT)** — 已有 `@lexical/yjs` 官方 binding，无需中心化冲突仲裁，天然支持离线编辑后合并 |
| 传输层 | **WebSocket (socket.io)** — 在 `docker/docker-compose-base.yml` 中增加 socket.io server，或复用 Quart WebSocket 扩展 |
| 数据流 | 编辑操作 → Yjs Y.Doc → WebSocket 广播 → 其他客户端 Yjs 合并 → Lexical 更新 |
| 房间模型 | 每个 `doc_id` 为一个 room，`socket.join(doc_id)` / `socket.leave(doc_id)` |
| 持久化 | Yjs update 事件流 + 定期 snapshot 写入 MySQL，替代当前全量 PUT 模式 |

**涉及改动**：
- 新增 `rag/svr/collab_socket_server.py` — WebSocket 服务，管理房间和 Yjs 同步
- `document-editor.tsx` — 替换 `AutoSavePlugin` 为 `@lexical/yjs` 的 `CollaborationPlugin`
- 新增 npm 包：`yjs`, `@lexical/yjs`, `socket.io-client`
- `docker/docker-compose-base.yml` — 可选暴露新端口

---

### 1.2 实时状态展示 — 光标/选区同步

**差距**：无。

**适配方案**：
- 使用 Yjs 内置的 `awareness` 协议，每个客户端广播光标位置 + 用户名 + 颜色
- `@lexical/yjs` 的 `CollaborationPlugin` 自带光标渲染，自动用不同颜色区分用户
- 颜色分配：服务端在用户加入 room 时分配固定颜色（从预定义调色板取）

**涉及改动**：
- `document-editor.tsx` — 配置 awareness 的 cursor 渲染
- `collab_socket_server.py` — 管理 awareness state

---

### 1.3 在线成员列表

**差距**：无。

**适配方案**：
- 服务端维护每个 room 成员列表：`{doc_id: [{user_id, user_name, avatar_color, online_at}]}`
- 用户加入/离开时 WebSocket 广播成员变更事件
- 前端在 Editor 顶部渲染头像组（参考 Google Docs 右上角）
- 心跳检测：10s 间隔，30s 超时清理离线成员

**涉及改动**：
- 新增 `collaboration/member-avatars.tsx`
- `collab_socket_server.py` — 成员管理 + 心跳检测
- `document-editor.tsx` — 顶部工具栏旁显示头像

---

### 1.4 评论与批注

**差距**：无。

**适配方案**：

| 层次 | 方案 |
|---|---|
| 数据库 | 新表 `collaboration_comment`: `id, doc_id, user_id, anchor_key(段落ID), anchor_offset, comment_text, parent_id(回复), resolved(bool), create_time` |
| 存储 | 评论与文档内容分开存储，通过 `doc_id` + `anchor_key` 关联到 Lexical 段落 |
| UI | 右侧评论面板，点击段落可添加评论，支持嵌套回复 |
| API | `GET/POST /collaboration/documents/<id>/comments`, `PUT/DELETE /comments/<id>`, `POST /comments/<id>/reply` |
| Anchor | 每个段落节点附加自定义 key，段落被删除时评论标记为 `orphaned` |

**涉及改动**：
- 新增 `collaboration/comment-panel.tsx` + `collaboration/comment-thread.tsx`
- `db_models.py` — 新增 `CollaborationComment` 表
- `collaboration_api.py` — 新增评论 CRUD 端点
- `document-editor.tsx` — 集成评论标记

---

### 1.5 @提及

**差距**：无。

**适配方案**：
- Lexical 端：扩展 TextNode，识别 `@用户名` 模式，渲染为特殊样式(inline tag)
- 输入时弹出用户搜索下拉（从 `/api/v1/tenant/users` 获取同租户用户列表）
- 被 @ 的用户收到通知 → 新表 `collaboration_notification`: `id, user_id, type(mention/comment/share), doc_id, from_user_id, read(bool), create_time`

**涉及改动**：
- 新增 `collaboration/mention-plugin.tsx` — Lexical 插件
- `db_models.py` — 新增通知表
- `collaboration_api.py` — 通知查询/标记已读端点
- 前端顶部栏 Bell 图标 + 未读计数

---

### 1.6 建议编辑（修订模式）

**差距**：无。

**适配方案**：
- 利用 Lexical node 属性存储 `suggestion` 状态
- Yjs 层面：建议编辑标记为 `suggested` 类型，不直接修改原文
- 新表 `collaboration_suggestion`: `id, doc_id, anchor_key, original_text, suggested_text, user_id, status(pending/accepted/rejected), create_time`
- UI：建议以不同背景色展示（绿色=新增，红色=删除），所有者可见 Accept/Reject 按钮

**涉及改动**：
- 新增 `collaboration/suggestion-plugin.tsx` + `collaboration/suggestion-panel.tsx`
- `db_models.py` — 新增 `CollaborationSuggestion` 表
- `collaboration_api.py` — suggestions CRUD + accept/reject 端点

---

### 1.7 自动保存与历史记录

**差距**：AutoSave 全量覆盖，无版本历史。

**适配方案**：
- **操作日志 + 定期快照**策略：
  - 每次 PUT 保存时，将新旧 diff 写入 `collaboration_operation_log` 表
  - 每 50 次操作或每 5 分钟生成一次 snapshot，存入 `collaboration_document_snapshot` 表
  - 快照存储完整 Lexical JSON
- 版本列表 API: `GET /collaboration/documents/<id>/versions`
- Yjs 引入后：Yjs update 事件本身就是操作日志

**涉及改动**：
- `db_models.py` — 新增 `CollaborationDocumentSnapshot` + `CollaborationOperationLog` 表
- `collaboration_api_service.py` — snapshot 逻辑 + 版本列表查询
- `collaboration_api.py` — 新增版本列表端点
- 前端 `collaboration/version-history-panel.tsx`

---

### 1.8 版本回滚与对比

**差距**：无。

**适配方案**：
- 回滚：选中目标 snapshot，将其 Lexical JSON 写入 doc.content，清空之后的操作日志
- 对比：使用 `diff-match-patch` (JS) 或 Python `difflib` 做文本级 diff
- UI：并排对比视图 (side-by-side)，红色删除/绿色新增

**涉及改动**：
- `collaboration_api.py` — 新增 `POST /documents/<id>/rollback` + `GET /documents/<id>/diff?v1=..&v2=..`
- 前端 `collaboration/diff-view.tsx`

---

## 二、内容与编辑功能

### 2.1 基础样式

**现状**：已实现。`ToolbarPlugin` 覆盖了加粗/斜体/下划线/删除线/字体(7种中文)/字号(7档)/颜色/对齐(4种)/行距(6档)/上下标/代码/清除格式。

**差距**：
- 字体颜色预设色板（当前仅有取色器）
- 格式刷（复制格式应用到其他段落）

---

### 2.2 高级插入

**差距**：仅支持表格插入，缺少图片、链接、代码块、数学公式、表情。

**参考项目**：La Suite Docs (BlockNote.js 自定义 Block 模式)

> La Suite Docs 使用 `createReactBlockSpec` API 创建自定义块（Callout/Image/File 等），通过 `propSchema` 定义块属性、`contentRef` 支持内联编辑、`toExternalHTML`/`parse` 实现导入导出闭环。Lexical 无此高级 API，需要用 `DecoratorNode` 子类手动实现等价功能。

**BlockNote.js → Lexical 概念映射**：

| BlockNote.js 概念 | Lexical 等价概念 |
|---|---|
| `createReactBlockSpec({ propSchema, ... })` | 自定义 `DecoratorNode` 子类 + `__properties` |
| `contentRef` (inline rich text) | `$isElementNode` + children 插槽 |
| `toExternalHTML` / `parse` | `exportJSON()` + `importJSON()` |
| slash menu (`/image`, `/callout`) | `KEY_DOWN_COMMAND` + `/` 触发命令面板 |

**npm 包现状**：
- `@lexical/code` v0.23.1 — **已在 node_modules**（transitive dep），仅需声明到 `package.json`
- `@lexical/link` v0.23.1 — **已在 node_modules**，仅需声明到 `package.json`
- `@lexical/image` — **Lexical 不提供此包**，必须自定义 ImageNode
- 已有 `react-dropzone` v14.3.5 — 复用于图片拖拽上传

**适配方案**：

| 功能 | 实现方式 | 存储 | 参考 |
|---|---|---|---|
| **图片** | 自定义 `ImageNode extends DecoratorNode`，上传到 MinIO | MinIO URL 引用 (key: `images/{uuid}.{ext}`) | La Suite Docs 自定义 Block 模式 |
| **链接** | `@lexical/link` 的 `LinkNode` + `LinkPlugin` + `ClickableLinkPlugin` | Lexical JSON 内属性 | Lexical Playground |
| **代码块** | `@lexical/code` 的 `CodeNode` + `CodeHighlightNode` + `CodeHighlightPlugin` | Lexical JSON 内 text | Lexical Playground |
| **数学公式** | KaTeX 渲染 + 自定义 `MathNode extends DecoratorNode` | LaTeX 字符串 | La Suite Docs 内联数学 block |
| **表情** | 硬编码 ~200 常用 emoji + Popover 选择器，插入 Unicode 文本 | 纯文本 Unicode | La Suite Docs emoji-mart 集成 |

**自定义 ImageNode 核心代码** (参考 La Suite Docs Block 模式 + Lexical Playground)：

```typescript
// nodes/image-node.tsx
class ImageNode extends DecoratorNode<JSX.Element> {
  __src: string; __altText: string; __width: number; __height: number;

  static getType(): string { return 'image'; }
  static clone(node: ImageNode): ImageNode {
    return new ImageNode(node.__src, node.__altText, node.__width, node.__height, node.__key);
  }

  // JSON 序列化 — 确保保存/加载不丢数据
  exportJSON(): SerializedImageNode {
    return { ...super.exportJSON(), src: this.__src, altText: this.__altText,
      width: this.__width, height: this.__height, type: 'image', version: 1 };
  }
  static importJSON(json: SerializedImageNode): ImageNode {
    return new ImageNode(json.src, json.altText, json.width, json.height);
  }

  createDOM(): HTMLElement { const div = document.createElement('div'); div.className = 'image-container'; return div; }
  updateDOM(): false { return false; }

  decorate(): JSX.Element {
    return (
      <div className="group relative my-2 inline-block">
        <img src={this.__src} alt={this.__altText}
          className="max-w-full h-auto rounded-lg" style={{ width: this.__width || undefined }} />
        <input className="absolute bottom-1 left-1 text-xs bg-black/50 text-white px-1 rounded hidden group-hover:block"
          placeholder="添加描述..." value={this.__altText}
          onChange={(e) => { /* update node via editor.update() */ }} />
      </div>
    );
  }
}
```

**后端图片上传 API** (参考现有 `file_api.py` multipart 模式)：

```python
# 新增端点: POST /api/v1/collaboration/images
@manager.route("/collaboration/images", methods=["POST"])
@login_required
async def upload_image():
    files = await request.files
    file_obj = files.get("image")
    if not file_obj:
        return get_error_argument_result("image is required")
    try:
        result = await collaboration_api_service.upload_image(current_user.id, file_obj)
        return get_json_result(data=result)
    except ValueError as e:
        return get_error_argument_result(str(e))

# Service: upload_image()
async def upload_image(tenant_id: str, file_obj) -> dict:
    ext = file_obj.filename.rsplit('.', 1)[-1].lower()
    if ext not in {'png', 'jpg', 'jpeg', 'gif', 'webp', 'svg'}:
        raise ValueError(f"Unsupported image type: {ext}")
    data = file_obj.read()
    if len(data) > 10 * 1024 * 1024:
        raise ValueError("Image too large (max 10MB)")
    key = f"images/{get_uuid()}.{ext}"
    settings.STORAGE_IMPL.put("collaboration", key, data)
    return {"key": key, "url": f"/api/v1/collaboration/images/{key}"}
```

**涉及改动**：
- `web/package.json` — 声明 `@lexical/link`, `@lexical/code` 依赖
- `toolbar-plugin.tsx` — 增加 Link/Image/Code/Math/Emoji 按钮组
- `document-editor.tsx` — 注册 `CodeNode`, `CodeHighlightNode`, `LinkNode`, `ImageNode`, `MathNode`; 添加 `CodeHighlightPlugin`, `LinkPlugin`, `ClickableLinkPlugin`; 扩展 theme
- **新建** `nodes/image-node.tsx` — 自定义 ImageNode (DecoratorNode)
- **新建** `nodes/math-node.tsx` — 自定义 MathNode (KaTeX DecoratorNode)
- **新建** `image-upload-plugin.tsx` — 图片拖拽/粘贴上传 (复用 `react-dropzone`)
- **新建** `emoji-picker.tsx` — Emoji 选择器 Popover (~200 emoji)
- **新建** `link-editor-popover.tsx` — 链接 URL 编辑 Popover
- `collaboration_api.py` — 新增 `POST /collaboration/images` 上传端点
- `collaboration_api_service.py` — 新增 `upload_image()` 方法

---

### 2.3 丰富的内容块 (Block)

**差距**：仅支持 Heading/Paragraph/List/Table，缺少引用/待办/分栏/Callout。

**参考项目**：La Suite Docs (BlockNote.js `createReactBlockSpec` 自定义 Block 系统)

> La Suite Docs 的 Callout 块是自定义 Block 的典型范例：使用 `propSchema` 定义 `type` 属性（info/warning/error/success），通过 `render` 函数返回带图标和颜色背景的 React 组件，通过 slash menu 或工具栏插入。RAGFlow 用 Lexical `DecoratorNode` 实现等价功能。

**La Suite Docs Callout 块核心模式**：

```typescript
// La Suite Docs: createReactBlockSpec 定义 Callout
const createCallout = createReactBlockSpec(
  {
    type: "callout",
    content: "inline",  // ← 映射为 Lexical 的 children 插槽
    propSchema: {
      type: { default: "info", values: ["warning", "error", "info", "success"] },
      textAlignment: { default: "left" },
      textColor: { default: "default" }
    }
  },
  {
    render: ({ block, editor, contentRef }) => (
      <div className={`callout callout-${block.props.type}`}>
        <span className="callout-icon">{ICON_MAP[block.props.type]}</span>
        <div ref={contentRef} className="callout-content" />
      </div>
    ),
    toExternalHTML: ({ block }) => (/* ... */),  // 导出/粘贴支持
    parse: (element) => (/* ... */)              // 粘贴→块映射
  }
);
```

**关键发现**：`QuoteNode` 和 `$createQuoteNode` 已在 `@lexical/rich-text` 中（toolbar 文件已导入 `$createHeadingNode`），`CheckListPlugin` 在 `@lexical/react` 中，`INSERT_CHECK_LIST_COMMAND` 在 `@lexical/list` 中。**Phase 1 仅 Callout 需要自定义实现。**

**适配方案**：

| Block 类型 | Lexical 实现 | 来源 |
|---|---|---|
| **引用 (Quote)** | `@lexical/rich-text` 的 `QuoteNode` — 已有，直接使用 | Lexical 内置 |
| **待办事项** | `@lexical/react` 的 `CheckListPlugin` + `@lexical/list` 的 `INSERT_CHECK_LIST_COMMAND` — 已有，直接使用 | Lexical 内置 |
| **Callout/提示框** | 自定义 `CalloutNode extends DecoratorNode` (4种类型: info/warning/tip/danger) | 参考 La Suite Docs |
| **分栏** | 延后到 Phase 2（需要复杂的 LayoutContainerNode + flexbox 嵌套逻辑） | — |

**自定义 CalloutNode 实现** (参考 La Suite Docs Callout Block)：

```typescript
// nodes/callout-node.tsx
class CalloutNode extends DecoratorNode<JSX.Element> {
  __calloutType: 'info' | 'warning' | 'tip' | 'danger';
  __emoji: string;

  static getType(): string { return 'callout'; }

  exportJSON() {
    return { ...super.exportJSON(),
      calloutType: this.__calloutType, emoji: this.__emoji,
      type: 'callout', version: 1 };
  }

  decorate(): JSX.Element {
    const styleMap = {
      info:    { bg: 'bg-blue-50',   border: 'border-blue-300',  icon: '💡' },
      warning: { bg: 'bg-amber-50',  border: 'border-amber-300', icon: '⚠️' },
      tip:     { bg: 'bg-green-50',  border: 'border-green-300', icon: '✅' },
      danger:  { bg: 'bg-red-50',    border: 'border-red-300',   icon: '🚫' },
    };
    const s = styleMap[this.__calloutType];
    return (
      <div className={`border-l-4 rounded-r-lg p-3 my-2 ${s.bg} ${s.border}`}>
        <span className="mr-2">{this.__emoji || s.icon}</span>
        <div className="inline" contentEditable={true} />
      </div>
    );
  }
}
```

**Toolbar 按钮添加模式** (精确复用现有 toolbar-plugin.tsx 模式)：

```tsx
// 复用现有 btn() helper 和 CendTooltip 模式
const [isQuote, setIsQuote] = useState(false);
// updateFormatState 中读取状态：
editor.getEditorState().read(() => {
  const anchorNode = $getSelection()?.anchor.getNode();
  setIsQuote(!!$getNearestNodeOfType(anchorNode, QuoteNode));
});
// 按钮渲染：
<CendTooltip title="引用">
  <button className={btn(isQuote)}
    onMouseDown={(e) => { e.preventDefault(); /* toggle Quote */ }}>
    <TextQuote className="size-3.5" />
  </button>
</CendTooltip>
```

**涉及改动**：
- `toolbar-plugin.tsx` — 增加 Quote/Todo/Callout 三个按钮（复用现有 `btn()` + `CendTooltip` + `onMouseDown` 模式）
- `document-editor.tsx` — 注册 `QuoteNode`、添加 `<CheckListPlugin />`、注册 `CalloutNode`; 扩展 theme (quote/checklist/callout CSS)
- **新建** `nodes/callout-node.tsx` — 自定义 CalloutNode (DecoratorNode)
- `collaboration_api_service.py` — 扩展 `_markdown_to_lexical_json()` 支持 `> quote`、`- [ ] todo`、`:::callout` 语法
- `document-editor.tsx` AutoSavePlugin — 扩展 markdown 生成器支持新 block 类型

---

### 2.4 导入与导出

**差距**：支持导出 `.docx` 和 `.pdf`、从 Markdown 导入。无 Word 导入、无批量导出。

**参考项目**：ONLYOFFICE (x2t 格式转换引擎) + La Suite Docs (python-docx 导入)

> ONLYOFFICE 的格式转换采用**两阶段 pipeline**：源格式 → 中间二进制格式(DOCT) → 目标格式。RAGFlow 映射为：`.docx → python-docx 解析 → Dict → Lexical JSON → DB 存储`，同样遵循"解析→中间表示→目标格式"的模式。La Suite Docs 使用 Django + python-docx 完成后端导入，流程可直接复用。

**ONLYOFFICE 转换 pipeline 映射**：

```
ONLYOFFICE  C++ 引擎:
  .docx → DocxFormatLib → DOCT(二进制) → 渲染引擎

RAGFlow Python 导入:
  .docx 上传 → python-docx 解析 → Dict(中间结构) → Lexical JSON → 存入 DB
```

**ONLYOFFICE 的 `COfficeFileFormatChecker`** — 根据文件内容(而非扩展名)检测真实格式：

```python
# RAGFlow 导入中的格式检测
import magic  # python-magic 已在 ragflow 依赖中
def detect_file_type(file_data: bytes) -> str:
    mime = magic.from_buffer(file_data, mime=True)
    if mime == 'application/vnd.openxmlformats-officedocument.wordprocessingml.document':
        return 'docx'
    raise ValueError(f"Unsupported file type: {mime}")
```

**python-docx → Lexical JSON 核心映射表** (参考 La Suite Docs 导入逻辑)：

| python-docx 元素 | Lexical JSON 映射 |
|---|---|
| `para.style.name.startswith("Heading 1")` | `{"type": "heading", "tag": "h1"}` |
| `para.style.name.startswith("Heading 2")` | `{"type": "heading", "tag": "h2"}` |
| `"List Bullet" in style_name` | `{"type": "list", "listType": "bullet"}` |
| `"List Number" in style_name` | `{"type": "list", "listType": "number"}` |
| `run.bold` | `format \|= 1` (IS_BOLD) |
| `run.italic` | `format \|= 2` (IS_ITALIC) |
| `run.underline` | `format \|= 8` (IS_UNDERLINE) |
| `run.font.strike` | `format \|= 4` (IS_STRIKETHROUGH) |
| `run.font.name` | `style: "font-family: SimSun"` |
| `run.font.size.pt` | `style: "font-size: 12pt"` |
| `doc.tables` → Table | 复用已有 `_build_lexical_table()` |

**导入 Service 核心代码**：

```python
async def import_docx(tenant_id: str, user_id: str, file_obj, folder_id: str = None) -> dict:
    from docx import Document as DocxDocument
    doc = DocxDocument(file_obj)
    doc_id = get_uuid()
    name = file_obj.filename.rsplit('.', 1)[0]
    lexical_children = []

    for para in doc.paragraphs:
        style_name = para.style.name if para.style else ""
        block = _build_lexical_block_from_docx_para(para, style_name)
        if block:
            lexical_children.append(block)

    for table in doc.tables:
        lexical_children.append(_build_lexical_table_from_docx_table(table))

    content = {"root": {"children": lexical_children, "direction": "ltr",
      "format": "", "indent": 0, "type": "root", "version": 1}}
    markdown = _lexical_to_markdown(lexical_children)

    CollaborationDocumentService.save(
        id=doc_id, name=name, content=content, markdown_content=markdown,
        tenant_id=tenant_id, created_by=user_id,
        permission="me", **({"folder_id": folder_id} if folder_id else {}))

    return {"id": doc_id, "name": name, "file_type": "docx"}
```

**适配方案**：

| 功能 | 方案 |
|---|---|
| **Word 导入** | `python-docx` 读取 .docx → 提取段落/表格/样式 → 转为 Lexical JSON（参考 ONLYOFFICE pipeline 模式） |
| **PDF 导入** | 延后（`pdfplumber` 方案复杂度高，优先做 Word） |
| **Markdown 导出** | 复用 AutoSavePlugin 中的 Markdown 转换逻辑，增加独立导出按钮 |
| **批量导出** | 延后到 Phase 4 |

**新 API 端点**：
```
POST /api/v1/collaboration/documents/import
  Content-Type: multipart/form-data
  参数: file (.docx), folder_id (可选)
  Response: { code: 0, data: { id: "xxx", name: "xxx" } }
```

**涉及改动**：
- `collaboration_api_service.py` — 新增 `import_docx()` 方法 + `_build_lexical_block_from_docx_para()` + `_lexical_to_markdown()` helper
- `collaboration_api.py` — 新增 `POST /documents/import` 端点
- **新建** `collaboration/docx-import-dialog.tsx` — 拖拽/选择 .docx 文件上传对话框 (复用 `react-dropzone`)
- `document-list.tsx` — 头部增加"导入"按钮
- `index.tsx` — 处理导入流程：上传成功 → 刷新列表 → 自动选中新文档

---

### 2.5 文件管理 — 文件夹/移动/批量操作

**差距**：仅支持创建/重命名/删除，无文件夹和移动。

**参考项目**：Docmost (空间/页面层次结构)

> Docmost 使用 `空间(Space) → 页面(Page) → 子页面(Sub-page)` 三级层次结构。`Page` 表通过 `parent_page_id` 可空字段支持无限嵌套。RAGFlow 引入 `CollaborationFolder` 表替代 Docmost 的 Space 概念，`CollaborationDocument.folder_id` 替代 Page.parent_page_id。

**Docmost 数据模型映射**：

```
Docmost:
  Workspace → Space → Page (parent_page_id, 支持无限嵌套)

RAGFlow:
  Tenant → CollaborationFolder (parent_id, 支持无限嵌套)
        → CollaborationDocument (folder_id)
```

**树形 API 响应格式** (参考 Docmost)：

```json
// GET /collaboration/folders/tree
{
  "data": [{
    "id": "folder-1", "name": "投标书模板", "parent_id": null,
    "children": [
      { "id": "folder-2", "name": "技术方案", "parent_id": "folder-1", "children": [] }
    ],
    "document_count": 3
  }]
}
```

**自定义树形组件** (参考 Docmost 前端递归渲染，Radix UI 无 tree 组件)：

```tsx
// folder-tree.tsx — 递归节点渲染
function TreeNode({ node, depth = 0 }: { node: TreeNode; depth: number }) {
  const [expanded, setExpanded] = useState(true);
  return (
    <div>
      <div className="flex items-center gap-1 py-1 px-2 hover:bg-black/[0.02] cursor-pointer"
        style={{ paddingLeft: `${12 + depth * 16}px` }}
        onClick={() => { node.isFolder ? setExpanded(!expanded) : onSelectDoc(node); }}>
        {node.isFolder && (expanded ? <ChevronDown className="size-3.5" /> : <ChevronRight className="size-3.5" />)}
        {node.isFolder ? <Folder className="size-4 text-amber-500" /> : <FileText className="size-4" />}
        <span className="text-sm truncate">{node.name}</span>
      </div>
      {expanded && node.children?.map(c => <TreeNode key={c.id} node={c} depth={depth + 1} />)}
    </div>
  );
}
```

**适配方案**：
- `CollaborationDocument` 表增加 `folder_id` 字段（可为 null = 根目录）+ `sort_order` 排序字段
- 新表 `collaboration_folder`: `id, name, parent_id, tenant_id, created_by`
- 前端列表改为树形结构（递归渲染，可折叠文件夹，4 层缩进）
- 支持拖拽移动文档到文件夹（PUT move 端点）
- 批量操作：多选 → 批量删除/移动/导出

**新增 API 端点**：

| 方法 | 路由 | 用途 |
|---|---|---|
| POST | `/collaboration/folders` | 创建文件夹 |
| GET | `/collaboration/folders/tree` | 获取完整文件夹树 |
| PUT | `/collaboration/folders/<id>` | 重命名文件夹 |
| DELETE | `/collaboration/folders/<id>` | 删除文件夹 (子文档移到根目录) |
| PUT | `/collaboration/documents/<id>/move` | 移动文档到文件夹 |

**数据库迁移** (在 `migrate_db()` 中添加)：
```python
alter_db_add_column(migrator, "collaboration_document", "folder_id",
    CharField(max_length=32, null=True, index=True, help_text="parent folder id"))
alter_db_add_column(migrator, "collaboration_document", "sort_order",
    IntegerField(default=0, help_text="sort order within folder"))
```

**涉及改动**：
- `db_models.py` — 新增 `CollaborationFolder` 表，`CollaborationDocument` migration 加 `folder_id` + `sort_order`
- `db/services/collaboration_service.py` — 新增 `CollaborationFolderService`
- `collaboration_api.py` — 新增 5 个文件夹端点 + 1 个移动端点
- `collaboration_api_service.py` — 新增 folder CRUD + tree + move 方法
- `document-list.tsx` — 改为树形视图 (整合 folder tree + document list)
- **新建** `collaboration/folder-tree.tsx` — 递归树形组件
- `index.tsx` — 增加 `folderId` 过滤状态 + loadDocuments 参数

---

### 2.6 附件管理

**差距**：无。

**适配方案**：
- 新表 `collaboration_attachment`: `id, doc_id, file_name, file_size, mime_type, storage_key, uploader_id, create_time`
- 上传到 MinIO，与现有 `STORAGE_IMPL` 一致
- 支持预览：图片直接展示，PDF 用 iframe 嵌入
- 在文档详情面板中增加附件 Tab

**涉及改动**：
- `db_models.py` — 新增 `CollaborationAttachment` 表
- `collaboration_api.py` — 上传/下载/删除附件端点
- 新增 `collaboration/attachment-panel.tsx`

---

## 三、管理与安全功能

### 3.1 精细化权限

**差距**：仅 `me` / `team` 两级。

**参考项目**：Docmost (CASL 能力工厂权限系统)

> Docmost 使用 **CASL (Code Access Security Library)** 实现了工业级细粒度权限系统。其 `WorkspaceAbilityFactory` 根据用户+上下文生成 `(Action, Subject)` 规则集，`SpaceAbilityFactory` 处理空间级 ADMIN/WRITER/READER 角色，通过 `@CheckPolicies` 装饰器在控制器层面守卫权限。RAGFlow 用 Peewee ORM + `_check_access()` 函数替代 CASL，角色层级直接映射。

**Docmost 权限架构映射**：

| Docmost CASL 概念 | RAGFlow 等价实现 |
|---|---|
| `WorkspaceAbilityFactory.createForUser(user, workspace)` | `_check_access(obj, user_id) → role` |
| `CaslAction: Manage / Read / Create / Edit / Delete` | `ROLE_HIERARCHY` 字典 (OWNER=4 > EDITOR=3 > VIEWER=2 > COMMENTER=1) |
| `SpaceRole: ADMIN / WRITER / READER` | `CollaborationRole: owner / editor / viewer / commenter` |
| `@CheckPolicies(ability => ability.can(...))` | `@require_role(CollaborationRole.OWNER)` 装饰器 |
| `useUserRole()` Hook → `{ isOwner, isAdmin }` | `useDocumentRole(docId)` → `{ isOwner, isEditor, canEdit }` |

**CASL 能力工厂模式 → Python 翻译**：

```python
class CollaborationRole(StrEnum):
    OWNER = 'owner'         # 完全控制：编辑、删除、管理 ACL
    EDITOR = 'editor'       # 可编辑内容但不能管理 ACL
    VIEWER = 'viewer'       # 只读
    COMMENTER = 'commenter' # 只读 + 评论 (Phase 3 启用)

ROLE_HIERARCHY = { 'owner': 4, 'editor': 3, 'viewer': 2, 'commenter': 1 }

def _check_access(obj, user_id: str, required_role: str = "viewer") -> str | None:
    """参考 Docmost WorkspaceAbilityFactory 模式"""
    if obj.created_by == user_id:
        return CollaborationRole.OWNER.value

    acl = CollaborationDocumentACL.select().where(
        (CollaborationDocumentACL.document_id == obj.id) &
        (CollaborationDocumentACL.user_id == user_id)
    ).first()
    if acl and ROLE_HIERARCHY.get(acl.role, 0) >= ROLE_HIERARCHY.get(required_role, 0):
        return acl.role

    # 降级兼容：旧 permission=team → viewer
    if obj.permission == TenantPermission.TEAM.value:
        team_user_ids = _get_shared_tenant_user_ids(user_id)
        if obj.created_by in team_user_ids:
            return CollaborationRole.VIEWER.value
    return None
```

**控制器权限守卫** (参考 Docmost `@CheckPolicies` 装饰器)：

```python
def require_role(required_role: str):
    """参考 Docmost CASL Guard 模式"""
    def decorator(f):
        @wraps(f)
        async def wrapper(*args, **kwargs):
            doc_id = kwargs.get('doc_id') or request.view_args.get('doc_id')
            doc = CollaborationDocumentService.get_by_id(doc_id)
            if not doc[0]:
                return get_json_result(RetCode.NOT_FOUND)
            role = _check_access(doc[1], current_user.id, required_role)
            if not role:
                return get_json_result(RetCode.OPERATING_ERROR, message="Access denied")
            request.collab_role = role
            return await f(*args, **kwargs)
        return wrapper
    return decorator
```

**前端角色感知 Hook** (参考 Docmost `useUserRole()`)：

```tsx
function useDocumentRole(docId: string) {
  const [role, setRole] = useState<string | null>(null);
  return {
    isOwner: role === 'owner', isEditor: role === 'owner' || role === 'editor',
    isViewer: role === 'viewer', canEdit: role === 'owner' || role === 'editor',
    canManageACL: role === 'owner',
  };
}
```

**适配方案**：
- 保留 `CollaborationDocument.permission` 字段作降级兼容（team → viewer），新增独立 ACL 表
- 新表 `collaboration_document_acl`: `id, document_id, user_id, role(owner/editor/viewer/commenter), granted_by, create_time`
- 创建者自动为 `owner`，可添加/移除其他用户并设置角色
- 后端 `_check_access()` 改为 ACL 优先 + 旧 permission 降级
- 新增 `@require_role` 装饰器用于需要特定角色的端点
- 前端根据角色控制 Toolbar 是否可用、编辑区是否 readonly

**ACL 数据库表** (参考 Docmost `space_members` 表结构)：

```python
class CollaborationDocumentACL(DataBaseModel):
    id = CharField(max_length=32, primary_key=True)
    document_id = CharField(max_length=32, null=False, index=True)
    user_id = CharField(max_length=32, null=False, index=True)
    role = CharField(max_length=16, null=False, default="viewer",
                     help_text="owner|editor|viewer|commenter", index=True)
    granted_by = CharField(max_length=32, null=False)
    create_time = BigIntegerField(null=False)

    class Meta:
        db_table = "collaboration_document_acl"
        indexes = ((("document_id", "user_id"), True),)  # UNIQUE
```

**新增 API 端点**：

| 方法 | 路由 | 用途 |
|---|---|---|
| GET | `/collaboration/documents/<id>/collaborators` | 列出文档的所有 ACL 条目 |
| POST | `/collaboration/documents/<id>/collaborators` | 添加协作者 |
| PUT | `/collaboration/documents/<id>/collaborators/<user_id>` | 修改协作者角色 |
| DELETE | `/collaboration/documents/<id>/collaborators/<user_id>` | 移除协作者 |

**涉及改动**：
- `db_models.py` — 新增 `CollaborationDocumentACL` 表
- `db/__init__.py` — 新增 `CollaborationRole` enum
- `db/services/collaboration_service.py` — 新增 `CollaborationDocumentACLService`
- `collaboration_api_service.py` — 重构 `_check_access()` 支持 ACL + 角色层级；新增 `add_collaborator()`、`remove_collaborator()`、`list_collaborators()`、`update_collaborator_role()` 方法
- `collaboration_api.py` — 新增 4 个 ACL 端点 + `@require_role` 装饰器
- `document-editor.tsx` — readonly 模式 (viewer/commenter 时 `editable={false}`，Toolbar 隐藏编辑按钮)
- **新建** `collaboration/share-dialog.tsx` — 邀请协作者对话框 (搜索用户 + 选择角色 + 列表管理)

---

### 3.2 分享与链接

**差距**：无。

**适配方案**：
- 新表 `collaboration_share_link`: `id, doc_id, token(唯一短链), permission(view/edit), password_hash, expires_at, created_by`
- 生成链接：`/share/doc/<token>` → 访客可在无需登录下查看/编辑
- 密码保护：访问时弹出密码输入框
- 过期自动清理（定时任务）

**涉及改动**：
- `db_models.py` — 新增 `CollaborationShareLink` 表
- 新增 `collaboration_share_api.py` 或扩展现有 API
- 前端新增 `/share/doc/<token>` 路由（免登录页）
- 新增 `collaboration/share-link-dialog.tsx`

---

### 3.3 用户认证

**现状**：已有完整的登录/注册/个人信息管理（Quart session + `@login_required`）。

**差距**：无 OAuth/LDAP 集成（非必须，优先级低）。

**适配方案**：
- OAuth：`authlib` 库，支持 Google/GitHub/微信登录
- LDAP：Python `ldap3` 库，配置企业 AD 服务器
- 现有 `/api/v1/user/*` 路由体系可扩展

---

### 3.4 操作审计

**差距**：无。

**适配方案**：
- 新表 `collaboration_audit_log`: `id, user_id, doc_id, action(create/edit/delete/permission_change/export/share/rollback), detail(JSON), ip_address, create_time`
- 在 API 各端点用装饰器或 middleware 统一记录
- 前端审计页面（仅 owner 可见）

**涉及改动**：
- `db_models.py` — 新增审计表
- `collaboration_api.py` — 审计记录中间件
- 新增 `collaboration/audit-log-panel.tsx`

---

### 3.5 数据安全增强

**差距**：传输层已有 HTTPS (Nginx)，但无端到端加密、无 API 限流。

**适配方案**：

| 项 | 方案 |
|---|---|
| **传输加密** | Nginx 已有 HTTPS，WebSocket 走 wss |
| **存储加密** | MinIO 服务端加密 (SSE-S3)；MySQL 敏感字段 AES 加密 |
| **API 限流** | 在 Blueprint 上加 `@rate_limit(100, 60)` 装饰器 |
| **内容安全** | 文档内容 XSS 过滤（bleach 库），防止富文本注入恶意脚本 |

---

## 实施优先级

```
Phase 1 (基础增强):
  ├── 2.2 高级插入 (图片/链接/代码块)
  ├── 2.3 内容块 (引用/待办)
  ├── 2.4 Word 导入
  ├── 2.5 文件夹管理
  └── 3.1 精细化权限

Phase 2 (实时协作内核):
  ├── 1.1 实时协同编辑 (Yjs + WebSocket)
  ├── 1.2 光标/选区同步
  ├── 1.3 在线成员列表
  └── 1.7 版本历史 (snapshot 初始化)

Phase 3 (协作体验):
  ├── 1.4 评论与批注
  ├── 1.5 @提及
  ├── 1.6 建议编辑
  └── 1.8 版本回滚与对比

Phase 4 (安全与管理):
  ├── 3.2 分享链接
  ├── 3.4 操作审计
  ├── 3.5 数据安全增强
  └── 2.6 附件管理
```

---

## 新增数据库表汇总

| 表名 | 所属 Phase | 用途 |
|---|---|---|
| `collaboration_comment` | P3 | 评论与批注 |
| `collaboration_notification` | P3 | @提及通知 |
| `collaboration_suggestion` | P3 | 建议编辑 |
| `collaboration_document_snapshot` | P2 | 版本快照 |
| `collaboration_operation_log` | P2 | 操作日志(版本) |
| `collaboration_attachment` | P4 | 附件管理 |
| `collaboration_folder` | P1 | 文件夹 |
| `collaboration_document_acl` | P1 | 精细化权限 |
| `collaboration_share_link` | P4 | 分享链接 |
| `collaboration_audit_log` | P4 | 操作审计 |

## 新增前端组件汇总

| 组件 | 所属 Phase | 用途 |
|---|---|---|
| `member-avatars.tsx` | P2 | 在线成员头像 |
| `comment-panel.tsx` | P3 | 评论面板 |
| `comment-thread.tsx` | P3 | 评论线程 |
| `mention-plugin.tsx` | P3 | @提及 Lexical 插件 |
| `suggestion-plugin.tsx` | P3 | 建议编辑插件 |
| `suggestion-panel.tsx` | P3 | 审阅面板 |
| `version-history-panel.tsx` | P2 | 版本历史面板 |
| `diff-view.tsx` | P3 | 版本对比视图 |
| `import-dialog.tsx` | P1 | 导入对话框 |
| `export-menu.tsx` | P1 | 导出菜单 |
| `folder-tree.tsx` | P1 | 文件夹树 |
| `attachment-panel.tsx` | P4 | 附件面板 |
| `share-dialog.tsx` | P1 | 邀请协作者 |
| `share-link-dialog.tsx` | P4 | 分享链接对话框 |
| `audit-log-panel.tsx` | P4 | 操作审计面板 |
