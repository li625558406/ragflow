# 协作功能全量测试清单

> 覆盖范围：文档基础能力、实时协同、内容编辑增强、文件与组织、协作体验、权限与安全、免登录分享。
> 测试账号：lg18629285296@163.com / 12345678（建议另备一个同租户账号 + 一个跨租户账号做权限测试）
> 入口：C端 `/home` → 协作页签

标记说明：`[ ]` 未测 | `[x]` 通过 | `[!]` 失败（记录到踩坑问题清单）

---

## 一、文档基础能力

### 1.1 文档 CRUD

- [ ] 新建文档：输入名称创建成功，列表出现新文档并自动选中
- [ ] 新建文档：名称为空时提示校验错误，不发请求
- [ ] 新建文档：名称超长（>128字符）被截断或提示
- [ ] 新建文档：名称含特殊字符（emoji、引号、`<script>`）正常显示不破版
- [ ] 文档列表：加载展示所有可见文档（自己创建 + team 共享 + ACL 授权）
- [ ] 文档列表：无文档时展示空状态
- [ ] 打开文档：点击列表项加载内容，编辑器正确渲染
- [ ] 重命名文档：修改后列表和编辑器标题同步更新
- [ ] 删除文档：确认弹窗 → 删除后从列表消失，若为当前打开文档则编辑器清空
- [ ] 删除文档：非 owner 无删除入口或操作被后端拒绝

### 1.2 编辑与自动保存

- [ ] 输入文字后停止 2s 自动保存，刷新页面内容不丢失
- [ ] 连续快速输入不产生保存请求风暴（防抖生效）
- [ ] 保存失败（断网模拟）有错误提示，恢复后能重新保存
- [ ] 大文档（>1万字、含多表格图片）打开/编辑/保存不卡顿

### 1.3 基础样式工具栏

- [ ] 加粗 / 斜体 / 下划线 / 删除线：应用与取消，选区状态高亮正确
- [ ] 字体切换（7种中文字体）：应用后保存刷新不丢失
- [ ] 字号切换（7档）：同上
- [ ] 字体颜色：取色器选色应用正确
- [ ] 对齐方式（左/中/右/两端）：段落级应用正确
- [ ] 行距（6档）：应用正确
- [ ] 上标 / 下标 / 行内代码：应用与取消正确
- [ ] 清除格式：混合格式文本一键还原为纯文本样式
- [ ] 标题（H1/H2/H3）与正文互相切换
- [ ] 有序 / 无序列表：创建、缩进、退出列表
- [ ] 表格插入：指定行列插入，单元格可编辑，保存刷新不丢失

### 1.4 Markdown 导入

- [ ] 导入含标题/列表/表格/加粗的 Markdown，转换后结构正确
- [ ] 导入含非法/极端 Markdown（嵌套过深、超大文件）不报错崩溃

### 1.5 docx / PDF 导出

- [ ] 导出 docx：本地 Word/WPS 打开，标题、样式、表格、图片保留
- [ ] 导出 PDF：分页正常，中文字体不乱码
- [ ] 空文档导出不报 500
- [ ] 文件名含中文/特殊字符时下载文件名正确

### 1.6 格式规则

- [ ] 创建规则：正则 + 目标样式保存成功
- [ ] 规则列表 / 编辑 / 删除正常
- [ ] 应用规则：匹配段落样式被批量修改，未匹配段落不受影响
- [ ] 非法正则创建时有错误提示，不导致应用接口 500

---

## 二、实时协同（Yjs + WebSocket）

### 2.1 连接与鉴权

- [ ] 打开文档自动建立 WS 连接（浏览器 Network 面板确认 ws/wss）
- [ ] 无效 token 连接被拒绝
- [ ] 无文档权限的用户 WS 连接被拒绝（PermissionError）
- [ ] viewer 角色连接后为只读，编辑操作不生效不广播
- [ ] WS 断线后自动重连，重连后文档状态与服务端一致

### 2.2 多人同时编辑

- [ ] 两个浏览器（A/B 两账号）同时打开同一文档，A 输入 B 实时可见（延迟 < 1s）
- [ ] A、B 同时编辑不同段落，双方内容都保留，无覆盖
- [ ] A、B 同时编辑同一段落同一位置，CRDT 合并后双方最终一致
- [ ] 一方删除段落、另一方在该段落输入，合并结果无异常（无 JS 报错）
- [ ] 3+ 客户端同时编辑，所有端最终内容一致
- [ ] 晚加入者（C 在 A/B 编辑中途打开）能收到完整最新状态（buffer 补发）
- [ ] 房间所有人退出后内容持久化落库，重新打开内容完整（`PUT /ydoc`）
- [ ] 一方离线编辑（断网）后恢复网络，变更自动合并不丢失

### 2.3 光标与在线成员

- [ ] 对方光标位置 + 用户名 + 颜色实时显示
- [ ] 选区高亮同步显示
- [ ] 在线成员头像列表实时更新（加入即出现）
- [ ] 成员关闭页面/断线后头像在超时后消失
- [ ] 同一用户开两个标签页，成员列表表现合理（不崩溃、不重复错乱）

---

## 三、内容与编辑增强

### 3.1 图片

- [ ] 工具栏插入图片：选择文件上传成功并在文档中渲染
- [ ] 拖拽 / 粘贴图片上传成功
- [ ] 非图片类型文件（.exe/.txt）被拒绝并提示
- [ ] 超大图片（>10MB）被拒绝并提示
- [ ] 图片保存刷新后仍能加载（MinIO URL 有效）
- [ ] 导出 docx/PDF 中图片正常渲染
- [ ] 协同场景：A 插入图片，B 实时看到

### 3.2 链接

- [ ] 选中文字添加链接，展示为链接样式
- [ ] 点击链接新标签页打开
- [ ] 编辑 / 移除链接正常
- [ ] `javascript:` 伪协议链接被过滤或不可执行（XSS）

### 3.3 代码块

- [ ] 插入代码块，输入代码有语法高亮
- [ ] 代码块内 Tab / 回车行为正常，可退出代码块
- [ ] 保存刷新后高亮和内容保留

### 3.4 数学公式

- [ ] 插入 LaTeX 公式，KaTeX 渲染正确
- [ ] 非法 LaTeX 输入不崩溃，有降级展示
- [ ] 保存刷新后公式保留

### 3.5 Callout 提示框

- [ ] 四种类型（info/warning/tip/danger）颜色和图标正确
- [ ] Callout 内可输入编辑文字
- [ ] 保存刷新后保留

### 3.6 表情选择器

- [ ] 打开 emoji 面板选择插入，以 Unicode 文本插入
- [ ] 导出 docx/PDF 中 emoji 不乱码（或有可接受降级）

### 3.7 引用 / 待办事项

- [ ] 段落转引用块，样式正确，可切换回正文
- [ ] 待办列表可勾选/取消，状态保存刷新后保留
- [ ] Markdown 导入 `> quote`、`- [ ] todo` 正确转换

### 3.8 Word (.docx) 导入

- [ ] 导入对话框拖拽/选择 .docx 上传成功，列表出现新文档并自动打开
- [ ] 标题层级（H1/H2/H3）正确映射
- [ ] 加粗/斜体/下划线/删除线/字体/字号保留
- [ ] 有序/无序列表正确映射
- [ ] 表格正确导入
- [ ] 伪装扩展名文件（.txt 改名 .docx）被格式检测拒绝
- [ ] 非 docx 类型（.doc 旧格式、.pdf）有明确错误提示
- [ ] 超大 docx（几十MB）不超时崩溃，有合理提示
- [ ] 空 docx 导入不报错

---

## 四、文件与组织

### 4.1 文件夹

- [ ] 创建文件夹（根目录 / 嵌套子文件夹）
- [ ] 文件夹树递归渲染，展开/折叠正常
- [ ] 重命名文件夹
- [ ] 删除文件夹：其中文档按设计移到根目录（不误删文档）
- [ ] 深层嵌套（4层+）缩进展示正常
- [ ] 同名文件夹处理符合预期

### 4.2 文档移动

- [ ] 移动文档到文件夹，树形视图位置更新
- [ ] 移动到根目录
- [ ] 无权限文档移动被拒绝

### 4.3 附件管理

- [ ] 上传附件（pdf/docx/xlsx/zip 等）成功，列表显示文件名/大小
- [ ] 下载附件内容一致（文件名中文不乱码）
- [ ] 删除附件后列表消失，MinIO 对象处理符合设计
- [ ] 超大附件限制生效并提示
- [ ] viewer 角色能否上传/删除附件符合权限设计
- [ ] 附件操作写入审计日志

---

## 五、协作体验

### 5.1 评论与批注

- [ ] 选中段落添加评论，右侧评论面板显示
- [ ] 评论内容支持多行，为空时不能提交
- [ ] 编辑自己的评论
- [ ] 删除自己的评论；删除他人评论被拒绝（非 owner）
- [ ] resolve / unresolve 状态切换正确，已解决评论有区分展示
- [ ] 被评论段落删除后评论表现符合设计（orphaned 不崩溃）
- [ ] 协同场景：A 添加评论 B 可见（刷新或实时）
- [ ] 评论内容含 `<script>` 不执行（XSS）

### 5.2 @提及

- [ ] 输入 `@` 弹出用户搜索下拉（同租户用户）
- [ ] 选择用户后渲染为 mention 标签样式
- [ ] mention 节点保存刷新后保留
- [ ] 搜索无结果时下拉有空状态
- [ ] 删除 mention 节点整体删除（不残留半截文本）

### 5.3 版本历史与恢复

- [ ] 版本列表按时间倒序展示（含操作人/时间）
- [ ] 恢复到历史版本，编辑器内容变为该版本
- [ ] 恢复操作本身产生新版本记录（不丢当前版本）
- [ ] 非 owner/editor 恢复被拒绝
- [ ] 协同编辑中执行恢复，其他在线客户端状态一致（无脑裂）

---

## 六、权限与安全

### 6.1 ACL 精细化权限

角色矩阵逐项验证（用第二账号）：

| 操作 | owner | editor | viewer | commenter |
|---|---|---|---|---|
| 查看文档 | [ ] ✓ | [ ] ✓ | [ ] ✓ | [ ] ✓ |
| 编辑内容 | [ ] ✓ | [ ] ✓ | [ ] ✗ | [ ] ✗ |
| 添加评论 | [ ] ✓ | [ ] ✓ | [ ] 按设计 | [ ] ✓ |
| 管理协作者 | [ ] ✓ | [ ] ✗ | [ ] ✗ | [ ] ✗ |
| 删除文档 | [ ] ✓ | [ ] ✗ | [ ] ✗ | [ ] ✗ |
| 创建分享链接 | [ ] ✓ | [ ] ✗ | [ ] ✗ | [ ] ✗ |
| 查看审计日志 | [ ] ✓ | [ ] ✗ | [ ] ✗ | [ ] ✗ |

- [ ] 添加协作者：搜索用户 + 选择角色，对方列表可见该文档
- [ ] 修改协作者角色即时生效（editor 降为 viewer 后编辑器变只读）
- [ ] 移除协作者后对方无法再访问（API 403 + WS 拒绝）
- [ ] 重复添加同一协作者被去重或提示（unique 索引）
- [ ] 旧 permission=team 文档降级为 viewer 兼容逻辑正确
- [ ] 前端隐藏无权限按钮的同时，直接调 API 也被后端拦截（不能仅靠前端）
- [ ] 跨租户用户无法通过 doc_id 猜测访问（越权测试）

### 6.2 分享链接（免登录）

- [ ] 创建分享链接（view 权限），无痕窗口打开可查看
- [ ] 创建 edit 权限链接，无痕窗口可编辑（若设计支持）
- [ ] 密码保护：错误密码拒绝，正确密码通过
- [ ] 密码暴力尝试有限制或不泄露信息
- [ ] 过期时间到期后链接失效（返回明确提示，非 500）
- [ ] 删除分享链接后立即失效
- [ ] 分享页 `/share/doc/<token>` 不暴露登录态功能（无编辑入口越权）
- [ ] token 不可枚举（足够随机长度）
- [ ] 分享页刷新/直接访问 URL 正常（SPA 路由 + nginx sub-path 配置）

### 6.3 审计日志

- [ ] create/edit/delete/permission_change/export/share 操作均产生日志
- [ ] 日志包含操作人、时间、动作、详情
- [ ] 仅 owner 可查看，其他角色 API 被拒绝
- [ ] 日志分页正常，大量日志不卡顿

### 6.4 通用安全

- [ ] 所有 `/collaboration/*` 端点未登录访问返回 401（除 `/share/doc/*`）
- [ ] doc_id/folder_id/comment_id 传入他人资源 ID 均返回 403/404（水平越权全扫一遍）
- [ ] 富文本内容注入 `<img onerror>`、`<script>` 存储后渲染不执行
- [ ] 文件上传路径穿越（filename 含 `../`）不生效
- [ ] SQL 注入尝试（名称/搜索参数带 `' OR 1=1`）无异常

---

## 七、兼容与回归

- [ ] 协作页签不影响 c-chat 其他页签（对话/标讯）功能
- [ ] B端路由（ADMIN_PREFIX）不受协作路由影响
- [ ] 旧数据兼容：改造前创建的文档（无 folder_id/无 ACL）打开、编辑、导出正常
- [ ] 浏览器兼容：Chrome / Edge 主流程通过
- [ ] 移动端浏览器打开分享链接可正常查看
- [ ] 前端构建 `npm run build` 无报错（生产 minify 变量冲突回归）
- [ ] 服务器部署后 WS 经 nginx 代理连接正常（wss 升级头配置）
- [ ] Docker 重启后协作文档、附件、评论、ACL 数据完整（DB 初始化脚本含全部 8 张表）

---

## 八、性能与稳定性

- [ ] 100+ 文档列表加载 < 2s
- [ ] 单文档 5 人同时在线编辑无明显延迟
- [ ] WS 服务长时间运行无内存泄漏（房间清理逻辑：全员退出后 `_rooms` 释放）
- [ ] 频繁开关文档不产生 WS 连接泄漏（浏览器连接数不增长）
- [ ] 附件/图片并发上传不互相覆盖（uuid key）

---

## 附：接口清单速查（约40个 REST + 1 WS）

| 模块 | 端点 |
|---|---|
| 文档 | POST/GET `/collaboration/documents`，GET/PUT/DELETE `/documents/<id>`，PUT `/documents/<id>/ydoc`，GET `/download`，POST `/apply-rule`，POST `/import`，POST `/move` |
| 格式规则 | POST/GET `/format-rules`，PUT/DELETE `/format-rules/<id>` |
| 文件夹 | POST/GET `/folders`，PUT/DELETE `/folders/<id>` |
| 协作者 | GET/POST `/documents/<id>/collaborators`，PUT/DELETE `/collaborators/<user_id>` |
| 评论 | GET/POST `/documents/<id>/comments`，PUT/DELETE `/comments/<id>`，POST `/resolve`，POST `/unresolve` |
| 版本 | GET `/documents/<id>/versions`，POST `/versions/<version>/restore` |
| 分享 | POST/GET/DELETE `/documents/<id>/share`，GET `/share/doc/<token>`（免登录），POST `/share/doc/<token>/verify` |
| 附件 | POST/GET `/documents/<id>/attachments`，GET/DELETE `/attachments/<id>` |
| 审计 | GET `/documents/<id>/audit-logs` |
| 实时协同 | WebSocket（`api/apps/collaboration_ws.py`） |
